---
quick_id: 260509-x43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/student/train.py
  - tests/test_v3_sft_config.py
  - tests/test_v3_sft_artifacts.py
autonomous: true
requirements:
  - SFT-03
  - SFT-04
must_haves:
  truths:
    - "Phase 4 dry-run can construct transformers.TrainingArguments without passing SFT-only evidence keys such as packing."
    - "SFT-03 evidence still records packing=false in locked config and emitted manifests/reports."
    - "Existing Phase 4 SFT config and artifact gate tests remain green."
  artifacts:
    - path: "tsc_cycle/student/train.py"
      provides: "TrainingArguments-safe kwargs filter and call-site wiring"
      contains: "TrainingArguments(**training_arguments_init_kwargs(targs_kwargs))"
    - path: "tests/test_v3_sft_config.py"
      provides: "Regression coverage for filtering SFT-only evidence keys before TrainingArguments init"
    - path: "tests/test_v3_sft_artifacts.py"
      provides: "Manifest evidence coverage preserving training_args.packing=false"
  key_links:
    - from: "tsc_cycle/student/sft_v3.py::locked_training_arguments_kwargs"
      to: "tsc_cycle/student/train.py::TrainingArguments"
      via: "training_arguments_init_kwargs filter"
      pattern: "packing.*not in.*init"
    - from: "tsc_cycle/student/train.py::write_sft_manifest"
      to: "Phase 4 SFT-03 report evidence"
      via: "unfiltered targs_kwargs passed as training_args"
      pattern: '"packing": False'
---

<objective>
修复 Phase 4 dry-run 启动失败：`transformers.TrainingArguments.__init__` 不接收 `packing`，但 SFT-03 仍必须保留 `packing=False` 作为机器可检查证据。

Purpose: 让训练启动路径使用 Transformers 支持的 kwargs，同时不削弱 SFT-03 的 no-packing 合同证据。
Output: 一个限定范围代码修复和相关回归测试。
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/CLAUDE.md
@/home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py

<interfaces>
当前关键合同：

From `tsc_cycle/student/sft_v3.py`:
```python
def locked_training_arguments_kwargs(output_dir: str | os.PathLike[str]) -> dict[str, Any]:
    return {
        "output_dir": str(output_dir),
        "per_device_train_batch_size": 1,
        "gradient_accumulation_steps": 16,
        "gradient_checkpointing": True,
        "gradient_checkpointing_kwargs": {"use_reentrant": False},
        "packing": False,
        "chat_template": False,
        "apply_chat_template": False,
        ...
    }
```

From `tsc_cycle/student/train.py`:
```python
targs_kwargs = locked_training_arguments_kwargs(train_output)
if max_steps > 0:
    targs_kwargs["max_steps"] = max_steps
training_args = TrainingArguments(**targs_kwargs)
```

Required behavior: filter `packing` before `TrainingArguments(**...)`; keep unfiltered `targs_kwargs` for `write_sft_manifest(..., training_args=targs_kwargs, ...)` so SFT-03 evidence remains visible.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Phase 4 dry-run must start instead of failing in `TrainingArguments.__init__` on `packing`. | Task 2 filters SFT-only evidence keys at the TrainingArguments call site. |
| REQ | SFT-03 no packing evidence must remain machine-checkable. | Tasks 1 and 2 keep `locked_training_arguments_kwargs(...)["packing"] is False` and preserve manifest `training_args.packing=false`. |
| REQ | SFT-04 dry-run gate must remain runnable. | Task 2 fixes dry-run bootstrap path; Task 3 verifies config/artifact gates. |
| CONTEXT | No research phase; single atomic quick plan; target about 30% context. | This plan has three focused tasks and no discovery work. |
| RESEARCH | Transformers TrainingArguments supports trainer args but not SFTTrainer-only `packing`. | Task 2 separates TrainingArguments init kwargs from evidence/report kwargs. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests for TrainingArguments-safe kwargs and SFT-03 evidence</name>
  <files>tests/test_v3_sft_config.py, tests/test_v3_sft_artifacts.py</files>
  <behavior>
    - `locked_training_arguments_kwargs(...)` still returns `packing=False`, `chat_template=False`, and `apply_chat_template=False` for SFT-03 evidence.
    - A new train-module helper returns kwargs suitable for `transformers.TrainingArguments`, excluding `packing`, `chat_template`, and `apply_chat_template` while preserving standard args such as `output_dir`, `per_device_train_batch_size`, `gradient_accumulation_steps`, and `gradient_checkpointing_kwargs`.
    - Manifest-writing tests assert emitted `training_args["packing"] is False` so report evidence is not lost when TrainingArguments init kwargs are filtered.
  </behavior>
  <action>In `tests/test_v3_sft_config.py`, add a regression test importing the new helper name `training_arguments_init_kwargs` from `tsc_cycle.student.train`. The test should build `raw = locked_training_arguments_kwargs(tmp_output_dir)`, call `init_kwargs = training_arguments_init_kwargs(raw)`, assert evidence keys remain in `raw` with `False`, and assert those evidence-only keys are absent from `init_kwargs`. In `tests/test_v3_sft_artifacts.py`, extend one existing green manifest test or add a focused test around `write_sft_manifest` to assert the JSON payload still contains `training_args.packing == False`.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_sft_config.py::test_training_arguments_init_kwargs_filters_sft_only_evidence_keys -q</automated>
  </verify>
  <done>The new config regression fails before Task 2 because `training_arguments_init_kwargs` does not exist, and the artifact assertion documents that SFT-03 report evidence must remain present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter SFT-only evidence keys before constructing TrainingArguments</name>
  <files>tsc_cycle/student/train.py</files>
  <behavior>
    - `TrainingArguments(**...)` receives no `packing`, `chat_template`, or `apply_chat_template` keys.
    - `targs_kwargs` remains unmodified and is still passed to `write_sft_manifest(..., training_args=targs_kwargs, ...)`.
    - Existing `max_steps` override remains honored in both TrainingArguments init kwargs and manifest/report evidence.
  </behavior>
  <action>Add a small module-level constant such as `SFT_EVIDENCE_ONLY_TRAINING_ARG_KEYS = frozenset({"packing", "chat_template", "apply_chat_template"})` and a pure helper `training_arguments_init_kwargs(kwargs: dict[str, Any]) -> dict[str, Any]` that returns a copied dict excluding those keys. Update `build_trainer` to call `TrainingArguments(**training_arguments_init_kwargs(targs_kwargs))`. Do not remove `packing=False` from `locked_training_arguments_kwargs`, do not mutate `targs_kwargs`, and do not change manifest/report serialization.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_sft_config.py::test_training_arguments_init_kwargs_filters_sft_only_evidence_keys tests/test_v3_sft_config.py::test_sft_03_d05_batch_accum_checkpointing_and_no_packing_are_locked -q</automated>
  </verify>
  <done>`TrainingArguments` construction path is protected from unsupported SFT-only evidence keys while SFT-03 locked config still exposes `packing=False`.</done>
</task>

<task type="auto">
  <name>Task 3: Verify config and artifact gates preserve SFT-03 evidence</name>
  <files>tests/test_v3_sft_config.py, tests/test_v3_sft_artifacts.py</files>
  <action>Run the focused Phase 4 SFT tests that cover locked training args and manifest/report gate behavior. If a test fails because an assertion still expects `packing` to be passed into `TrainingArguments`, update that assertion to distinguish evidence kwargs from init kwargs. Do not relax SFT-03 assertions that require no packing evidence.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_sft_config.py tests/test_v3_sft_artifacts.py -q</automated>
  </verify>
  <done>Both focused test files pass; `packing` is filtered before `TrainingArguments`, and manifest/report evidence still records `packing=False`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/config → TrainingArguments | Local training configuration crosses into the Transformers constructor. |
| Training runtime → manifest/report JSON | Runtime configuration is serialized as machine-checkable gate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260509-x43-01 | T | `tsc_cycle/student/train.py::TrainingArguments` | mitigate | Filter unsupported evidence-only keys before constructor call. |
| T-260509-x43-02 | R | `write_sft_manifest` training_args evidence | mitigate | Keep unfiltered `targs_kwargs` for manifest serialization and assert `packing=False` in tests. |
</threat_model>

<verification>
Run:

```bash
cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_sft_config.py tests/test_v3_sft_artifacts.py -q
```
</verification>

<success_criteria>
- `TrainingArguments.__init__` is never called with `packing`.
- SFT-03 locked config and manifest/report evidence still contain `packing=False`.
- Focused Phase 4 SFT config and artifact tests pass.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/quick/260509-x43-phase-4-dry-run-trainingarguments-init-p/260509-x43-SUMMARY.md`.
</output>
