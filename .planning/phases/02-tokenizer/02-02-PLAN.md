---
phase: 02-tokenizer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - outputs/sft/train.jsonl.bak
  - outputs/data/train.jsonl
autonomous: true
requirements:
  - TOK-01
  - TOK-02

must_haves:
  truths:
    - "GLM tokenizer 与自定义标签兼容性通过 SFT 训练验证"
    - "若 tokenizer 有问题会在训练中暴露，无需单独验证"
  artifacts:
    - path: "outputs/data/train.jsonl"
      provides: "与 GLM 模型兼容的训练数据"
    - path: "outputs/sft/train.jsonl.bak"
      provides: "原始数据备份"
  key_links:
    - from: "sft_train.jsonl"
      to: "train.jsonl"
      via: "复制增强版数据到主训练数据路径"
      pattern: "shutil.copy"
---

<objective>
将增强版 SFT 数据部署到训练路径，完成数据准备工作。

Purpose: 为 Phase 3 SFT 训练迁移准备就绪的训练数据
Output: 备份原数据 + 部署增强版数据到 outputs/data/train.jsonl
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tokenizer/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 备份原数据并部署增强版数据</name>
  <files>outputs/data/train.jsonl, outputs/sft/train.jsonl.bak</files>
  <action>
1. 备份原有训练数据：
   - 将 outputs/data/train.jsonl 复制到 outputs/sft/train.jsonl.bak
   - 保留原始的 16788 条样本作为备份

2. 转换增强版数据格式：
   - 读取 outputs/sft/sft_train.jsonl（messages 格式）
   - 转换为 outputs/data/train.jsonl 兼容的格式
   - 注意：原 train.jsonl 格式是 {prompt, prediction, state_file, metadata}
   - SFT 训练格式是 {messages: [{role, content}, ...]}
   - 需要确认两种格式的兼容性或创建转换脚本

3. 关于 Tokenizer 验证（按用户决策）：
   - 跳过专门的 tokenizer 词表分析
   - GLM tokenizer 兼容性将在 Phase 3 SFT 训练中验证
   - 若训练出现标签输出异常，再回退排查

实现方式：
- 创建 Python 脚本 src/scripts/deploy_sft_data.py
- 或者直接在命令行中完成复制操作
- 打印操作日志确认完成
  </action>
  <verify>
ls -la /home/samuel/TSC_CYCLE/outputs/sft/train.jsonl.bak
wc -l /home/samuel/TSC_CYCLE/outputs/data/train.jsonl
  </verify>
  <done>
- outputs/sft/train.jsonl.bak 存在且包含 16788 条原始样本
- outputs/data/train.jsonl 包含增强版数据
  </done>
</task>

<task type="auto">
  <name>Task 2: 生成数据统计报告</name>
  <files>outputs/sft/data_statistics.txt</files>
  <action>
生成数据统计报告文件，包含：
1. 原始数据统计：
   - 样本数量：16788
   - 来源：多个 arterial4x4 场景 + chengdu 场景

2. 增强版数据统计：
   - 样本数量：100（用于 SFT 训练）
   - 思考链长度分布（min/max/mean/median）
   - Token 估算长度

3. Tokenizer 验证策略说明：
   - 采用"训练中验证"策略
   - GLM tokenizer 兼容性将在 Phase 3 中验证
   - 自定义标签（<start_working_out> 等）不在 GLM 词表中，将被拆分为 sub-token

4. 数据文件位置：
   - 原始数据备份：outputs/sft/train.jsonl.bak
   - SFT 训练数据：outputs/sft/sft_train.jsonl
   - GRPO 数据（待生成）：outputs/grpo/grpo_train.jsonl
  </action>
  <verify>
cat /home/samuel/TSC_CYCLE/outputs/sft/data_statistics.txt
  </verify>
  <done>
- data_statistics.txt 文件存在且包含完整统计信息
- 统计数据与实际文件一致
  </done>
</task>

</tasks>

<verification>
1. 原始数据已备份到 outputs/sft/train.jsonl.bak
2. 增强版数据已部署到 outputs/sft/sft_train.jsonl
3. 数据统计报告生成完成
4. Tokenizer 验证策略已记录
</verification>

<success_criteria>
- [ ] 原始 train.jsonl 已备份（16788 条）
- [ ] 增强版 SFT 数据部署完成（100 条）
- [ ] 数据统计报告生成
- [ ] Phase 2 数据准备工作完成，可进入 Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-tokenizer/02-02-SUMMARY.md`
</output>
