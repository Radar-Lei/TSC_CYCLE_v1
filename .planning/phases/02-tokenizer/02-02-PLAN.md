---
phase: 02-tokenizer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - outputs/data/train.jsonl.bak
  - outputs/sft/data_statistics.txt
autonomous: true
requirements:
  - TOK-01
  - TOK-02

must_haves:
  truths:
    - "原始 GRPO 数据已备份，可恢复"
    - "增强版 SFT 数据与 GLM 模型兼容"
    - "TOK-01/TOK-02 验证在 Phase 3 SFT 训练中完成"
  artifacts:
    - path: "outputs/data/train.jsonl.bak"
      provides: "原始数据备份"
    - path: "outputs/sft/data_statistics.txt"
      provides: "数据统计报告"
  key_links: []
---

> **注**: TOK-01/TOK-02 (Tokenizer 验证) 跳过专门验证，将在 Phase 3 SFT 训练中验证。若训练出现标签输出异常，再回退排查 tokenizer 问题。

<objective>
将增强版 SFT 数据部署到训练路径，完成数据准备工作。

Purpose: 为 Phase 3 SFT 训练迁移准备就绪的训练数据
Output: 备份原数据 + 部署增强版数据到 outputs/data/train.jsonl
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tokenizer/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 备份原数据</name>
  <files>outputs/data/train.jsonl.bak</files>
  <action>
1. 备份原有 GRPO 训练数据：
   - 将 outputs/data/train.jsonl 复制到 outputs/data/train.jsonl.bak
   - 保留原始的 16788 条样本作为备份

2. 注意数据格式差异：
   - outputs/data/train.jsonl 是 GRPO 数据格式：{prompt, prediction, state_file, metadata}
   - outputs/sft/sft_train.jsonl 是 SFT 数据格式：{messages: [{role, content}, ...]}
   - **两种格式用于不同训练阶段，不需要转换**
   - SFT 训练直接使用 outputs/sft/sft_train.jsonl
   - GRPO 数据（outputs/data/train.jsonl）在后续 Phase 生成

3. 打印操作日志确认完成
  </action>
  <verify>
ls -la /home/samuel/TSC_CYCLE/outputs/data/train.jsonl.bak
wc -l /home/samuel/TSC_CYCLE/outputs/data/train.jsonl.bak
  </verify>
  <done>
- outputs/data/train.jsonl.bak 存在且包含 16788 条原始样本
- 日志输出确认备份成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 生成数据统计报告</name>
  <files>outputs/sft/data_statistics.txt</files>
  <action>
生成数据统计报告文件，包含：
1. 原始数据统计：
   - 样本数量：16788
   - 来源：多个 arterial4x4 场景 + chengdu 场景
   - 格式：{prompt, prediction, state_file, metadata}（GRPO 格式）

2. 增强版 SFT 数据统计：
   - 样本数量：100（用于 SFT 训练）
   - 格式：{messages: [{role, content}, ...]}（SFT 格式）
   - 思考链长度分布（min/max/mean/median）
   - Token 估算长度

3. Tokenizer 验证策略：
   - 跳过专门验证：SFT 训练足够快，若 tokenizer 有问题会在训练中暴露
   - TOK-01/TOK-02 将在 Phase 3 SFT 训练中验证

4. 数据文件位置：
   - 原始 GRPO 数据备份：outputs/data/train.jsonl.bak
   - SFT 训练数据：outputs/sft/sft_train.jsonl
  </action>
  <verify>
cat /home/samuel/TSC_CYCLE/outputs/sft/data_statistics.txt
  </verify>
  <done>
- data_statistics.txt 文件存在且包含完整统计信息
- 统计数据与实际文件一致
  </done>
</task>

</tasks>

<verification>
1. 原始数据已备份到 outputs/data/train.jsonl.bak
2. 数据统计报告生成完成
3. 明确 SFT 数据（messages 格式）和 GRPO 数据（prompt/prediction 格式）是不同格式
4. TOK-01/TOK-02 验证推迟到 Phase 3 SFT 训练中进行
</verification>

<success_criteria>
- [ ] 原始 train.jsonl 已备份到 outputs/data/train.jsonl.bak（16788 条）
- [ ] 数据统计报告生成
- [ ] Phase 2 数据准备工作完成，可进入 Phase 3（TOK-01/TOK-02 将在训练中验证）
</success_criteria>

<output>
After completion, create `.planning/phases/02-tokenizer/02-02-SUMMARY.md`
</output>
