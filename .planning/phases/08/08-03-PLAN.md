---
phase: 08
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tsc_cycle/teacher/labeler.py
  - scripts/teacher_smoke.py
  - scripts/cache_protocol_audit.py
autonomous: false
requirements: [DATA-01, DATA-03]
user_setup:
  - service: openai
    why: "GPT-5.5 high 教师标注 (≤10 worker)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "用户已有；export 在 shell 即可"
      - name: OPENAI_BASE_URL
        source: "v1.0 用 http://148.135.118.86:8080；A1 假设仍可用，需 1-sample smoke 验证"
      - name: GPT5_5_INPUT_PER_M
        source: "OpenAI 实时定价（cost.json 准确性用，可不设走 default 1.25）"
      - name: GPT5_5_OUTPUT_PER_M
        source: "OpenAI 实时定价（可不设走 default 10.00）"
must_haves:
  truths:
    - "labeler.py 接受 --targeted-inputs 参数，把 targeted 样本并入 all_inputs"
    - "cache_protocol_audit.py 能抽样 raw_responses/ 报告新/旧协议比例"
    - "1-sample smoke 通过（OPENAI_BASE_URL 仍可用）"
    - "10K full label run 在 ≤10 worker + 断点续跑下完成，labeled.jsonl + rejected.jsonl + cost.json + reject_stats.json 全部落盘"
    - "labeled 总数 ≥ 9000（reject_rate ≤ 10% 上限），且至少 1500 OOD + 800 targeted"
  artifacts:
    - path: "tsc_cycle/teacher/labeler.py"
      provides: "main() 接受 --targeted-inputs；all_inputs 合并三源"
    - path: "scripts/teacher_smoke.py"
      provides: "1-sample 标注 smoke probe（验证 OPENAI_BASE_URL + 协议）"
    - path: "scripts/cache_protocol_audit.py"
      provides: "抽样 raw_responses/ 检查 <end_working_out> vs </end_working_out> 协议占比"
    - path: "data/labeled.jsonl"
      provides: "全量 v2.0 标注结果（append v1.0 cache hit + 新标注）"
    - path: "data/rejected.jsonl"
      provides: "被 lint 拒绝的样本（含 reject_kind）"
    - path: "runs/{ts}/teacher_cost.json"
      provides: "本次 run 的 token 与 USD 估计"
    - path: "runs/{ts}/teacher_reject_stats.json"
      provides: "reject 分布"
  key_links:
    - from: "scripts/teacher_smoke.py"
      to: "OpenAI Responses API via TeacherClient"
      via: "1 个 in-dist 样本 + reasoning_effort=high"
      pattern: "responses.create"
    - from: "labeler.py main()"
      to: "constraint_lint.validate"
      via: "lint 失败 → rejected.jsonl，不重试"
      pattern: "validate_constraints"
---

<objective>
为 Phase 8 完成最长跑、最贵、最网络敏感的步骤：先做协议安全网（cache 协议审计 + 1-sample smoke 探针），再扩展 `teacher/labeler.py` 接受 targeted 输入，最后跑 10K 全量教师标注（断点续跑 + cache 复用 v1.0 的 3000 hit）。

Purpose: DATA-01（10K 标注完成）+ DATA-03（lint-pass 标注作为 split 基础）的网络/成本核心阶段。**必须先排雷再花钱**：先确认 v1.0 cache 协议可用（避免 +$60 重标），再确认 OPENAI_BASE_URL proxy 通（A1 假设），最后才能放全量。

Output: 完整的 data/labeled.jsonl（≥ 9000 行）+ rejected.jsonl + cost.json + reject_stats.json + 协议审计报告。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-CONTEXT.md
@CLAUDE.md

@tsc_cycle/teacher/labeler.py
@tsc_cycle/teacher/client.py
@tsc_cycle/prompt_builder.py
@tsc_cycle/constraint_lint.py
@scripts/teacher_smoke.py
@.planning/phases/08/08-01-PLAN.md

<interfaces>
<!-- 来自 v1.0 已落地的契约（不变） -->

From tsc_cycle/teacher/labeler.py:
```python
# CLI: --inputs --ood-inputs --labeled --rejected --max-workers --limit --raw-responses-dir
# 续跑核心: _read_done_ids 从 labeled+rejected 读 sample_id 集合
# Lint 失败不重试: rejected.jsonl append, 不再调 client
```

From tsc_cycle/teacher/client.py:
```python
class TeacherClient:
    def __init__(self, model="gpt-5.5", reasoning_effort="high",
                 base_url=os.environ.get("OPENAI_BASE_URL"),
                 api_key=os.environ["OPENAI_API_KEY"],
                 require_reasoning_tokens_min=100,
                 raw_responses_dir=Path("raw_responses"))
    def label(self, prompt: str, sample_id: str) -> LabelResult
# Cache: raw_responses/{prompt_hash}.json，atomic rename
# RateLimit 不计 retry budget；APITimeout/Connection/API/BadRequest 指数退避
```

From tsc_cycle/prompt_builder.py（Phase 7 已锁，绝不能改）:
```python
# 协议: <start_working_out>...<end_working_out><SOLUTION>{json}</SOLUTION>
# 任何 prompt 文本变更 → cache 全失效
```

成本/规模红线（per RESEARCH §"Pitfall 1" + §"Common Pitfalls"）:
- smoke phase reject_rate > 0.10 → halt
- cost_per_sample > $0.05 → halt
- estimated_usd_total > $80 → halt
- targeted reject_rate 与 ood reject_rate 应在同一量级（差异 > 3× 报警）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cache 协议审计 + 1-sample smoke probe</name>
  <files>
    scripts/cache_protocol_audit.py,
    scripts/teacher_smoke.py
  </files>
  <action>
    1. 创建 scripts/cache_protocol_audit.py:
       - 从 raw_responses/ 抽样 30 个 .json 文件
       - 解析每个文件的 assistant 输出文本，统计含 `<end_working_out>` vs `</end_working_out>` 的数量
       - 输出 stdout: counts + 比例；如果旧协议占比 > 0% 则 exit code 1
       - 用法: python scripts/cache_protocol_audit.py [--n 30] [--cache-dir raw_responses]

    2. 增强已有 scripts/teacher_smoke.py（如果不存在则创建）:
       - 从 data/inputs.jsonl 读取第 1 个 sample
       - 调 TeacherClient.label() 一次（不走 cache: 用 --no-cache 或 临时 cache 目录）
       - 验证: response.parse_ok=True, lint_ok=True, reasoning_tokens >= 100, 协议含 `<end_working_out>` 但不含 `</end_working_out>`
       - 任一 fail → exit 1 + 打印响应文本前 500 字
       - 任一 RateLimitError/APIConnectionError → exit 2 (区分网络故障 vs 协议故障)
       - exit 0 = OPENAI_BASE_URL + API key + 协议三件套全 OK，可以放全量

    3. **执行**:
       - 跑 `python scripts/cache_protocol_audit.py`
         - 期望 exit 0；若 exit 1（cache 含旧协议）→ 在 SUMMARY 中报告并提议两条路：(a) 接受重标 v1.0 全 3000，(b) 写一次性迁移脚本替换 cache 中 `</end_working_out>` 为 `<end_working_out>` 保持 prompt_hash 不变。**不在本 task 中实施迁移**——升到下一个 checkpoint 让用户决策。
       - 跑 `python scripts/teacher_smoke.py`
         - 期望 exit 0；若 exit 1/2 → 在 SUMMARY 中报告并提议 fallback（切回官方 OpenAI base_url 或换 API key）。

    4. 把 cache_audit 的统计 + smoke 的 reasoning_tokens 数值写入 .planning/phases/08/08-03-PROBE.md（手工 markdown），供后续 checkpoint 参考。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python scripts/cache_protocol_audit.py && python scripts/teacher_smoke.py</automated>
  </verify>
  <done>
    - 两个脚本都 exit 0
    - .planning/phases/08/08-03-PROBE.md 记录 cache 协议审计结果 + smoke 响应字段
    - 如果任一 exit != 0：本 task 不算完成，进入 checkpoint:decision (Task 2)
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>是否在 cache/proxy 出现问题时调整路径继续</decision>
  <context>
    Task 1 执行 cache_protocol_audit.py 与 teacher_smoke.py 的结果决定接下来：
    - 若 cache 含旧协议（exit 1）→ 增量成本约 +$60 (重标 3000) 或编写一次性 cache 迁移脚本
    - 若 OPENAI_BASE_URL 失效（smoke exit 2）→ 切官方 OpenAI base_url（unset OPENAI_BASE_URL）
    - 若两者都通过 → 直接进入 Task 3 全量标注，跳过本 checkpoint
  </context>
  <options>
    <option id="proceed-direct">
      <name>两项都 OK：直接进入全量标注</name>
      <pros>零额外成本，最快路径</pros>
      <cons>无</cons>
    </option>
    <option id="cache-migrate">
      <name>Cache 含旧协议：写一次性迁移脚本替换 cache 文本</name>
      <pros>保留 v1.0 的 3000 cache hit，节省 ~$60</pros>
      <cons>引入新代码，需验证 prompt_hash 不变</cons>
    </option>
    <option id="cache-restamp">
      <name>Cache 含旧协议：接受重标全 3000 样本（清空旧 cache 文件中协议错误的）</name>
      <pros>实现最简单</pros>
      <cons>+$60 成本，多 30-60 min API 时间</cons>
    </option>
    <option id="proxy-fallback">
      <name>Proxy 失效：unset OPENAI_BASE_URL 走官方 OpenAI</name>
      <pros>立即可恢复</pros>
      <cons>账单走用户实际 OpenAI 账户而非 proxy；定价可能不同</cons>
    </option>
  </options>
  <resume-signal>选择 proceed-direct / cache-migrate / cache-restamp / proxy-fallback（可组合，例如 "cache-migrate + proceed-direct"）</resume-signal>
</task>

<task type="auto">
  <name>Task 3: labeler 接受 --targeted-inputs + 跑 10K 全量标注</name>
  <files>
    tsc_cycle/teacher/labeler.py
  </files>
  <action>
    1. 修改 tsc_cycle/teacher/labeler.py main():
       - argparse 加 `--targeted-inputs PATH`（可选）
       - 读取后与 inputs/ood_inputs 合并到 all_inputs；保持 split_hint 不变（targeted 已在 sample_inputs 标 split_hint=id）
       - 续跑逻辑（_read_done_ids）自然兼容三源
       - 不改 client 调用、不改 prompt_builder、不改 lint 拒绝行为

    2. 先生成三源 input 文件（若 Wave 1 已落地 sample_inputs 扩展，则一条命令产出全部三个）:
       ```bash
       python -m tsc_cycle.sample_inputs \
         --n-id 7000 --n-ood 2000 --n-targeted 1000 \
         --seed 42 --targeted-seed 1337 \
         --targeted-eval-path runs/20260507T032419Z/eval/per_sample.jsonl \
         --v1-inputs-paths data/inputs.jsonl data/ood_inputs.jsonl \
         --out-id data/inputs.jsonl --out-ood data/ood_inputs.jsonl \
         --out-targeted data/targeted_inputs.jsonl \
         --mae-thresh 5.0 --perturbation-ratio 0.1
       ```
       注意：v1.0 已有 inputs.jsonl (2700) + ood_inputs.jsonl (300)。**不要覆盖**：先备份 (cp ...{,.v1bak})，然后让 sample_inputs 写新文件。如果 sample_inputs 是"追加扩展"语义则写入新文件名，否则替换之；最终要保证：
         - 旧 sample_id 全部仍存在于新 inputs.jsonl/ood_inputs.jsonl 中（不丢 cache hit），
         - 新增 sample_id 累计达到 (7000-2700) + (2000-300) = 4300 + 1700 = 6000 个 + 1000 targeted = 7000 个新 prompt。
       具体策略：在 sample_inputs.main 用 --append 模式（如未实现，临时手写一个 merge 脚本：读旧 file → 用旧 id 扩展到目标 n → 新增个数补齐）。**或者**：保留 v1.0 inputs.jsonl 不动，新文件命名 data/inputs_v2.jsonl 并把 labeler 的 --inputs 改成 v1+v2 的合并临时文件。executor 二选一，必须保证 cache hit ≥ 2700 + 300 = 3000。

    3. smoke 50: 先用 --limit 50 跑 labeler 验证：
       ```bash
       python -m tsc_cycle.teacher.labeler \
         --inputs data/inputs.jsonl --ood-inputs data/ood_inputs.jsonl \
         --targeted-inputs data/targeted_inputs.jsonl \
         --labeled data/labeled.jsonl --rejected data/rejected.jsonl \
         --max-workers 10 --limit 50
       ```
       - 期望 ok ≥ 45/50（reject_rate ≤ 10%）
       - cost_per_sample ≤ $0.05
       - 任一红线触发 → halt，写 .planning/phases/08/08-03-SMOKE-FAIL.md

    4. 全量 run（建议在 tmux/screen 里跑，2-4h）:
       ```bash
       python -m tsc_cycle.teacher.labeler \
         --inputs data/inputs.jsonl --ood-inputs data/ood_inputs.jsonl \
         --targeted-inputs data/targeted_inputs.jsonl \
         --labeled data/labeled.jsonl --rejected data/rejected.jsonl \
         --max-workers 10
       ```
       完成后落盘 runs/{ts}/teacher_cost.json + teacher_reject_stats.json（labeler.py 现有逻辑）。

    5. 完成后写 .planning/phases/08/08-03-LABEL-RESULT.md 记录:
       - n_labeled / n_rejected / reject_rate（按 source 拆分: id/ood/targeted）
       - estimated_usd_total
       - 实际 wall time
       - cache hit 比例（从 labeler 日志或 raw_responses/ 文件数 - smoke 前的基线）
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && wc -l data/labeled.jsonl data/rejected.jsonl && jq -r '.estimated_usd_total' runs/$(ls -t runs | head -1)/teacher_cost.json && python -c "
import json, collections
src_count = collections.Counter()
with open('data/labeled.jsonl') as f:
    for line in f:
        rec = json.loads(line)
        src_count[rec.get('source', 'unknown')] += 1
print(src_count)
assert sum(src_count.values()) >= 9000, f'too few labeled: {sum(src_count.values())}'
assert src_count.get('ood', 0) >= 1500, f'too few ood: {src_count}'
assert src_count.get('targeted', 0) >= 800, f'too few targeted: {src_count}'
print('OK')
"</automated>
  </verify>
  <done>
    - data/labeled.jsonl 行数 ≥ 9000
    - 三源标注 ≥ 7000(id) + 1500(ood) + 800(targeted)
    - runs/{ts}/teacher_cost.json estimated_usd_total ≤ $80
    - runs/{ts}/teacher_reject_stats.json 含 reject_kinds 计数
    - .planning/phases/08/08-03-LABEL-RESULT.md 记录全部数字
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI API ↔ TeacherClient | 网络边界；返回值需校验 |
| TeacherClient → constraint_lint | 教师 JSON 进入 lint 必须 schema-safe |
| raw_responses/ cache | 文件信任；但跨 phase 协议变更可能污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Spoofing | OPENAI_BASE_URL 指向 v1.0 proxy | mitigate | Task 1 smoke probe 验证；checkpoint:decision 提供 fallback |
| T-08-07 | Information Disclosure | OPENAI_API_KEY 泄漏到日志/cache | mitigate | client.py 已不写 key 到 raw；cost.json 仅 token 数；plan 不打印 env |
| T-08-08 | DoS (钱包) | 失控并发或 prompt 改变导致 cache 全 miss | mitigate | smoke 50 → 红线判定；prompt_builder.py 锁定不改；max_workers=10 上限 |
| T-08-09 | Tampering | 教师返回非法 JSON | mitigate | parse_assistant_output 已 catch + lint 拒；rejected.jsonl 记录 |
| T-08-10 | Repudiation | run 中途崩溃数据丢失 | mitigate | jsonl flush after each line（labeler.py 现有）+ atomic cache rename |
</threat_model>

<verification>
- 1-sample smoke + cache 协议审计先于全量标注（Task 1 必须 pass 或经 Task 2 checkpoint 决策）
- smoke 50 红线（reject_rate ≤ 10%, cost/sample ≤ $0.05）通过
- 全量 labeled.jsonl ≥ 9000 行，cost ≤ $80
- runs/{ts}/teacher_cost.json + reject_stats.json 落盘
</verification>

<success_criteria>
- DATA-01：10K 候选 → ≥ 9000 通过 lint 的标注样本（三源构成符合 7:2:1 ± 余量）
- DATA-03：所有 lint-pass 样本可作为 Plan 04 split 的输入
- 续跑可从任意中断点恢复（jsonl flush + cache hit 已验证）
- 协议风险（cache 旧标签 / proxy 失效）在花钱前被发现并由 checkpoint 决策
</success_criteria>

<output>
完成后创建 .planning/phases/08/08-03-SUMMARY.md（含 cache audit 结果、smoke 数字、全量 cost 与 reject 拆分）
</output>
