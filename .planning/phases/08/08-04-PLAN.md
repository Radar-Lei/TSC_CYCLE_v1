---
phase: 08
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - scripts/run_pipeline.sh
  - data/dataset_manifest.json
  - data/dataset_report.md
  - data/split_manifest.json
  - data/dataset_card.md
  - data/tokenized/
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "run_pipeline.sh phase 2 调用 sample_inputs 三源采样"
    - "run_pipeline.sh phase 4 调用 dist_check 含 targeted 段"
    - "data/dataset_manifest.json 写入三源比例 + seeds + sha256"
    - "data/dataset_report.md 含 5 个 section（Source/KS/Boundary/v1.0 Comparison/Targeted）"
    - "data/split_manifest.json 含 input_version + labeled_version + seeds + splits"
    - "user 通过 checkpoint 验证报告内容（v1.0 对比可读、targeted 防泄漏 0）"
  artifacts:
    - path: "scripts/run_pipeline.sh"
      provides: "v2.0 端到端 pipeline（sample 三源 + 标注续跑 + dataset build + 报告）"
    - path: "data/dataset_manifest.json"
    - path: "data/dataset_report.md"
    - path: "data/split_manifest.json"
    - path: "data/tokenized/{train,val_id,val_ood}/data.parquet"
  key_links:
    - from: "scripts/run_pipeline.sh"
      to: "tsc_cycle.sample_inputs + teacher.labeler + scripts/dist_check + tsc_cycle.student.dataset"
      via: "顺序 bash 调用"
      pattern: "python -m tsc_cycle"
---

<objective>
把 Plan 01-03 的产物组装为端到端 v2.0 数据扩容 pipeline：更新 `scripts/run_pipeline.sh` 调用三源采样 → 教师标注 → dist_check → dataset build (含 dataset_report + split_manifest + dataset_manifest)，并由用户在 checkpoint 验证最终报告与 split metadata。

Purpose: 收尾 DATA-01/02/03，让一键复现成为脚本而非 5 个手工命令。Phase 9 的训练只需读取 data/tokenized/ + split_manifest.json 即可启动。

Output: 更新后的 run_pipeline.sh + 三个数据制品（dataset_manifest / dataset_report / split_manifest）+ 用户 checkpoint 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08/08-RESEARCH.md
@CLAUDE.md

@scripts/run_pipeline.sh
@.planning/phases/08/08-01-PLAN.md
@.planning/phases/08/08-02-PLAN.md
@.planning/phases/08/08-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 run_pipeline.sh + 写 dataset_manifest 入口 + 跑端到端 build</name>
  <files>
    scripts/run_pipeline.sh
  </files>
  <action>
    1. Read scripts/run_pipeline.sh 当前 phases。v2.0 调整为：
       - phase 1: dist_prior fit（v1.0 已就位 → skip if exists）
       - phase 2: sample_inputs 三源（id 7000 / ood 2000 / targeted 1000；--seed 42 --targeted-seed 1337）
       - phase 3: build_dataset_manifest 写 data/dataset_manifest.json（独立小步骤，调用 python -c "from tsc_cycle.manifest import build_dataset_manifest; ..."）
       - phase 4: dist_check 含 --targeted-inputs（输出 data/dist_check_report.md）
       - phase 5: teacher labeler（含 --targeted-inputs；smoke 50 → full）
       - phase 6: student.dataset main()（产 data/tokenized/* + dataset_report.md + split_manifest.json + dataset_card.md）
       - phase 7: final report（cat data/dataset_report.md 末段；echo final 数字）

    2. 添加防误操作：
       - 在 phase 2 执行前 `cp data/inputs.jsonl{,.v1bak}; cp data/ood_inputs.jsonl{,.v1bak}` 若已存在
       - 每 phase 末尾打印 ✓ 或 ✗ 状态行

    3. **执行 pipeline**（如果 Plan 03 已跑完全量标注，本 task 只需跑 phase 6-7）:
       - 如果 data/labeled.jsonl 已 ≥ 9000（Plan 03 产物）：直接 `python -m tsc_cycle.student.dataset` 跑 build；产生 dataset_report.md + split_manifest.json + tokenized parquets
       - 如果 dataset_manifest.json 尚未生成：跑 phase 3 入口
       - 跑完后做 health check:
         - jq 检查 data/dataset_manifest.json schema_version=1
         - jq 检查 data/split_manifest.json input_version 含 4 个 sha256
         - grep "v1.0 Comparison" data/dataset_report.md
         - grep "98.7%" data/dataset_report.md（v1.0 baseline 数字）
         - test -f data/tokenized/train/data.parquet
         - test -f data/tokenized/val_id/data.parquet
         - test -f data/tokenized/val_ood/data.parquet

    4. 把全部数字汇总到 .planning/phases/08/08-04-FINAL-NUMBERS.md：
       - n_labeled / split sizes / KS p 表 / reject_rate 拆分 / cost / wall time
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && jq -e '.schema_version == 1' data/dataset_manifest.json && jq -e '.input_version | keys | length >= 4' data/split_manifest.json && grep -q "v1.0 Comparison" data/dataset_report.md && grep -q "98.7%" data/dataset_report.md && test -f data/tokenized/train/data.parquet && test -f data/tokenized/val_id/data.parquet && test -f data/tokenized/val_ood/data.parquet && echo "OK"</automated>
  </verify>
  <done>
    - scripts/run_pipeline.sh 包含 7 个 phase，可一键从 sample 跑到 dataset build
    - dataset_manifest.json / dataset_report.md / split_manifest.json / tokenized/* 全部生成
    - dataset_report.md 含 5 个 section + v1.0 baseline 数字
    - .planning/phases/08/08-04-FINAL-NUMBERS.md 记录全部关键指标
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 8 完整数据扩容 pipeline：
    - data/inputs.jsonl (7000 id) + ood_inputs.jsonl (2000) + targeted_inputs.jsonl (1000)
    - data/labeled.jsonl ≥ 9000 + rejected.jsonl
    - data/dataset_manifest.json（三源比例 + seeds + sha256）
    - data/dataset_report.md（DATA-02：5 sections，含 v1.0 对比）
    - data/split_manifest.json（DATA-03：input_version + labeled_version + seeds + splits + reproduction）
    - data/tokenized/{train,val_id,val_ood}/data.parquet
  </what-built>
  <how-to-verify>
    1. 打开 data/dataset_report.md，确认：
       - "Source Composition" 表：id ≈ 7000、ood ≈ 2000、targeted ≈ 1000，reject_rate 每源 ≤ 5%
       - "Field Distribution" 表：id KS p > 0.05；ood KS p < 0.001；targeted KS p < 0.01
       - "Boundary Coverage" 表：phase_count ∈ {2,6,7,8} 累计 ≥ 200 样本（v1.0 是 29，应 6×+）
       - "v1.0 Comparison" 表：v2.0 ood_labeled ≥ 1500（v1.0 是 300）
       - "Targeted Sample Source"：seed_overlap_count == 0（防泄漏）

    2. 打开 data/split_manifest.json，确认：
       - input_version.{inputs,ood_inputs,targeted_inputs,dist_prior}_jsonl_sha256 都非空
       - labeled_version.labeled_n ≥ 9000
       - seeds.sample_inputs_seed=42, targeted_seed=1337
       - splits.train.n + val_id.n + val_ood.n ≈ labeled_n
       - reproduction.command 可读、git_commit 非空

    3. 跑 `cat .planning/phases/08/08-04-FINAL-NUMBERS.md` 通读 → 确认 cost ≤ $80, wall time 合理（2-5h）

    4. 跑 `pytest tests/ -x -q` → 期望全绿（含 Plan 01/02 新增的测试 + v1.0 现有测试）
  </how-to-verify>
  <resume-signal>
    输入 "approved" 或描述具体问题（例如 "ood KS p 不够低 / targeted seed 重叠 N 个 / dataset_report 缺 Boundary 表"）
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline shell ↔ python modules | 由用户启动；shell 内变量信任 |
| 最终 jsonl/parquet → Phase 9 训练 | 必须 schema 稳定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | run_pipeline.sh 误覆盖 v1.0 inputs.jsonl | mitigate | Task 1 强制 cp .v1bak 备份 |
| T-08-12 | Repudiation | dataset 重建后无法溯源 | mitigate | dataset_manifest + split_manifest 含 git_commit + sha256 |
</threat_model>

<verification>
- 端到端跑通 scripts/run_pipeline.sh（或单步执行 phase 6-7 if labeling 已完成）
- 6 个 health check（jq + grep + test -f）全 pass
- 用户 checkpoint 通过
</verification>

<success_criteria>
- DATA-01: data/inputs/ood_inputs/targeted_inputs.jsonl 三源 + dataset_manifest.json 比例可读
- DATA-02: data/dataset_report.md 5 sections + v1.0 对比 + 边界覆盖表
- DATA-03: data/split_manifest.json schema 完整 + train/val_id/val_ood parquets 决定性可复现
- pytest 全绿、用户 checkpoint approve
- Phase 9 可直接读取 data/tokenized/* + split_manifest.json 启动训练
</success_criteria>

<output>
完成后创建 .planning/phases/08/08-04-SUMMARY.md（汇总 Phase 8 全部 4 个 plan 的产物 + 数字 + 给 Phase 9 的交付清单）
</output>
