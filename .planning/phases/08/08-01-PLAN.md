---
phase: 08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/sample_inputs.py
  - tsc_cycle/manifest.py
  - tests/conftest.py
  - tests/test_sample_inputs.py
  - tests/test_split_manifest.py
  - tests/test_labeler_resume.py
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "sample_inputs.py 在同一 seed 下重跑产出完全相同 sample_id 集合"
    - "sample_targeted() 从 v1.0 per_sample.jsonl 读取失败种子后产出新 sample_id (与 v1.0 OOD val_ood 不重叠)"
    - "manifest.build_dataset_manifest() 写出含三源比例 + seeds + input/labeled 版本哈希的 JSON"
    - "manifest.build_split_manifest() schema 包含 input_version/labeled_version/seeds/splits/reproduction 五段"
    - "labeler 续跑（mocked client）跳过已记录在 labeled.jsonl/rejected.jsonl 的 sample_id"
  artifacts:
    - path: "tsc_cycle/sample_inputs.py"
      provides: "sample_targeted() + main() 接受 --targeted-eval-path / --n-targeted / --targeted-seed"
      contains: "def sample_targeted"
    - path: "tsc_cycle/manifest.py"
      provides: "build_dataset_manifest() + build_split_manifest()"
      contains: "def build_dataset_manifest"
    - path: "tests/conftest.py"
      provides: "mini_dist_prior / mini_eval_per_sample / mini_labeled_jsonl fixtures"
    - path: "tests/test_sample_inputs.py"
      provides: "三源比例 + 决定性 + targeted 防泄漏 + targeted 种子 provenance 单测"
    - path: "tests/test_split_manifest.py"
      provides: "split_manifest schema 单测"
    - path: "tests/test_labeler_resume.py"
      provides: "labeler 续跑跳过 done sample_id 单测（mock client）"
  key_links:
    - from: "tsc_cycle/sample_inputs.py::sample_targeted"
      to: "runs/20260507T032419Z/eval/per_sample.jsonl"
      via: "json.loads + lint_ok=False ∪ mae>thresh 过滤"
      pattern: "per_sample.jsonl"
    - from: "tsc_cycle/manifest.py::build_split_manifest"
      to: "data/inputs.jsonl + ood_inputs.jsonl + targeted_inputs.jsonl + labeled.jsonl"
      via: "sha256 文件指纹"
      pattern: "sha256_hex"
---

<objective>
为 Phase 8 数据扩容打底：扩展 `sample_inputs.py` 支持 v1.0 失败样本回放（targeted 源），将 `manifest.py` 占位实现为 `build_dataset_manifest()` 与 `build_split_manifest()`，并落地 Wave 0 测试脚手架（DATA-01、DATA-03 单测）。

Purpose: DATA-01（三源采样）+ DATA-03（split metadata 可复现）的代码骨架与可执行单测必须先于网络重操作（教师标注）就位，以便在不烧钱的前提下验证决定性、防泄漏、续跑正确性。

Output: 可单测通过的 sample_targeted/manifest 模块 + 5 个新测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-CONTEXT.md
@CLAUDE.md

# 已存在源（不可改的锚点）
@tsc_cycle/sample_inputs.py
@tsc_cycle/manifest.py
@tsc_cycle/hashing.py
@tsc_cycle/teacher/labeler.py
@runs/20260507T032419Z/eval/per_sample.jsonl

<interfaces>
<!-- 从既有代码提取的关键 API；executor 不需再 grep -->

From tsc_cycle/hashing.py:
```python
def sample_id(input_dict: dict) -> str        # sha256 of canonical_json
def canonical_json(obj) -> str
def sha256_hex(s: str | bytes) -> str
```

From tsc_cycle/sample_inputs.py (现有):
```python
def sample_in_distribution(rng, prior, n) -> list[dict]   # split_hint="id"
def sample_ood(rng, prior, n) -> list[dict]               # split_hint="ood"
def main(argv) -> None                                    # CLI: --n-id --n-ood --seed --out-id --out-ood
```

From tsc_cycle/teacher/labeler.py (续跑核心):
```python
def _read_done_ids(labeled: Path, rejected: Path) -> set[str]
# main(): pending = [s for s in all_inputs if s["sample_id"] not in done]
# ThreadPoolExecutor(max_workers=args.max_workers, default=10)
```

From tsc_cycle/manifest.py (现有占位):
```python
def git_sha() -> str
def canonical_json, sha256_hex (re-exported)
# build_dataset_manifest / build_split_manifest 尚未实现
```

v1.0 per_sample.jsonl 行结构（从 RESEARCH.md §"v1.0 失败样本回放"）:
```json
{"sample_id": "...", "backend": "hf_bf16|gguf_bf16|gguf_q4_k_m",
 "split_hint": "id|ood", "lint_ok": true|false, "violations": [...],
 "mae": 7.7, "exact_match": true|false, ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 测试脚手架 + 三源采样决定性 + targeted 防泄漏单测</name>
  <files>
    tests/conftest.py,
    tests/test_sample_inputs.py,
    tests/test_split_manifest.py,
    tests/test_labeler_resume.py
  </files>
  <behavior>
    conftest.py 导出 fixtures:
      - mini_dist_prior: dict（5 个 phase modes，覆盖 phase_count {3,4,5}，wait/saturation 字段足够支撑 KS 测试 100 样本）
      - mini_eval_per_sample(tmp_path): 写出 ~10 行 v1.0 风格 per_sample.jsonl，含 lint_ok=False(2)、mae>10(3)、healthy(5)
      - mini_inputs_jsonl(tmp_path): 写出 ~5 个 OOD 输入 + 5 个 id 输入，sample_id 真实 hash
      - mini_labeled_jsonl(tmp_path): 写出 ~6 行 labeled.jsonl，含 v2.0 协议 SOLUTION JSON

    test_sample_inputs.py:
      - test_three_source_proportions: --n-id 70 --n-ood 20 --n-targeted 10 → 实际生成 70/20/10，split_hint 字段正确
      - test_deterministic: 同 seed 重跑 sample_in_distribution + sample_ood + sample_targeted 产出完全相同 sample_id 集合（DATA-03）
      - test_targeted_seed_provenance: sample_targeted 输出的每个样本的 source_seed_id 都 ∈ (lint_ok=False ∪ mae>thresh)
      - test_targeted_no_leak: targeted 样本的 sample_id 与 mini_eval_per_sample 中所有 sample_id 不重叠
      - test_targeted_input_schema_unchanged: targeted 输出 dict 的 keys 与 sample_in_distribution 输出 dict 的 keys 完全一致（防 cache 失效，per RESEARCH §"关键续跑约束"）

    test_split_manifest.py:
      - test_schema: build_split_manifest 输出含 schema_version, input_version{4 个 sha256}, labeled_version{sha256+n}, seeds, splits{train,val_id,val_ood}, reproduction{command,git_commit,teacher_model,teacher_effort}
      - test_idempotent: 同输入文件重跑产出相同 sha256（决定性）

    test_labeler_resume.py:
      - test_skip_done: mock client + 6 个输入 + labeled.jsonl 已含 4 个 + rejected.jsonl 已含 1 个 → labeler 只对剩余 1 个发起 client 调用
  </behavior>
  <action>
    1. 创建 tests/conftest.py，注册上述 fixtures。fixture 用 tmp_path 构造文件路径；mini_dist_prior 直接构造 dict 不读真实 reality.log。
    2. 创建 tests/test_sample_inputs.py（5 个测试，覆盖 DATA-01 比例 + DATA-03 决定性 + targeted 防泄漏 + 种子 provenance + schema 不变）。导入将在 Task 2 实现的 sample_targeted；先写测试再写实现（RED → GREEN）。
    3. 创建 tests/test_split_manifest.py（schema + idempotent，2 个测试）。
    4. 创建 tests/test_labeler_resume.py，使用 unittest.mock.patch 拦截 tsc_cycle.teacher.labeler 中的 TeacherClient.label 方法，断言只对未 done 的 sample_id 调用。注意：labeler.py 已经有 _read_done_ids，测试只需提供已 done 的 jsonl。
    5. 跑 `pytest tests/test_sample_inputs.py tests/test_split_manifest.py tests/test_labeler_resume.py -x` 期望全部 RED（因为 Task 2/3 实现未就位），记录失败用例数。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/conftest.py tests/test_sample_inputs.py tests/test_split_manifest.py tests/test_labeler_resume.py --collect-only -q | grep -v '^#' | grep -c 'test_' </automated>
  </verify>
  <done>
    - 4 个测试文件存在，pytest --collect-only 显示至少 8 个测试函数被收集
    - 测试当前应 fail/error（实现未就位），不能因语法/import 错误 collect-fail
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 sample_targeted() 和 sample_inputs.main 扩展</name>
  <files>
    tsc_cycle/sample_inputs.py
  </files>
  <behavior>
    在 tsc_cycle/sample_inputs.py 末尾追加：

    def load_failure_seeds(eval_path: Path, mae_thresh: float = 5.0) -> set[str]:
      读 per_sample.jsonl，返回 lint_ok=False ∪ mae>thresh 的 sample_id 集合。

    def sample_targeted(
        rng: random.Random,
        prior: dict,
        eval_path: Path,
        v1_inputs_paths: list[Path],   # [data/inputs.jsonl, data/ood_inputs.jsonl]
        n_targeted: int = 1000,
        perturbation_ratio: float = 0.1,
        mae_thresh: float = 5.0,
    ) -> list[dict]:
      返回 list of input dict（与 sample_in_distribution 同 schema），每个 dict 标 split_hint="id"
      （因为 targeted 进训练，不进 val_ood，per RESEARCH §"为什么 split_hint=id"）。
      每个 dict 增加 source="targeted" 和 source_seed_id=<v1.0 失败种子>（用于 provenance 测试）。

    扰动策略（per RESEARCH §"邻域扰动策略"）：
      - 字段轻扰: pred_wait *= U(1-r, 1+r); pred_saturation *= U(1-r, 1+r) clip [0,1]
      - 范围扰: min_green ± 2; max_green ± 5（保 min<max 且 ≥ 5/≥ 10）
      - 整数字段保持 int

    扩展 main()：
      - 新增参数: --targeted-eval-path, --n-targeted (default 0; 0=不生成 targeted 文件),
                 --targeted-seed (default 1337), --out-targeted (default data/targeted_inputs.jsonl),
                 --v1-inputs-paths (nargs='+', default [data/inputs.jsonl, data/ood_inputs.jsonl]),
                 --mae-thresh (default 5.0), --perturbation-ratio (default 0.1)
      - 在 sample_in_distribution 和 sample_ood 之后，若 --n-targeted > 0 则调用 sample_targeted 写出 --out-targeted
      - 所有三个输出文件每行一个 input dict（与现有格式一致）

    实现要点：
      - 必须在所有现有 input dict 上加 "source" 字段（id/ood/targeted），用于下游 manifest 统计。注意：source 字段会改变 sample_id 哈希——所以必须先用现有 schema 算 sample_id，再附加 source 字段（或 source 不进 sample_id 输入字典）。**正确做法：sample_id 只对实际 input 字段算，source 是 sidecar metadata**。检查 hashing.sample_id 现签名，若它对整个 dict 算 hash，则 source 不能进入该 dict——改为 yield (input_dict, source) 元组，由调用方决定是否合并；或在 sample_id 计算时显式排除 source/source_seed_id/split_hint key。
      - 现有 sample_in_distribution 已写出 split_hint，所以排除 list 已存在。新增 source / source_seed_id 加入排除即可。
  </behavior>
  <action>
    1. 读 tsc_cycle/sample_inputs.py 现有实现，确认 sample_id 计算路径（应该用 hashing.sample_id(input_no_meta_dict)）和 split_hint 是如何写入的。
    2. 实现 load_failure_seeds + sample_targeted，按上面 behavior 严格遵循。targeted 样本的 sample_id 必须与 source_seed_id 不同（因扰动后 input 变了）。在生成时显式 assert id != seed_id。
    3. 扩展 main() argparser；保持向后兼容（--n-targeted 默认 0）。
    4. 在 sample_targeted 内部按 (n_targeted // n_seeds) + 余数策略均匀分配每个 seed 的扰动数量。
    5. 跑 `pytest tests/test_sample_inputs.py -x -q`，目标全绿。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/test_sample_inputs.py -x -q</automated>
  </verify>
  <done>
    - sample_targeted 函数存在并被 main() 在 --n-targeted > 0 时调用
    - tests/test_sample_inputs.py 全部 5 个测试通过
    - 手测：python -m tsc_cycle.sample_inputs --n-id 0 --n-ood 0 --n-targeted 5 --targeted-eval-path runs/20260507T032419Z/eval/per_sample.jsonl --v1-inputs-paths data/inputs.jsonl data/ood_inputs.jsonl --out-targeted /tmp/targeted_smoke.jsonl 产出 5 行 jsonl，每行 source=targeted
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 manifest.build_dataset_manifest + build_split_manifest + labeler 续跑回归</name>
  <files>
    tsc_cycle/manifest.py
  </files>
  <behavior>
    在 tsc_cycle/manifest.py 内（保留现有 git_sha/canonical_json/sha256_hex），实现：

    def build_dataset_manifest(
        inputs_jsonl: Path,
        ood_inputs_jsonl: Path,
        targeted_inputs_jsonl: Path | None,
        dist_prior_path: Path,
        out_path: Path,
        seeds: dict,                 # {"sample_inputs_seed": 42, "targeted_seed": 1337}
    ) -> dict:
      返回并写入 dict:
      {
        "schema_version": 1,
        "generated_at": ISO 8601 UTC,
        "git_commit": git_sha(),
        "sources": {
          "id":       {"path": ..., "n": int, "sha256": ...},
          "ood":      {"path": ..., "n": int, "sha256": ...},
          "targeted": {"path": ..., "n": int, "sha256": ...} | null,
        },
        "ratios": {"id": 0.7, "ood": 0.2, "targeted": 0.1},   # 来自实际 n
        "seeds": seeds,
        "dist_prior_sha256": ...,
      }

    def build_split_manifest(
        inputs_jsonl: Path,
        ood_inputs_jsonl: Path,
        targeted_inputs_jsonl: Path | None,
        dist_prior_path: Path,
        labeled_jsonl: Path,
        splits: dict,                # {"train": {"n": ..., "id_count": ..., "targeted_count": ..., "ood_count": 0}, ...}
        seeds: dict,
        out_path: Path,
        teacher_model: str = "gpt-5.5",
        teacher_effort: str = "high",
        reproduction_command: str | None = None,
    ) -> dict:
      按 RESEARCH.md §"Split Metadata 字段" schema 写出。input_version 含 4 个 sha256（含 targeted 可为 null），labeled_version 含 sha256+n。

    辅助：file_sha256(p: Path) -> str；空/缺失文件返回 sha256("")。
  </behavior>
  <action>
    1. 实现两个函数 + file_sha256 helper。schema 严格按 RESEARCH §"Split Metadata 字段"。
    2. 跑 `pytest tests/test_split_manifest.py tests/test_labeler_resume.py -x -q`。注意 test_labeler_resume 不需要改 labeler.py（其续跑逻辑已存在）；只需用 mock 验证现有行为。
    3. 整套回归：`pytest tests/ -x -q` 必须全绿（包含 v1.0 已有的 test_constraint_lint / test_hashing / test_prompt_builder）。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/ -x -q</automated>
  </verify>
  <done>
    - manifest.build_dataset_manifest / build_split_manifest 实现并通过 schema 单测
    - test_labeler_resume.py 通过（验证 _read_done_ids 续跑逻辑）
    - pytest tests/ 全绿
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → Python 进程 | per_sample.jsonl / inputs.jsonl 内容信任 (本地产物) |
| 测试 fixture → 测试代码 | tmp_path 隔离，测试间无状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | sample_targeted 读 per_sample.jsonl | mitigate | json.loads 在 try/except 内；非法行跳过；在测试中加 malformed-line fixture |
| T-08-02 | Information Disclosure | dataset_manifest.json 落盘 | accept | 仅含哈希、文件名、计数；无 API key |
| T-08-03 | Tampering | sample_id 与 source 字段交叉污染（cache 击穿） | mitigate | 测试 test_targeted_input_schema_unchanged 强制 schema 不变；source 字段为 sidecar 不进 sample_id |
</threat_model>

<verification>
- pytest tests/ -x -q 全绿
- python -m tsc_cycle.sample_inputs --help 显示新增参数
- python -c "from tsc_cycle.manifest import build_dataset_manifest, build_split_manifest" 不报错
</verification>

<success_criteria>
- DATA-01 测试覆盖到位（三源比例 + targeted 防泄漏 + 种子 provenance）
- DATA-03 测试覆盖到位（决定性 + manifest schema + labeler 续跑）
- 不触发任何网络调用；零 OpenAI cost
- 所有新代码不修改 prompt_builder.py / constraint_lint.py / teacher/client.py / hashing.py（cache hit 命脉）
</success_criteria>

<output>
完成后创建 .planning/phases/08/08-01-SUMMARY.md
</output>
