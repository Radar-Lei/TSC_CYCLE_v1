---
phase: 17-audit-saturation-policy-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/v4_gates/saturation_policy.py
  - tests/test_v4_phase17_saturation_policy.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - POLICY-01
must_haves:
  truths:
    - "Maintainer can classify every phase decision into the required saturation band intervals."
    - "Maintainer can quantify v4 teacher final==max_green while pred_saturation<1.0 by band, split, and source."
    - "Maintainer can inspect deterministic representative dataset and replay failures with required phase-level fields."
  artifacts:
    - path: "tsc_cycle/v4_gates/saturation_policy.py"
      provides: "Canonical saturation band classifier, per-phase projectors, audit aggregation, representative examples"
      exports: ["classify_saturation_band", "classify_violation", "project_dataset_phase_decisions", "project_replay_phase_decisions", "compute_saturation_audit"]
    - path: "tests/test_v4_phase17_saturation_policy.py"
      provides: "Phase 17 classifier, projection, audit, denominator, and representative-example contract tests"
      contains: "test_saturation_band_boundaries"
  key_links:
    - from: "tsc_cycle/v4_gates/saturation_policy.py"
      to: "data/v4/phase8/labeled_merged.jsonl"
      via: "project_dataset_phase_decisions reads sample input/result.solution rows"
      pattern: "project_dataset_phase_decisions"
    - from: "tsc_cycle/v4_gates/saturation_policy.py"
      to: "artifacts/v4/phase12/manifest.json + per_sample.jsonl"
      via: "project_replay_phase_decisions aligns records and outputs by sample_id"
      pattern: "project_replay_phase_decisions"
    - from: "compute_saturation_audit"
      to: "classify_saturation_band"
      via: "all band/source/split denominators use the canonical classifier"
      pattern: "classify_saturation_band"
---

<objective>
Create the canonical offline saturation-policy core for Phase 17.

Purpose: Put all band classification, phase-decision projection, denominator handling, audit aggregation, and representative failure extraction behind one tested module so later CLI/data/eval/replay gates cannot drift.
Output: `tsc_cycle/v4_gates/saturation_policy.py` plus Phase 17 tests covering AUDIT-01, AUDIT-02, and POLICY-01.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-audit-saturation-policy-gate/17-CONTEXT.md
@.planning/phases/17-audit-saturation-policy-gate/17-RESEARCH.md
@.planning/phases/17-audit-saturation-policy-gate/17-VALIDATION.md
@.planning/phases/17-audit-saturation-policy-gate/17-PATTERNS.md
@tsc_cycle/eval/phase11_metrics.py
@tsc_cycle/eval/phase11_decision.py
@tsc_cycle/constraint_lint.py
@tsc_cycle/v4_gates/phase12_report.py
@tsc_cycle/v4_gates/phase12_reality_test.py
@tests/test_phase12_reality_log_generation.py
@tests/test_v4_phase11_eval_matrix.py
@tests/test_v4_phase8_dataset_rebuild.py

<interfaces>
Use the lightweight stdlib gate pattern from Phase 11/12: JSONL rows are dictionaries, invalid JSON/non-object rows raise or produce fatal evidence, and report payloads expose counts instead of hiding exclusions.
Use `tsc_cycle.constraint_lint.validate(prediction_input, output)` before policy interpretation when full sample input/output is available.
Use per-phase triviality as `min_green == max_green`; report these rows separately and exclude them from low-saturation max-green threshold denominators by default.
Do not import torch, transformers, peft, bitsandbytes, vllm, flash_attn, OpenAI SDKs, or training code in Phase 17 modules/tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical saturation bands and violation categories</name>
  <read_first>
    @tsc_cycle/v4_gates/saturation_policy.py if it already exists
    @tests/test_v4_phase17_saturation_policy.py if it already exists
    @.planning/REQUIREMENTS.md lines covering POLICY-01
    @.planning/phases/17-audit-saturation-policy-gate/17-RESEARCH.md sections Phase Requirements, Common Pitfalls, and Validation Architecture
    @.planning/phases/17-audit-saturation-policy-gate/17-PATTERNS.md sections saturation_policy.py and tests/test_v4_phase17_saturation_policy.py
    @tsc_cycle/eval/phase11_decision.py for finite numeric checks and fail-closed helper style
    @tsc_cycle/constraint_lint.py for hard-constraint and triviality naming conventions
  </read_first>
  <files>tsc_cycle/v4_gates/saturation_policy.py, tests/test_v4_phase17_saturation_policy.py</files>
  <behavior>
    - `classify_saturation_band(0.1999)` returns the near-min band; `0.2` starts the interpolated band.
    - `classify_saturation_band(0.5999)` remains interpolated; `0.6` starts high-not-max.
    - `classify_saturation_band(0.9999)` remains high-not-max; `1.0` starts allowed-max.
    - Non-finite or non-numeric saturation values are never silently coerced into a passing band.
    - `classify_violation(...)` identifies low-saturation max-green violations, allowed saturated max-green decisions, and forced trivial ranges.
  </behavior>
  <action>Create `tsc_cycle/v4_gates/saturation_policy.py` as a lightweight stdlib module with explicit band label constants, `REQUIREMENTS_COVERED`, `classify_saturation_band(sat)`, finite-float validation, per-phase trivial detection, and `classify_violation(row)` using the Phase 17 intervals from POLICY-01. Follow the explicit analogs in 17-PATTERNS: use `tsc_cycle/eval/phase11_decision.py` for `_finite_float`-style numeric validation, threshold/fail-closed helper naming, and non-finite rejection, and use `tsc_cycle/eval/phase11_metrics.py` for lightweight stdlib imports and dictionary-row metric inputs. Add lazy-import tests in `tests/test_v4_phase17_saturation_policy.py` that forbid heavyweight model/training imports and assert all boundary cases. Use Claude discretion from 17-CONTEXT by choosing stable machine-readable labels, and document that forced ranges are reported separately rather than counted as policy failures.</action>
  <acceptance_criteria>
    - Source contains exactly one canonical band classifier function named `classify_saturation_band`.
    - Tests cover `0.1999`, `0.2`, `0.5999`, `0.6`, `0.9999`, `1.0`, non-finite saturation, and forced `min_green == max_green` rows.
    - No Phase 17 module imports GPU/model/training dependencies.
    - `REQUIREMENTS_COVERED` includes `POLICY-01`.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase17_saturation_policy.py::test_saturation_band_boundaries -q</automated>
  </verify>
  <done>POLICY-01 band classification and violation-category semantics are implemented once and tested at exact half-open boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project dataset and replay artifacts into per-phase audit rows</name>
  <read_first>
    @tsc_cycle/v4_gates/saturation_policy.py
    @tests/test_v4_phase17_saturation_policy.py
    @data/v4/phase8/labeled_merged.jsonl only sample a few rows; do not load the whole file into the plan context
    @data/v4/phase8/splits/*.index.jsonl only inspect schema as needed
    @artifacts/v4/phase12/manifest.json
    @artifacts/v4/phase12/per_sample.jsonl only sample a few rows; do not load the whole file into the plan context
    @tsc_cycle/v4_gates/phase12_reality_test.py for structured replay evidence shape
    @tsc_cycle/v4_gates/phase12_report.py for sample/output alignment and fail-closed evidence patterns
    @.planning/phases/17-audit-saturation-policy-gate/17-PATTERNS.md sections Per-Phase Decision Projection and Phase 12 structured evidence pattern
  </read_first>
  <files>tsc_cycle/v4_gates/saturation_policy.py, tests/test_v4_phase17_saturation_policy.py</files>
  <behavior>
    - Dataset rows become one row per `input.prediction.phase_waits[*]` phase decision with sample id, phase id, saturation, min/max green, final green, split, source, and origin artifact.
    - Split attribution prefers Phase 8 split index membership over ambiguous source hints while preserving source/source_origin separately.
    - Replay rows align Phase 12 manifest records with per-sample outputs by sample id/order and fail closed on missing/mismatched evidence.
    - Malformed JSONL rows, non-object rows, missing solution fields, and hard-constraint-invalid rows are surfaced as exclusions or fatal projection errors, not silent passes.
  </behavior>
  <action>Add JSONL/object loading helpers, split-index loading, `project_dataset_phase_decisions(...)`, and `project_replay_phase_decisions(...)` to `saturation_policy.py`. Implement these with the `tsc_cycle/eval/phase11_metrics.py` analog patterns for `_read_jsonl` object validation, per-row projection into dictionaries, and explicit aggregation inputs; implement replay mismatch and malformed-row handling with the `tsc_cycle/eval/phase11_decision.py` fail-closed style for fatal evidence rather than silent skips. The dataset projector must read `result.solution` or the existing parsed solution shape used in v4 artifacts, validate full-sample outputs with `constraint_lint.validate` when possible, and emit explicit excluded/error counts for malformed or invalid rows. The replay projector must prefer Phase 12 `manifest.json` plus `per_sample.jsonl` structured evidence and include `origin_artifact` values that distinguish dataset and replay. Extend tests with tiny `tmp_path` JSONL/manifest/per-sample fixtures covering split/source attribution, sample-output mismatch, malformed row failure, hard-constraint invalid exclusion, and expected per-phase row fields.</action>
  <acceptance_criteria>
    - Every projected row includes `origin_artifact`, `sample_id`, `phase_id`, `pred_saturation`, `saturation_band`, `min_green`, `max_green`, `final_green`, `split`, `source`, `violation_category`, and `trivial_range`.
    - Dataset split/source tests prove banded stats can be broken down by split and source for AUDIT-01.
    - Replay projection tests prove Phase 12 structured evidence is used before rendered-log parsing.
    - Projection cannot hide malformed JSONL/log rows or mismatched replay evidence.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase17_saturation_policy.py::test_dataset_audit_bands_by_split_and_source -q</automated>
  </verify>
  <done>AUDIT-01 has a shared, validated per-phase evidence layer for both data and replay artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute banded statistics and representative failure examples</name>
  <read_first>
    @tsc_cycle/v4_gates/saturation_policy.py
    @tests/test_v4_phase17_saturation_policy.py
    @.planning/REQUIREMENTS.md lines covering AUDIT-01 and AUDIT-02
    @.planning/phases/17-audit-saturation-policy-gate/17-VALIDATION.md Per-Task Verification Map
    @.planning/phases/17-audit-saturation-policy-gate/17-PATTERNS.md sections Aggregation pattern, Report-build pattern, Human-Inspectable Examples, and Metric denominator manipulation
    @tsc_cycle/eval/phase11_metrics.py for aggregation/report shape analogs
    @tests/test_v4_phase8_dataset_rebuild.py for JSONL fixture helpers and gate report assertions
  </read_first>
  <files>tsc_cycle/v4_gates/saturation_policy.py, tests/test_v4_phase17_saturation_policy.py</files>
  <behavior>
    - `compute_saturation_audit(rows, example_limit=...)` returns banded counts and max-green rates by saturation band, split, source, and origin artifact.
    - Denominators distinguish total rows, included rows, trivial forced rows, malformed/excluded rows, low-saturation rows, and unsaturated final==max rows.
    - Representative examples include both dataset and replay origins when violations exist in both inputs.
    - Examples are deterministic and include all fields required by AUDIT-02.
  </behavior>
  <action>Implement `compute_saturation_audit(...)` and deterministic representative-example selection in `saturation_policy.py`. Follow `tsc_cycle/eval/phase11_metrics.py` analog patterns for `_rate`/aggregate dictionaries, nested slice report shape, and JSON-safe numeric outputs; follow `tsc_cycle/eval/phase11_decision.py` analog patterns for fail-closed treatment of missing/non-finite denominator data and gate-oriented reason fields that Plan 02 can consume. Aggregate per-phase rows into nested `bands`, `by_split`, `by_source`, `by_origin`, `excluded_counts`, and `representative_examples` sections. Ensure `final == max_green` under `pred_saturation < 1.0` is counted for audit visibility, while trivial `min_green == max_green` rows are tracked separately to prevent denominator manipulation. Extend tests to assert exact counts/rates for mixed fixtures and required example fields from both `data/v4/phase8/labeled_merged.jsonl`-style rows and `reality_test.log`/Phase 12 replay-style rows.</action>
  <acceptance_criteria>
    - Audit output includes `requirements_covered` with `AUDIT-01`, `AUDIT-02`, and `POLICY-01`.
    - Banded stats expose counts and rates for `final_equals_max_when_unsaturated` by band, split, source, and origin.
    - Representative examples contain `origin_artifact`, `sample_id`, `phase_id`, `pred_saturation`, `saturation_band`, `min_green`, `max_green`, `final_green`, `split`, `source`, and `violation_category`.
    - Tests prove malformed/excluded rows and trivial rows cannot improve denominators silently.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase17_saturation_policy.py::test_representative_failures_include_dataset_and_replay_fields -q</automated>
  </verify>
  <done>Maintainer-facing audit data can quantify mismatch rates and inspect representative failures from both required sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local JSONL/log artifacts -> audit core | Existing dataset/replay evidence may contain malformed rows, missing fields, non-finite saturation, stale sample/output alignment, or hard-constraint-invalid outputs. |
| audit core -> downstream gate reports | Aggregated counts/rates influence Phase 18 data rebuild and later eval/replay gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `classify_saturation_band` | mitigate | Exact half-open boundary tests for POLICY-01; reject non-finite saturation instead of assigning a passing band. |
| T-17-02 | Tampering | JSONL/projector helpers | mitigate | Fail closed on malformed JSONL, non-object rows, missing solutions, and invalid hard constraints; surface exclusions in audit output. |
| T-17-03 | Tampering | audit denominators | mitigate | Report total/included/trivial/excluded denominators per band/split/source; exclude forced trivial ranges from threshold denominators but keep visible counts. |
| T-17-04 | Repudiation | replay projection | mitigate | Align Phase 12 manifest records and per-sample outputs by sample id/order; treat stale/mismatched evidence as fatal projection evidence. |
| T-17-05 | Information Disclosure | saturation policy text | mitigate | Keep band rules in offline `v4_gates` module only; Plan 02 adds prompt leakage guard before final gate use. |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs planned; use Python stdlib plus existing project modules only. |
</threat_model>

<verification>
Run quick Phase 17 tests after each task:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase17_saturation_policy.py -q</automated>
Run wave-level regression after Plan 01:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase17_saturation_policy.py tests/test_phase12_reality_log_generation.py tests/test_v4_phase8_dataset_rebuild.py -q</automated>
</verification>

<success_criteria>
- POLICY-01 classifier boundaries are correct and centralized.
- AUDIT-01 band/split/source/source-origin statistics are computable from projected phase rows.
- AUDIT-02 representative examples from dataset and replay evidence expose all required fields.
- Malformed evidence, stale replay alignment, and denominator manipulation are caught by tests.
</success_criteria>

<output>
Create `.planning/phases/17-audit-saturation-policy-gate/17-01-SUMMARY.md` when done.
</output>
