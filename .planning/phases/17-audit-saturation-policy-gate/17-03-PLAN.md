---
phase: 17-audit-saturation-policy-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - tsc_cycle/v4_gates/phase17_audit.py
  - tsc_cycle/v4_gates/saturation_policy.py
  - tsc_cycle/v4_gates/fixtures/v4_prompt_protocol_golden.json
  - tests/test_v4_phase17_saturation_policy.py
  - artifacts/v4/phase17/saturation_audit_report.json
autonomous: true
gap_closure: true
requirements:
  - AUDIT-02
  - POLICY-03
must_haves:
  truths:
    - "Prompt protocol guard detects byte-for-byte deployment prompt drift using an independent frozen v4 prompt fixture/hash, not a value recomputed from build_user_prompt at import time."
    - "Phase 17 report writers remain constrained to a canonical or explicitly Phase-17-scoped artifact root, and broad roots such as the repository root cannot authorize writes to data/ or tsc_cycle/."
    - "Top-level saturation_audit_report.json representative_examples include maintainer-facing examples from both dataset and replay/reality origins when violations exist in both."
  artifacts:
    - path: "tsc_cycle/v4_gates/fixtures/v4_prompt_protocol_golden.json"
      provides: "Independent frozen v4 prompt fixture text and sha256 used by the POLICY-03 guard"
      contains: "prompt_sha256"
    - path: "tsc_cycle/v4_gates/phase17_audit.py"
      provides: "Prompt fixture loading, artifact-root validation, safe report writing, and CLI orchestration"
      exports: ["evaluate_prompt_protocol_guard", "reject_unsafe_phase17_output_path", "main"]
    - path: "tsc_cycle/v4_gates/saturation_policy.py"
      provides: "Per-origin representative example selection for top-level audit output"
      exports: ["compute_saturation_audit"]
    - path: "artifacts/v4/phase17/saturation_audit_report.json"
      provides: "Regenerated maintainer-facing audit report with dataset and replay/reality representative examples"
  key_links:
    - from: "tsc_cycle/v4_gates/phase17_audit.py"
      to: "tsc_cycle/v4_gates/fixtures/v4_prompt_protocol_golden.json"
      via: "loads frozen prompt text/hash before comparing rendered build_user_prompt output"
      pattern: "v4_prompt_protocol_golden"
    - from: "tsc_cycle/v4_gates/phase17_audit.py"
      to: "artifacts/v4/phase17/*.json"
      via: "validated Phase 17 artifact root plus per-output safe path check"
      pattern: "reject_unsafe_phase17_output_path"
    - from: "tsc_cycle/v4_gates/saturation_policy.py"
      to: "artifacts/v4/phase17/saturation_audit_report.json"
      via: "compute_saturation_audit representative_examples consumed by evaluate_phase17_audit report write"
      pattern: "representative_examples"
---

<objective>
Close the three Phase 17 verification gaps without replanning completed Plans 01-02.

Purpose: Make the prompt drift guard independent, make CLI artifact-root handling safe against broad trust roots, and ensure the committed maintainer audit report exposes both required evidence origins.
Output: One autonomous gap-closure implementation plan plus regenerated Phase 17 audit artifact.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-audit-saturation-policy-gate/17-VERIFICATION.md
@.planning/phases/17-audit-saturation-policy-gate/17-REVIEW.md
@.planning/phases/17-audit-saturation-policy-gate/17-01-SUMMARY.md
@.planning/phases/17-audit-saturation-policy-gate/17-02-SUMMARY.md
@tsc_cycle/v4_gates/phase17_audit.py
@tsc_cycle/v4_gates/saturation_policy.py
@tests/test_v4_phase17_saturation_policy.py

<interfaces>
Existing contracts from Plans 01-02:
- `compute_saturation_audit(rows, example_limit=10, excluded_counts=None)` returns `representative_examples` used by the top-level audit JSON.
- `evaluate_prompt_protocol_guard(prompt_text=None, prompt_surfaces=None)` must return `ok`, `prompt_sha256`, `expected_prompt_sha256`, `fatal_failures`, and `requirements_covered` containing `POLICY-03`.
- `reject_unsafe_phase17_output_path(path)` and `_write_json(path, payload)` are the only approved report write path gate.
- The CLI may exit nonzero because current v4 evidence intentionally fails saturation thresholds; that red policy result is not a failure of this gap closure.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Phase 17 goal requires measuring mismatch, inspecting representative failures, running offline policy gate, and preserving unchanged deployment prompt protocol. | Tasks 1-3 close the failed prompt, safe-write, and representative-example truths while preserving existing Plans 01-02. |
| REQ | AUDIT-02 requires representative examples from both `data/v4/phase8/labeled_merged.jsonl` and `reality_test.log`/replay with required fields. | Task 3 changes example selection and regenerates `saturation_audit_report.json`. |
| REQ | POLICY-03 requires deployment prompts unchanged and no explicit saturation band rule. | Task 1 replaces self-referential golden with independent frozen fixture/hash and drift regression. |
| RESEARCH | Phase 17 policy remains offline-only; final deployment prompt must not expose saturation bands. | Task 1 keeps prompt_builder unchanged and only changes guard/fixture/tests. |
| CONTEXT | Discuss was skipped; no locked decisions or deferred ideas. | No deferred items included. |
| VERIFICATION | Gap 1: self-referential prompt golden cannot detect drift. | Task 1. |
| VERIFICATION | Gap 2: broad `--artifact-root` allows writes outside Phase 17 subtree. | Task 2. |
| VERIFICATION | Gap 3: top-level representative examples omit replay/reality origin. | Task 3. |
| REVIEW | Clean code review found no additional changes required. | No extra review-driven scope added. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace self-referential prompt golden with frozen fixture and drift regression</name>
  <read_first>
    @tsc_cycle/v4_gates/phase17_audit.py lines defining `FIXTURE_PREDICTION_INPUT`, `EXPECTED_V4_PROMPT`, `EXPECTED_V4_PROMPT_SHA256`, and `evaluate_prompt_protocol_guard`
    @tests/test_v4_phase17_saturation_policy.py prompt protocol tests and import helpers
    @tsc_cycle/prompt_builder.py only as needed to confirm current `build_user_prompt` output shape; do not modify it
    @.planning/phases/17-audit-saturation-policy-gate/17-VERIFICATION.md first prompt gap
  </read_first>
  <files>tsc_cycle/v4_gates/phase17_audit.py, tsc_cycle/v4_gates/fixtures/v4_prompt_protocol_golden.json, tests/test_v4_phase17_saturation_policy.py</files>
  <behavior>
    - If `build_user_prompt(FIXTURE_PREDICTION_INPUT)` returns current v4 prompt bytes, `evaluate_prompt_protocol_guard()` passes the byte/hash check.
    - If `build_user_prompt` is monkeypatched before `tsc_cycle.v4_gates.phase17_audit` import to return different prompt bytes, `evaluate_prompt_protocol_guard()` fails with `prompt_byte_for_byte`.
    - `EXPECTED_V4_PROMPT` and `EXPECTED_V4_PROMPT_SHA256` are loaded from an independent frozen fixture file or literal fixture data, not computed by calling `build_user_prompt` at module import.
  </behavior>
  <action>Create an independent frozen prompt fixture at `tsc_cycle/v4_gates/fixtures/v4_prompt_protocol_golden.json` containing the deterministic Phase 17 fixture input identity, the exact current v4 prompt text, and its sha256. Change `phase17_audit.py` so `EXPECTED_V4_PROMPT` and `EXPECTED_V4_PROMPT_SHA256` come only from that frozen fixture and never from `build_user_prompt` at import. Keep `build_user_prompt` usage only for rendering the actual prompt under test inside `evaluate_prompt_protocol_guard`. Add a regression test that removes `tsc_cycle.v4_gates.phase17_audit` from `sys.modules`, monkeypatches or replaces `tsc_cycle.prompt_builder.build_user_prompt` before re-import, then proves the guard reports `ok=false` and a `prompt_byte_for_byte` fatal failure. Do not modify `tsc_cycle/prompt_builder.py` and do not add saturation band rules to any prompt surface.</action>
  <acceptance_criteria>
    - `grep -v '^#' tsc_cycle/v4_gates/phase17_audit.py | grep -c 'EXPECTED_V4_PROMPT = build_user_prompt'` returns `0`.
    - The frozen fixture file exists and contains a sha256 matching its stored prompt text.
    - Prompt drift before import is tested and fails the guard.
    - Existing prompt leakage tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_prompt_protocol_unchanged_and_no_band_rule /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_prompt_protocol_guard_fails_on_preimport_prompt_drift -q</automated>
  </verify>
  <done>POLICY-03 byte-for-byte prompt drift detection is independent of the implementation under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constrain artifact-root trust boundary to Phase 17 report subtree</name>
  <read_first>
    @tsc_cycle/v4_gates/phase17_audit.py `ARTIFACT_ROOT`, `reject_unsafe_phase17_output_path`, `_write_json`, `build_parser`, and `main`
    @tests/test_v4_phase17_saturation_policy.py path safety and CLI artifact-root tests
    @.planning/phases/17-audit-saturation-policy-gate/17-VERIFICATION.md artifact-root gap
  </read_first>
  <files>tsc_cycle/v4_gates/phase17_audit.py, tests/test_v4_phase17_saturation_policy.py</files>
  <behavior>
    - Default CLI writes remain under `/home/samuel/TSC_CYCLE/artifacts/v4/phase17`.
    - Explicit temporary artifact roots are accepted only when their resolved path is explicitly Phase-17-scoped, such as a path ending in `artifacts/v4/phase17`.
    - `--artifact-root /home/samuel/TSC_CYCLE` is rejected as a broad trust root and cannot authorize writes to `/home/samuel/TSC_CYCLE/data/*` or `/home/samuel/TSC_CYCLE/tsc_cycle/*`.
    - Explicit `--out`, `--audit-out`, and `--prompt-protocol-out` paths must resolve under the validated Phase 17 artifact root.
  </behavior>
  <action>Add artifact-root validation in `phase17_audit.py` before assigning the global `ARTIFACT_ROOT` in `main`. The validator must accept the canonical Phase 17 root and explicitly Phase-17-scoped alternate roots, while rejecting broad roots including the repository root, `artifacts`, `artifacts/v4`, `data`, `tsc_cycle`, and any parent traversal that escapes the validated root. Keep `_write_json` guarded by `reject_unsafe_phase17_output_path` so direct API calls and CLI calls share the same safety invariant. Extend tests to assert `--artifact-root` defaults still derive the three report paths under the provided Phase 17 root, broad repository root is rejected, and even if a caller attempts to use the repository root as the root, writes to `data/v4/phase8/labeled_merged.jsonl`, `tsc_cycle/prompt_builder.py`, and other non-Phase-17 paths are rejected.</action>
  <acceptance_criteria>
    - Path-safety tests cover broad repo root, `data/`, `tsc_cycle/`, `artifacts/v4`, traversal outside the Phase 17 root, and accepted temp paths ending in `artifacts/v4/phase17`.
    - CLI no longer broadens the trust root by assigning arbitrary `args.artifact_root` to `ARTIFACT_ROOT` without validation.
    - `_write_json` creates directories only after path validation succeeds.
    - Existing parser default and CLI exit semantics remain intact.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_phase17_report_paths_are_constrained_to_artifact_root /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_phase17_cli_rejects_broad_artifact_root -q</automated>
  </verify>
  <done>Maintainer-controlled artifact roots cannot expand Phase 17 report writes outside the intended artifact subtree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guarantee top-level representative examples cover dataset and replay origins, then regenerate audit artifact</name>
  <read_first>
    @tsc_cycle/v4_gates/saturation_policy.py `compute_saturation_audit` representative example selection
    @tsc_cycle/v4_gates/phase17_audit.py `evaluate_phase17_audit` report generation
    @tests/test_v4_phase17_saturation_policy.py representative example and integrated report tests
    @artifacts/v4/phase17/saturation_audit_report.json current top-level report shape
    @.planning/phases/17-audit-saturation-policy-gate/17-VERIFICATION.md representative-example gap
  </read_first>
  <files>tsc_cycle/v4_gates/saturation_policy.py, tests/test_v4_phase17_saturation_policy.py, artifacts/v4/phase17/saturation_audit_report.json</files>
  <behavior>
    - When unsaturated max-green violations exist for both `dataset:labeled_merged.jsonl` and `replay:phase12`, top-level `representative_examples` contains at least one example from each origin before filling remaining slots.
    - Representative examples remain deterministic and keep required AUDIT-02 fields: sample id, phase id, saturation, min/max green, final green, and violation category.
    - The committed `artifacts/v4/phase17/saturation_audit_report.json` is regenerated from current Phase 8 and Phase 12 evidence and includes both dataset and replay/reality origins at top level.
  </behavior>
  <action>Change `compute_saturation_audit` representative example selection so it groups unsaturated max-green violations by `origin_artifact`, selects deterministic per-origin coverage first, then fills the remaining `example_limit` with deterministic globally sorted violations without duplicates. Preserve the existing aggregate metrics and required example fields. Add a regression test with more than `example_limit` dataset violations before a replay violation and assert the top-level examples still include `replay:phase12` when `example_limit` is at least the number of origins. After tests pass, regenerate `artifacts/v4/phase17/saturation_audit_report.json` through the Phase 17 CLI; keep the expected red policy gate exit due existing threshold failures, but verify the regenerated top-level audit artifact includes both `dataset:labeled_merged.jsonl` and `replay:phase12` origins.</action>
  <acceptance_criteria>
    - Existing representative example field tests still pass.
    - New regression proves dataset ordering cannot exhaust the example limit before replay/reality is represented.
    - Regenerated `saturation_audit_report.json` top-level `representative_examples` contains both `dataset:labeled_merged.jsonl` and `replay:phase12` origins when current evidence has both.
    - No saturation policy thresholds are loosened to make the CLI green.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_representative_failures_include_dataset_and_replay_fields /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py::test_representative_examples_keep_replay_when_dataset_exhausts_limit -q</automated>
    <automated>/bin/bash -lc 'cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase17_audit; code=$?; test "$code" -eq 1; /home/samuel/TSC_CYCLE/.venv/bin/python -c "import json, pathlib; report=json.loads(pathlib.Path(\"/home/samuel/TSC_CYCLE/artifacts/v4/phase17/saturation_audit_report.json\").read_text()); origins={e[\"origin_artifact\"] for e in report[\"representative_examples\"]}; assert \"dataset:labeled_merged.jsonl\" in origins and \"replay:phase12\" in origins, origins"'</automated>
  </verify>
  <done>AUDIT-02 top-level maintainer report exposes representative failures from both required sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt_builder implementation -> prompt protocol guard | Runtime prompt rendering is untrusted for golden creation; it must be compared to an independent frozen fixture. |
| CLI args -> filesystem writes | Maintainer-provided roots and output paths can accidentally or maliciously target non-artifact project files. |
| local evidence -> audit report examples | Dataset and replay ordering can hide one origin from top-level representative examples if selection is purely first-N sorted rows. |
| report artifacts -> downstream phases | Phase 18+ decisions depend on prompt, path safety, and representative-example report correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | prompt golden fixture | mitigate | Store frozen prompt text/hash independently and test pre-import monkeypatched prompt drift fails `prompt_byte_for_byte`. |
| T-17-13 | Tampering | `--artifact-root` and report paths | mitigate | Validate the artifact root itself as Phase-17-scoped before global assignment; reject broad repo/data/source roots and traversal; keep per-write guard. |
| T-17-14 | Repudiation | regenerated audit artifact | mitigate | Regenerate JSON through the CLI and verify top-level origin coverage in the committed artifact. |
| T-17-15 | Information Disclosure | report writer | mitigate | Prevent report writes into source/data directories where audit JSON could overwrite project inputs or code. |
| T-17-16 | Tampering | representative example selection | mitigate | Select deterministic per-origin examples before global fill so dataset ordering cannot suppress replay/reality examples. |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs planned; use existing Python stdlib and project modules only. |
</threat_model>

<verification>
Run the targeted gap-closure test suite:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py -q</automated>
Run prior adjacent regressions:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py /home/samuel/TSC_CYCLE/tests/test_phase12_reality_log_generation.py /home/samuel/TSC_CYCLE/tests/test_v4_phase8_dataset_rebuild.py -q</automated>
Regenerate and inspect the Phase 17 audit artifact; CLI exit 1 is expected while current evidence violates policy thresholds:
<automated>/bin/bash -lc 'cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase17_audit; test "$?" -eq 1'</automated>
</verification>

<success_criteria>
- The prompt protocol guard fails on simulated pre-import prompt drift and no longer derives its expected prompt/hash from the same runtime `build_user_prompt` call being tested.
- `--artifact-root /home/samuel/TSC_CYCLE` and other broad roots cannot authorize writes to `data/`, `tsc_cycle/`, or any non-Phase-17 artifact location.
- `artifacts/v4/phase17/saturation_audit_report.json` top-level representative examples include both dataset and replay/reality origins with required AUDIT-02 fields.
- Existing Phase 17 threshold semantics remain unchanged; the current CLI may stay red because the policy gate is detecting real v4 evidence violations.
</success_criteria>

<output>
Create `.planning/phases/17-audit-saturation-policy-gate/17-03-SUMMARY.md` when done.
</output>
