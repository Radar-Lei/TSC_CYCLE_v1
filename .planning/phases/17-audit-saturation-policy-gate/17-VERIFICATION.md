---
phase: 17-audit-saturation-policy-gate
verified: 2026-05-18T07:21:38Z
status: gaps_found
score: 5/8 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Maintainer can verify that final deployment system/inference prompts remain byte-for-byte aligned with the v4 inference protocol and do not explicitly include the saturation band rule."
    status: failed
    reason: "The prompt guard locks EXPECTED_V4_PROMPT at import time from the current build_user_prompt output, so it cannot detect a changed deployment prompt; a simulated replacement prompt imported as the golden and evaluate_prompt_protocol_guard returned ok=true."
    artifacts:
      - path: "tsc_cycle/v4_gates/phase17_audit.py"
        issue: "EXPECTED_V4_PROMPT and EXPECTED_V4_PROMPT_SHA256 are derived dynamically from build_user_prompt at module import instead of an independent frozen v4 golden string/hash."
      - path: "tests/test_v4_phase17_saturation_policy.py"
        issue: "test_prompt_protocol_unchanged_and_no_band_rule compares report prompt_text to mod.EXPECTED_V4_PROMPT, which is generated by the same implementation under test."
    missing:
      - "Store an independent frozen v4 prompt fixture or hash that is not recomputed from build_user_prompt at import time."
      - "Add a regression test that monkeypatches/replaces build_user_prompt before import and proves prompt drift fails the guard."
  - truth: "Maintainer CLI writes safe Phase 17 JSON reports under artifacts/v4/phase17/ only."
    status: failed
    reason: "The CLI accepts --artifact-root and sets ARTIFACT_ROOT to that value, so choosing the repository root allows report writes outside artifacts/v4/phase17."
    artifacts:
      - path: "tsc_cycle/v4_gates/phase17_audit.py"
        issue: "main() assigns global ARTIFACT_ROOT = Path(args.artifact_root); reject_unsafe_phase17_output_path then permits any path under that caller-chosen root except the frozen v1 root."
      - path: "tests/test_v4_phase17_saturation_policy.py"
        issue: "Existing path-safety tests cover default ARTIFACT_ROOT and tmp phase17 roots but not broad artifact roots such as /home/samuel/TSC_CYCLE."
    missing:
      - "Constrain --artifact-root itself to the canonical artifacts/v4/phase17 subtree or derive outputs under a fixed Phase 17 root without broadening the trust root."
      - "Add a test that --artifact-root /home/samuel/TSC_CYCLE still rejects writes to data/, tsc_cycle/, and other non-Phase-17 paths."
  - truth: "Maintainer can inspect representative failure examples from both data/v4/phase8/labeled_merged.jsonl and reality_test.log with sample id, phase id, saturation, min/max green, final green, and violation category."
    status: failed
    reason: "The combined audit report only keeps the first 10 sorted violations; all persisted representative_examples are dataset examples, while replay examples only appear nested inside policy_gate.replay and not in the top-level audit artifact promised for inspection."
    artifacts:
      - path: "tsc_cycle/v4_gates/saturation_policy.py"
        issue: "compute_saturation_audit sorts all rows by origin/sample/phase and appends violations until example_limit, so dataset:labeled_merged.jsonl can exhaust the limit before replay:phase12 is represented."
      - path: "artifacts/v4/phase17/saturation_audit_report.json"
        issue: "Top-level audit representative_examples contain only origin_artifact=dataset:labeled_merged.jsonl; no replay:phase12/reality example is present."
    missing:
      - "Select representative_examples with per-origin coverage or separate dataset/replay example sections."
      - "Regenerate saturation_audit_report.json so it includes both dataset and reality/replay representative examples in the maintainer-facing audit output."
---

# Phase 17: Audit & Saturation Policy Gate Verification Report

**Phase Goal:** Maintainer can measure the saturation/green mismatch, inspect representative failures, and run an offline policy gate that protects data, evaluation, and replay outputs while preserving the unchanged v4 deployment prompt protocol.
**Verified:** 2026-05-18T07:21:38Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Maintainer can generate banded statistics showing how often v4 teacher labels set `final == max_green` when `pred_saturation < 1.0`, broken down by saturation band, split, and source. | VERIFIED | `compute_saturation_audit` aggregates `bands`, `by_split`, `by_source`, `by_origin`; `artifacts/v4/phase17/saturation_audit_report.json` contains non-empty band/split/source rates and counts. |
| 2 | Maintainer can inspect representative failure examples from both `data/v4/phase8/labeled_merged.jsonl` and `reality_test.log` with required fields. | FAILED | `saturation_audit_report.json` top-level `representative_examples` are all `origin_artifact: dataset:labeled_merged.jsonl`; replay examples are absent because the first 10 sorted dataset violations exhaust `example_limit`. |
| 3 | Maintainer can run a saturation policy gate that classifies phase decisions into intended bands and fails outputs exceeding configured low-saturation max-green thresholds. | VERIFIED | `evaluate_saturation_policy_gate` uses canonical band threshold keys for data/replay/eval, emits `fatal_failures` on threshold excess; CLI regeneration produced exit 1 and six fatal failures for current evidence. |
| 4 | Maintainer can verify final deployment system/inference prompts remain byte-for-byte aligned with v4 inference protocol and do not explicitly include saturation band rule. | FAILED | Forbidden-snippet scanning exists, but byte-for-byte alignment is self-referential: `EXPECTED_V4_PROMPT = build_user_prompt(...)` at import. A simulated replacement prompt imported as golden and the guard returned `ok: True`. |
| 5 | Maintainer can classify every phase decision into required saturation band intervals. | VERIFIED | `classify_saturation_band` implements `<0.2`, `<0.6`, `<1.0`, and `>=1.0`; boundary tests pass. |
| 6 | Maintainer can quantify v4 teacher final==max_green while pred_saturation<1.0 by band, split, and source. | VERIFIED | `saturation_audit_report.json` includes `final_equals_max_when_unsaturated` metrics under `bands`, `by_split`, and `by_source`. |
| 7 | Maintainer can run one offline CLI that writes Phase 17 audit, gate, and prompt-protocol JSON reports. | VERIFIED | Temporary CLI run wrote `saturation_audit_report.json`, `saturation_policy_gate.json`, and `prompt_protocol_report.json`; exit 1 reflected expected current policy failures. |
| 8 | Maintainer CLI writes safe Phase 17 JSON reports under `artifacts/v4/phase17/` only. | FAILED | Default path guard works, but setting `ARTIFACT_ROOT`/`--artifact-root` to `/home/samuel/TSC_CYCLE` permits `/home/samuel/TSC_CYCLE/data/v4/phase8/labeled_merged.jsonl` as an output path. |

**Score:** 5/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tsc_cycle/v4_gates/saturation_policy.py` | Canonical saturation classifier, projectors, audit aggregation, examples | PARTIAL | Exists and substantive; classifier/projectors/audit are wired. Example selection is insufficient for combined dataset+replay top-level audit. |
| `tsc_cycle/v4_gates/phase17_audit.py` | CLI/report orchestration, path guard, threshold gate, eval-output gate, prompt protocol report | PARTIAL | Exists and substantive; CLI and gate are wired. Prompt golden is self-referential and configurable artifact root weakens path safety. |
| `tests/test_v4_phase17_saturation_policy.py` | Phase 17 contract/regression tests | PARTIAL | 55 tests pass, but tests miss prompt drift before import, broad `--artifact-root`, and combined audit example coverage. |
| `artifacts/v4/phase17/saturation_audit_report.json` | Persisted maintainer audit report | PARTIAL | Contains statistics, but top-level representative examples lack replay/reality origin. |
| `artifacts/v4/phase17/saturation_policy_gate.json` | Persisted fail-closed policy gate report | VERIFIED | Contains `ok:false`, `next_phase_allowed:false`, thresholds, fatal failures, reports, and all requirements covered. |
| `artifacts/v4/phase17/prompt_protocol_report.json` | Persisted prompt protocol report | PARTIAL | Contains prompt hash and scan results, but code cannot independently detect byte drift because expected hash is recomputed from the current prompt at import. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `saturation_policy.py` | `data/v4/phase8/labeled_merged.jsonl` | `project_dataset_phase_decisions` | WIRED | Projector reads JSONL rows, validates solution, emits per-phase rows. |
| `saturation_policy.py` | `artifacts/v4/phase12/manifest.json + per_sample.jsonl` | `project_replay_phase_decisions` | WIRED | Projector aligns manifest and per-sample rows by sample id/order. |
| `compute_saturation_audit` | `classify_saturation_band` | canonical classifier | WIRED | `_normalise_audit_row` recomputes band with `classify_saturation_band` and rejects inconsistent derived fields. |
| `phase17_audit.py` | `saturation_policy.py` | imports canonical projectors/audit compute | WIRED | Imports `compute_saturation_audit`, projectors, and band constants from saturation policy module. |
| `phase17_audit.py` | `artifacts/v4/phase17/*.json` | safe JSON writers | PARTIAL | `_write_json` uses guard and `allow_nan=False`, but caller-controlled `ARTIFACT_ROOT` can broaden allowed output root. |
| `phase17_audit.py` | `build_user_prompt` | prompt protocol guard | PARTIAL | Guard calls `build_user_prompt` and scans surfaces, but golden prompt/hash are generated from that same call at import. |
| `phase17_audit.py` | eval-style phase-decision JSONL | `--phase-decisions-jsonl/--eval-outputs` | WIRED | Parser exposes aliases and `evaluate_phase17_audit` routes eval rows through `evaluate_saturation_policy_gate(source_type="eval")`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `saturation_policy.py` | audit rows | Phase 8 dataset JSONL and Phase 12 manifest/per_sample | Yes | FLOWING |
| `phase17_audit.py` | threshold reports | `evaluate_saturation_policy_gate` over dataset/replay/eval projections | Yes | FLOWING |
| `phase17_audit.py` | prompt report | `build_user_prompt(FIXTURE_PREDICTION_INPUT)` and scanned prompt files | Partial | HOLLOW for byte drift: expected and actual prompt are from same runtime source. |
| `saturation_audit_report.json` | representative examples | combined sorted rows with `example_limit=10` | Partial | HOLLOW for replay coverage in top-level audit: dataset examples consume the limit. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Regression tests | `/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py /home/samuel/TSC_CYCLE/tests/test_phase12_reality_log_generation.py /home/samuel/TSC_CYCLE/tests/test_v4_phase8_dataset_rebuild.py -q` | 55 passed | PASS |
| CLI help exposes eval and threshold options | `/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase17_audit --help` | Exit 0; options include `--phase-decisions-jsonl`, `--eval-outputs`, threshold flags | PASS |
| CLI safely regenerates report set in temporary root | `python -m tsc_cycle.v4_gates.phase17_audit --artifact-root <tmp>` | Exit 1; all three reports written under tmp; `ok:false`, `next_phase_allowed:false`, six fatal failures | PASS |
| Prompt guard rejects simulated prompt drift | import with fake `tsc_cycle.prompt_builder.build_user_prompt` returning drifted text, then run `evaluate_prompt_protocol_guard()` | Returned `{'ok': True, 'prompt_sha256_equals_expected': True, 'fatal_failures': []}` | FAIL |
| Artifact-root path safety resists broad root | set `ARTIFACT_ROOT=/home/samuel/TSC_CYCLE`, call `reject_unsafe_phase17_output_path('/home/samuel/TSC_CYCLE/data/v4/phase8/labeled_merged.jsonl')` | Path accepted | FAIL |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| Phase 17 CLI/report generation | `/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase17_audit --artifact-root <tmp>` | Reports regenerated; exit 1 because current evidence violates configured thresholds | PASS (expected red gate) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| AUDIT-01 | 17-01, 17-02 | Quantify `final == max_green` when `pred_saturation < 1.0`, broken down by bands and split/source | SATISFIED | Audit JSON and `compute_saturation_audit` expose band/split/source metrics. |
| AUDIT-02 | 17-01, 17-02 | Inspect representative failures from both dataset and reality/replay with required fields | BLOCKED | Core can produce replay examples in replay-only gate, but persisted top-level audit report lacks replay examples. |
| POLICY-01 | 17-01, 17-02 | Classify phase decisions into intended saturation bands | SATISFIED | Canonical classifier and threshold gates use band constants; boundary tests pass. |
| POLICY-02 | 17-02 | Fail data/model-eval/replay outputs when thresholds exceeded | SATISFIED | Gate supports data/replay/eval source types, thresholds, fatal failures, CLI flags. |
| POLICY-03 | 17-02 | Final deployment prompts unchanged and no explicit band rule | BLOCKED | No independent golden prompt; byte-for-byte drift check is self-referential, though forbidden-snippet scan exists. |

No Phase 17 requirement IDs are orphaned: AUDIT-01, AUDIT-02, POLICY-01, POLICY-02, and POLICY-03 all appear in plan frontmatter and REQUIREMENTS.md traceability.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `tsc_cycle/v4_gates/saturation_policy.py` | 113, 116 | `return {}` | Info | Empty fallback for missing split index; not a stub because dataset rows still project with unknown split/source. |
| `tsc_cycle/v4_gates/phase17_audit.py` | 200 | `return {}` | Info | Empty excluded counts fallback; not a stub. |

No unreferenced `TBD`, `FIXME`, or `XXX` blocker markers found in modified implementation/test files.

### Human Verification Required

None. The blocking gaps are programmatically reproducible.

### Gaps Summary

Phase 17 substantially implements the offline audit and reusable saturation threshold gate, and the tests pass. However, three must-have truths fail under goal-backward verification: the prompt byte-for-byte guard cannot detect prompt drift because its golden is recomputed from the current prompt implementation, the configurable artifact root can broaden write permissions beyond `artifacts/v4/phase17`, and the persisted top-level audit report does not include representative replay/reality failures despite the requirement for examples from both sources.

---

_Verified: 2026-05-18T07:21:38Z_
_Verifier: Claude (gsd-verifier)_
