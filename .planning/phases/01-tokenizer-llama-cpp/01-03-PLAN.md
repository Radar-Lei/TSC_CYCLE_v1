---
phase: 01-tokenizer-llama-cpp
plan: "03"
type: execute
wave: 2
depends_on: ["02", "05"]
files_modified:
  - tsc_cycle/v3_gates/tokenizer_parity_v3.py
  - tests/test_v3_tokenizer_parity.py
autonomous: true
requirements: [TOK-03]
must_haves:
  truths:
    - "HF AutoTokenizer.encode and llama-tokenize match exactly on 100 deterministic prompts."
    - "llama-tokenize is called with a GGUF tokenizer/model fixture when the binary requires `-m`."
    - "Parity fixture is deterministic and reusable by Phase 5 GGUF validation."
  artifacts:
    - path: "tsc_cycle/v3_gates/tokenizer_parity_v3.py"
      provides: "HF vs llama-tokenize parity CLI"
      exports: ["main", "parse_llama_tokenize_ids", "build_prompt_fixture"]
    - path: "artifacts/v3/phase1/tokenizer_parity_prompts.jsonl"
      provides: "Exactly 100 deterministic parity prompts"
    - path: "artifacts/v3/phase1/tokenizer_parity.json"
      provides: "Runtime parity result"
    - path: "runs/v3.0-gates/gguf_microconvert/tokenizer.gguf"
      provides: "GGUF tokenizer/model fixture from Plan 05 when llama-tokenize requires -m"
  key_links:
    - from: "tsc_cycle/v3_gates/tokenizer_parity_v3.py"
      to: "runs/v3.0-gates/gguf_microconvert/tokenizer.gguf"
      via: "--gguf passed to llama-tokenize when required"
      pattern: "--gguf|-m"
    - from: "tsc_cycle/v3_gates/tokenizer_parity_v3.py"
      to: "tsc_cycle/prompt_builder.py"
      via: "build_user_prompt for labeled prompt fixture"
      pattern: "build_user_prompt"
---

<objective>
实现 Qwen3.5 HF tokenizer 与 llama.cpp `llama-tokenize` 的 100-prompt exact parity 门禁。

Purpose: 在 Phase 5 导出前提前发现 HF/GGUF tokenizer 不一致，避免部署端 prompt/tag tokenization 错位。
Output: deterministic prompt fixture、parity CLI、parser/fixture 单元测试。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@.planning/phases/01-tokenizer-llama-cpp/01-PATTERNS.md
@tsc_cycle/student/parity_prompts.py
@tsc_cycle/student/tokenize_sanity.py
@tsc_cycle/prompt_builder.py

<interfaces>
Plan 05 must create or locate a Qwen3.5 architecture/config GGUF tokenizer fixture before this plan executes. This plan must not assume no-GGUF `llama-tokenize` syntax works. The runner in Plan 06 must pass `--gguf runs/v3.0-gates/gguf_microconvert/tokenizer.gguf` or the actual fixture path recorded by `gguf_microconvert.json` when `llama-tokenize` requires `-m`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01: Implement deterministic parity fixture builder and llama-tokenize parser</name>
  <files>tsc_cycle/v3_gates/tokenizer_parity_v3.py, tests/test_v3_tokenizer_parity.py</files>
  <read_first>
  - `tsc_cycle/student/parity_prompts.py` — deterministic sample selection pattern.
  - `tsc_cycle/student/tokenize_sanity.py` — GGUF tokenizer diagnostics and parser analogs.
  - `tsc_cycle/prompt_builder.py` — prompt construction source of truth.
  </read_first>
  <behavior>
  - Building the fixture twice with seed 42 yields identical `prompt_id` order.
  - Synthetic prompts cover boundary strings containing `min_green` and `max_green`.
  - Parser extracts token IDs from representative llama-tokenize output and fail-closes when no IDs are parseable.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/tokenizer_parity_v3.py` with helpers `build_prompt_fixture(...)`, `parse_llama_tokenize_ids(stdout)`, and `first_diff(a, b)`. Argparse options: `--model Qwen/Qwen3.5-9B`, `--labeled data/labeled.jsonl`, `--ood-inputs data/ood_inputs.jsonl`, `--prompt-fixture artifacts/v3/phase1/tokenizer_parity_prompts.jsonl`, `--llama-tokenize` required absolute path supplied by Plan 06 from `gguf_microconvert.json`, `--gguf` required path supplied by Plan 06, `--require-gguf` action store_true default true behavior in runner, `--out artifacts/v3/phase1/tokenizer_parity.json`, `--n 100`, `--seed 42`. Build candidate prompts from valid `data/labeled.jsonl` using `build_user_prompt(record["input"])`, optional OOD inputs, and synthetic boundary prompts until at least 100 candidates exist. Sort by stable id, sample exactly 100 using `random.Random(seed)`, and write JSONL rows `prompt_id`, `source`, `text`. Create `tests/test_v3_tokenizer_parity.py` covering deterministic selection, boundary prompts, parser success, parser failure, and `first_diff`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_tokenizer_parity.py</automated>
  </verify>
  <acceptance_criteria>
  - Module contains `tokenizer_parity_prompts.jsonl`, `seed=42`, `min_green`, and `max_green`.
  - Tests do not require external model downloads or llama.cpp binaries.
  - Parser failures are represented as controlled parse failures, not silent matches.
  </acceptance_criteria>
  <done>Parity fixture generation and llama-tokenize output parsing are deterministic and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02: Implement HF ↔ llama-tokenize parity gate with required GGUF handling</name>
  <files>tsc_cycle/v3_gates/tokenizer_parity_v3.py</files>
  <read_first>
  - `tsc_cycle/v3_gates/tokenizer_parity_v3.py` — helpers from Task 03-01.
  - `runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json` — produced by Plan 05 at execution time.
  </read_first>
  <behavior>
  - Gate exits nonzero when `--require-gguf` is active and no usable `--gguf` path exists.
  - Gate calls llama-tokenize with `--model <gguf> --prompt <text> --ids --no-bos --log-disable` when `--gguf` is supplied.
  - Gate passes only when `matched == 100`, `mismatched == 0`, and `parse_failed == 0`.
  </behavior>
  <action>Complete the parity CLI. Load HF tokenizer via `AutoTokenizer.from_pretrained(args.model)` and compute `tokenizer.encode(text, add_special_tokens=False)`. Before parity, validate `args.llama_tokenize` exists and is executable; this path must come from Plan 05 `gguf_microconvert.json` via Plan 06 runner, not from a hardcoded missing EvoProgTSC binary. If `--require-gguf` is true or the binary help indicates `-m`/model is required, require `--gguf` to point to an existing file; otherwise exit nonzero with a clear message. Invoke llama-tokenize with exact argv `[llama_tokenize, "--model", gguf, "--prompt", text, "--ids", "--no-bos", "--log-disable"]` when a GGUF is supplied; `-m` may be used only as a documented alias after help/smoke proves equivalence. Only use no-GGUF syntax when explicitly proven by help/smoke and `--require-gguf` is false. For each prompt, record `prompt_id`, `matched`, `first_diff_index`, `hf_ids_sample`, `llama_ids_sample`, `stdout_tail`, and `stderr_tail`. Write JSON keys exactly: `ok`, `n`, `matched`, `mismatched`, `parse_failed`, `fixture`, `llama_tokenize`, `gguf`, `require_gguf`, `results`. Exit nonzero unless exact 100/100 parity is achieved.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/tokenizer_parity_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - Module contains `--llama-tokenize`, `--gguf`, `--require-gguf`, `--ids`, `--no-bos`, `--log-disable`, and `--prompt` command construction.
  - Module exits nonzero if GGUF is required but missing.
  - Pass condition requires `matched == args.n`, `mismatched == 0`, and `parse_failed == 0`.
  </acceptance_criteria>
  <done>TOK-03 parity gate cannot accidentally run before/without the required GGUF fixture when llama-tokenize needs one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF tokenizer -> llama.cpp tokenizer | Two tokenizer implementations must produce identical token IDs. |
| Local binary stdout -> parser | llama-tokenize output format is external process text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `tokenizer_parity_v3.py` GGUF selection | mitigate | Require existing GGUF fixture when binary requires `-m`; fail closed otherwise. |
| T-03-02 | Repudiation | parity artifact | mitigate | Persist per-prompt mismatch diagnostics and first differing index. |
| T-03-03 | Denial of Service | llama-tokenize subprocess | mitigate | Run one prompt at a time with controlled command and parse failure accounting. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_tokenizer_parity.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/tokenizer_parity_v3.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.tokenizer_parity_v3 --model Qwen/Qwen3.5-9B --llama-tokenize "$(/home/samuel/TSC_CYCLE/.venv/bin/python -c 'import json; print(json.load(open("runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json"))["llama_tokenize"])')" --gguf runs/v3.0-gates/gguf_microconvert/tokenizer.gguf --require-gguf --out artifacts/v3/phase1/tokenizer_parity.json</automated>
</verification>

<success_criteria>
- `artifacts/v3/phase1/tokenizer_parity_prompts.jsonl` contains exactly 100 deterministic prompts.
- `artifacts/v3/phase1/tokenizer_parity.json` reports `ok=true`, `matched=100`, `mismatched=0`, `parse_failed=0`, and records the GGUF path used.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-03-SUMMARY.md`.
</output>
