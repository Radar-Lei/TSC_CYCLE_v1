---
phase: 01-tokenizer-llama-cpp
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tsc_cycle/v3_gates/memory_budget_v3.py
  - tests/test_v3_memory_budget.py
autonomous: true
requirements: [MEM-01, MEM-02, ENV-03]
must_haves:
  truths:
    - "All five required max_seq_length candidates are measured, not extrapolated."
    - "The selected max_seq_length is the largest successful candidate with peak reserved memory <85GB."
    - "The 100-step Qwen3.5-9B LoRA dry-run executes under run_safe 100G scope."
  artifacts:
    - path: "tsc_cycle/v3_gates/memory_budget_v3.py"
      provides: "Memory sweep and 100-step dry-run CLI"
      exports: ["main", "select_max_seq", "default_seqs"]
    - path: "artifacts/v3/phase1/memory_budget.json"
      provides: "Per-seq peak memory measurements"
    - path: "artifacts/v3/phase1/train_100step.json"
      provides: "100-step dry-run evidence"
    - path: "artifacts/v3/phase1/run_safe_scope.json"
      provides: "ENV-03 scope evidence from Plan 01"
  key_links:
    - from: "tsc_cycle/v3_gates/memory_budget_v3.py"
      to: "scripts/dgx_spark/run_safe.sh"
      via: "verification and runner invoke long jobs through run_safe 100G"
      pattern: "run_safe.sh 100G"
    - from: "tsc_cycle/v3_gates/memory_budget_v3.py"
      to: "peft.LoraConfig"
      via: "r=64 alpha=64 dropout=0.0 target_modules='all-linear'"
      pattern: "target_modules=.*all-linear"
---

<objective>
实现 Qwen3.5-9B 显存预算实测硬门禁。

Purpose: 用真实 DGX Spark 运行数据选择 Phase 3/4 可用 max_seq_length，并证明 9B+r64 LoRA+bs=1+grad_ckpt 100-step dry-run 不会突破 100GB cap。
Output: `memory_budget_v3.py`、选择逻辑单元测试、memory/dry-run artifact 规范。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@.planning/phases/01-tokenizer-llama-cpp/01-PATTERNS.md
@tsc_cycle/student/train.py
@scripts/dgx_spark/run_safe.sh

<interfaces>
From Plan 01, `artifacts/v3/phase1/run_safe_scope.json` must prove swap disabled and `MemoryMax=100G MemorySwapMax=0` before long GPU memory gates are trusted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01: Implement memory sweep and selection helpers</name>
  <files>tsc_cycle/v3_gates/memory_budget_v3.py, tests/test_v3_memory_budget.py</files>
  <read_first>
  - `tsc_cycle/student/train.py` — model, LoRA, gradient checkpointing, optimizer patterns.
  - `scripts/dgx_spark/run_safe.sh` — required long-job wrapper.
  </read_first>
  <behavior>
  - `default_seqs()` returns exactly `[1536, 2048, 2560, 3072, 4096]`.
  - `select_max_seq` chooses 3072 when 4096 has peak 90.0GB and 3072 has 84.9GB.
  - `select_max_seq` returns `None` when all candidates fail or have peak >=85GB.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/memory_budget_v3.py` with helper functions `default_seqs()` and `select_max_seq(results, threshold_gb=85.0)`. Add argparse options `--model Qwen/Qwen3.5-9B`, `--seqs 1536 2048 2560 3072 4096`, `--seq` optional, `--steps 1`, `--out artifacts/v3/phase1/memory_budget.json`, `--batch-size 1`, `--grad-accum 16`, `--lora-r 64`, and `--lora-alpha 64`. Create `tests/test_v3_memory_budget.py` for pure helper logic listed in behavior. The CLI must record enough command metadata to show executor used the documented run_safe command, but it must not itself call sudo or systemd-run.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_memory_budget.py</automated>
  </verify>
  <acceptance_criteria>
  - Tests contain `[1536, 2048, 2560, 3072, 4096]`, `84.9`, and `90.0`.
  - Module contains strict `<85.0` threshold logic.
  - Module defaults to Qwen/Qwen3.5-9B.
  </acceptance_criteria>
  <done>MEM-01 selection rules are deterministic, unit-tested, and match the strict peak<85GB requirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02: Implement Qwen3.5 LoRA micro-step runner</name>
  <files>tsc_cycle/v3_gates/memory_budget_v3.py</files>
  <read_first>
  - `tsc_cycle/student/train.py` — QLoRA preparation and gradient checkpointing patterns.
  - `.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md` — MEM-01/MEM-02 fatal gates.
  </read_first>
  <behavior>
  - Each requested seq candidate records status, peak allocated/reserved GB, elapsed seconds, steps, and error.
  - `--seq` dry-run mode writes a train_100step-compatible artifact and selected_max_seq equal to the seq on success.
  </behavior>
  <action>Implement runtime model path in `memory_budget_v3.py`: load tokenizer/model with `AutoModelForCausalLM`, bnb NF4, bf16, SDPA, `device_map={"": 0}`; prepare with `prepare_model_for_kbit_training(..., gradient_checkpointing_kwargs={"use_reentrant": False})`; attach `LoraConfig(r=64, lora_alpha=64, lora_dropout=0.0, bias="none", target_modules="all-linear", task_type="CAUSAL_LM")`; construct synthetic bs=1 batches of exact sequence length; run forward/backward/optimizer for requested `steps`, using grad accumulation 16 when steps allow; record CUDA `max_memory_allocated` and `max_memory_reserved` in GB. For sweep mode, process every candidate from `--seqs` and select largest successful peak_reserved_gb <85.0. For dry-run mode, run only `--seq`, write keys compatible with report: `ok`/`status`, `steps`, `seq`, `selected_max_seq`, `peak_reserved_gb`, `run_safe_required=true`, `error`. Catch OOM per candidate, clear CUDA cache when possible, and continue sweep; do not silently pass failed candidates.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/memory_budget_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - Module contains `target_modules="all-linear"`, `use_reentrant`, `False`, `lora_dropout=0.0`, and `grad_accum` 16 handling.
  - Sweep artifact records all five candidates.
  - Dry-run artifact can satisfy report rule `steps >= 100` when run with `--steps 100`.
  </acceptance_criteria>
  <done>MEM-01 and MEM-02 runtime gates are implemented with required 9B QLoRA configuration and artifact output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic batch -> GPU memory | Generated tensors can consume large GPU memory. |
| CLI seq values -> training loop | User-provided sequence length determines memory load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `memory_budget_v3.py` | mitigate | Require external run_safe 100G invocation and catch per-candidate OOM. |
| T-04-02 | Tampering | selected max_seq | mitigate | Select only from measured candidates with peak_reserved_gb <85.0. |
| T-04-03 | Repudiation | memory artifacts | mitigate | Persist per-seq measurements and errors in JSON. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_memory_budget.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/memory_budget_v3.py</automated>
<automated>scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.memory_budget_v3 --model Qwen/Qwen3.5-9B --seqs 1536 2048 2560 3072 4096 --out artifacts/v3/phase1/memory_budget.json</automated>
<automated>scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.memory_budget_v3 --model Qwen/Qwen3.5-9B --seq $(python -c 'import json; print(json.load(open("artifacts/v3/phase1/memory_budget.json"))["selected_max_seq"])') --steps 100 --out artifacts/v3/phase1/train_100step.json</automated>
</verification>

<success_criteria>
- `memory_budget.json` records all five required seq candidates and selects the largest successful candidate with peak reserved memory `<85GB`.
- `train_100step.json` reports successful `steps >= 100` execution for the selected seq under run_safe 100G.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-04-SUMMARY.md`.
</output>
