---
phase: 01-tokenizer-llama-cpp
plan: "06"
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - tsc_cycle/v3_gates/phase1_report.py
  - scripts/run_v3_phase1_gates.sh
  - tests/test_v3_phase1_report.py
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03, TOK-01, TOK-02, TOK-03, TOK-04, MEM-01, MEM-02, MEM-03]
must_haves:
  truths:
    - "Phase 1 runner executes all gates in dependency-correct order and stops on first failure."
    - "Runner passes the tokenizer GGUF fixture from micro-convert into tokenizer parity."
    - "Phase 1 report allows Phase 2 only when every fatal gate artifact passes."
  artifacts:
    - path: "scripts/run_v3_phase1_gates.sh"
      provides: "End-to-end Phase 1 gate runner"
    - path: "tsc_cycle/v3_gates/phase1_report.py"
      provides: "Aggregate fatal pass/fail report"
      exports: ["main", "evaluate_gates"]
    - path: "tests/test_v3_phase1_report.py"
      provides: "Report pass/fail unit tests"
    - path: "artifacts/v3/phase1/phase1_gate_report.json"
      provides: "Final Phase 1 go/no-go evidence"
  key_links:
    - from: "scripts/run_v3_phase1_gates.sh"
      to: "tsc_cycle/v3_gates/tokenizer_parity_v3.py"
      via: "extract tokenizer_gguf and llama_tokenize from gguf_microconvert.json and pass --llama-tokenize --gguf --require-gguf"
      pattern: "llama_tokenize.*tokenizer_gguf.*--llama-tokenize.*--gguf"
    - from: "tsc_cycle/v3_gates/phase1_report.py"
      to: "artifacts/v3/phase1/*.json"
      via: "aggregate exact fatal pass rules"
      pattern: "next_phase_allowed"
---

<objective>
实现 Phase 1 总控 runner 与 fatal gate 报告。

Purpose: 将 10 个 Phase 1 requirements 的 runtime artifacts 汇总成唯一 go/no-go 信号，防止任一 fatal gate 失败后继续进入数据扩量或训练。
Output: `phase1_report.py`、`run_v3_phase1_gates.sh`、report 单元测试。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-VALIDATION.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@scripts/dgx_spark/run_safe.sh

<interfaces>
The runner must execute Plan 05 micro-convert before Plan 03 parity because parity requires `tokenizer_gguf`. It must read `runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json`, extract `tokenizer_gguf` and `llama_tokenize`, and pass both paths to `/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.tokenizer_parity_v3 --llama-tokenize <path> --gguf <path> --require-gguf`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-01: Implement aggregate Phase 1 report</name>
  <files>tsc_cycle/v3_gates/phase1_report.py, tests/test_v3_phase1_report.py</files>
  <read_first>
  - `.planning/phases/01-tokenizer-llama-cpp/01-VALIDATION.md` — required artifact list and pass rules.
  - `tsc_cycle/student/parity_merge.py` — JSON aggregation and fail-closed report analog from PATTERNS.md.
  - `tsc_cycle/eval/decision.py` — JSON report style.
  </read_first>
  <behavior>
  - All gates passing yields `ok=true` and `next_phase_allowed=true`.
  - tokenizer parity `matched=99` fails.
  - env `vision_param_count=1` fails.
  - memory selected peak `85.0` fails because threshold is strictly `<85.0`.
  - missing `run_safe_scope.json` or swap/scope failure fails ENV-03.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/phase1_report.py` with argparse `--artifacts artifacts/v3/phase1`, `--gguf-report runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json`, and `--out artifacts/v3/phase1/phase1_gate_report.json`. Implement `evaluate_gates(...)` reading `env_smoke.json`, `run_safe_scope.json`, `tokenizer_audit.json`, `tokenizer_parity.json`, `memory_budget.json`, `train_100step.json`, and `gguf_microconvert.json`. Apply exact pass rules: env passes only when `ok==true`, Qwen3.5 causal LM evidence exists, and `vision_param_count==0`; run_safe passes only when `swap_disabled==true`, lowercase JSON key `memory_max` indicates 100G, and lowercase JSON key `memory_swap_max==0`; tokenizer audit passes when all custom tag lengths are `>=3`, native think IDs are present, and dataset raw-text/no-chat-template evidence exists; tokenizer parity passes when `matched==100`, `mismatched==0`, `parse_failed==0`, and a GGUF path is recorded; memory budget passes when selected peak is `<85.0`; train 100-step passes when `steps>=100`; gguf passes when `ok==true`, Qwen3.5 fixture evidence exists, `dummy_lora_created==true`, `dummy_lora_merged==true`, q4 GGUF exists, tokenizer GGUF exists, and `llama_tokenize` points to an executable. Write JSON keys exactly `ok`, `fatal_failures`, `warnings`, `gates`, `requirements_covered`, `next_phase_allowed`; `next_phase_allowed` must equal `ok`. Create `tests/test_v3_phase1_report.py` with pure JSON fixture tests for the behavior list.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_phase1_report.py</automated>
  </verify>
  <acceptance_criteria>
  - Report contains `next_phase_allowed`, `matched == 100`, `vision_param_count`, `selected_max_seq`, `85.0`, and `run_safe_scope` checks.
  - Tests cover parity 99 fail, vision count fail, strict 85.0 fail, and missing/failed run_safe scope.
  - Report exits nonzero if `ok` is false.
  </acceptance_criteria>
  <done>The final Phase 1 report fail-closes on every checker-identified fatal gate and covers all Phase 1 requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-02: Implement dependency-correct all-gates runner</name>
  <files>scripts/run_v3_phase1_gates.sh</files>
  <read_first>
  - `scripts/run_pipeline.sh` — local shell orchestration style.
  - `scripts/dgx_spark/run_safe.sh` — long gate wrapper.
  - `tsc_cycle/v3_gates/gguf_microconvert_v3.py` and `tokenizer_parity_v3.py` — micro-convert/parity dependency contract.
  </read_first>
  <behavior>
  - Runner has `set -euo pipefail`.
  - Runner performs run_safe scope empty-run before long GPU gates.
  - Runner runs micro-convert before tokenizer parity and passes extracted `tokenizer_gguf` into parity with `--require-gguf`.
  </behavior>
  <action>Create executable `scripts/run_v3_phase1_gates.sh`. Hard-require `/home/samuel/TSC_CYCLE/.venv/bin/python`: set `PY=/home/samuel/TSC_CYCLE/.venv/bin/python`, test it is executable, and exit nonzero if it is missing; never fall back to system `python`. Run in this order: source `scripts/dgx_spark/env.sh`; run `$PY scripts/dgx_spark/verify.py`; run `scripts/dgx_spark/run_safe.sh 100G -- $PY -m tsc_cycle.v3_gates.run_safe_scope_check_v3 --out artifacts/v3/phase1/run_safe_scope.json`; run env smoke under run_safe 100G; run tokenizer audit; run micro-convert under run_safe 100G; extract `TOKENIZER_GGUF` and `LLAMA_TOKENIZE` from `runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json` using Python; run tokenizer parity with `--llama-tokenize "$LLAMA_TOKENIZE" --gguf "$TOKENIZER_GGUF" --require-gguf`; run memory sweep under run_safe 100G; extract selected seq; run 100-step dry-run under run_safe 100G; run phase1_report. Do not call `sudo swapoff -a`; if scope check fails because swap is enabled, stop and let the user approve swap changes separately.</action>
  <verify>
    <automated>bash -n scripts/run_v3_phase1_gates.sh</automated>
  </verify>
  <acceptance_criteria>
  - Script contains `set -euo pipefail`.
  - Script contains `/home/samuel/TSC_CYCLE/.venv/bin/python` and exits nonzero if that interpreter is not executable.
  - Script contains `run_safe_scope_check_v3` before env/memory/micro-convert long gates.
  - Script contains `gguf_microconvert_v3` before `tokenizer_parity_v3`.
  - Script extracts `tokenizer_gguf` and `llama_tokenize` and passes `--llama-tokenize`, `--gguf`, and `--require-gguf` to parity.
  - Script never contains `swapoff`.
  </acceptance_criteria>
  <done>Phase 1 can be executed by one command with correct dependencies and no silent swap mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate artifacts -> phase decision | JSON artifacts determine whether Phase 2 may proceed. |
| Shell runner -> host/GPU | Runner launches long GPU and systemd-scope commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Repudiation | `phase1_report.py` | mitigate | Persist fatal failures and per-gate pass rules in final JSON. |
| T-06-02 | Tampering | runner dependency order | mitigate | Run micro-convert before parity and pass explicit tokenizer_gguf path. |
| T-06-03 | Denial of Service | runner swap handling | mitigate | Run scope check and stop on swap enabled; never silently run swapoff. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_phase1_report.py</automated>
<automated>bash -n scripts/run_v3_phase1_gates.sh</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/phase1_report.py</automated>
<automated>scripts/run_v3_phase1_gates.sh</automated>
</verification>

<success_criteria>
- `scripts/run_v3_phase1_gates.sh` hard-requires `/home/samuel/TSC_CYCLE/.venv/bin/python`, stops on first fatal failure, and passes `--llama-tokenize` plus `--gguf` to tokenizer parity.
- `phase1_gate_report.json` reports `ok=true` and `next_phase_allowed=true` only when all ENV/TOK/MEM fatal gates pass.
- No Phase 1 runner step mutates v1.0 artifacts or silently disables swap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-06-SUMMARY.md`.
</output>
