---
phase: 01-tokenizer-llama-cpp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/v3_gates/__init__.py
  - tsc_cycle/v3_gates/env_smoke_v3.py
  - tsc_cycle/v3_gates/run_safe_scope_check_v3.py
  - scripts/dgx_spark/verify.py
  - tests/test_v3_env_gate.py
autonomous: true
requirements: [ENV-01, ENV-03, MEM-03]
must_haves:
  truths:
    - "Qwen/Qwen3.5-9B loads through Qwen3_5ForCausalLM / Qwen3.5 causal LM, not conditional-generation or vision classes."
    - "Loaded model parameters contain no vision, visual, or vision_tower namespace entries."
    - "Phase 1 GPU gates have artifact evidence that swap is disabled and run_safe executes inside MemoryMax=100G MemorySwapMax=0."
  artifacts:
    - path: "tsc_cycle/v3_gates/env_smoke_v3.py"
      provides: "Qwen3.5-9B causal-LM load and forward hard gate"
      exports: ["main", "count_vision_params", "assert_qwen35_causal_lm"]
    - path: "tsc_cycle/v3_gates/run_safe_scope_check_v3.py"
      provides: "ENV-03 swap=0 and systemd-run memory scope artifact"
      exports: ["main", "check_swap_disabled", "inspect_systemd_scope"]
    - path: "artifacts/v3/phase1/env_smoke.json"
      provides: "Runtime evidence for ENV-01 and MEM-03"
    - path: "artifacts/v3/phase1/run_safe_scope.json"
      provides: "Runtime evidence for ENV-03"
  key_links:
    - from: "tsc_cycle/v3_gates/env_smoke_v3.py"
      to: "transformers.AutoModelForCausalLM"
      via: "from_pretrained(... attn_implementation='sdpa', quantization_config=BitsAndBytesConfig(...nf4...))"
      pattern: "AutoModelForCausalLM\.from_pretrained"
    - from: "tsc_cycle/v3_gates/run_safe_scope_check_v3.py"
      to: "scripts/dgx_spark/run_safe.sh"
      via: "executor runs run_safe 100G empty scope check before long GPU gates"
      pattern: "MemoryMax=100G.*MemorySwapMax=0"
---

<objective>
建立 Phase 1 的环境、模型加载、run_safe memory scope 硬门禁。

Purpose: 在任何数据扩量或训练成本发生前，证明本机 `.venv` 能以 Qwen3.5 causal LM 路径加载 9B，并证明长 GPU gate 不会绕过 swap=0 与 100GB systemd scope。
Output: `env_smoke_v3.py`、`run_safe_scope_check_v3.py`、更新后的 `verify.py`、无下载单元测试，以及运行时 JSON artifact 规范。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@.planning/phases/01-tokenizer-llama-cpp/01-PATTERNS.md
@tsc_cycle/student/train.py
@scripts/dgx_spark/verify.py
@scripts/dgx_spark/run_safe.sh

<interfaces>
From `tsc_cycle/student/train.py`, preserve these implementation contracts:
```python
BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.bfloat16, bnb_4bit_use_double_quant=True)
AutoModelForCausalLM.from_pretrained(..., attn_implementation="sdpa", torch_dtype=torch.bfloat16, device_map={"": 0})
prepare_model_for_kbit_training(..., gradient_checkpointing_kwargs={"use_reentrant": False})
```
From `scripts/dgx_spark/run_safe.sh`, long gates must be invoked through:
```bash
scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m <module>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01: Implement Qwen3.5 causal-LM environment smoke gate</name>
  <files>tsc_cycle/v3_gates/__init__.py, tsc_cycle/v3_gates/env_smoke_v3.py</files>
  <read_first>
  - `tsc_cycle/student/train.py` — reuse bnb NF4, bf16, SDPA, `AutoModelForCausalLM` loading pattern.
  - `.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md` — ENV-01 and MEM-03 fatal gate definition.
  </read_first>
  <behavior>
  - Parser defaults expose `--model Qwen/Qwen3.5-9B` and `--out artifacts/v3/phase1/env_smoke.json`.
  - A loaded model whose class/config does not identify Qwen3.5 causal LM exits nonzero.
  - Any loaded parameter name containing `vision`, `.vision`, `visual`, or `vision_tower` exits nonzero.
  </behavior>
  <action>Create package file `tsc_cycle/v3_gates/__init__.py` and CLI module `tsc_cycle/v3_gates/env_smoke_v3.py`. Implement argparse options `--model` default `Qwen/Qwen3.5-9B`, `--out` default `artifacts/v3/phase1/env_smoke.json`, and `--prompt` default `DGX Spark Qwen3.5 smoke test`. Load tokenizer and model with `AutoModelForCausalLM.from_pretrained(..., quantization_config=bnb_cfg, attn_implementation="sdpa", torch_dtype=torch.bfloat16, device_map={"": 0})`, run one inference forward pass, and write JSON keys exactly: `ok`, `model`, `model_class`, `architectures`, `model_type`, `torch_version`, `torch_cuda`, `gpu_name`, `logits_shape`, `vision_param_count`, `vision_params_sample`, `attn_implementation`, `quantization`, `sys_executable`, `expected_venv`, `error`. Add `assert_qwen35_causal_lm(model)` that passes only when model class is `Qwen3_5ForCausalLM` or config `architectures`/`model_type` explicitly identify Qwen3.5 causal LM; it must reject `ConditionalGeneration`, `Vision`, and `visual` paths. Add `count_vision_params(named_parameters)` that counts names starting with `vision`, containing `.vision`, containing `vision_tower`, starting with `visual`, or containing `.visual`. Exit 0 only when `ok is true`, Qwen3.5 causal LM assertion passes, `vision_param_count == 0`, and `sys.executable == "/home/samuel/TSC_CYCLE/.venv/bin/python"`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/env_smoke_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - `env_smoke_v3.py` contains `AutoModelForCausalLM.from_pretrained`, `attn_implementation="sdpa"`, and `bnb_4bit_quant_type="nf4"`.
  - `env_smoke_v3.py` contains `assert_qwen35_causal_lm` and rejects non-Qwen3.5 causal LM architecture/config paths.
  - `env_smoke_v3.py` records `vision_param_count` and fails when it is nonzero.
  - `env_smoke_v3.py` records `sys_executable` and fails unless it equals `/home/samuel/TSC_CYCLE/.venv/bin/python`.
  </acceptance_criteria>
  <done>ENV-01 and MEM-03 have an executable smoke gate whose runtime artifact proves Qwen3.5 causal LM loading, NF4+SDPA forward pass, and absence of vision/visual parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02: Implement run_safe scope and swap artifact gate</name>
  <files>tsc_cycle/v3_gates/run_safe_scope_check_v3.py</files>
  <read_first>
  - `scripts/dgx_spark/run_safe.sh` — current systemd-run wrapper and property names.
  - `.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md` — security note forbidding silent `sudo swapoff -a`.
  </read_first>
  <behavior>
  - If `swapon --show --noheadings` is non-empty, the gate exits nonzero and prints that user approval is required before swap changes.
  - When run through `run_safe.sh 100G --`, artifact records `MemoryMax=100G` and `MemorySwapMax=0` evidence.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/run_safe_scope_check_v3.py` with argparse `--out` default `artifacts/v3/phase1/run_safe_scope.json`. Implement `check_swap_disabled()` using `swapon --show --noheadings`; do not call `sudo swapoff -a`. Implement `inspect_systemd_scope()` by reading `/proc/self/cgroup` and querying the active systemd scope with `systemctl show <scope> -p MemoryMax -p MemorySwapMax` when a scope is discoverable; also record `RUN_SAFE_MEMORY_MAX` or equivalent environment hints if `run_safe.sh` exports them. Write JSON keys exactly: `ok`, `swap_disabled`, `swapon_output`, `scope`, `memory_max`, `memory_swap_max`, `inside_systemd_scope`, `error`. Exit nonzero unless swap is disabled and the current scope proves `MemorySwapMax=0` plus a 100G MemoryMax value. The executor must use `scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.run_safe_scope_check_v3 --out artifacts/v3/phase1/run_safe_scope.json` as the empty-run evidence command.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/run_safe_scope_check_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - The module contains `swapon --show --noheadings` and does not contain `swapoff`.
  - The module writes `run_safe_scope.json` with `swap_disabled`, `memory_max`, and `memory_swap_max`.
  - The documented command uses `scripts/dgx_spark/run_safe.sh 100G --`.
  </acceptance_criteria>
  <done>ENV-03 has a replayable empty-run artifact proving swap=0 and run_safe 100GB scope, without silently changing host swap state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03: Update environment verifier and unit coverage</name>
  <files>scripts/dgx_spark/verify.py, tests/test_v3_env_gate.py</files>
  <read_first>
  - `scripts/dgx_spark/verify.py` — current package checks.
  - `CLAUDE.md` — project instruction that vLLM is unavailable.
  - `tsc_cycle/v3_gates/env_smoke_v3.py` and `tsc_cycle/v3_gates/run_safe_scope_check_v3.py` — helpers created above.
  </read_first>
  <behavior>
  - Missing `vllm` is a warning, not a hard environment failure.
  - Unit tests cover Qwen3.5 parser defaults, architecture rejection, and vision/visual parameter counting without downloading a model.
  </behavior>
  <action>Modify `scripts/dgx_spark/verify.py` so required imports remain `transformers`, `accelerate`, `datasets`, and `peft`, while optional imports include `deepspeed` and `vllm`. Required import failure returns nonzero; optional import failure prints `optional {name}: unavailable` and continues. Keep the upstream `flash_attn` rejection and `TRITON_PTXAS_PATH=/usr/local/cuda/bin/ptxas` hard check unchanged. Create `tests/test_v3_env_gate.py` that imports the two Phase 1 gate modules and tests: parser defaults, `count_vision_params` counts `vision.foo`, `model.vision_tower.bar`, and `model.visual.patch_embed`, `assert_qwen35_causal_lm` accepts a fake config with `architectures=["Qwen3_5ForCausalLM"]`, and rejects fake configs/classes containing `ConditionalGeneration` or `Vision`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_env_gate.py</automated>
  </verify>
  <acceptance_criteria>
  - `verify.py` contains `required_packages` and `optional_packages` or equivalent separation.
  - `tests/test_v3_env_gate.py` contains `Qwen/Qwen3.5-9B`, `Qwen3_5ForCausalLM`, `ConditionalGeneration`, and `vision_tower`.
  - `/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_env_gate.py` exits 0.
  </acceptance_criteria>
  <done>Environment validation no longer treats vLLM as a project hard dependency, and Phase 1 env gate helpers have no-download unit coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> model loader | User-provided model id controls remote/local model loading. |
| Host memory state -> GPU gate | Host swap/systemd state affects OOM safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `env_smoke_v3.py` model selection | mitigate | Assert Qwen3.5 causal LM architecture/config and reject conditional-generation/vision paths. |
| T-01-02 | Denial of Service | GPU gates | mitigate | Require run_safe 100G scope artifact and swap-disabled preflight before long 9B commands. |
| T-01-03 | Information Disclosure | env artifact | accept | Artifact records versions/shapes only, not secrets or dataset contents. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_env_gate.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/env_smoke_v3.py tsc_cycle/v3_gates/run_safe_scope_check_v3.py scripts/dgx_spark/verify.py</automated>
<automated>scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.run_safe_scope_check_v3 --out artifacts/v3/phase1/run_safe_scope.json</automated>
<automated>scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.env_smoke_v3 --model Qwen/Qwen3.5-9B --out artifacts/v3/phase1/env_smoke.json</automated>
</verification>

<success_criteria>
- `artifacts/v3/phase1/env_smoke.json` has `ok=true`, Qwen3.5 causal LM evidence, `vision_param_count=0`, and `sys_executable=/home/samuel/TSC_CYCLE/.venv/bin/python`.
- `artifacts/v3/phase1/run_safe_scope.json` has `ok=true`, `swap_disabled=true`, `MemoryMax=100G`, and `MemorySwapMax=0` evidence.
- Missing vLLM does not fail Phase 1 environment verification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-01-SUMMARY.md`.
</output>
