---
phase: 01-tokenizer-llama-cpp
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/v3_gates/gguf_microconvert_v3.py
  - tests/test_v3_gguf_microconvert.py
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "llama.cpp micro-convert uses a Qwen3.5 architecture/config path; non-Qwen3.5 minimal fixtures cannot satisfy ENV-02."
    - "The mandated EvoProgTSC llama.cpp path provides convert_hf_to_gguf.py, llama-quantize, llama-cli, and tokenizer fixture support for llama-tokenize."
    - "Micro-convert produces or locates a GGUF tokenizer/model fixture before tokenizer parity runs."
  artifacts:
    - path: "tsc_cycle/v3_gates/gguf_microconvert_v3.py"
      provides: "Qwen3.5 llama.cpp micro-convert and tokenizer fixture gate"
      exports: ["main", "assert_qwen35_config", "resolve_llama_cpp_paths"]
    - path: "runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json"
      provides: "Runtime evidence for ENV-02 and tokenizer fixture path"
    - path: "runs/v3.0-gates/gguf_microconvert/tokenizer.gguf"
      provides: "GGUF fixture for Plan 03 llama-tokenize parity when required"
  key_links:
    - from: "tsc_cycle/v3_gates/gguf_microconvert_v3.py"
      to: "/home/samuel/projects/EvoProgTSC/llama.cpp/convert_hf_to_gguf.py"
      via: "subprocess convert from Qwen3.5 HF config/fixture"
      pattern: "convert_hf_to_gguf.py"
    - from: "tsc_cycle/v3_gates/gguf_microconvert_v3.py"
      to: "tsc_cycle/v3_gates/tokenizer_parity_v3.py"
      via: "gguf_microconvert.json records tokenizer_gguf consumed as --gguf"
      pattern: "tokenizer_gguf"
---

<objective>
实现 Phase 1 的 llama.cpp micro-convert 和 tokenizer GGUF fixture 硬门禁。

Purpose: 在 Phase 5 之前证明 Qwen3.5 architecture/config 能走本机 EvoProgTSC llama.cpp convert/quantize/llama-cli 链路，并为 Plan 03 的 llama-tokenize parity 提供必要 GGUF。
Output: `gguf_microconvert_v3.py`、无外部二进制单元测试、`gguf_microconvert.json` artifact 规范。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@.planning/phases/01-tokenizer-llama-cpp/01-PATTERNS.md
@tsc_cycle/student/export_gguf.py

<interfaces>
ENV-02 requires a Qwen3.5 dummy LoRA → merged HF → bf16/fp16 GGUF → q4_K_M GGUF path. A generic tiny GPT-2 or non-Qwen3.5 minimal HF fixture is invalid and must fail the hard gate. If a reduced fixture is used for runtime cost, its `config.json` must still identify Qwen3.5 causal LM architecture/model_type sufficiently for `convert_hf_to_gguf.py` to exercise the Qwen3.5 branch, and the gate must still create a dummy LoRA adapter, merge it into an HF output directory, convert that merged output, quantize it, and run llama-cli; otherwise it fails.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-01: Implement Qwen3.5 fixture validation and llama.cpp path checks</name>
  <files>tsc_cycle/v3_gates/gguf_microconvert_v3.py, tests/test_v3_gguf_microconvert.py</files>
  <read_first>
  - `tsc_cycle/student/export_gguf.py` — llama.cpp path and subprocess pattern.
  - `.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md` — ENV-02 fatal gate definition.
  </read_first>
  <behavior>
  - `assert_qwen35_config` accepts config architectures containing `Qwen3_5ForCausalLM`.
  - `assert_qwen35_config` rejects non-Qwen3.5 fixtures such as GPT2, Llama, Qwen3-4B, ConditionalGeneration, or Vision configs.
  - Path resolver requires convert, quantize, llama-cli, and resolves a usable llama-tokenize absolute path: prefer `/home/samuel/projects/EvoProgTSC/llama.cpp/llama-tokenize`; if missing, build/locate the tool in the EvoProgTSC tree or record fallback `/home/samuel/llama.cpp/build/bin/llama-tokenize` with provenance; fail if no usable binary exists.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/gguf_microconvert_v3.py` with helpers `assert_qwen35_config(config_dir_or_model_id)`, `resolve_llama_cpp_paths(llama_cpp_dir)`, `resolve_llama_tokenize(llama_cpp_dir)`, `create_dummy_lora_adapter(...)`, `merge_dummy_lora_to_hf(...)`, and command summary helpers. Argparse options: `--model Qwen/Qwen3.5-9B`, `--llama-cpp /home/samuel/projects/EvoProgTSC/llama.cpp`, `--llama-tokenize-fallback /home/samuel/llama.cpp/build/bin/llama-tokenize`, `--out runs/v3.0-gates/gguf_microconvert`, `--n-predict 5`, `--fixture-hf` optional, `--tokenizer-gguf` default `runs/v3.0-gates/gguf_microconvert/tokenizer.gguf`. Validate that the source config/model identifies Qwen3.5 causal LM before conversion; if a local `--fixture-hf` is used, read its `config.json` and apply the same Qwen3.5 causal LM assertion. `resolve_llama_tokenize` must return a real executable path and store both `llama_tokenize` and `llama_tokenize_source` in the later JSON. Create `tests/test_v3_gguf_microconvert.py` covering Qwen3.5 config acceptance, non-Qwen3.5 fixture rejection, conditional/vision rejection, and path resolver behavior using temp files.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_gguf_microconvert.py</automated>
  </verify>
  <acceptance_criteria>
  - Tests contain `Qwen3_5ForCausalLM`, `GPT2`, `ConditionalGeneration`, and `Vision` rejection cases.
  - Module default llama.cpp path is `/home/samuel/projects/EvoProgTSC/llama.cpp`.
  - Non-Qwen3.5 minimal fixtures cannot pass helper validation.
  </acceptance_criteria>
  <done>ENV-02 can no longer be satisfied by a non-Qwen3.5 minimal fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-02: Implement convert, quantize, inference, and tokenizer fixture artifact</name>
  <files>tsc_cycle/v3_gates/gguf_microconvert_v3.py</files>
  <read_first>
  - `tsc_cycle/v3_gates/gguf_microconvert_v3.py` — validation helpers from Task 05-01.
  - `tsc_cycle/student/export_gguf.py` — subprocess `check=True` style.
  </read_first>
  <behavior>
  - Convert command runs `convert_hf_to_gguf.py` on the validated Qwen3.5 source/fixture.
  - Quantize command runs `llama-quantize ... Q4_K_M`.
  - llama-cli command generates 5 tokens from q4 GGUF with no segfault.
  - Artifact records `tokenizer_gguf` path to feed Plan 03.
  </behavior>
  <action>Complete `gguf_microconvert_v3.py` runtime: create output dirs only under `runs/v3.0-gates/gguf_microconvert` by default; verify required llama.cpp files `convert_hf_to_gguf.py`, `llama-quantize`, `llama-cli` exist and resolve a usable `llama-tokenize` executable. Use the validated Qwen3.5 model or `--fixture-hf` to create a dummy LoRA adapter under `runs/v3.0-gates/gguf_microconvert/dummy_lora`, then merge that dummy adapter into `runs/v3.0-gates/gguf_microconvert/merged_hf`; run `convert_hf_to_gguf.py` only on the merged HF directory and produce a bf16/fp16 GGUF plus a tokenizer fixture path suitable for `llama-tokenize --model`; run `llama-quantize <fp16> <q4> Q4_K_M`; run `llama-cli -m <q4> -n 5 -p "<start_working_out>smoke"`. Write `gguf_microconvert.json` keys exactly: `ok`, `model`, `fixture_hf`, `fixture_architecture`, `llama_cpp`, `convert`, `quantize`, `llama_cli`, `llama_tokenize`, `llama_tokenize_source`, `dummy_lora_created`, `dummy_lora_dir`, `dummy_lora_merged`, `merged_hf`, `bf16_or_fp16_gguf`, `q4_gguf`, `tokenizer_gguf`, `commands`, `inference_tail`, `error`. Exit nonzero unless dummy LoRA creation succeeds, dummy LoRA merge succeeds, all required commands return 0, q4 GGUF exists, `tokenizer_gguf` exists, and `llama_tokenize` points to an executable. If the implementation cannot produce a tokenizer GGUF before parity, it must exit nonzero rather than leaving Plan 03 to guess no-GGUF syntax.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/gguf_microconvert_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - Module contains `convert_hf_to_gguf.py`, `llama-quantize`, `Q4_K_M`, `llama-cli`, `tokenizer_gguf`, `create_dummy_lora_adapter`, `merge_dummy_lora_to_hf`, `dummy_lora_created`, and `dummy_lora_merged`.
  - Runtime artifact records command return codes, output paths, `llama_tokenize` absolute path, `dummy_lora_created`, and `dummy_lora_merged`.
  - Gate fails if q4 GGUF, tokenizer GGUF fixture, usable llama-tokenize binary, dummy LoRA creation, or dummy LoRA merge is missing.
  </acceptance_criteria>
  <done>ENV-02 has a Qwen3.5 dummy-LoRA merge and architecture/config micro-convert gate and creates the GGUF fixture required by TOK-03 parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF config/fixture -> llama.cpp convert | Fixture identity determines whether Qwen3.5 path is actually exercised. |
| Local subprocesses -> artifacts | llama.cpp binaries execute external commands and produce files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | `--fixture-hf` | mitigate | Assert Qwen3.5 causal LM config; reject non-Qwen3.5 fixtures. |
| T-05-02 | Tampering | llama.cpp paths | mitigate | Resolve and record mandated EvoProgTSC binary paths before running. |
| T-05-03 | Repudiation | `gguf_microconvert.json` | mitigate | Persist command argv, return codes, output paths, and inference tail. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_gguf_microconvert.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/gguf_microconvert_v3.py</automated>
<automated>scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.gguf_microconvert_v3 --model Qwen/Qwen3.5-9B --out runs/v3.0-gates/gguf_microconvert</automated>
</verification>

<success_criteria>
- `runs/v3.0-gates/gguf_microconvert/gguf_microconvert.json` reports `ok=true`.
- The artifact proves a Qwen3.5 causal LM architecture/config path was used.
- The artifact contains existing `q4_gguf` and `tokenizer_gguf` paths, an executable `llama_tokenize` path, and `dummy_lora_created=true` plus `dummy_lora_merged=true`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-05-SUMMARY.md`.
</output>
