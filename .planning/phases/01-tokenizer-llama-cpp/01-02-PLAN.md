---
phase: 01-tokenizer-llama-cpp
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/tokenizer_check.py
  - tsc_cycle/student/dataset.py
  - tsc_cycle/v3_gates/tokenizer_audit_v3.py
  - tests/test_tokenizer_check.py
  - tests/test_v3_dataset_raw_text.py
autonomous: true
requirements: [TOK-01, TOK-02, TOK-04]
must_haves:
  truths:
    - "Custom thinking tags tokenize into at least 3 subtokens under Qwen3.5 tokenizer."
    - "Native <think> and </think> IDs are looked up dynamically and written to tokenizer_audit.json; v1.0 constants are not authoritative."
    - "Student dataset assembly bypasses tokenizer chat_template and rejects dynamically discovered native think IDs."
  artifacts:
    - path: "tsc_cycle/tokenizer_check.py"
      provides: "Dynamic tokenizer invariant helpers"
      exports: ["check_tokenizer", "lookup_native_think_ids", "native_think_token_ids", "assert_no_native_think_in_ids"]
    - path: "tsc_cycle/student/dataset.py"
      provides: "Raw-text prompt assembly path for SFT dataset"
      exports: ["dataset construction helpers"]
    - path: "tsc_cycle/v3_gates/tokenizer_audit_v3.py"
      provides: "Qwen3.5 tokenizer audit CLI"
    - path: "artifacts/v3/phase1/tokenizer_audit.json"
      provides: "Runtime evidence for TOK-01/TOK-02/TOK-04"
  key_links:
    - from: "tsc_cycle/student/dataset.py"
      to: "tsc_cycle/tokenizer_check.py"
      via: "dynamic native_think_token_ids / assert_no_native_think_in_ids, not hardcoded 151667/151668"
      pattern: "native_think_token_ids|assert_no_native_think_in_ids"
    - from: "tsc_cycle/student/dataset.py"
      to: "tsc_cycle/prompt_builder.py"
      via: "raw text build_user_prompt/build_full_assistant path, not tokenizer.apply_chat_template"
      pattern: "build_user_prompt|build_full_assistant"
---

<objective>
升级 tokenizer 与 dataset 安全门禁为 Qwen3.5 动态审计路径。

Purpose: 确保自定义思考协议在 Qwen3.5 tokenizer 下安全，native `<think>` 不会通过硬编码漏检或 chat_template 注入进入训练数据。
Output: 动态 tokenizer helper、审计 CLI、dataset raw-text/native-ID 检查 wiring、无模型下载单元测试。
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tokenizer-llama-cpp/01-RESEARCH.md
@.planning/phases/01-tokenizer-llama-cpp/01-PATTERNS.md
@tsc_cycle/tokenizer_check.py
@tsc_cycle/prompt_builder.py
@tsc_cycle/student/dataset.py

<interfaces>
From `tsc_cycle/prompt_builder.py`, use these tags as the single source of truth:
```python
TAG_THINK_OPEN = "<start_working_out>"
TAG_THINK_CLOSE = "<end_working_out>"
TAG_SOLUTION_OPEN = "<SOLUTION>"
TAG_SOLUTION_CLOSE = "</SOLUTION>"
```
From existing tokenizer checks, preserve this public result shape:
```python
@dataclass
class CheckResult:
    ok: bool
    details: dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-01: Refactor tokenizer checks to dynamic Qwen3.5 invariants</name>
  <files>tsc_cycle/tokenizer_check.py, tests/test_tokenizer_check.py</files>
  <read_first>
  - `tsc_cycle/tokenizer_check.py` — current v1.0 hardcoded implementation.
  - `tsc_cycle/prompt_builder.py` — custom tag constants.
  </read_first>
  <behavior>
  - A custom tag with 2 IDs fails when `min_custom_subtokens=3`.
  - Native `<think>` IDs are reported from current tokenizer encode output without comparing to 151667/151668.
  - Native ID rejection accepts a caller-provided dynamic set.
  </behavior>
  <action>Refactor `tsc_cycle/tokenizer_check.py` to remove v1.0 native ID constants as validation truth. Keep `CheckResult(ok, details)`. Import custom tags from `prompt_builder.py`, define `NATIVE_THINK_TAGS=("<think>", "</think>")` and `MIN_CUSTOM_TAG_SUBTOKENS=3`, add `lookup_native_think_ids(tokenizer) -> dict[str, list[int]]`, `native_think_token_ids(tokenizer) -> set[int]` that returns only single-token native encodings, and update `check_tokenizer(tokenizer, min_custom_subtokens=MIN_CUSTOM_TAG_SUBTOKENS)` so details contain keys exactly `custom_tags`, `native_think`, `vocab_size`, `min_custom_subtokens`, `bad_custom_tags`, `bad_native_think`. `bad_native_think` flags native tags that are not single-token but must not compare to 151667/151668. Update `assert_no_native_think_in_ids(ids, native_ids=None)` so callers can pass dynamic native IDs; if omitted, it must not silently fall back to v1.0 constants. Create/update `tests/test_tokenizer_check.py` with fake tokenizer coverage for the behaviors above.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_tokenizer_check.py</automated>
  </verify>
  <acceptance_criteria>
  - `tokenizer_check.py` contains `MIN_CUSTOM_TAG_SUBTOKENS = 3`, `lookup_native_think_ids`, and `native_think_token_ids`.
  - `tokenizer_check.py` does not contain `NATIVE_THINK_OPEN_ID = 151667` or `NATIVE_THINK_CLOSE_ID = 151668`.
  - `tests/test_tokenizer_check.py` contains `min_custom_subtokens=3` and dynamic `native_ids={99, 100}` coverage.
  </acceptance_criteria>
  <done>TOK-01 and TOK-02 have dynamic helper coverage and no Qwen3.5 gate depends on v1.0 native think ID constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02: Wire dataset raw-text assembly and dynamic native-ID rejection</name>
  <files>tsc_cycle/student/dataset.py, tests/test_v3_dataset_raw_text.py</files>
  <read_first>
  - `tsc_cycle/student/dataset.py` — current dataset tokenization and native leakage check path.
  - `tsc_cycle/prompt_builder.py` — raw text prompt and assistant builders.
  - `tsc_cycle/tokenizer_check.py` — dynamic helpers from Task 02-01.
  </read_first>
  <behavior>
  - Dataset sample text is assembled from raw `build_user_prompt` / `build_full_assistant` style strings, not `tokenizer.apply_chat_template`.
  - Dataset tokenization rejects native think IDs supplied by `native_think_token_ids(tokenizer)`.
  - Tests fail if `apply_chat_template` is invoked during dataset assembly.
  </behavior>
  <action>Modify `tsc_cycle/student/dataset.py` so the SFT dataset assembly path explicitly bypasses chat templates for TOK-04: raw user/assistant text must be concatenated using project prompt-builder functions and custom tags, with a recorded/returnable flag or metadata equivalent to `chat_template_used=False`. Replace any hardcoded native think IDs imported from `tokenizer_check.py` with dynamic `native_think_token_ids(tokenizer)` and pass that set into `assert_no_native_think_in_ids`. Add `tests/test_v3_dataset_raw_text.py` with a fake tokenizer whose `apply_chat_template` raises AssertionError, whose `encode` can emit a native ID for `<think>`, and tests that normal custom-tag examples tokenize without calling chat_template while examples containing native `<think>` are rejected by dynamic native ID leakage checks.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_raw_text.py</automated>
  </verify>
  <acceptance_criteria>
  - `dataset.py` uses raw project prompt builder functions or existing raw text fields and does not call `apply_chat_template` in the v3 SFT path.
  - `dataset.py` calls `native_think_token_ids(tokenizer)` or accepts dynamic native IDs from tokenizer audit output.
  - `tests/test_v3_dataset_raw_text.py` contains `apply_chat_template`, `chat_template_used`, and native `<think>` rejection coverage.
  </acceptance_criteria>
  <done>TOK-04 is implemented in the real dataset assembly/tokenization path, not only claimed in tokenizer audit metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03: Implement Qwen3.5 tokenizer audit CLI</name>
  <files>tsc_cycle/v3_gates/tokenizer_audit_v3.py</files>
  <read_first>
  - `tsc_cycle/tokenizer_check.py` — dynamic tokenizer helper API.
  - `tsc_cycle/student/dataset.py` — chat_template bypass metadata/check path.
  </read_first>
  <behavior>
  - Audit writes dynamic native think IDs and custom tag lengths.
  - Audit records `chat_template_used=false` only because the dataset path is wired to bypass chat_template.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/tokenizer_audit_v3.py` with argparse `--model` default `Qwen/Qwen3.5-9B` and `--out` default `artifacts/v3/phase1/tokenizer_audit.json`. Load tokenizer with `AutoTokenizer.from_pretrained(args.model)`, call `check_tokenizer(tokenizer, min_custom_subtokens=3)`, and write JSON keys exactly: `ok`, `model`, `vocab_size`, `custom_tags`, `native_think`, `min_custom_subtokens`, `chat_template_used`, `dataset_raw_text_path`, `bad_custom_tags`, `bad_native_think`, `error`. Set `chat_template_used` false only after importing/inspecting the dataset raw-text path or metadata introduced in Task 02-02; do not use it as an unsupported constant. Exit nonzero if tokenizer check fails or if dataset wiring indicates chat_template use.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/tokenizer_audit_v3.py</automated>
  </verify>
  <acceptance_criteria>
  - `tokenizer_audit_v3.py` contains `Qwen/Qwen3.5-9B`, `artifacts/v3/phase1/tokenizer_audit.json`, and `min_custom_subtokens=3`.
  - Audit JSON includes dynamic `native_think` and `dataset_raw_text_path` evidence.
  - Audit exits nonzero if `ok` is false or dataset wiring does not prove raw-text/no-chat-template mode.
  </acceptance_criteria>
  <done>`tokenizer_audit.json` can prove TOK-01, TOK-02, and TOK-04 from the Qwen3.5 tokenizer plus real dataset assembly wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tokenizer output -> dataset lint | Model-specific token IDs determine whether training data contains forbidden native tags. |
| Dataset records -> prompt assembly | Untrusted/generated records become SFT raw text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `tokenizer_check.py` | mitigate | Replace hardcoded v1.0 native IDs with tokenizer-derived IDs. |
| T-02-02 | Spoofing | `dataset.py` prompt assembly | mitigate | Bypass chat_template and use project custom tags only. |
| T-02-03 | Repudiation | `tokenizer_audit.json` | mitigate | Write explicit dynamic IDs, bad tag lists, and dataset raw-text evidence. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_tokenizer_check.py tests/test_v3_dataset_raw_text.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/tokenizer_check.py tsc_cycle/student/dataset.py tsc_cycle/v3_gates/tokenizer_audit_v3.py</automated>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.tokenizer_audit_v3 --model Qwen/Qwen3.5-9B --out artifacts/v3/phase1/tokenizer_audit.json</automated>
</verification>

<success_criteria>
- `tokenizer_audit.json` contains all four custom tags with lengths `>=3`.
- `tokenizer_audit.json` contains Qwen3.5-derived native `<think>` and `</think>` IDs.
- Dataset raw-text assembly avoids chat_template and rejects dynamic native think IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tokenizer-llama-cpp/01-02-SUMMARY.md`.
</output>
