---
phase: 05-merge-gguf-export
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 03
files_modified:
  - runs/20260507T032419Z/PHASE05_REPORT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EXP-05
must_haves:
  truths:
    - "PHASE05_REPORT.md 汇总 export_summary / tokenize_sanity / parity_report 三个 JSON 的关键指标"
    - "STATE.md 把 Phase 5 状态从 queued 改为 done（或 done-with-flag 若 MAE>3s）"
    - "ROADMAP.md 把 Phase 5 标 [x] complete"
  artifacts:
    - path: "runs/20260507T032419Z/PHASE05_REPORT.md"
      provides: "Single-file Phase 5 closure summary for downstream Phase 6"
      min_lines: 40
      contains: "Phase 5"
  key_links:
    - from: "runs/20260507T032419Z/PHASE05_REPORT.md"
      to: "runs/20260507T032419Z/{export_summary,gguf/tokenize_sanity,gguf/parity_report}.json"
      via: "Embedded key metrics from each JSON"
      pattern: "overall_mae_q4_vs_hf|all_custom_match|gguf_q4_K_M"
---

<objective>
Phase 5 收尾：把 export_summary.json / tokenize_sanity.json / parity_report.json 三个 JSON 的关键字段汇总到一份 markdown 报告 `runs/20260507T032419Z/PHASE05_REPORT.md`，更新 STATE.md 把 Phase 5 状态置为 done（或 done-with-flag），更新 ROADMAP.md Progress 表把 Phase 5 标完成。

Purpose: 给 Phase 6 evaluation 提供 single-file phase closure，并触发 watchdog 后续判断（`PHASE05_REPORT.md` 存在即视为 Phase 5 done）。
Output: PHASE05_REPORT.md + STATE.md & ROADMAP.md 更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-merge-gguf-export/05-CONTEXT.md
@.planning/phases/05-merge-gguf-export/05-01-SUMMARY.md
@.planning/phases/05-merge-gguf-export/05-03-SUMMARY.md
@runs/20260507T032419Z/export_summary.json
@runs/20260507T032419Z/gguf/tokenize_sanity.json
@runs/20260507T032419Z/gguf/parity_report.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 PHASE05_REPORT.md</name>
  <files>runs/20260507T032419Z/PHASE05_REPORT.md</files>
  <read_first>
    - runs/20260507T032419Z/export_summary.json
    - runs/20260507T032419Z/gguf/tokenize_sanity.json
    - runs/20260507T032419Z/gguf/parity_report.json
  </read_first>
  <action>
    用 Python 一次性读三个 JSON，渲染 markdown 模板写到 `runs/20260507T032419Z/PHASE05_REPORT.md`。

    模板（必须包含的 H2 sections，简体中文）：

    ```
    # Phase 5: Merge + GGUF Export 报告

    **Run TS:** 20260507T032419Z
    **生成时间:** <UTC ISO timestamp now>

    ## 1. Export 产物 (EXP-01/02/03)

    | Artifact | Path | Size (MB) |
    |---|---|---|
    | merged_bf16 dir | <path> | — |
    | GGUF bf16 | <path> | <size_mb> |
    | GGUF q4_K_M | <path> | <size_mb> |

    ## 2. Tokenize Sanity (EXP-04)

    | Tag | HF token ids | GGUF token ids | Match | Multi-token |
    |---|---|---|---|---|
    | <start_working_out> | ... | ... | ✓/✗ | ✓/✗ |
    | <end_working_out>   | ... | ... | ✓/✗ | ✓/✗ |
    | <SOLUTION>          | ... | ... | ✓/✗ | ✓/✗ |
    | </SOLUTION>         | ... | ... | ✓/✗ | ✓/✗ |
    | <think>             | [151667] | ... | ✓/✗ | ✗ (单 token) |
    | </think>            | [151668] | ... | ✓/✗ | ✗ (单 token) |

    全部 custom match: <bool>; 全部 custom multi-token: <bool>

    ## 3. Parity Test (EXP-05)

    - 20 prompts (10 same-dist + 10 OOD) from labeled.jsonl, seed=42
    - n_predict=384, greedy (temp=0, top-k=1, seed=42)
    - GGUF GPU 卸载 -ngl=99, threads=4, timeout=600s

    | Backend | Total wall (s) | parse_failures |
    |---|---|---|
    | HF bf16     | <hf_total_sec> | — |
    | GGUF bf16   | <bf16_total_sec> | — |
    | GGUF q4_K_M | <q4_total_sec> | — |

    **q4_K_M vs HF bf16 整体 MAE:** <overall_mae_q4_vs_hf> 秒
    **阈值:** 3.0 秒
    **状态:** <PASS / FLAG-imatrix-backlog>

    ## 4. Phase 5 收尾状态

    - [x] EXP-01 merged_bf16 (vocab=151936, 无 resize)
    - [x] EXP-02 GGUF bf16
    - [x] EXP-03 GGUF q4_K_M (preset 15)
    - [x] EXP-04 tokenize sanity 全绿
    - [<x|!>] EXP-05 parity MAE ≤ 3s（>3s 进 backlog）

    ## 5. Backlog (若有)

    <若 mae_exceeded == true，列：
     "- imatrix 重量化（用 train 子集生成 imatrix.dat → llama-quantize --imatrix → 重跑 parity）"
     否则 "无（Phase 5 完整通过）">
    ```

    渲染细节：
    - 用 `datetime.utcnow().isoformat(timespec="seconds") + "Z"` 作为生成时间
    - tokenize sanity 表里 hf_ids/gguf_ids 用 str(list)（如 `[2378, 9821, 4421]`）；match/multi_token 用 ✓/✗
    - parity 表里 split 桶分别打 (10/10)；若有 parse_failures 在表后加一行 `parse_failures: <n>`
    - mae_exceeded 状态决定 EXP-05 markdown checkbox：True → `[!] EXP-05 parity MAE ...`；False → `[x]`
    - 文件 mkdir parents=True exist_ok=True
  </action>
  <verify>
    <automated>test -f runs/20260507T032419Z/PHASE05_REPORT.md && grep -c '^## ' runs/20260507T032419Z/PHASE05_REPORT.md | awk '$1>=4{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f runs/20260507T032419Z/PHASE05_REPORT.md`
    - At least 4 H2 sections: `grep -c '^## ' runs/20260507T032419Z/PHASE05_REPORT.md` ≥ 4
    - Contains EXP-01..EXP-05 IDs: `grep -c 'EXP-0[12345]' runs/20260507T032419Z/PHASE05_REPORT.md` ≥ 5
    - Contains MAE value: `grep -c 'MAE' runs/20260507T032419Z/PHASE05_REPORT.md` ≥ 1
    - Lines ≥ 40: `wc -l < runs/20260507T032419Z/PHASE05_REPORT.md` ≥ 40
  </acceptance_criteria>
  <done>PHASE05_REPORT.md 落盘，4 个章节齐全，状态字段反映 parity_report.mae_exceeded。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 STATE.md 和 ROADMAP.md 关闭 Phase 5</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md（当前 P5 状态 = `⚙ queued (watchdog)`）
    - .planning/ROADMAP.md（Progress 表 Phase 5 行 = `Not started`）
    - runs/20260507T032419Z/gguf/parity_report.json（读 mae_exceeded 字段决定 done vs done-with-flag）
  </read_first>
  <action>
    **前置：STATE.md 行格式 fail-fast 检查（在做任何 Edit 之前）**：
    ```
    grep -qE '^\| 5\. Merge \+ GGUF Export \|' .planning/STATE.md \
      || (echo "STATE.md Phase 5 行格式不符预期，手动同步后重跑"; exit 1)
    ```
    若 fail，**不进行任何修改**直接退出 1，请求人工介入。

    1. **STATE.md 更新**（用 Edit 工具，不要全文重写）：
       - 顶部 `**Last Activity:**` 行替换为 `**Last Activity:** <UTC ISO now>`
       - `**Status:**` 行替换为 `**Status:** Phase 5 完成。produced merged_bf16 + GGUF bf16/q4_K_M + tokenize_sanity + parity_report (MAE=<x>s)。<若 mae_exceeded: "已记 imatrix backlog；下一步 Phase 6 评测可继续">`
       - Phase Status 表中 `| 5. Merge + GGUF Export | ⚙ queued (watchdog) | watchdog 自动触发 |` 行替换为：
         若 mae_exceeded == False → `| 5. Merge + GGUF Export | ✓ done | parity MAE=<x>s ≤3s; report: runs/20260507T032419Z/PHASE05_REPORT.md |`
         若 mae_exceeded == True  → `| 5. Merge + GGUF Export | ⚠ done-with-flag | parity MAE=<x>s >3s; imatrix backlog; report: runs/20260507T032419Z/PHASE05_REPORT.md |`

    2. **ROADMAP.md 更新**（用 Edit 工具）：
       - `- [ ] **Phase 5: Merge + GGUF Export**` → `- [x] **Phase 5: Merge + GGUF Export**`
       - Progress 表中 `| 5. Merge + GGUF Export | 0/0 | Not started | - |` 行替换为：
         `| 5. Merge + GGUF Export | 4/4 | Complete | <YYYY-MM-DD UTC> |`
       - Traceability 表中 EXP-01..EXP-05 五行的 Status 从 `Pending` 改为 `Done`

    3. 不动其它任何行。

    **后置：ROADMAP.md 改动行数防回归断言**：
    ```
    git diff --numstat .planning/ROADMAP.md | awk '{print $1+$2}' | head -1
    ```
    必须 ≤ 8（仅 5 行 EXP-* 状态变更 + 1 行 Phase 5 checkbox + 1 行 Progress 表 + 1 行余量；超过则说明误改了其它行，立刻 fail-fast）。
  </action>
  <verify>
    <automated>grep -c '^| 5\. Merge + GGUF Export | \(✓ done\|⚠ done-with-flag\)' .planning/STATE.md | awk '$1==1{exit 0} {exit 1}' && grep -c '^- \[x\] \*\*Phase 5:' .planning/ROADMAP.md | awk '$1==1{exit 0} {exit 1}' && test "$(git diff --numstat .planning/ROADMAP.md | awk '{print $1+$2}' | head -1)" -le 8</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Phase 5 row marked done: `grep -cE '^\| 5\. Merge \+ GGUF Export \| (✓ done|⚠ done-with-flag)' .planning/STATE.md` == 1
    - ROADMAP Phase 5 checked: `grep -c '^- \[x\] \*\*Phase 5:' .planning/ROADMAP.md` == 1
    - ROADMAP Progress row complete: `grep -c '^| 5\. Merge + GGUF Export | 4/4 | Complete' .planning/ROADMAP.md` == 1
    - Traceability EXP-* updated: `grep -cE '^\| EXP-0[1-5] \| Phase 5 \| Done' .planning/ROADMAP.md` == 5
    - 没有破坏其它阶段行：`grep -c '^- \[x\] \*\*Phase 4:' .planning/ROADMAP.md` == 1（保持原状）
    - **ROADMAP 改动行数 ≤ 8**：`git diff --numstat .planning/ROADMAP.md | awk '{print $1+$2}' | head -1` 数值 ≤ 8
    - **STATE.md 改前置检查通过**：动作里前置 grep 必须先成功才进入 Edit
  </acceptance_criteria>
  <done>STATE.md / ROADMAP.md 反映 Phase 5 完成状态；MAE flag 字段在 STATE.md 中保留可查；改动行数限定在阈值内。</done>
</task>

</tasks>

<verification>
- 7 acceptance_criteria 全 pass（含新增 numstat ≤ 8 与 STATE.md fail-fast）
- STATE.md 和 ROADMAP.md 没有 unintended 修改（diff 仅限指定行）
</verification>

<success_criteria>
Phase 5 完整收尾：
- 单文件报告 PHASE05_REPORT.md 可作为 Phase 6 入口
- ROADMAP / STATE 对外正确反映 Phase 5 = done（或 done-with-flag）
- EXP-01 ~ EXP-05 全部 Pending → Done
</success_criteria>

<output>
完成后创建 `.planning/phases/05-merge-gguf-export/05-04-SUMMARY.md`，记录：
- 报告文件路径
- 是否触发 imatrix backlog flag
- ROADMAP / STATE 修改前后的关键 diff（grep 行）
- ROADMAP numstat 数值（应 ≤ 8）
</output>
