---
phase: 02-data-generation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data_generator/cycle_detector.py
  - src/data_generator/day_simulator.py
autonomous: true

must_haves:
  truths:
    - "CycleDetector 动态检测第一个绿灯相位的 index，而非硬编码 0"
    - "当 SUMO 相位从非首绿相位切换到首绿相位时，CycleDetector 正确触发周期边界"
    - "首绿相位 index 从 phase_config 中获取（保留过滤后的原始 phase_index）"
  artifacts:
    - path: "src/data_generator/cycle_detector.py"
      provides: "动态首绿相位检测的周期检测器"
      contains: "first_green_phase"
    - path: "src/data_generator/day_simulator.py"
      provides: "传递 phase_config 给 CycleDetector 的仿真 worker"
      contains: "CycleDetector.*phase_config"
  key_links:
    - from: "src/data_generator/day_simulator.py"
      to: "src/data_generator/cycle_detector.py"
      via: "传递 phase_config 到 CycleDetector 构造函数"
      pattern: "CycleDetector\\(tl_id.*phase_config"
    - from: "src/data_generator/cycle_detector.py"
      to: "phase_config['traffic_lights']"
      via: "从 phase_config 查找第一个绿灯相位 index"
      pattern: "first_green_phase"
---

<objective>
修复 CycleDetector 硬编码 phase 0 作为周期边界的 bug，改为从 phase_config 动态检测第一个绿灯相位的 index。

Purpose: 相位处理（过滤黄灯、冲突解决）后，保留的相位保持原始 phase_index 不重新编号，第一个绿灯相位 ID 不一定是 0。当前硬编码 `current_phase == 0` 会在首绿相位非 0 的场景中完全无法检测周期边界，导致零采样。
Output: CycleDetector 能正确检测任意首绿相位 index 的周期边界。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/data_generator/cycle_detector.py
@src/data_generator/day_simulator.py
@src/data_generator/traffic_collector.py
@src/phase_processor/processor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 CycleDetector 支持动态首绿相位检测</name>
  <files>
    src/data_generator/cycle_detector.py
  </files>
  <action>
    修改 CycleDetector 类，从 phase_config 动态获取第一个绿灯相位的 index，替代硬编码 0。

    1. 修改 `__init__(self, tl_id: str)` 签名，增加 `phase_config` 参数：
       ```python
       def __init__(self, tl_id: str, phase_config: Dict[str, Any]):
       ```
       - 从 `phase_config['traffic_lights'][tl_id]` 获取该信号灯的相位列表
       - 取列表中第一个相位的 `phase_index` 作为 `self.first_green_phase`
       - 如果该信号灯不在 phase_config 中或相位列表为空，回退默认值 0 并记录警告日志
       - 添加 `from typing import Dict, Any` 到 imports（已有 Optional, List, Dict, Any 但需确认 Dict 和 Any 是否已导入）

    2. 修改 `update()` 方法中的周期边界检测逻辑：
       - 将硬编码的 `current_phase == 0` 和 `self.last_phase != 0` 替换为使用 `self.first_green_phase`
       - 原始代码:
         ```python
         if (self.last_phase is not None
             and self.last_phase != 0
             and current_phase == 0):
         ```
       - 修改为:
         ```python
         if (self.last_phase is not None
             and self.last_phase != self.first_green_phase
             and current_phase == self.first_green_phase):
         ```

    3. 更新类的 docstring 和 `update()` 方法的 docstring：
       - 类 docstring: 将 "检测信号灯周期边界（phase 0 开始）" 改为 "检测信号灯周期边界（首绿相位开始）"
       - `update()` docstring: 将 "当 phase 从非 0 切换到 0 时" 改为 "当 phase 从非首绿相位切换到首绿相位时"
       - 更新 Example 中的注释，反映动态首绿相位逻辑

    4. 更新模块顶部的 docstring：
       - 将 "当 phase 从非 0 切换到 0 时，视为新周期开始" 改为 "当 phase 切换到首绿相位时，视为新周期开始"

    5. 更新 `__repr__` 方法，增加 `first_green_phase` 字段显示

    注意事项：
    - phase_config['traffic_lights'][tl_id] 是一个列表，每个元素是 dict，包含 'phase_index', 'state', 'green_lanes', 'min_dur', 'max_dur'
    - 列表中的相位已经过过滤（只保留绿灯相位）和冲突解决，按原始 phase_index 排序
    - 第一个元素的 phase_index 就是第一个绿灯相位的 SUMO 内部索引
    - traci.trafficlight.getPhase() 返回的是 SUMO 内部的相位索引（包含被过滤掉的黄灯相位），所以用 phase_config 中第一个绿灯相位的原始 phase_index 来匹配是正确的
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
from src.data_generator.cycle_detector import CycleDetector

# 模拟 phase_config：第一个绿灯相位 index 是 2（phase 0 和 1 被过滤了）
mock_config = {
    'traffic_lights': {
        'tl_test': [
            {'phase_index': 2, 'state': 'GGrr', 'green_lanes': ['l1'], 'min_dur': 20, 'max_dur': 60},
            {'phase_index': 4, 'state': 'rrGG', 'green_lanes': ['l2'], 'min_dur': 20, 'max_dur': 60}
        ]
    }
}

# 测试 1: 首绿相位 index 不是 0
detector = CycleDetector('tl_test', mock_config)
assert detector.first_green_phase == 2, f'Expected 2, got {detector.first_green_phase}'

# 测试 2: 周期边界检测 - phase 从 5 切换到 2 应触发
detector.update(5, 0.0)     # 初始化 last_phase
result = detector.update(2, 90.0)  # 切换到首绿相位
assert result == True, f'Expected True, got {result}'

# 测试 3: phase 从 5 切换到 0 不应触发（0 不是首绿相位）
detector2 = CycleDetector('tl_test', mock_config)
detector2.update(5, 0.0)
result2 = detector2.update(0, 30.0)
assert result2 == False, f'Expected False, got {result2}'

# 测试 4: 回退默认值（tl_id 不在 phase_config 中）
detector3 = CycleDetector('tl_unknown', mock_config)
assert detector3.first_green_phase == 0, f'Fallback expected 0, got {detector3.first_green_phase}'

print('All CycleDetector tests passed')
"
  </verify>
  <done>
    CycleDetector 从 phase_config 动态获取首绿相位 index，周期边界检测使用动态值而非硬编码 0。首绿相位为 2 时，phase 切换到 2 触发周期边界；phase 切换到 0 不触发。tl_id 不在 phase_config 中时回退默认值 0。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 DaySimulator 传递 phase_config 给 CycleDetector</name>
  <files>
    src/data_generator/day_simulator.py
  </files>
  <action>
    修改 DaySimulator.run() 中 CycleDetector 的实例化，传入 phase_config。

    1. 修改 day_simulator.py 第 234 行附近的 CycleDetector 实例化：
       - 原始代码:
         ```python
         cycle_detectors = {tl_id: CycleDetector(tl_id) for tl_id in tl_ids}
         ```
       - 修改为:
         ```python
         cycle_detectors = {tl_id: CycleDetector(tl_id, phase_config) for tl_id in tl_ids}
         ```
       - phase_config 已在第 213 行加载（`phase_config = load_phase_config(self.phase_config_path)`），无需额外修改

    2. 更新模块顶部的 docstring：
       - 将 "检测周期边界（phase 从非0切换到0）" 改为 "检测周期边界（phase 切换到首绿相位）"

    注意事项：
    - 仅修改 CycleDetector 构造调用，不修改其他代码
    - phase_config 变量在同一个 run() 方法作用域内，直接可用
  </action>
  <verify>
    grep -n "CycleDetector" src/data_generator/day_simulator.py
    # 确认 CycleDetector 构造调用包含 phase_config 参数
    grep "CycleDetector(tl_id, phase_config)" src/data_generator/day_simulator.py && echo "phase_config passed to CycleDetector - OK"
  </verify>
  <done>
    DaySimulator.run() 中 CycleDetector 实例化传入 phase_config，使周期检测器能动态获取首绿相位 index。
  </done>
</task>

</tasks>

<verification>
1. python3 验证 CycleDetector 在首绿相位非 0 时正确检测周期边界
2. python3 验证 CycleDetector 在首绿相位非 0 时不会误触发 phase 0 的边界
3. python3 验证 CycleDetector 在 tl_id 不在 phase_config 中时回退默认值 0
4. grep 验证 day_simulator.py 中 CycleDetector 构造调用包含 phase_config 参数
5. grep 验证 cycle_detector.py 中包含 first_green_phase 字段
6. grep 验证 cycle_detector.py 中不再硬编码 current_phase == 0
</verification>

<success_criteria>
- CycleDetector.__init__() 接受 phase_config 参数并从中提取首绿相位 index
- CycleDetector.update() 使用动态 first_green_phase 而非硬编码 0 检测周期边界
- DaySimulator.run() 传递 phase_config 给每个 CycleDetector 实例
- 首绿相位 index 非 0 时仍能正确检测周期边界
- tl_id 不在 phase_config 中时安全回退默认值 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-generation/02-03-SUMMARY.md`
</output>
