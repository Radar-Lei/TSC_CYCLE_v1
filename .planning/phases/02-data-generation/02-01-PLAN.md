---
phase: 02-data-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/generate_training_data.py
  - src/data_generator/day_simulator.py
  - config/config.json
autonomous: true

must_haves:
  truths:
    - "系统自动发现 environments/ 下所有包含 .sumocfg 和 .net.xml 的场景"
    - "缺少 .net.xml 或 .sumocfg 的场景立即报错停止（不是警告跳过）"
    - "每个交叉口仿真运行 3600 秒（不是 86400 秒）"
    - "并行执行以交叉口为单位，任一失败立即停止所有执行"
    - "支持通过 --scenarios 参数指定场景子集"
    - "状态快照保存到 outputs/states/（统一输出路径）"
    - "状态快照保存为未压缩 .xml 格式（不是 .xml.gz）"
  artifacts:
    - path: "src/scripts/generate_training_data.py"
      provides: "数据生成 CLI 入口，扁平任务池并行"
      contains: "--scenarios"
    - path: "src/data_generator/day_simulator.py"
      provides: "单交叉口仿真 worker"
      contains: "sim_end.*3600"
    - path: "config/config.json"
      provides: "仿真配置"
      contains: "sim_duration"
  key_links:
    - from: "src/scripts/generate_training_data.py"
      to: "src/data_generator/day_simulator.py"
      via: "DaySimulator 实例化"
      pattern: "DaySimulator"
    - from: "src/scripts/generate_training_data.py"
      to: "src/scripts/process_phases.py"
      via: "process_traffic_lights 调用"
      pattern: "process_traffic_lights"
---

<objective>
修复数据生成核心流程：仿真时长、输出路径、场景发现验证和并行执行策略。

Purpose: 确保数据生成流程参数正确、场景发现严格验证、并行执行稳定可靠。
Output: 可正确执行数据生成的 CLI 入口和仿真 worker。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-cleanup/01-03-SUMMARY.md

@src/scripts/generate_training_data.py
@src/data_generator/day_simulator.py
@config/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复仿真参数和输出路径</name>
  <files>
    src/data_generator/day_simulator.py
    config/config.json
  </files>
  <action>
    1. 修改 `src/data_generator/day_simulator.py` 中 DaySimulator.__init__():
       - 将 `self.sim_end = config.get('sim_end', 86400)` 改为 `self.sim_end = config.get('sim_end', 3600)`
       - 原因：Phase 2 决策要求固定 3600 秒仿真时长，86400 是旧的 24 小时默认值

    2. 修改 `config/config.json`:
       - 在 `simulation` 段添加 `"sim_duration": 3600` 字段，作为仿真时长的显式配置
       - 这提供了一个显式配置点，虽然代码默认已是 3600

    3. 修改 `create_temp_sumocfg()` 函数：
       - 将默认 `end_time` 参数从 `86400` 改为 `3600`
       - 保持与 DaySimulator 的默认值一致

    4. 修改状态快照为不压缩（用户决策：不压缩，直接保存 .xml 格式）：
       - `day_simulator.py` 中 PredictiveSampler 初始化参数 `compress=True` 改为 `compress=False`
       - 确保状态快照保存为 `.xml` 而不是 `.xml.gz`
  </action>
  <verify>
    在 day_simulator.py 中 grep 确认：
    - `sim_end` 默认值为 3600（不是 86400）
    - `create_temp_sumocfg` 默认 end_time 为 3600
    - `compress=False`（不压缩状态快照）
    在 config.json 中确认 sim_duration 字段存在且为 3600
  </verify>
  <done>
    DaySimulator 默认仿真 3600 秒，create_temp_sumocfg 默认 end_time=3600，config.json 显式声明 sim_duration=3600，状态快照保存为未压缩 .xml 格式
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复场景发现验证和添加 --scenarios 参数</name>
  <files>
    src/scripts/generate_training_data.py
  </files>
  <action>
    1. 修改 `discover_environments()` 函数：
       - 将缺少 .sumocfg 或 .net.xml 的场景从「警告跳过」改为「立即报错停止」（raise SystemExit）
       - 缺少 .rou.xml 保持为警告跳过（.rou.xml 可能由外部工具动态生成）
       - 添加明确错误消息，指出哪个文件缺失

    2. 添加 `--scenarios` 命令行参数：
       - 类型：逗号分隔的场景名列表（如 `--scenarios arterial4x4_1,chengdu`）
       - 功能：只处理指定的场景子集
       - 在 discover_environments() 返回后，根据 --scenarios 过滤场景列表
       - 如果指定的场景名不在发现的列表中，立即报错

    3. 修改 `main()` 中的输出路径：
       - 将默认 `--state-dir` 从 `data/states` 改为 `outputs/states`
       - 将默认 `--output-dir` 保持为 `outputs/data`（已正确）
       - 这与 Phase 1 的统一输出路径决策一致

    4. 修改 `main()` 中读取 sim_duration：
       - 从 config.json 读取 `simulation.sim_duration` 作为仿真时长
       - 传入每个任务的 config 字典中作为 `sim_end` 字段
       - 如果未配置，默认 3600

    5. 修改 `main()` 中的 worker 数限制：
       - 确保 workers 数量不超过 config.json 中 `simulation.parallel_workers` 的值
       - 这是用户在 config.json 中配置的最大并发数（当前为 12）
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
from src.scripts.generate_training_data import parse_args
# 验证 --scenarios 参数存在
import argparse
parser = argparse.ArgumentParser()
# 如果能解析 --scenarios 参数说明添加成功
"
    确认 --state-dir 默认值为 outputs/states
    确认缺少 .net.xml 时会报错而不是跳过
  </verify>
  <done>
    场景发现对缺少 .sumocfg/.net.xml 立即报错停止；支持 --scenarios 参数过滤场景子集；状态快照默认保存到 outputs/states/；仿真时长从 config.json 读取 sim_duration（默认 3600）
  </done>
</task>

</tasks>

<verification>
1. grep 验证 day_simulator.py 中 sim_end 默认值为 3600
2. grep 验证 generate_training_data.py 中 --state-dir 默认值为 outputs/states
3. grep 验证 generate_training_data.py 中包含 --scenarios 参数定义
4. grep 验证 config.json 中包含 sim_duration: 3600
5. python3 -c "from src.scripts.generate_training_data import discover_environments; print('import OK')"
</verification>

<success_criteria>
- DaySimulator 默认仿真 3600 秒
- generate_training_data.py 支持 --scenarios 参数
- 缺少 .sumocfg 或 .net.xml 时立即报错
- 状态快照默认输出到 outputs/states/
- 仿真时长可通过 config.json 配置
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-generation/02-01-SUMMARY.md`
</output>
