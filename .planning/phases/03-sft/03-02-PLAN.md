---
phase: 03-sft
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/sft/train.py
  - outputs/sft/model/
  - outputs/sft/checkpoints/
autonomous: false
requirements:
  - SFT-05
  - SFT-06

must_haves:
  truths:
    - "端到端 SFT 训练流程无错误完成"
    - "训练产出模型存在于 outputs/sft/model/"
    - "模型能正确推理，输出包含自定义标签"
    - "输出格式符合 <SOLUTION>...</SOLUTION> 规范"
  artifacts:
    - path: "outputs/sft/model/config.json"
      provides: "训练后模型配置"
      exists: true
    - path: "outputs/sft/model/model.safetensors"
      provides: "训练后模型权重"
      exists: true
    - path: "outputs/sft/model/tokenizer.json"
      provides: "训练后 tokenizer"
      exists: true
  key_links:
    - from: "sft_train.sh"
      to: "src/sft/train.py"
      via: "docker run python3 -m src.sft.train"
      pattern: "src\\.sft\\.train"
    - from: "src/sft/train.py"
      to: "outputs/sft/model/"
      via: "save_model()"
      pattern: "save_pretrained_merged"
---
<objective>
跑通端到端 SFT 训练流程，产出 GLM-4.7-Flash 微调模型

Purpose: 验证模型加载、LoRA 配置、数据加载和训练流程全部正常工作，产出可用的微调模型
Output: 训练完成的 GLM-4.7-Flash SFT 模型，保存到 outputs/sft/model/
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 依赖的前置计划
@.planning/phases/03-sft/03-01-PLAN.md

# SFT 训练数据 (Phase 2 产出)
# outputs/sft/sft_train.jsonl - 增强样本

# 训练脚本
@src/sft/train.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 train.py 中的模型引用</name>
  <files>src/sft/train.py</files>
  <action>
更新 `src/sft/train.py` 中的模型名称引用：

1. 更新文件头部文档字符串（第 5 行）：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

2. 更新 `ensure_model()` 函数注释（第 21-22 行）：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

3. 更新 `setup_model()` 函数注释（第 53-57 行）：
   - 移除 FP8 相关的注释说明
   - 更新为 GLM-4.7-Flash BF16 加载说明

4. 更新 `main()` 函数中的日志输出（第 358 行）：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

5. 在 `setup_model()` 中更新 LoRA 配置（第 124-131 行）：
   - 确保 `use_gradient_checkpointing="unsloth"` 保持不变
   - LoRA 的 dropout 参数通过 config.json 控制，代码中不需要显式设置

关键变更：
- 移除所有 FP8 相关的注释和说明
- 确保使用 Unsloth 标准加载方式（BF16）
- 双重加载策略保持不变（Unsloth 优先，Hugging Face 回退）
  </action>
  <verify>
```bash
# 验证无 FP8-Dynamic 引用
grep -n "FP8-Dynamic" src/sft/train.py && echo "ERROR: FP8-Dynamic 引用仍存在" || echo "OK: 无 FP8-Dynamic 引用"

# 验证 GLM-4.7-Flash 引用存在
grep -n "GLM-4.7-Flash" src/sft/train.py | head -3
```
  </verify>
  <done>
train.py 中所有模型引用更新为 GLM-4.7-Flash，无 FP8-Dynamic 残留
  </done>
</task>

<task type="auto">
  <name>Task 2: 执行 SFT 训练</name>
  <files>outputs/sft/model/, outputs/sft/checkpoints/</files>
  <action>
执行端到端 SFT 训练：

1. 确认训练数据存在：
   ```bash
   ls -la outputs/sft/sft_train.jsonl
   wc -l outputs/sft/sft_train.jsonl
   ```

2. 执行训练脚本：
   ```bash
   ./docker/sft_train.sh
   ```

3. 训练参数说明（来自更新后的 config.json）：
   - 模型: unsloth/GLM-4.7-Flash (BF16)
   - 2 epochs
   - learning_rate: 2e-4
   - batch_size: 1
   - LoRA rank: 16, alpha: 16, dropout: 0

4. 监控训练过程：
   - 观察是否有 OOM 错误
   - 观察 loss 下降趋势
   - 确认 tokenizer 兼容性检查输出
   - 确认 checkpoint 保存正常

5. 训练完成后验证输出：
   ```bash
   ls -la outputs/sft/model/
   # 应包含: config.json, model.safetensors, tokenizer.json, tokenizer_config.json
   ```

如果训练失败：
- 检查 GPU 内存（可能需要减小 batch_size 或 max_seq_length）
- 检查 Unsloth 是否正确安装
- 检查 Hugging Face 网络连接
- 检查数据格式是否正确
  </action>
  <verify>
```bash
# 验证模型文件存在
ls outputs/sft/model/config.json outputs/sft/model/model.safetensors 2>/dev/null && echo "OK: 模型文件存在" || echo "ERROR: 模型文件缺失"
```
  </verify>
  <done>
outputs/sft/model/ 目录包含完整的训练后模型文件（config.json, model.safetensors, tokenizer.*）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证训练结果</name>
  <files>N/A (checkpoint task)</files>
  <action>
[CHECKPOINT TASK - 人工验证]

GLM-4.7-Flash SFT 训练流程已完成，模型保存到 outputs/sft/model/

训练配置：
- 模型: unsloth/GLM-4.7-Flash (BF16)
- LoRA rank: 16, alpha: 16, dropout: 0
- Epochs: 2
- 数据: 增强样本（思考链 300-400 token）

**验证步骤**:

**步骤 1**: 检查模型文件完整性
```bash
ls -la outputs/sft/model/
# 确认存在: config.json, model.safetensors, tokenizer.json
```

**步骤 2**: 运行推理测试
```bash
./docker/sft_test.sh 3
```

**步骤 3**: 人工检查输出质量
观察模型输出是否符合以下标准：
1. 输出包含 `<start_working_out>...<end_working_out>` 思考链
2. 输出包含 `<SOLUTION>[...]</SOLUTION>` 方案
3. SOLUTION 内是合法 JSON 数组格式
4. 每个元素包含 phase_id 和 final 字段

**预期输出示例**:
```
<start_working_out>
分析交通状态...（思考链内容）
<end_working_out>
<SOLUTION>
[{"phase_id": 0, "final": 30}, {"phase_id": 1, "final": 25}, ...]
</SOLUTION>
```

**通过标准**: 格式正确（包含完整标签和合法 JSON）= 迁移成功
  </action>
  <verify>
人工审核确认：输出格式正确 = approved
  </verify>
  <done>
人工审核通过，确认 GLM-4.7-Flash SFT 训练迁移成功
  </done>
  <resume-signal>Type "approved" to confirm training success, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. outputs/sft/model/ 目录存在且包含模型文件
2. sft_test.sh 推理测试成功运行
3. 模型输出格式符合预期（包含自定义标签）
4. 人工审核通过
</verification>

<success_criteria>
- 训练完成无错误
- 模型文件保存在 outputs/sft/model/
- 推理测试输出格式正确
- 人工审核确认迁移成功
</success_criteria>

<output>
After completion, create `.planning/phases/03-sft/03-02-SUMMARY.md`
</output>
