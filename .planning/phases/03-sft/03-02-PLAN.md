---
phase: 03-sft
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - outputs/sft/model/
  - outputs/sft/checkpoints/
autonomous: false
requirements:
  - SFT-05
  - SFT-06

must_haves:
  truths:
    - "端到端 SFT 训练流程无错误完成"
    - "训练产出模型存在于 outputs/sft/model/"
    - "模型能正确推理，输出包含自定义标签"
    - "输出格式符合 <SOLUTION>...</SOLUTION> 规范"
  artifacts:
    - path: "outputs/sft/model/config.json"
      provides: "训练后模型配置"
      exists: true
    - path: "outputs/sft/model/model.safetensors"
      provides: "训练后模型权重"
      exists: true
    - path: "outputs/sft/model/tokenizer.json"
      provides: "训练后 tokenizer"
      exists: true
  key_links:
    - from: "sft_train.sh"
      to: "src/sft/train.py"
      via: "docker run python3 -m src.sft.train"
      pattern: "src\\.sft\\.train"
    - from: "src/sft/train.py"
      to: "outputs/sft/model/"
      via: "save_model()"
      pattern: "save_pretrained_merged"
---

<objective>
跑通端到端 SFT 训练流程，产出 GLM-4.7 微调模型

Purpose: 验证模型加载、LoRA 配置、数据加载和训练流程全部正常工作，产出可用的微调模型
Output: 训练完成的 GLM-4.7 SFT 模型，保存到 outputs/sft/model/
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 依赖的前置计划
@.planning/phases/03-sft/03-01-PLAN.md

# SFT 训练数据 (Phase 2 产出)
# outputs/sft/sft_train.jsonl - 100 条增强样本
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行 SFT 训练</name>
  <files>outputs/sft/model/, outputs/sft/checkpoints/</files>
  <action>
执行端到端 SFT 训练：

1. 确认训练数据存在：
   ```bash
   ls -la outputs/sft/sft_train.jsonl
   wc -l outputs/sft/sft_train.jsonl  # 应该是 100 条
   ```

2. 执行训练脚本：
   ```bash
   ./docker/sft_train.sh
   ```

3. 训练参数说明（来自 config.json）：
   - 2 epochs
   - learning_rate: 2e-4
   - batch_size: 1
   - LoRA rank: 32, alpha: 64

4. 监控训练过程：
   - 观察是否有 OOM 错误
   - 观察 loss 下降趋势
   - 确认 checkpoint 保存正常

5. 训练完成后验证输出：
   ```bash
   ls -la outputs/sft/model/
   # 应包含: config.json, model.safetensors, tokenizer.json, tokenizer_config.json
   ```

如果训练失败：
- 检查 GPU 内存（可能需要减小 batch_size 或 max_seq_length）
- 检查 FP8 加载是否成功（可能需要转换为 BF16）
- 检查数据格式是否正确
  </action>
  <verify>
`ls outputs/sft/model/config.json outputs/sft/model/model.safetensors` 应显示文件存在
  </verify>
  <done>
outputs/sft/model/ 目录包含完整的训练后模型文件（config.json, model.safetensors, tokenizer.*）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证训练结果</name>
  <files>N/A (checkpoint task)</files>
  <action>
[CHECKPOINT TASK - 人工验证]

GLM-4.7-Flash-FP8-Dynamic SFT 训练流程已完成，模型保存到 outputs/sft/model/

训练配置：
- 模型: GLM-4.7-Flash-FP8-Dynamic
- LoRA rank: 32, alpha: 64
- Epochs: 2
- 数据: 100 条增强样本（思考链 300-400 token）

**验证步骤**:

**步骤 1**: 检查模型文件完整性
```bash
ls -la outputs/sft/model/
# 确认存在: config.json, model.safetensors, tokenizer.json
```

**步骤 2**: 运行推理测试
```bash
./docker/sft_test.sh 3
```

**步骤 3**: 人工检查输出质量
观察模型输出是否符合以下标准：
1. 输出包含 `<start_working_out>...<end_working_out>` 思考链
2. 输出包含 `<SOLUTION>[...]</SOLUTION>` 方案
3. SOLUTION 内是合法 JSON 数组格式
4. 每个元素包含 phase_id 和 final 字段

**预期输出示例**:
```
<start_working_out>
分析交通状态...（思考链内容）
<end_working_out>
<SOLUTION>
[{"phase_id": 0, "final": 30}, {"phase_id": 1, "final": 25}, ...]
</SOLUTION>
```

**通过标准**: 格式正确（包含完整标签和合法 JSON）= 迁移成功
  </action>
  <verify>
人工审核确认：输出格式正确 = approved
  </verify>
  <done>
人工审核通过，确认 GLM-4.7 SFT 训练迁移成功
  </done>
  <resume-signal>Type "approved" to confirm training success, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. outputs/sft/model/ 目录存在且包含模型文件
2. sft_test.sh 推理测试成功运行
3. 模型输出格式符合预期（包含自定义标签）
4. 人工审核通过
</verification>

<success_criteria>
- 训练完成无错误
- 模型文件保存在 outputs/sft/model/
- 推理测试输出格式正确
- 人工审核确认迁移成功
</success_criteria>

<output>
After completion, create `.planning/phases/03-sft/03-02-SUMMARY.md`
</output>
