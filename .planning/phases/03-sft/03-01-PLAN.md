---
phase: 03-sft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sft/train.py
  - config/config.json
autonomous: true
requirements:
  - SFT-01
  - SFT-02
  - SFT-03
  - SFT-04

must_haves:
  truths:
    - "GLM-4.7-Flash-FP8-Dynamic 模型可被成功加载"
    - "LoRA 适配器正确应用于 GLM 模型架构"
    - "训练配置指向 GLM 模型路径"
    - "自定义标签 chat template 正确设置"
  artifacts:
    - path: "src/sft/train.py"
      provides: "GLM 模型加载和训练逻辑"
      contains: "GLM-4.7"
    - path: "config/config.json"
      provides: "GLM 模型和 LoRA 配置"
      contains: "GLM-4.7-Flash"
  key_links:
    - from: "src/sft/train.py"
      to: "config/config.json"
      via: "load_config()"
      pattern: "config\\['training'\\]\\['sft'\\]\\['model'\\]"
    - from: "src/sft/train.py"
      to: "FastLanguageModel"
      via: "from_pretrained()"
      pattern: "FastLanguageModel\\.from_pretrained"
---

<objective>
更新 SFT 训练代码和配置以适配 GLM-4.7-Flash-FP8-Dynamic 模型

Purpose: 将当前基于 Qwen3-4B-Base 的训练流程迁移到 GLM-4.7-Flash-FP8-Dynamic，这是模型迁移的核心步骤
Output: 更新后的 train.py 和 config.json，支持 GLM 模型加载和 LoRA 微调
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 现有 SFT 训练代码参考
@src/sft/train.py
@config/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新配置文件适配 GLM 模型</name>
  <files>config/config.json</files>
  <action>
更新 `config/config.json` 中 SFT 模型配置：

1. 修改 `training.sft.model` 配置块：
   - `model_name`: 改为 `"model/GLM-4.7-Flash-FP8-Dynamic"` (本地路径)
   - `model_id`: 改为 `"zai-org/GLM-4.7-Flash-FP8-Dynamic"` (ModelScope 下载 ID)
   - `max_seq_length`: 保持 `2048` (GLM 支持)
   - `lora_rank`: 保持 `32`
   - `lora_alpha`: 保持 `64`
   - `target_modules`: GLM 架构需要更新为 `["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]` (与 Qwen 相同的 target modules，GLM 使用类似架构)

2. 其他训练参数保持不变（learning_rate、batch_size 等在 Claude's Discretion 范围内，暂时保持现有值）

注意：FP8-Dynamic 是量化格式，Unsloth 需要特殊处理加载。如果 Unsloth 不直接支持 FP8，可能需要先转换为 BF16 或使用 Hugging Face 原生加载方式。
  </action>
  <verify>
`grep -A 10 '"model_name".*GLM' config/config.json` 应显示 GLM 模型配置
  </verify>
  <done>
config/config.json 中 training.sft.model 指向 GLM-4.7-Flash-FP8-Dynamic，target_modules 正确配置
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 train.py 支持 GLM 模型加载</name>
  <files>src/sft/train.py</files>
  <action>
更新 `src/sft/train.py` 以适配 GLM 模型：

1. 更新文件头部文档字符串，将 "Qwen3-4B-Base" 改为 "GLM-4.7-Flash-FP8-Dynamic"

2. 在 `setup_model()` 函数中添加 GLM FP8 模型的加载逻辑：
   - 检测配置中的模型名称是否包含 "GLM"
   - 如果是 GLM FP8 模型，可能需要使用不同的加载方式（FP8 量化模型可能需要 `torch_dtype=torch.float8_e4m3fn` 或先转换为 BF16）
   - 如果 Unsloth 不支持 FP8，使用 `AutoModelForCausalLM.from_pretrained` + `PeftModel` 的标准方式

3. 确保 LoRA 适配器配置与 GLM 架构兼容（GLM 使用类似 LLaMA 的 attention 架构，target_modules 应该兼容）

4. 更新 `ensure_model()` 函数注释，说明 GLM 模型的下载来源

5. 保持 `setup_chat_template()` 不变（自定义标签格式与具体模型无关）

关键点：
- GLM-4.7-Flash-FP8-Dynamic 可能需要特殊处理才能与 Unsloth 兼容
- 如果 FP8 加载失败，考虑使用 BF16 版本或自动转换
- 保留原有 Qwen3 加载逻辑作为 fallback
  </action>
  <verify>
`grep -n "GLM" src/sft/train.py | head -5` 应显示 GLM 相关代码
`python3 -c "from src.sft.train import load_config, setup_model; c=load_config('config/config.json'); print(c['training']['sft']['model'])"` 应无语法错误
  </verify>
  <done>
train.py 支持 GLM-4.7-Flash-FP8-Dynamic 模型加载，LoRA 配置正确，无语法错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证数据格式兼容性</name>
  <files>src/sft/train.py</files>
  <action>
验证 SFT 数据格式与 GLM 兼容（主要检查 tokenizer 行为）：

1. 在 `load_sft_data()` 函数中添加数据格式检查日志：
   - 打印 tokenizer 的 `vocab_size` 和 `pad_token` 设置
   - 验证自定义标签 (`<start_working_out>` 等) 被 tokenizer 正确处理（拆分为 sub-tokens，而非 treated as added tokens）

2. 添加一个简单的测试函数 `test_tokenizer_compatibility()`：
   ```python
   def test_tokenizer_compatibility(tokenizer):
       """测试 tokenizer 对自定义标签的处理"""
       test_tags = ["<start_working_out>", "<end_working_out>", "<SOLUTION>", "</SOLUTION>"]
       for tag in test_tags:
           tokens = tokenizer.encode(tag, add_special_tokens=False)
           print(f"[Tokenizer] {tag} -> {tokens} ({len(tokens)} tokens)")
           # 期望：每个标签被拆分为多个 token（不在词表中）
           # 如果是单 token，说明是 added token，需要警告
           if len(tokens) == 1:
               print(f"  [警告] {tag} 是单 token，可能是 added token!")
       return True
   ```

3. 在 `main()` 中调用此测试函数（在 setup_chat_template 之后）

这一步确保 GLM tokenizer 没有类似 Qwen3 的 added token 问题。
  </action>
  <verify>
`grep -n "test_tokenizer_compatibility" src/sft/train.py` 应显示函数定义
  </verify>
  <done>
train.py 包含 tokenizer 兼容性检查，可以检测 added token 冲突问题
  </done>
</task>

</tasks>

<verification>
1. 配置文件指向 GLM 模型路径
2. train.py 代码无语法错误，可正常导入
3. tokenizer 兼容性检查函数存在
4. GLM 模型加载逻辑正确实现
</verification>

<success_criteria>
- config/config.json 中 model_name 指向 GLM-4.7-Flash-FP8-Dynamic
- train.py 包含 GLM 模型加载代码
- tokenizer 兼容性检查函数已添加
- Python 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-sft/03-01-SUMMARY.md`
</output>
