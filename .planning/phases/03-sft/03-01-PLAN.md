---
phase: 03-sft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config.json
autonomous: true
requirements:
  - SFT-01
  - SFT-02
  - SFT-03
  - SFT-04

must_haves:
  truths:
    - "config.json 中的模型配置指向 unsloth/GLM-4.7-Flash"
    - "LoRA 配置使用 r=16, alpha=16, dropout=0"
    - "Python 配置加载无语法错误"
  artifacts:
    - path: "config/config.json"
      provides: "GLM 模型和 LoRA 配置"
      contains: "GLM-4.7-Flash"
  key_links:
    - from: "src/sft/train.py"
      to: "config/config.json"
      via: "load_config()"
      pattern: "config\\['training'\\]\\['sft'\\]\\['model'\\]"
---
<objective>
更新配置文件以使用 unsloth/GLM-4.7-Flash (BF16) 和新的 LoRA 参数

Purpose: 将模型配置从 FP8-Dynamic 切换到标准 BF16 版本，并使用 Unsloth 官方推荐的 LoRA 配置
Output: 更新后的 config.json，指向正确模型和优化的 LoRA 参数
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 现有配置文件
@config/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新模型和 LoRA 配置</name>
  <files>config/config.json</files>
  <action>
更新 `config/config.json` 中 SFT 模型配置：

1. 修改 `training.sft.model` 配置块：
   - `model_name`: 改为 `"unsloth/GLM-4.7-Flash"` (Unsloth Hugging Face ID)
   - `model_id`: 改为 `"unsloth/GLM-4.7-Flash"` (与 model_name 一致)
   - `lora_rank`: 改为 `16` (从 32 降低，参考 Unsloth notebook)
   - `lora_alpha`: 改为 `16` (从 64 降低，与 rank 匹配)
   - `lora_dropout`: 添加 `"lora_dropout": 0` (Unsloth 推荐 dropout=0)
   - `target_modules`: 保持不变 `["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]`
   - `max_seq_length`: 保持 `2048`
   - `load_in_4bit`: 保持 `false`
   - `gpu_memory_utilization`: 保持 `0.9`

2. 同时更新 `training.grpo.model` 配置块（确保 GRPO 也使用相同的 LoRA 参数）：
   - `lora_rank`: 改为 `16`
   - `lora_alpha`: 改为 `16`
   - 添加 `lora_dropout: 0`

注意：
- 使用 `unsloth/GLM-4.7-Flash` 而非 `unsloth/GLM-4.7-Flash-FP8-Dynamic`
- BF16 格式在训练中更稳定，FP8-Dynamic 已验证有问题
  </action>
  <verify>
```bash
# 验证模型名称
grep -A 2 '"model_name"' config/config.json | grep "GLM-4.7-Flash"

# 验证 LoRA 配置
grep -A 5 '"lora_rank"' config/config.json | grep "16"
```
  </verify>
  <done>
config.json 中 model_name 为 `unsloth/GLM-4.7-Flash`，lora_rank=16, lora_alpha=16, lora_dropout=0
  </done>
</task>

</tasks>

<verification>
1. config/config.json 包含 `unsloth/GLM-4.7-Flash`
2. lora_rank=16, lora_alpha=16
3. Python JSON 解析无错误
</verification>

<success_criteria>
- 模型名称更新为 unsloth/GLM-4.7-Flash
- LoRA 配置符合 Unsloth 官方推荐 (r=16, alpha=16, dropout=0)
- JSON 格式正确无语法错误
</success_criteria>

<output>
After completion, create `.planning/phases/03-sft/03-01-SUMMARY.md`
</output>
