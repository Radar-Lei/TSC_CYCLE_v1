---
phase: 03-sft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config.json
  - src/sft/train.py
autonomous: true
requirements:
  - SFT-01
  - SFT-02
  - SFT-03
  - SFT-04

must_haves:
  truths:
    - "config.json 中的模型配置指向 unsloth/GLM-4.7-Flash"
    - "LoRA 配置使用 r=16, alpha=16, dropout=0"
    - "train.py 从 config 读取 lora_dropout 参数"
    - "Python 配置加载无语法错误"
  artifacts:
    - path: "config/config.json"
      provides: "GLM 模型和 LoRA 配置"
      contains: "GLM-4.7-Flash"
    - path: "src/sft/train.py"
      provides: "SFT 训练脚本"
      contains: "lora_dropout"
  key_links:
    - from: "src/sft/train.py"
      to: "config/config.json"
      via: "config['training']['sft']['model']['lora_dropout']"
      pattern: "lora_dropout"
---
<objective>
更新配置文件和训练脚本以使用 unsloth/GLM-4.7-Flash (BF16) 和新的 LoRA 参数

Purpose: 将模型配置从 FP8-Dynamic 切换到标准 BF16 版本，使用 Unsloth 官方推荐的 LoRA 配置，并确保 train.py 正确读取所有 LoRA 参数
Output: 更新后的 config.json 和 train.py，支持完整的 LoRA 配置读取
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 现有配置文件
@config/config.json
@src/sft/train.py

# 用户决策（来自 03-CONTEXT.md）
# - 模型: unsloth/GLM-4.7-Flash (BF16)
# - LoRA: rank=16, alpha=16, dropout=0
# - 数据路径: outputs/sft/sft_train.jsonl (config.json paths.sft_data_dir)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 config.json 模型和 LoRA 配置</name>
  <files>config/config.json</files>
  <action>
更新 `config/config.json` 中 SFT 模型配置：

1. 修改 `training.sft.model` 配置块：
   - `model_name`: 改为 `"model/GLM-4.7-Flash"` (本地模型路径)
   - `model_id`: 改为 `"unsloth/GLM-4.7-Flash"` (Hugging Face ID，用于下载)
   - `lora_rank`: 改为 `16` (从 32 降低，参考 Unsloth notebook)
   - `lora_alpha`: 改为 `16` (从 64 降低，与 rank 匹配)
   - `lora_dropout`: 添加 `"lora_dropout": 0` (Unsloth 推荐 dropout=0)
   - `target_modules`: 保持不变 `["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]`
   - `max_seq_length`: 保持 `2048`
   - `load_in_4bit`: 保持 `false`
   - `gpu_memory_utilization`: 保持 `0.9`

2. 同时更新 `training.grpo.model` 配置块（确保 GRPO 也使用相同的 LoRA 参数）：
   - `lora_rank`: 改为 `16`
   - `lora_alpha`: 改为 `16`
   - 添加 `lora_dropout: 0`

**数据路径说明**（确认一致性）：
- `paths.sft_data_dir`: `"outputs/sft"` (train.py 使用此路径 + "/sft_train.jsonl")
- 实际数据文件: `outputs/sft/sft_train.jsonl` (已存在)
- CONTEXT.md 中的 `data/sft/train.jsonl` 是旧路径，当前代码实际使用 `outputs/sft/`

注意：
- 使用 `unsloth/GLM-4.7-Flash` 而非 `unsloth/GLM-4.7-Flash-FP8-Dynamic`
- BF16 格式在训练中更稳定，FP8-Dynamic 已验证有问题
  </action>
  <verify>
```bash
# 验证模型名称
grep -A 2 '"model_id"' config/config.json | grep "GLM-4.7-Flash"

# 验证 LoRA 配置
grep '"lora_rank"' config/config.json | grep "16"
grep '"lora_alpha"' config/config.json | grep "16"
grep '"lora_dropout"' config/config.json | grep "0"
```
  </verify>
  <done>
config.json 中 model_id 为 `unsloth/GLM-4.7-Flash`，lora_rank=16, lora_alpha=16, lora_dropout=0
</done>
</task>

<task type="auto">
  <name>Task 2: 更新 train.py 支持 lora_dropout 配置读取</name>
  <files>src/sft/train.py</files>
  <action>
更新 `src/sft/train.py` 以从 config 读取 lora_dropout 参数：

1. **更新文档字符串（第 5 行）**：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

2. **更新 `setup_model()` 函数注释（第 53-57 行）**：
   - 移除 FP8 相关的注释说明
   - 更新为 GLM-4.7-Flash BF16 加载说明

3. **修复 Hugging Face PEFT 回退路径（第 116 行）**：
   - 将硬编码的 `lora_dropout=0.05` 改为从 config 读取：
   ```python
   lora_dropout=model_config.get("lora_dropout", 0),
   ```

4. **更新 Unsloth PEFT 配置（第 124-131 行）**：
   - 添加 `lora_dropout` 参数：
   ```python
   model = FastLanguageModel.get_peft_model(
       model,
       r=model_config["lora_rank"],
       target_modules=model_config["target_modules"],
       lora_alpha=model_config["lora_alpha"],
       lora_dropout=model_config.get("lora_dropout", 0),  # 添加此行
       use_gradient_checkpointing="unsloth",
       random_state=seed,
   )
   ```

5. **更新 `main()` 函数中的日志输出（第 358 行）**：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

6. **更新 `ensure_model()` 函数注释（第 21-22 行）**：
   - 将 "GLM-4.7-Flash-FP8-Dynamic" 改为 "GLM-4.7-Flash"

关键变更：
- 移除所有 FP8-Dynamic 相关的注释和说明
- 确保两处 LoRA 配置都从 config 读取 lora_dropout
- 双重加载策略保持不变（Unsloth 优先，Hugging Face 回退）
  </action>
  <verify>
```bash
# 验证无 FP8-Dynamic 引用
grep -n "FP8-Dynamic" src/sft/train.py && echo "ERROR: FP8-Dynamic 引用仍存在" || echo "OK: 无 FP8-Dynamic 引用"

# 验证 lora_dropout 从 config 读取
grep -n "lora_dropout=model_config" src/sft/train.py | wc -l
# 应该有 2 处（Unsloth 路径和 HF PEFT 路径）

# 验证 GLM-4.7-Flash 引用存在
grep -n "GLM-4.7-Flash" src/sft/train.py | head -3
```
  </verify>
  <done>
train.py 中所有模型引用更新为 GLM-4.7-Flash，两处 LoRA 配置都从 config 读取 lora_dropout
</done>
</task>

<task type="auto">
  <name>Task 3: 验证数据格式与 GLM 模型兼容性 (SFT-04)</name>
  <files>src/sft/train.py (验证输出)</files>
  <action>
验证训练数据在 GLM 模型下可正常加载和处理：

1. **确认训练数据存在**：
   ```bash
   ls -la outputs/sft/sft_train.jsonl
   wc -l outputs/sft/sft_train.jsonl
   head -1 outputs/sft/sft_train.jsonl
   ```

2. **运行 tokenizer 兼容性测试**：
   ```bash
   # 使用 Python 验证 tokenizer 对自定义标签的处理
   python3 -c "
from transformers import AutoTokenizer
tokenizer = AutoTokenizer.from_pretrained('unsloth/GLM-4.7-Flash', trust_remote_code=True)
test_tags = ['<start_working_out>', '<end_working_out>', '<SOLUTION>', '</SOLUTION>']
for tag in test_tags:
    tokens = tokenizer.encode(tag, add_special_tokens=False)
    print(f'{tag} -> {tokens} ({len(tokens)} tokens)')
    if len(tokens) == 1:
        print(f'  WARNING: {tag} is single token - may conflict with vocab!')
"
   ```

3. **验证数据加载逻辑**：
   确认 `test_tokenizer_compatibility()` 函数（train.py 第 187-220 行）在训练时会自动运行并报告警告。

验证目标（SFT-04）：
- 确认数据文件存在且格式正确（JSONL，包含 messages 字段）
- 确认自定义标签被拆分为多个 sub-token（非单 token）
- 确认 train.py 的 tokenizer 兼容性检查正常工作
  </action>
  <verify>
```bash
# 验证数据文件存在
test -f outputs/sft/sft_train.jsonl && echo "OK: 训练数据存在" || echo "ERROR: 训练数据缺失"

# 验证数据格式
head -1 outputs/sft/sft_train.jsonl | python3 -c "import sys,json; d=json.load(sys.stdin); print('OK: JSON格式正确' if 'messages' in d else 'ERROR: 缺少messages字段')"
```
  </verify>
  <done>
训练数据存在于 outputs/sft/sft_train.jsonl，格式正确（JSONL + messages 字段），tokenizer 兼容性检查已集成到训练流程
</done>
</task>

</tasks>

<verification>
1. config.json 包含 `unsloth/GLM-4.7-Flash` 和 `lora_dropout: 0`
2. train.py 从 config 读取 lora_dropout（两处 LoRA 配置）
3. 训练数据存在于 outputs/sft/sft_train.jsonl
4. Python JSON 解析无错误
</verification>

<success_criteria>
- 模型名称更新为 unsloth/GLM-4.7-Flash
- LoRA 配置符合 Unsloth 官方推荐 (r=16, alpha=16, dropout=0)
- train.py 从 config 读取 lora_dropout（SFT-04 验证）
- JSON 格式正确无语法错误
</success_criteria>

<output>
After completion, create `.planning/phases/03-sft/03-01-SUMMARY.md`
</output>
