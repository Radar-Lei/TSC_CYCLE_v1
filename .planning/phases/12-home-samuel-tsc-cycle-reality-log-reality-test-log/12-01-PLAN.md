---
phase: 12-home-samuel-tsc-cycle-reality-log-reality-test-log
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_phase12_reality_log_generation.py
autonomous: true
requirements:
  - PHASE12-GOAL
user_setup: []
must_haves:
  truths:
    - "Phase 12 contracts prove only framed prompt inputs from /home/samuel/TSC_CYCLE/reality.log are eligible input facts."
    - "Phase 12 contracts prove old result/RAW/REASONING/PARSED content cannot contaminate generated outputs."
    - "Phase 12 contracts prove final model output must use <start_working_out>...</end_working_out><SOLUTION>...</SOLUTION> and reject native <think> tags."
    - "Phase 12 contracts prove final reality_test.log is written only after parse, protocol, and hard-constraint gates pass."
  artifacts:
    - path: "tests/test_phase12_reality_log_generation.py"
      provides: "RED parser, protocol, path-safety, report, and atomic-write contracts for Phase 12"
      min_lines: 120
  key_links:
    - from: "tests/test_phase12_reality_log_generation.py"
      to: "tsc_cycle.v4_gates.phase12_reality_test"
      via: "lazy import of parser/runner contracts"
      pattern: "importlib\.import_module\(.*phase12_reality_test"
    - from: "tests/test_phase12_reality_log_generation.py"
      to: "tsc_cycle.v4_gates.phase12_report"
      via: "lazy import of report/gate contracts"
      pattern: "importlib\.import_module\(.*phase12_report"
---

<objective>
Create the Phase 12 RED contract suite that defines how `/home/samuel/TSC_CYCLE/reality.log` is replayed through the latest approved GGUF model into `/home/samuel/TSC_CYCLE/reality_test.log`.

Purpose: lock the input-only parsing, model artifact selection, custom reasoning protocol, output-path safety, fail-closed report, and atomic final-write behavior before implementation.
Output: `tests/test_phase12_reality_log_generation.py` with executable RED tests for Phase 12.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-CONTEXT.md
@.planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-RESEARCH.md
@.planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-PATTERNS.md
@.planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-VALIDATION.md
@tests/test_prompt_builder.py
@tests/test_constraint_lint.py
@tests/test_v4_phase11_eval_matrix.py

<interfaces>
Expected implementation modules for later plans:
- `tsc_cycle.v4_gates.phase12_reality_test`
  - `extract_reality_inputs(reality_log: Path) -> list[RealityInputRecord]`
  - `default_model_artifact() -> Path`
  - `reject_unsafe_phase12_output_path(path: str | Path) -> Path`
  - `render_reality_test_log(records, outputs) -> str`
  - `write_final_log_atomically(text: str, out_log: Path) -> None`
  - `build_parser() -> argparse.ArgumentParser`
  - `main(argv: list[str] | None = None) -> int`
- `tsc_cycle.v4_gates.phase12_report`
  - `evaluate_phase12_report(...) -> dict[str, Any]`
  - `build_parser() -> argparse.ArgumentParser`
  - `main(argv: list[str] | None = None) -> int`

Use lazy imports in tests so collection does not import torch, transformers, peft, bitsandbytes, vllm, or flash_attn.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED parser and artifact-selection contracts</name>
  <files>tests/test_phase12_reality_log_generation.py</files>
  <read_first>
    - tests/test_phase12_reality_log_generation.py (create this file)
    - .planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-CONTEXT.md
    - .planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-RESEARCH.md
    - .planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-PATTERNS.md
    - tests/test_v4_phase11_eval_matrix.py
    - tests/test_prompt_builder.py
  </read_first>
  <behavior>
    - Test extracts only `type=prompt` framed JSON between `【cycle_predict_input_json】` and `【/cycle_predict_input_json】` from a fixture containing misleading preceding/following `type=result`, `RAW:`, `REASONING:`, and `PARSED:` blocks.
    - Test production parsing of `/home/samuel/TSC_CYCLE/reality.log` returns exactly 426 records with 426 unique input hashes when the file exists.
    - Test default artifact selection reads/returns `/home/samuel/TSC_CYCLE/runs/v4.0-4B-20260509T184844Z/gguf/model.q4_K_M.gguf` and does not select `/home/samuel/TSC_CYCLE/runs/20260507T032419Z/gguf/model.q4_K_M.gguf`.
    - Test collection-time imports reject torch, transformers, peft, bitsandbytes, vllm, and flash_attn.
  </behavior>
  <action>Create `tests/test_phase12_reality_log_generation.py` with pytest fixtures modeled on `tests/test_v4_phase11_eval_matrix.py`. Use `importlib.import_module` inside helper functions for `tsc_cycle.v4_gates.phase12_reality_test` and `tsc_cycle.v4_gates.phase12_report`. Include compact inline fixture log text that starts with a result block before the first prompt block to prove old output contamination is ignored. Include absolute constants for `/home/samuel/TSC_CYCLE/reality.log`, `/home/samuel/TSC_CYCLE/reality_test.log`, `/home/samuel/TSC_CYCLE/artifacts/v4/phase12`, the approved q4_K_M model artifact, and the frozen baseline root. Because CONTEXT has no locked D-XX decisions, use Claude's discretion to encode the research recommendation as tests: Phase 11 recommended q4_K_M artifact, framed JSON only, and fail-closed behavior.</action>
  <acceptance_criteria>
    - Source contains a guarded import fixture with `FORBIDDEN_COLLECTION_IMPORTS = {"torch", "transformers", "peft", "bitsandbytes", "vllm", "flash_attn"}`.
    - Source contains no parser logic that reads `RAW:`, `REASONING:`, or `PARSED:` as an input source.
    - RED pytest output fails because planned Phase 12 implementation symbols are missing, not because of syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_phase12_reality_log_generation.py -q >/tmp/phase12_plan01_red.out 2>&1; code=$?; set -e; test "$code" -ne 0 && grep -E "phase12_reality_test|phase12_report|ModuleNotFoundError|AttributeError|ImportError|AssertionError" /tmp/phase12_plan01_red.out</automated>
  </verify>
  <done>RED parser/artifact tests exist, run under pytest, and fail for the missing Phase 12 implementation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED protocol, report, and atomic-write contracts</name>
  <files>tests/test_phase12_reality_log_generation.py</files>
  <read_first>
    - tests/test_phase12_reality_log_generation.py
    - .planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-VALIDATION.md
    - .planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-PATTERNS.md
    - tests/test_prompt_builder.py
    - tests/test_constraint_lint.py
  </read_first>
  <behavior>
    - Test rendered output includes `RAW:` containing the full custom protocol `<start_working_out>...</end_working_out><SOLUTION>...</SOLUTION>`.
    - Test rendered output rejects native `<think>` and `</think>` and malformed `<end_working_out>` close tags.
    - Test report evaluation fails closed if any sample has `parse_error`, `lint_ok=false`, missing reasoning, wrong input count, or missing artifact hash.
    - Test final `/home/samuel/TSC_CYCLE/reality_test.log` is not written when any sample fails parse/lint/protocol gates; only a `.tmp` or artifact cache may exist before success.
  </behavior>
  <action>Extend the same test file with report and output-safety contracts. Use synthetic records and outputs to assert that `render_reality_test_log` emits prompt/result blocks with `RAW`, `PARSED`, and `LINT` sections, while the model-owned raw text remains the full custom protocol. Add path-safety tests requiring writes to be allowed only for `/home/samuel/TSC_CYCLE/reality_test.log`, `/home/samuel/TSC_CYCLE/reality_test.log.tmp`, and descendants of `/home/samuel/TSC_CYCLE/artifacts/v4/phase12`, while rejecting outputs under `/home/samuel/TSC_CYCLE/runs/20260507T032419Z` and unrelated paths. Do not add any implementation fallback that clamps, repairs, or rewrites model solutions.</action>
  <acceptance_criteria>
    - Tests assert `parse_assistant_output()` compatibility rather than duplicating a parser in test logic.
    - Tests assert atomic final-write behavior by checking failed gates leave the final output path absent in `tmp_path` fixtures.
    - Tests assert `phase12_report.json`-style payload includes `ok`, `next_phase_allowed`, `input_count`, `parse_ok_count`, `lint_ok_count`, `model_artifact`, `input_sha256`, and `output_sha256`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_phase12_reality_log_generation.py -q >/tmp/phase12_plan01_red2.out 2>&1; code=$?; set -e; test "$code" -ne 0 && grep -E "phase12_reality_test|phase12_report|ModuleNotFoundError|AttributeError|ImportError|AssertionError" /tmp/phase12_plan01_red2.out</automated>
  </verify>
  <done>RED protocol/report/path-safety/atomic-write tests exist and define the implementation contract for Plans 12-02 and 12-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `reality.log` -> parser | Local prompt log contains trusted inputs mixed with untrusted stale outputs. |
| model raw output -> parser/linter | Generated text may be malformed, truncated, or contain forbidden tags. |
| CLI args -> filesystem writes | User-provided paths can overwrite unrelated files if not constrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `extract_reality_inputs` contract | mitigate | Tests require framed prompt JSON only and reject old result/RAW/REASONING/PARSED as inputs. |
| T-12-02 | Tampering | protocol parser contract | mitigate | Tests require `parse_assistant_output()` compatibility and fail on native `<think>` or malformed close tags. |
| T-12-03 | Tampering | output path contract | mitigate | Tests require `reject_unsafe_phase12_output_path` to allow only `reality_test.log`, its tmp file, and `artifacts/v4/phase12`. |
| T-12-04 | Repudiation | report contract | mitigate | Tests require hashes, counts, and fail-closed `ok/next_phase_allowed` evidence in report payload. |
</threat_model>

<verification>
Overall plan verification: the test file exists and the RED command exits nonzero for missing Phase 12 implementation symbols while proving pytest can collect the contract file.
</verification>

<success_criteria>
- `tests/test_phase12_reality_log_generation.py` exists and encodes parser, protocol, path-safety, report, and atomic-write contracts.
- No tests import heavyweight model stacks at collection time.
- RED failure surface points to `tsc_cycle.v4_gates.phase12_reality_test` and/or `tsc_cycle.v4_gates.phase12_report` missing contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-home-samuel-tsc-cycle-reality-log-reality-test-log/12-01-SUMMARY.md`.
</output>
