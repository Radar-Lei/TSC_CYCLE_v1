---
phase: 01-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_generator/time_period.py
  - src/data_generator/day_simulator.py
  - src/data_generator/models.py
  - src/grpo/data_loader.py
  - src/grpo/__init__.py
  - config/config.json
  - config/schema.json
  - config/validate_config.py
autonomous: true

must_haves:
  truths:
    - "time_period.py 文件不再存在"
    - "代码中没有 time_ranges、TimePeriod、identify_time_period、get_time_period_stats 的引用"
    - "config.json 中没有 time_ranges 配置项"
    - "schema.json 和 validate_config.py 不再存在"
    - "DaySimulator 不再维护 time_ranges 或 simulation_ranges 逻辑"
    - "config.json 是唯一配置源，结构简洁无冗余"
  artifacts:
    - path: "config/config.json"
      provides: "唯一配置源，无 time_ranges"
    - path: "src/data_generator/day_simulator.py"
      provides: "简化的仿真器，无时段逻辑"
  key_links:
    - from: "src/data_generator/day_simulator.py"
      to: "config/config.json"
      via: "config dict"
      pattern: "config.get\\("
---

<objective>
移除所有时段配置相关代码和冗余配置文件，简化 DaySimulator 的仿真流程。

Purpose: 消除不再需要的时段配置代码（早高峰/晚高峰/平峰），以及不再使用的 schema 验证逻辑，使代码库更简洁。
Output: 清理后的 day_simulator.py、config.json，以及删除 time_period.py、schema.json、validate_config.py。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/data_generator/time_period.py
@src/data_generator/day_simulator.py
@src/data_generator/models.py
@src/grpo/data_loader.py
@src/grpo/__init__.py
@config/config.json
@config/schema.json
@config/validate_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除时段配置模块和清理引用</name>
  <files>
    src/data_generator/time_period.py
    src/data_generator/day_simulator.py
    src/data_generator/models.py
    src/grpo/data_loader.py
    src/grpo/__init__.py
  </files>
  <action>
1. **删除 `src/data_generator/time_period.py` 文件**（整文件删除）。

2. **修改 `src/data_generator/day_simulator.py`:**
   - 移除 `from src.data_generator.time_period import identify_time_period` 导入。
   - 删除 `get_simulation_ranges()` 函数（整个函数，L70-L108）。该函数将 time_ranges 转换为秒数区间，不再需要。
   - 在 `DaySimulator.__init__` 中：
     - 移除 `self.time_ranges = config.get('time_ranges', [])` (L212)。
     - 移除 `self.simulation_ranges = get_simulation_ranges(...)` 计算 (L225-229)。
   - 在 `DaySimulator.run()` 中：
     - 移除时段遍历逻辑（`for start_sec, end_sec in self.simulation_ranges` 循环结构，L293-L314 的快进和预热阶段）。
     - 简化为：启动仿真 -> warmup_steps 步预热 -> 逐步采样直到仿真结束（`self.sim_end`）。仿真结束由 `self.simulator.step()` 返回 False 或者 `current_step >= self.sim_end` 判断。
     - 移除 `time_period = identify_time_period(sim_time)` (L378)。
     - 从 TrainingSample 的 metadata 中移除 `'time_period': time_period.value` (L389)。
   - 更新模块 docstring：移除 `get_simulation_ranges` 的提及，移除"只仿真 time_ranges 指定的时段"的描述。

3. **修改 `src/data_generator/models.py`:**
   - TrainingSample 的 metadata 注释中移除 "time_period" (L77 的注释 `元数据 (tl_id, sim_time, date, time_period 等)` 改为 `元数据 (tl_id, sim_time, date 等)`)。

4. **修改 `src/grpo/data_loader.py`:**
   - 删除 `filter_by_time_period()` 函数（L89-L117 整个函数）。
   - 删除底部 `if __name__ == "__main__"` 中与 `filter_by_time_period` 相关的测试代码（L204-L218）。

5. **修改 `src/grpo/__init__.py`:**
   - 移除 `filter_by_time_period` 的导入和导出声明 (L44, L75)。
  </action>
  <verify>
运行以下命令确认没有残留引用：
```bash
grep -r "time_period\|time_ranges\|TimePeriod\|identify_time_period\|get_time_period_stats\|filter_by_time_period\|get_simulation_ranges" src/ --include="*.py" | grep -v __pycache__
```
应返回空结果。

确认文件已删除：
```bash
test ! -f src/data_generator/time_period.py && echo "OK: time_period.py deleted"
```
  </verify>
  <done>
time_period.py 已删除，所有时段相关代码从 day_simulator.py、models.py、data_loader.py、__init__.py 中清除，grep 搜索无残留引用。
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除 schema 验证和清理配置文件</name>
  <files>
    config/config.json
    config/schema.json
    config/validate_config.py
  </files>
  <action>
1. **删除 `config/schema.json` 文件**（整文件删除）。

2. **删除 `config/validate_config.py` 文件**（整文件删除）。

3. **修改 `config/config.json`:**
   - 移除 `"time_ranges": []` 配置项（simulation 段中 L20）。
   - 确认最终结构包含且仅包含:
     ```json
     {
       "training": {
         "sft": { "max_steps", "batch_size", "learning_rate", "gradient_accumulation_steps" },
         "grpo": { "max_steps", "num_generations", "learning_rate", "gradient_accumulation_steps" }
       },
       "simulation": {
         "parallel_workers": 12,
         "warmup_steps": 300,
         "max_rou_files": 1
       },
       "rewards": {
         "format_weight": 0.2,
         "simulation_weight": 0.8
       },
       "paths": {
         "data_dir": "data/training",
         "sft_output": "outputs/sft",
         "grpo_output": "outputs/grpo",
         "environments_dir": "sumo_simulation/environments"
       }
     }
     ```
   - 注意: `paths` 中保留现有字段不变。`simulation` 中的 `time_ranges` 删除后不需要替代字段。

4. **检查是否有其他代码引用 `validate_config` 或 `schema.json`:**
   - 搜索 `from config.validate_config`、`import validate_config`、`schema.json` 在 src/ 中的引用，如有则清除。
  </action>
  <verify>
运行以下命令：
```bash
test ! -f config/schema.json && echo "OK: schema.json deleted"
test ! -f config/validate_config.py && echo "OK: validate_config.py deleted"
python3 -c "import json; json.load(open('config/config.json'))" && echo "OK: config.json valid JSON"
grep -r "schema.json\|validate_config" src/ config/ --include="*.py" --include="*.json" | grep -v __pycache__
```
最后一个 grep 应返回空结果。
  </verify>
  <done>
schema.json 和 validate_config.py 已删除，config.json 中 time_ranges 已移除，配置文件是有效 JSON，无残留引用。
  </done>
</task>

</tasks>

<verification>
1. `grep -r "time_period\|time_ranges\|TimePeriod\|identify_time_period\|get_time_period_stats\|filter_by_time_period\|get_simulation_ranges" src/ config/ --include="*.py" --include="*.json" | grep -v __pycache__` 返回空
2. `test ! -f src/data_generator/time_period.py` 成功
3. `test ! -f config/schema.json` 成功
4. `test ! -f config/validate_config.py` 成功
5. `python3 -c "import json; json.load(open('config/config.json'))"` 成功
6. DaySimulator 仿真流程简化为：启动 -> 预热 -> 逐步采样 -> 结束
</verification>

<success_criteria>
- 时段配置相关代码全部移除（CLEAN-01）
- 配置文件结构统一，无冗余配置项（CLEAN-04 部分）
- schema 验证逻辑删除
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup/01-01-SUMMARY.md`
</output>
