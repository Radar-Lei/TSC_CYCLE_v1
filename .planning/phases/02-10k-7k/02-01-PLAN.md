---
phase: 02-10k-7k
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_v3_datagen_inputs.py
  - tests/test_v3_labeler.py
  - tests/test_v3_datagen_merge.py
autonomous: true
requirements: [DATAGEN-01, DATAGEN-02, DATAGEN-03, DATAGEN-04, DATAGEN-05, DATAGEN-06, DATAGEN-07]
must_haves:
  truths:
    - "Phase 2 has RED tests for three-source reservoir generation before implementation."
    - "Phase 2 has RED tests for resume-safe GPT-5.5 high labeling before implementation."
    - "Phase 2 has RED tests proving frozen baseline bytes remain unchanged and merged valid samples reach at least 9000."
  artifacts:
    - path: "tests/test_v3_datagen_inputs.py"
      provides: "DATAGEN-01/02 reservoir, dedupe, and targeted provenance tests"
      min_lines: 80
    - path: "tests/test_v3_labeler.py"
      provides: "DATAGEN-03/04/05 labeler API, resume, lint-drop, and worker-cap tests"
      min_lines: 90
    - path: "tests/test_v3_datagen_merge.py"
      provides: "DATAGEN-06/07 merge and frozen-baseline invariant tests"
      min_lines: 80
  key_links:
    - from: "tests/test_v3_datagen_inputs.py"
      to: "tsc_cycle/sample_inputs.py"
      via: "imports build_v3_phase2_reservoir or equivalent public function"
      pattern: "build_v3_phase2_reservoir"
    - from: "tests/test_v3_labeler.py"
      to: "tsc_cycle/teacher/labeler.py"
      via: "imports build_parser/run_labeling entry points"
      pattern: "run_labeling|build_parser"
    - from: "tests/test_v3_datagen_merge.py"
      to: "tsc_cycle/v3_gates/phase2_datagen_report.py"
      via: "imports build_phase2_report gate function"
      pattern: "build_phase2_report"
---

<objective>
Create the Phase 2 Wave 0 validation scaffold so every DATAGEN requirement is represented by executable tests before implementation.

Purpose: Prevent API budget waste and frozen-baseline mutation by making the critical invariants executable first.
Output: RED pytest coverage for candidate generation, labeler hardening, and merge/report gates.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-10k-7k/02-CONTEXT.md
@.planning/phases/02-10k-7k/02-RESEARCH.md
@.planning/phases/02-10k-7k/02-VALIDATION.md
@.planning/phases/02-10k-7k/02-PATTERNS.md
@tsc_cycle/sample_inputs.py
@tsc_cycle/teacher/labeler.py
@tsc_cycle/constraint_lint.py
@tsc_cycle/hashing.py
@tsc_cycle/prompt_builder.py
@tests/test_hashing.py
@tests/test_constraint_lint.py
@tests/test_prompt_builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reservoir and targeted-sampler RED tests</name>
  <read_first>
    .planning/phases/02-10k-7k/02-RESEARCH.md
    .planning/phases/02-10k-7k/02-PATTERNS.md
    tsc_cycle/sample_inputs.py
    tsc_cycle/hashing.py
    tests/test_hashing.py
    tests/test_constraint_lint.py
  </read_first>
  <files>tests/conftest.py, tests/test_v3_datagen_inputs.py</files>
  <behavior>
    - Test 1: a Phase 2 reservoir contains exactly the requested source counts for `same_dist`, `ood`, and `targeted` records.
    - Test 2: every generated record has a 64-character `sample_id`, a `source` in `{"same_dist", "ood", "targeted"}`, and no `sample_id` overlap with frozen baseline labels.
    - Test 3: targeted records are derived from `lint_ok=false` or `mae > 10.0` eval rows but do not reuse the seed `sample_id` verbatim.
  </behavior>
  <action>Create reusable tiny fixtures in `tests/conftest.py` only if they are not already present: `mini_phase2_prior`, `mini_old_labeled_jsonl`, and `mini_per_sample_eval_jsonl`. Create `tests/test_v3_datagen_inputs.py` that imports `build_v3_phase2_reservoir` from `tsc_cycle.sample_inputs` and asserts DATAGEN-01/DATAGEN-02 behavior using small counts such as 6 same-dist, 3 OOD, and 2 targeted samples. The tests must use `hashing.sample_id()` for identity and must not read or write production `data/labeled.jsonl`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_inputs.py</automated>
  </verify>
  <done>The test file is syntactically valid and fails only because the Phase 2 reservoir API is not implemented yet, or passes if that API already exists.</done>
  <acceptance_criteria>
    - `tests/test_v3_datagen_inputs.py` contains exact strings `same_dist`, `ood`, `targeted`, `mae > 10.0`, and `build_v3_phase2_reservoir`.
    - The focused pytest command above collects the tests without syntax errors.
    - No test writes to `data/labeled.jsonl`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add labeler hardening RED tests</name>
  <read_first>
    .planning/phases/02-10k-7k/02-RESEARCH.md
    .planning/phases/02-10k-7k/02-PATTERNS.md
    tsc_cycle/teacher/labeler.py
    tsc_cycle/teacher/client.py
    tsc_cycle/constraint_lint.py
    tsc_cycle/prompt_builder.py
    tests/test_prompt_builder.py
  </read_first>
  <files>tests/test_v3_labeler.py</files>
  <behavior>
    - Test 1: CLI/config rejects or exits nonzero when `workers > 10` and defaults to `model=gpt-5.5` with `effort=high`.
    - Test 2: `--input-files`, `--exclude-labeled`, `--labeled`, `--rejected`, and `--cache-dir` are honored, and the protected baseline path cannot be used as the Phase 2 label output.
    - Test 3: done IDs from both labeled and rejected JSONL files are skipped on resume so duplicate API calls are not made.
    - Test 4: a lint-failing successful teacher response is appended to rejected output once and is not retried.
  </behavior>
  <action>Create `tests/test_v3_labeler.py` with fake `TeacherClient`/result objects and tmp_path JSONL fixtures. The tests must not import OpenAI, must not require `OPENAI_API_KEY`, and must assert that no serialized output contains an API key value. Target public entry points `build_parser` and `run_labeling(args, client_factory=...)`; if the current labeler lacks those entry points, leave tests RED for the implementation plan.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_labeler.py</automated>
  </verify>
  <done>The labeler tests encode DATAGEN-03/DATAGEN-04/DATAGEN-05 invariants without making network calls.</done>
  <acceptance_criteria>
    - `tests/test_v3_labeler.py` contains exact strings `workers > 10`, `--input-files`, `--exclude-labeled`, `--cache-dir`, `gpt-5.5`, and `high`.
    - The focused pytest command collects the tests without syntax errors.
    - The tests fail only on missing labeler behavior or pass if the behavior already exists.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add merge and frozen-baseline RED tests</name>
  <read_first>
    .planning/phases/02-10k-7k/02-RESEARCH.md
    .planning/phases/02-10k-7k/02-VALIDATION.md
    tsc_cycle/constraint_lint.py
    tsc_cycle/manifest.py
    tests/test_v3_phase1_report.py
  </read_first>
  <files>tests/test_v3_datagen_merge.py</files>
  <behavior>
    - Test 1: merge gate returns `ok=true` when old valid count is 3000, new valid count is at least 6000, merged valid count is at least 9000, and old/new overlap is zero.
    - Test 2: merge gate returns `ok=false` when the frozen baseline SHA before and after differ.
    - Test 3: merge gate returns `ok=false` when any new accepted record violates `constraint_lint.validate()`.
  </behavior>
  <action>Create `tests/test_v3_datagen_merge.py` that imports `build_phase2_report` from `tsc_cycle.v3_gates.phase2_datagen_report`. Use tmp_path files for old labels, new labels, rejects, manifest, merged output, and report output. Tests must assert exact report fields: `old_sha_before`, `old_sha_after`, `old_count`, `new_valid`, `merged_valid`, `old_new_overlap`, `all_new_lint_ok`, `ok`, and `requirements_covered` containing DATAGEN-01 through DATAGEN-07.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_merge.py</automated>
  </verify>
  <done>The merge tests encode DATAGEN-06/DATAGEN-07 and malformed-output rejection gates without touching production data.</done>
  <acceptance_criteria>
    - `tests/test_v3_datagen_merge.py` contains exact strings `new_valid`, `merged_valid`, `old_sha_before`, `old_sha_after`, and `requirements_covered`.
    - The focused pytest command collects the tests without syntax errors.
    - No test writes to `data/labeled.jsonl`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests→production paths | Tests must not mutate production baseline or live cache paths. |
| fake client→labeler | Mocked teacher results cross the same parsing/linting boundary as live API results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-api-key | I | `tests/test_v3_labeler.py` | mitigate | Tests must run without `OPENAI_API_KEY` and assert serialized records do not contain a sentinel secret string. |
| T-02-budget-replay | D | resume tests | mitigate | Done-set tests require labeled and rejected IDs to be skipped before any fake client call. |
| T-02-baseline-tamper | T | merge tests | mitigate | Tests use tmp_path baseline copies and assert SHA mismatch fails the gate. |
| T-02-malformed-output | T | labeler/merge tests | mitigate | Tests require lint-failing outputs to be rejected and all accepted new labels to pass `constraint_lint.validate()`. |
</threat_model>

<verification>
Run all Wave 0 tests together:

```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_inputs.py tests/test_v3_labeler.py tests/test_v3_datagen_merge.py
```

RED is acceptable only when caused by missing implementation symbols or assertions that later plans implement; syntax/import errors unrelated to planned public APIs must be fixed in this plan.
</verification>

<success_criteria>
- Every DATAGEN-01..07 requirement has at least one RED/green test assertion.
- Tests are local, deterministic, and never call OpenAI.
- Tests do not mutate `data/labeled.jsonl` or root `raw_responses/`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-10k-7k/02-01-SUMMARY.md`.
</output>
