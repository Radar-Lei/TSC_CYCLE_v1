---
phase: 02-10k-7k
plan: "02"
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - tsc_cycle/sample_inputs.py
  - scripts/generate_v3_phase2_inputs.sh
  - data/v3/phase2/inputs_same_dist.jsonl
  - data/v3/phase2/inputs_ood.jsonl
  - data/v3/phase2/inputs_targeted.jsonl
  - data/v3/phase2/inputs_all.jsonl
  - data/v3/phase2/datagen_manifest.json
autonomous: true
requirements: [DATAGEN-01, DATAGEN-02]
must_haves:
  truths:
    - "User can generate a Phase 2 candidate reservoir with same-dist, OOD, and targeted sources."
    - "User can execute the reservoir wrapper before any labeling and prove at least 7,000 new inputs exist."
    - "Generated candidate sample IDs are unique and do not overlap v1.0 `data/labeled.jsonl`."
    - "Targeted samples are derived from v1.0 high-MAE or lint-fail seeds but are not verbatim seed copies."
  artifacts:
    - path: "tsc_cycle/sample_inputs.py"
      provides: "Phase 2 reservoir builder and CLI support"
      exports: ["build_v3_phase2_reservoir"]
    - path: "scripts/generate_v3_phase2_inputs.sh"
      provides: "Reproducible reservoir generation command"
      contains: "data/v3/phase2/inputs_all.jsonl"
    - path: "data/v3/phase2/inputs_all.jsonl"
      provides: "Generated Phase 2 candidate reservoir with at least 7,000 records"
      min_lines: 7000
    - path: "data/v3/phase2/datagen_manifest.json"
      provides: "Source counts, old-overlap gate, self-duplicate gate, and frozen-baseline SHA evidence"
      contains: "counts_written"
  key_links:
    - from: "scripts/generate_v3_phase2_inputs.sh"
      to: "tsc_cycle/sample_inputs.py"
      via: "python module invocation"
      pattern: "python -m tsc_cycle.sample_inputs"
    - from: "tsc_cycle/sample_inputs.py"
      to: "data/labeled.jsonl"
      via: "exclude-labeled read-only identity set"
      pattern: "exclude.*labeled"
---

<objective>
Implement the isolated Phase 2 candidate reservoir generator.

Purpose: Produce new inputs for GPT-5.5 high labeling while preserving v1.0 baseline bytes and adding targeted coverage of v1.0 failure neighborhoods.
Output: `data/v3/phase2/inputs_*.jsonl` generation capability and a reproducible shell wrapper.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-10k-7k/02-CONTEXT.md
@.planning/phases/02-10k-7k/02-RESEARCH.md
@.planning/phases/02-10k-7k/02-PATTERNS.md
@tests/test_v3_datagen_inputs.py
@tsc_cycle/sample_inputs.py
@tsc_cycle/distribution_fit.py
@tsc_cycle/hashing.py
@tsc_cycle/constraint_lint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement three-source Phase 2 reservoir builder</name>
  <read_first>
    tests/test_v3_datagen_inputs.py
    tsc_cycle/sample_inputs.py
    tsc_cycle/distribution_fit.py
    tsc_cycle/hashing.py
    .planning/phases/02-10k-7k/02-PATTERNS.md
  </read_first>
  <files>tsc_cycle/sample_inputs.py</files>
  <behavior>
    - `build_v3_phase2_reservoir(prior, counts, seed, exclude_ids, per_sample_path=None)` returns deterministic records with source counts matching the requested dict.
    - Same-dist records use existing `_sample_in_distribution`; OOD records use existing `_apply_ood_mutations`; targeted records use eval seed rows where `lint_ok is False` or `mae > 10.0`.
    - Every output record has `sample_id`, `split_hint`, `trivial`, `source`, and for targeted samples `targeted_seed_id` and `targeted_reason`.
  </behavior>
  <action>Extend `tsc_cycle/sample_inputs.py` rather than creating a parallel generator. Add a public `build_v3_phase2_reservoir` function that accepts a count mapping with exact keys `same_dist`, `ood`, `targeted`; excludes every ID in `exclude_ids`; self-deduplicates using `hashing.sample_id`; tags records with `source`; and for targeted records loads seed IDs from `runs/20260507T032419Z/eval/per_sample.jsonl`-style rows using `lint_ok is False` or `mae > 10.0`. Targeted generation must perturb at least one numeric field (`pred_wait`, `pred_saturation`, `min_green`, `max_green`, or `capacity`) and recompute `sample_id`, so the targeted output ID is never equal to `targeted_seed_id`. Use fixed random seed behavior so reruns are byte-stable.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_inputs.py::test_three_source_reservoir_counts tests/test_v3_datagen_inputs.py::test_targeted_seed_provenance tests/test_v3_datagen_inputs.py::test_no_overlap_with_v1_labeled</automated>
  </verify>
  <done>DATAGEN-01 and DATAGEN-02 input-generation tests pass with deterministic source tagging, targeted provenance, and zero old-ID overlap.</done>
  <acceptance_criteria>
    - `tsc_cycle/sample_inputs.py` exports `build_v3_phase2_reservoir`.
    - Generated records contain exact source values `same_dist`, `ood`, and `targeted`.
    - Targeted records contain `targeted_seed_id` and never have `sample_id == targeted_seed_id`.
    - The focused pytest command passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add reproducible Phase 2 reservoir CLI and wrapper</name>
  <read_first>
    tsc_cycle/sample_inputs.py
    tests/test_v3_datagen_inputs.py
    .planning/phases/02-10k-7k/02-RESEARCH.md
  </read_first>
  <files>tsc_cycle/sample_inputs.py, scripts/generate_v3_phase2_inputs.sh</files>
  <action>Add CLI arguments to `tsc_cycle/sample_inputs.py` for Phase 2 generation without breaking the existing v1 CLI: `--v3-phase2`, `--prior data/dist_prior.json`, `--exclude-labeled data/labeled.jsonl`, `--per-sample runs/20260507T032419Z/eval/per_sample.jsonl`, `--out-dir data/v3/phase2`, `--same-dist 5250`, `--ood 1500`, `--targeted 750`, and `--seed 42`. When `--v3-phase2` is set, write `inputs_same_dist.jsonl`, `inputs_ood.jsonl`, `inputs_targeted.jsonl`, `inputs_all.jsonl`, and `datagen_manifest.json` under the out-dir. The manifest must include `counts_requested`, `counts_written`, `old_id_exclusion_count`, `overlap_with_old_labeled`, `self_duplicate_count`, `seed`, and source output paths. Create `scripts/generate_v3_phase2_inputs.sh` with `set -euo pipefail` and an absolute venv Python path `/home/samuel/TSC_CYCLE/.venv/bin/python`; it must invoke the CLI with the default 5250/1500/750 reservoir and must never write to `data/labeled.jsonl`.</action>
  <verify>
    <automated>bash -n /home/samuel/TSC_CYCLE/scripts/generate_v3_phase2_inputs.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_inputs.py</automated>
  </verify>
  <done>The CLI and wrapper can generate the 7,500-candidate reservoir under `data/v3/phase2/` with manifest overlap gates set to zero.</done>
  <acceptance_criteria>
    - `scripts/generate_v3_phase2_inputs.sh` contains `/home/samuel/TSC_CYCLE/.venv/bin/python`, `--v3-phase2`, `--same-dist 5250`, `--ood 1500`, `--targeted 750`, and `data/v3/phase2`.
    - `tsc_cycle/sample_inputs.py` writes `datagen_manifest.json` with `overlap_with_old_labeled` and `self_duplicate_count`.
    - `data/labeled.jsonl` is read only as `--exclude-labeled`, never opened for writing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Execute and gate the Phase 2 reservoir before labeling</name>
  <read_first>
    scripts/generate_v3_phase2_inputs.sh
    tsc_cycle/sample_inputs.py
    data/labeled.jsonl
  </read_first>
  <files>data/v3/phase2/inputs_same_dist.jsonl, data/v3/phase2/inputs_ood.jsonl, data/v3/phase2/inputs_targeted.jsonl, data/v3/phase2/inputs_all.jsonl, data/v3/phase2/datagen_manifest.json</files>
  <action>After Tasks 1-2 pass, execute `/home/samuel/TSC_CYCLE/scripts/generate_v3_phase2_inputs.sh` before any smoke or full labeling task is run. Capture `data/labeled.jsonl` SHA-256 immediately before and after generation in `datagen_manifest.json` fields `old_sha_before` and `old_sha_after`. Verify `inputs_all.jsonl` has at least 7000 records and normally exactly 7500 records from the pre-declared reservoir decision in 02-RESEARCH.md. Verify source counts match 5250 same-dist, 1500 OOD, and 750 targeted either by counting `source` in `inputs_all.jsonl` or by comparing against `datagen_manifest.json.counts_written`. Verify `overlap_with_old_labeled == 0`, `self_duplicate_count == 0`, and `old_sha_before == old_sha_after`. If any gate fails, stop before Plan 03 smoke/full labeling; do not generate replacement samples after seeing lint outcomes.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/scripts/generate_v3_phase2_inputs.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -c "import json, pathlib; root=pathlib.Path('/home/samuel/TSC_CYCLE'); rows=[json.loads(x) for x in (root/'data/v3/phase2/inputs_all.jsonl').read_text().splitlines() if x.strip()]; manifest=json.loads((root/'data/v3/phase2/datagen_manifest.json').read_text()); counts={k:sum(1 for r in rows if r.get('source')==k) for k in ['same_dist','ood','targeted']}; assert len(rows) >= 7000, len(rows); assert counts == {'same_dist':5250,'ood':1500,'targeted':750}, counts; assert manifest.get('overlap_with_old_labeled') == 0; assert manifest.get('self_duplicate_count') == 0; assert manifest.get('old_sha_before') == manifest.get('old_sha_after')"</automated>
  </verify>
  <done>Before any paid GPT-5.5 labeling, Phase 2 has a generated reservoir with ≥7000 new inputs, expected source counts, zero old overlap, zero self duplicates, and unchanged baseline SHA.</done>
  <acceptance_criteria>
    - `data/v3/phase2/inputs_all.jsonl` exists with at least 7000 JSONL records.
    - Source counts are exactly `same_dist=5250`, `ood=1500`, and `targeted=750` unless the manifest explicitly records an equivalent count mapping.
    - `datagen_manifest.json` records `overlap_with_old_labeled: 0`, `self_duplicate_count: 0`, and equal old SHA fields.
    - `data/labeled.jsonl` SHA is unchanged and labeling plans remain blocked if this gate fails.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen baseline→generator | `data/labeled.jsonl` is trusted historical data and must be read-only. |
| v1 eval artifacts→targeted sampler | Historical eval rows seed new records but must not be copied verbatim into training. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-api-key | I | reservoir generator | accept | This plan makes no API calls and must not read environment secrets. |
| T-02-budget-replay | D | candidate identity | mitigate | Stable `sample_id` and manifest dedupe prevent duplicate candidate submission downstream. |
| T-02-baseline-tamper | T | `sample_inputs.py` CLI | mitigate | Baseline path is only accepted as `--exclude-labeled`; all writes are under `data/v3/phase2/`. |
| T-02-malformed-output | T | candidate schema | mitigate | Use existing input schema and `is_trivial`/phase fields; malformed teacher outputs are handled in later labeler plan. |
</threat_model>

<verification>
```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_inputs.py
bash -n /home/samuel/TSC_CYCLE/scripts/generate_v3_phase2_inputs.sh
```
</verification>

<success_criteria>
- `scripts/generate_v3_phase2_inputs.sh` produces and gates at least 7,000 new candidates in isolated Phase 2 paths before labeling.
- `datagen_manifest.json` proves old/new overlap is zero, self-duplicate count is zero, and `data/labeled.jsonl` SHA is unchanged.
- No Phase 3 tokenization or split artifacts are created.
</success_criteria>

<output>
After completion, create `.planning/phases/02-10k-7k/02-02-SUMMARY.md`.
</output>
