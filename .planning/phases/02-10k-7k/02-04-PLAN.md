---
phase: 02-10k-7k
plan: "04"
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - tsc_cycle/v3_gates/phase2_datagen_report.py
  - scripts/run_v3_phase2_merge.sh
autonomous: true
requirements: [DATAGEN-01, DATAGEN-02, DATAGEN-03, DATAGEN-04, DATAGEN-05, DATAGEN-06, DATAGEN-07]
must_haves:
  truths:
    - "User can merge frozen v1.0 valid labels with Phase 2 new lint-pass labels into an isolated v3 dataset."
    - "Merge gate proves old/new overlap is zero and baseline SHA is unchanged."
    - "Merge report includes DATAGEN-03 labeler evidence: model gpt-5.5, effort high, worker cap <=10, and attempted new-label count."
    - "Merge report includes DATAGEN-05 resume/no-duplicate evidence from labeled/rejected done IDs."
    - "Merge gate fails closed unless new_valid >= 6000 and merged_valid >= 9000."
  artifacts:
    - path: "tsc_cycle/v3_gates/phase2_datagen_report.py"
      provides: "Phase 2 merge/report gate covering DATAGEN-01..07"
      exports: ["build_phase2_report", "main"]
    - path: "scripts/run_v3_phase2_merge.sh"
      provides: "Reproducible merge/report command"
      contains: "labeled_merged.jsonl"
    - path: "data/v3/phase2/labeled_merged.jsonl"
      provides: "Phase 3 raw merged dataset input"
    - path: "data/v3/phase2/merge_report.json"
      provides: "Final Phase 2 evidence report covering DATAGEN-01..07 including labeler and resume/no-duplicate evidence"
      contains: "requirements_covered"
  key_links:
    - from: "tsc_cycle/v3_gates/phase2_datagen_report.py"
      to: "tsc_cycle.constraint_lint.validate"
      via: "all accepted new records must lint-pass"
      pattern: "validate\(.*solution"
    - from: "scripts/run_v3_phase2_merge.sh"
      to: "data/labeled.jsonl"
      via: "read-only old labels path"
      pattern: "--old-labeled data/labeled.jsonl"
---

<objective>
Implement the Phase 2 merge/report gate.

Purpose: Convert isolated new labels into a Phase 3-ready merged raw JSONL only when all data-governance gates pass.
Output: `labeled_merged.jsonl`, `merge_report.json`, and a fail-closed CLI/script.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-10k-7k/02-CONTEXT.md
@.planning/phases/02-10k-7k/02-RESEARCH.md
@.planning/phases/02-10k-7k/02-PATTERNS.md
@tests/test_v3_datagen_merge.py
@tsc_cycle/manifest.py
@tsc_cycle/constraint_lint.py
@tsc_cycle/hashing.py
@tsc_cycle/v3_gates/phase1_report.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fail-closed Phase 2 report builder</name>
  <read_first>
    tests/test_v3_datagen_merge.py
    tsc_cycle/constraint_lint.py
    tsc_cycle/hashing.py
    tsc_cycle/manifest.py
    tsc_cycle/v3_gates/phase1_report.py
    .planning/phases/02-10k-7k/02-PATTERNS.md
  </read_first>
  <files>tsc_cycle/v3_gates/phase2_datagen_report.py</files>
  <behavior>
    - `build_phase2_report` reads old labels, new labels, rejected labels, and datagen manifest; computes SHA/count/overlap/lint gates.
    - It writes merged output only when old SHA is unchanged, old/new overlap is zero, all accepted new records lint-pass, `new_valid >= 6000`, and `merged_valid >= 9000`.
    - It returns `ok=false` with `fatal_failures` when any gate fails.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/phase2_datagen_report.py` following the JSON report style from `phase1_report.py`. Implement `build_phase2_report(old_labeled, new_labeled, rejected, datagen_manifest, merged_out, report_out=None, expected_old_sha=None, min_new_valid=6000, min_merged_valid=9000)`. Read old and new JSONL records, compute SHA-256 before and after for `old_labeled`, derive `old_count`, `new_valid`, `rejected_count`, `merged_valid`, `old_new_overlap`, `source_counts`, and `all_new_lint_ok`. For each new accepted record, validate `record["input"]` against `record["result"]["solution"]` using `constraint_lint.validate()`. Include `requirements_covered` exactly as `["DATAGEN-01", "DATAGEN-02", "DATAGEN-03", "DATAGEN-04", "DATAGEN-05", "DATAGEN-06", "DATAGEN-07"]`. Include a `labeler_evidence` object proving DATAGEN-03 with fields `model`, `effort`, `workers_max`, `workers_within_cap`, `attempted_new`, `accepted_new`, and `rejected_new`; derive counts from accepted and rejected JSONL records and require model/effort defaults `gpt-5.5`/`high` unless overridden by optional CLI evidence args. Include a `resume_evidence` object proving DATAGEN-05 with fields `done_ids_total`, `duplicate_done_ids`, `duplicate_api_attempt_ids`, and `append_outputs_present`; compute duplicate done IDs across `new_labeled` plus `rejected`, and fail closed if duplicate accepted/rejected records exist for the same `sample_id`. If gates pass, write merged records as old records followed by new accepted records to `merged_out`; if gates fail, do not overwrite an existing `merged_out`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_merge.py</automated>
  </verify>
  <done>Phase 2 merge/report tests pass and fail-closed behavior protects baseline and Phase 3 input quality.</done>
  <acceptance_criteria>
    - `tsc_cycle/v3_gates/phase2_datagen_report.py` exports `build_phase2_report` and `main`.
    - Report JSON contains exact fields `old_sha_before`, `old_sha_after`, `old_count`, `new_valid`, `merged_valid`, `old_new_overlap`, `all_new_lint_ok`, `labeler_evidence`, `resume_evidence`, `ok`, `fatal_failures`, and `requirements_covered`.
    - The focused pytest command passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add merge CLI and operational wrapper</name>
  <read_first>
    tsc_cycle/v3_gates/phase2_datagen_report.py
    tests/test_v3_datagen_merge.py
    .planning/phases/02-10k-7k/02-RESEARCH.md
  </read_first>
  <files>tsc_cycle/v3_gates/phase2_datagen_report.py, scripts/run_v3_phase2_merge.sh</files>
  <action>Add argparse CLI to `phase2_datagen_report.py` with defaults: `--old-labeled data/labeled.jsonl`, `--new-labeled data/v3/phase2/labeled_new.jsonl`, `--rejected data/v3/phase2/rejected_new.jsonl`, `--datagen-manifest data/v3/phase2/datagen_manifest.json`, `--merged-out data/v3/phase2/labeled_merged.jsonl`, `--report-out data/v3/phase2/merge_report.json`, `--min-new-valid 6000`, `--min-merged-valid 9000`, optional `--expected-old-sha`, optional `--labeler-model gpt-5.5`, optional `--labeler-effort high`, and optional `--workers-max 10`. The CLI must print the report JSON and exit 0 only when `ok=true`. Create `scripts/run_v3_phase2_merge.sh` with `set -euo pipefail`, absolute venv Python path, and `git diff --quiet -- data/labeled.jsonl` before and after the CLI call. The wrapper must not run tokenization or split work; Phase 3 owns that.</action>
  <verify>
    <automated>bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase2_merge.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/phase2_datagen_report.py && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_merge.py</automated>
  </verify>
  <done>The merge/report gate is executable from CLI and wrapper with Phase 2 thresholds and frozen-baseline guards.</done>
  <acceptance_criteria>
    - Wrapper contains exact strings `--min-new-valid 6000`, `--min-merged-valid 9000`, `labeled_merged.jsonl`, and `merge_report.json`.
    - Wrapper runs `git diff --quiet -- data/labeled.jsonl` before and after the merge command.
    - The automated verify command passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new label JSONL→merged dataset | Accepted records become Phase 3 raw training data only after lint and count gates. |
| frozen baseline→merge process | v1.0 data is read for inclusion and integrity checks but must not be modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-api-key | I | report builder | accept | This plan reads only local JSONL/report files and no environment secrets. |
| T-02-budget-replay | D | merge report | mitigate | Report surfaces `new_valid`, `rejected_count`, and overlap so operators know whether reserve labeling is needed without blind reruns. |
| T-02-baseline-tamper | T | old label SHA gate | mitigate | Compute before/after SHA, require equality, and wrapper checks git diff. |
| T-02-malformed-output | T | `all_new_lint_ok` gate | mitigate | Re-run `constraint_lint.validate()` on every accepted new record before merge. |
</threat_model>

<verification>
```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v3_datagen_merge.py
/home/samuel/TSC_CYCLE/.venv/bin/python -m py_compile tsc_cycle/v3_gates/phase2_datagen_report.py
bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase2_merge.sh
```
</verification>

<success_criteria>
- Merge report covers DATAGEN-01..07 and fails closed on SHA mismatch, overlap, insufficient counts, lint violations, worker cap violations, or duplicate done IDs.
- Passing merge writes `data/v3/phase2/labeled_merged.jsonl` with old records followed by new valid records.
- Phase 3 can consume the merged JSONL, but no tokenization/split is performed here.
</success_criteria>

<output>
After completion, create `.planning/phases/02-10k-7k/02-04-SUMMARY.md`.
</output>
