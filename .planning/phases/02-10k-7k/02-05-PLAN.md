---
phase: 02-10k-7k
plan: "05"
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - scripts/run_v3_phase2_all.sh
  - data/v3/phase2/README.phase2.txt
autonomous: false
requirements: [DATAGEN-01, DATAGEN-02, DATAGEN-03, DATAGEN-04, DATAGEN-05, DATAGEN-06, DATAGEN-07]
user_setup:
  - service: openai
    why: "Full GPT-5.5 high labeling requires a paid API key and explicit operator approval after smoke"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard or user's shell secret manager; must be exported before running full labeling"
must_haves:
  truths:
    - "User can run Phase 2 in an ordered operational flow: generate reservoir, smoke label, full label, merge/report."
    - "Full-run wrapper checkpoints every 500 attempted samples using local reports, worker-cap evidence, reject stats, and baseline SHA checks."
    - "Phase 2 completion evidence includes datagen manifest, cost/reject stats, merge report, and frozen-baseline proof."
  artifacts:
    - path: "scripts/run_v3_phase2_all.sh"
      provides: "End-to-end operational orchestrator for Phase 2 with 500-attempt full-run checkpoints"
      contains: "run_v3_phase2_merge.sh"
    - path: "data/v3/phase2/README.phase2.txt"
      provides: "Plain-text artifact contract for Phase 2 outputs"
      contains: "data/labeled.jsonl is frozen"
  key_links:
    - from: "scripts/run_v3_phase2_all.sh"
      to: "scripts/generate_v3_phase2_inputs.sh"
      via: "first operational step"
      pattern: "generate_v3_phase2_inputs.sh"
    - from: "scripts/run_v3_phase2_all.sh"
      to: "scripts/run_v3_phase2_label_full.sh"
      via: "post-smoke full labeling step"
      pattern: "run_v3_phase2_label_full.sh"
    - from: "scripts/run_v3_phase2_all.sh"
      to: "scripts/run_v3_phase2_merge.sh"
      via: "final gate step"
      pattern: "run_v3_phase2_merge.sh"
---

<objective>
Provide the Phase 2 operational runbook as executable scripts and artifact contract.

Purpose: Make the long-running paid API phase repeatable, checkpointed, and safe for the frozen v1.0 baseline.
Output: End-to-end wrapper and explicit artifact contract under `data/v3/phase2/`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-10k-7k/02-CONTEXT.md
@.planning/phases/02-10k-7k/02-RESEARCH.md
@.planning/phases/02-10k-7k/02-VALIDATION.md
@scripts/generate_v3_phase2_inputs.sh
@scripts/run_v3_phase2_label_smoke.sh
@scripts/run_v3_phase2_label_full.sh
@scripts/run_v3_phase2_merge.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add end-to-end Phase 2 orchestrator with safe checkpoints</name>
  <read_first>
    scripts/generate_v3_phase2_inputs.sh
    scripts/run_v3_phase2_label_smoke.sh
    scripts/run_v3_phase2_label_full.sh
    scripts/run_v3_phase2_merge.sh
    .planning/phases/02-10k-7k/02-VALIDATION.md
  </read_first>
  <files>scripts/run_v3_phase2_all.sh</files>
  <action>Create `scripts/run_v3_phase2_all.sh` with `set -euo pipefail`, `ROOT=/home/samuel/TSC_CYCLE`, and absolute calls to the four Phase 2 scripts. The wrapper must support modes `generate`, `smoke`, `full`, `merge`, and `all` via first positional arg defaulting to `all`. In `all` mode, run generate, then smoke, then print a blocking message requiring the user to rerun with `full` after smoke approval; do not automatically run the paid full label step after smoke. In `full` mode, run the full label script and then the merge script. Before and after every mode, run `git diff --quiet -- data/labeled.jsonl`. Add local status printing that reports only file counts and paths; never print environment variable values. Implement full-run checkpoint enforcement in the wrapper, not just prose. In `full` mode, run labeling in resumable chunks of 500 attempted samples by invoking `scripts/run_v3_phase2_label_full.sh` or the underlying labeler with a wrapper-controlled limit/offset/done-set loop so each chunk appends to the same `labeled_new.jsonl` and `rejected_new.jsonl`. Before each chunk, compute and print `old_sha_before`, current accepted count, rejected count, attempted count, and configured workers (must be <=10). After each chunk, recompute old SHA, accepted/rejected/attempted totals, reject rate, and duplicate sample_id counts across labeled+rejected; stop immediately if SHA changes, workers exceed 10, duplicates appear, or append files are missing. Continue chunks until at least 7000 attempts have been made and `accepted >= 6000`, or until the 7500 reservoir is exhausted; if exhausted before `accepted >= 6000`, stop for a user decision. Include a checkpoint check after full labeling that counts lines in `data/v3/phase2/labeled_new.jsonl` and `data/v3/phase2/rejected_new.jsonl` using Python, not shell `wc` parsing, and prints attempted/accepted/rejected totals.</action>
  <verify>
    <automated>bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase2_all.sh</automated>
  </verify>
  <done>Operator can run deterministic Phase 2 modes without accidentally triggering paid full labeling before smoke approval.</done>
  <acceptance_criteria>
    - Script contains exact mode names `generate`, `smoke`, `full`, `merge`, and `all`.
    - `all` mode does not call `run_v3_phase2_label_full.sh`; it prints an instruction to rerun with `full` after approval.
    - Script runs `git diff --quiet -- data/labeled.jsonl` before and after mode execution.
    - Script contains no `OPENAI_API_KEY` value interpolation except presence checks delegated to label scripts.
    - `full` mode contains an executable 500-attempt chunk/checkpoint loop or calls a wrapper-supported equivalent with the same checks.
    - Checkpoint logic verifies accepted count, rejected count, attempted count, reject rate, duplicate IDs, worker cap, and unchanged old SHA between chunks.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 2 artifact contract text file</name>
  <read_first>
    .planning/phases/02-10k-7k/02-RESEARCH.md
    .planning/REQUIREMENTS.md
    scripts/run_v3_phase2_all.sh
  </read_first>
  <files>data/v3/phase2/README.phase2.txt</files>
  <action>Create `data/v3/phase2/README.phase2.txt` as a plain-text artifact contract, not project documentation. It must list the required Phase 2 files exactly: `inputs_same_dist.jsonl`, `inputs_ood.jsonl`, `inputs_targeted.jsonl`, `inputs_all.jsonl`, `datagen_manifest.json`, `labeled_new.jsonl`, `rejected_new.jsonl`, `labeled_merged.jsonl`, and `merge_report.json`. It must state that `data/labeled.jsonl is frozen` and that Phase 3 consumes only `data/v3/phase2/labeled_merged.jsonl`. It must state that lint-failed samples are discarded, not regenerated, that raw API responses live under `raw_responses/v3_phase2/` without secrets, and that full labeling is checkpointed every 500 attempted samples with baseline SHA, accepted/rejected counts, worker cap, duplicate-ID, and reject-rate evidence.</action>
  <verify>
    <automated>test -f /home/samuel/TSC_CYCLE/data/v3/phase2/README.phase2.txt && grep -v '^#' /home/samuel/TSC_CYCLE/data/v3/phase2/README.phase2.txt | grep -c 'data/labeled.jsonl is frozen'</automated>
  </verify>
  <done>The artifact contract exists in the Phase 2 output directory and names exactly what downstream Phase 3 should consume.</done>
  <acceptance_criteria>
    - File contains exact string `data/labeled.jsonl is frozen`.
    - File contains exact string `Phase 3 consumes only data/v3/phase2/labeled_merged.jsonl`.
    - File contains exact string `lint-failed samples are discarded, not regenerated`.
    - File contains exact string `checkpointed every 500 attempted samples`.
    - File does not contain any secret value or environment variable expansion syntax.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human approval for paid full labeling and final Phase 2 gate</name>
  <read_first>
    scripts/run_v3_phase2_all.sh
    data/v3/phase2/README.phase2.txt
    data/v3/phase2/merge_report.json
  </read_first>
  <files>scripts/run_v3_phase2_all.sh, data/v3/phase2/merge_report.json</files>
  <action>After automation is in place, pause for the user to approve paid full labeling. The executor must not run `scripts/run_v3_phase2_all.sh full` until the user explicitly approves based on the smoke output. After approval, run full labeling and merge via the wrapper, then inspect every 500-attempt checkpoint printed or written by the wrapper for unchanged old SHA, workers <=10, monotonic accepted/rejected counts, zero duplicate sample IDs, and understandable reject stats. Then inspect `data/v3/phase2/merge_report.json` for `ok=true`, `new_valid >= 6000`, `merged_valid >= 9000`, `old_new_overlap == 0`, `old_sha_before == old_sha_after`, `requirements_covered` containing DATAGEN-01 through DATAGEN-07, `labeler_evidence.workers_within_cap == true`, and `resume_evidence.duplicate_done_ids == 0`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.phase2_datagen_report --old-labeled data/labeled.jsonl --new-labeled data/v3/phase2/labeled_new.jsonl --rejected data/v3/phase2/rejected_new.jsonl --datagen-manifest data/v3/phase2/datagen_manifest.json --merged-out data/v3/phase2/labeled_merged.jsonl --report-out data/v3/phase2/merge_report.json --min-new-valid 6000 --min-merged-valid 9000</automated>
  </verify>
  <what-built>Executable Phase 2 operation flow and final merge gate.</what-built>
  <how-to-verify>
    1. Review the 50-sample smoke artifacts from Plan 03.
    2. If acceptable, export `OPENAI_API_KEY` locally and run `/home/samuel/TSC_CYCLE/scripts/run_v3_phase2_all.sh full`.
    3. During full execution, confirm each 500-attempt checkpoint reports unchanged old SHA, workers <=10, accepted/rejected/attempted totals, reject rate, and zero duplicate sample IDs.
    4. Confirm `data/v3/phase2/merge_report.json` has `ok: true`, `new_valid` at least 6000, `merged_valid` at least 9000, `old_new_overlap: 0`, equal old SHA fields, `requirements_covered` DATAGEN-01..07, green labeler evidence, and green resume evidence.
    5. Confirm `git diff -- data/labeled.jsonl` prints no diff.
  </how-to-verify>
  <resume-signal>Type "approved" before the executor runs the paid full labeling command; after completion, type "approved" again if merge_report gates are green.</resume-signal>
  <done>Phase 2 has a human-approved full-run path and final report gates prove DATAGEN-01..07 completion.</done>
  <acceptance_criteria>
    - Paid full labeling is not run before explicit approval.
    - Final `merge_report.json` has `ok` true, all DATAGEN gates green, labeler evidence for DATAGEN-03, and resume/no-duplicate evidence for DATAGEN-05.
    - Full-run checkpoint evidence exists for every 500 attempted samples or wrapper chunks of equivalent size.
    - `git diff -- data/labeled.jsonl` is clean after full run.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell→paid API scripts | Human approval controls when paid full GPT-5.5 calls happen. |
| Phase 2 artifacts→Phase 3 | Only merge-gated raw JSONL should be consumed downstream. |
| wrapper status→logs | Operational logs may be saved, so they must not expose secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-api-key | I | operational wrappers | mitigate | Wrappers never echo secret values; key presence is checked only by label scripts. |
| T-02-budget-replay | D | full-run mode | mitigate | `all` mode stops after smoke and requires explicit `full`; `full` mode runs 500-attempt resumable chunks and checks duplicate done IDs before continuing. |
| T-02-baseline-tamper | T | wrapper baseline guards | mitigate | Run `git diff --quiet -- data/labeled.jsonl` before and after each mode and final human verification. |
| T-02-malformed-output | T | final report gate | mitigate | Final automated verify re-runs the merge/report gate before Phase 2 acceptance. |
</threat_model>

<verification>
```bash
bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase2_all.sh
test -f /home/samuel/TSC_CYCLE/data/v3/phase2/README.phase2.txt
grep -v '^#' /home/samuel/TSC_CYCLE/data/v3/phase2/README.phase2.txt | grep -c 'data/labeled.jsonl is frozen'
```
</verification>

<success_criteria>
- Phase 2 operational flow is executable in safe modes.
- Paid full labeling is gated by human approval after smoke.
- Final merge report proves DATAGEN-01..07, including labeler evidence and resume/no-duplicate evidence, without mutating `data/labeled.jsonl`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-10k-7k/02-05-SUMMARY.md`.
</output>
