---
phase: 06-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "完整流水线（数据生成 → 过滤 → baseline → 训练 → 分析）无报错地跑完"
    - "过滤后 grpo_train.jsonl 被覆盖为过滤后版本"
    - "baseline.json 基于过滤后数据重新生成"
    - "训练完成后分析报告包含 zero-std 比例、reward 分布、趋势"
    - "Zero-std 无效步比例从 ~20% 显著下降"
    - "Reward 分布呈现连续梯度而非 0/5 二值化"
  artifacts:
    - path: "outputs/grpo/grpo_train.log"
      provides: "完整训练日志"
    - path: "outputs/grpo/grpo_analysis.txt"
      provides: "训练分析报告"
    - path: "outputs/grpo/model"
      provides: "GRPO 训练后的模型"
  key_links:
    - from: "docker/grpo_pipeline.sh"
      to: "outputs/grpo/"
      via: "端到端流水线生成所有输出"
      pattern: "outputs/grpo"
---

<objective>
运行完整的端到端 GRPO 流水线，验证所有 v1.1 改进（reward 改善、数据过滤、pipeline 集成）协同工作正常。

Purpose: 验证 v1.1 核心假设：改进后的 reward 公式 + 过滤后的数据集 → zero-std 无效步显著减少，reward 有区分度和趋势提升。
Output: 完整训练日志、分析报告、训练后模型
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration/06-CONTEXT.md
@.planning/phases/06-integration/06-01-SUMMARY.md
@docker/grpo_pipeline.sh
@config/config.json
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: 端到端流水线运行与验证</name>
  <files>outputs/grpo/grpo_analysis.txt, outputs/grpo/grpo_train.log, outputs/grpo/model</files>
  <action>
用户运行完整的 GRPO 流水线，验证 v1.1 所有改进协同工作。

如果已有 GRPO 数据（grpo_train.jsonl），可跳过数据生成步骤：
```bash
./docker/grpo_pipeline.sh --skip-data
```

如果需要从头开始：
```bash
./docker/grpo_pipeline.sh
```

等待训练完成后，检查分析报告 `outputs/grpo/grpo_analysis.txt` 确认改进效果。
  </action>
  <verify>
1. 流水线无报错跑完所有步骤
2. 检查分析报告 `outputs/grpo/grpo_analysis.txt`：
   - Zero-std 比例应显著低于 20%（v1.0 水平）
   - Reward 分布应呈现连续梯度（标准差 > 0.5）
   - Reward 应有提升趋势（后期步骤的均值高于前期）
3. 检查 `outputs/grpo/grpo_train.jsonl` 已被覆盖（行数应少于原始 16788 条）
4. 检查 `outputs/grpo/baseline.json` 基于过滤后数据重新生成
5. 检查 `outputs/grpo/model/` 包含训练后的模型文件
6. 日志文件存在：grpo_filter.log, grpo_baseline.log, grpo_train.log, grpo_analysis.log
  </verify>
  <done>
完整流水线无报错完成。Zero-std 比例显著低于 20%。Reward 分布为连续梯度。训练后模型和分析报告均已生成。
  </done>
</task>

</tasks>

<verification>
1. 完整流水线无报错完成
2. 分析报告存在且包含 zero-std 比例、reward 分布、趋势数据
3. Zero-std 比例相比 v1.0 (~20%) 有显著下降
4. Reward 分布为连续梯度（非二值化）
5. 训练后模型文件存在
</verification>

<success_criteria>
- 端到端流水线完整执行无错误
- Zero-std 无效步比例 < 20%（改善于 v1.0）
- Reward 标准差 > 0.5（非二值化）
- Reward 趋势呈上升
- 所有日志文件和分析报告生成
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration/06-02-SUMMARY.md`
</output>
