---
phase: 18-calibrated-dataset-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/v4_gates/calibrated_dataset_rebuild.py
  - tests/test_v4_phase18_calibrated_dataset_rebuild.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Maintainer can rebuild a calibrated v4.2 dataset from the existing v4 Phase 8 merged dataset by filtering saturation-policy violations."
    - "Retained examples preserve hard constraints and the v4 prompt/assistant protocol format."
    - "Maintainer can inspect a reconstruction report with source counts, rejected/relabelled counts, pass rates, hashes, and deterministic split artifacts."
  artifacts:
    - path: "tsc_cycle/v4_gates/calibrated_dataset_rebuild.py"
      provides: "Phase 18 calibrated dataset rebuild CLI, filtering logic, split reconstruction, path safety, and reconstruction report"
      exports: ["Phase18DatasetConfig", "build_calibrated_dataset", "build_parser"]
    - path: "tests/test_v4_phase18_calibrated_dataset_rebuild.py"
      provides: "Phase 18 dataset filtering, protocol, split, report, and path safety regression tests"
      contains: "test_filter_mode_removes_unsaturated_max_green_violations"
    - path: "data/v4_2/phase18/labeled_calibrated.jsonl"
      provides: "Default calibrated v4.2 training JSONL output"
    - path: "artifacts/v4_2/phase18/reconstruction_report.json"
      provides: "Maintainer-facing reconstruction report for DATA-02"
  key_links:
    - from: "tsc_cycle/v4_gates/calibrated_dataset_rebuild.py"
      to: "data/v4/phase8/labeled_merged.jsonl"
      via: "source dataset JSONL read and per-row calibration decision"
      pattern: "_read_jsonl"
    - from: "tsc_cycle/v4_gates/calibrated_dataset_rebuild.py"
      to: "tsc_cycle/v4_gates/saturation_policy.py"
      via: "classify_violation/project_dataset_phase_decisions/compute_saturation_audit/evaluate_saturation_policy_gate reuse"
      pattern: "classify_violation"
    - from: "tsc_cycle/v4_gates/calibrated_dataset_rebuild.py"
      to: "data/v4/phase8/splits/*.index.jsonl"
      via: "retained sample split membership preservation"
      pattern: "_load_split_index"
    - from: "tsc_cycle/v4_gates/calibrated_dataset_rebuild.py"
      to: "tsc_cycle/constraint_lint.py"
      via: "validate retained source row solutions before writing calibrated dataset"
      pattern: "validate"
---

<objective>
Create the Phase 18 calibrated v4.2 dataset rebuild path.

Purpose: Convert the current v4 Phase 8 merged training dataset into a v4.2 calibrated dataset by filtering rows that violate the offline saturation policy, while preserving row schema, hard constraints, prompt/assistant protocol, provenance, deterministic split membership, hashes, and maintainer-facing reconstruction evidence.

Default policy: `filter` mode only. Filtering satisfies DATA-01 because the requirement allows filtering or relabeling, and it avoids creating contradictions between existing reasoning text and modified numeric labels.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-calibrated-dataset-rebuild/18-CONTEXT.md
@.planning/phases/18-calibrated-dataset-rebuild/18-RESEARCH.md
@.planning/phases/18-calibrated-dataset-rebuild/18-VALIDATION.md
@.planning/phases/18-calibrated-dataset-rebuild/18-PATTERNS.md
@.planning/phases/17-audit-saturation-policy-gate/17-VERIFICATION.md
@tsc_cycle/v4_gates/dataset_rebuild.py
@tsc_cycle/v4_gates/saturation_policy.py
@tsc_cycle/v4_gates/phase17_audit.py
@tsc_cycle/constraint_lint.py
@tests/test_v4_phase8_dataset_rebuild.py
@tests/test_v4_phase17_saturation_policy.py

<interfaces>
Use existing Phase 8 row/provenance/hash/prompt helper behavior as the compatibility target.
Use Phase 17 saturation policy helpers as the single source of truth for policy classification and threshold gating.
Do not modify deployment prompt surfaces.
Do not import torch, transformers, peft, bitsandbytes, vllm, flash_attn, or training code in Phase 18 module/tests.
Default outputs must be isolated under `data/v4_2/phase18/` and `artifacts/v4_2/phase18/`; never overwrite `data/v4/phase8/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build filter-mode calibration core</name>
  <read_first>
    @tsc_cycle/v4_gates/saturation_policy.py
    @tsc_cycle/v4_gates/dataset_rebuild.py row helper and hash sections
    @tests/test_v4_phase17_saturation_policy.py policy fixture patterns
    @.planning/phases/18-calibrated-dataset-rebuild/18-RESEARCH.md Phase 17 Reuse and Recommended Implementation Shape
  </read_first>
  <files>tsc_cycle/v4_gates/calibrated_dataset_rebuild.py, tests/test_v4_phase18_calibrated_dataset_rebuild.py</files>
  <behavior>
    - `build_calibrated_dataset(...)` reads v4 Phase 8 source JSONL and writes retained rows to v4.2 output JSONL.
    - Rows with any non-trivial `final_green == max_green` decision where `pred_saturation < 1.0` are rejected.
    - Rows with saturated max-green decisions and forced trivial ranges are retained when hard constraints pass.
    - Malformed/missing/hard-constraint-invalid rows are rejected and counted, never treated as policy successes.
  </behavior>
  <action>Add `tsc_cycle/v4_gates/calibrated_dataset_rebuild.py` with `Phase18DatasetConfig`, JSON/JSONL helpers, source row accessors compatible with Phase 8, per-row hard-constraint validation, per-row saturation decision projection, and filter-mode keep/reject decisions. Reuse `classify_violation`, `VIOLATION_UNSATURATED_MAX_GREEN`, and `constraint_lint.validate`. Add RED tests first in `tests/test_v4_phase18_calibrated_dataset_rebuild.py` using tiny tmp_path JSONL/split fixtures, then implement until they pass. Keep all imports lightweight.</action>
  <acceptance_criteria>
    - Unsaturated max-green fixture rows are absent from the calibrated JSONL.
    - Allowed saturated max-green and forced trivial rows remain present.
    - Rejection records include sample id, phase id, saturation band, min/max/final green, split/source when available, and reason.
    - `requirements_covered` includes `DATA-01`.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase18_calibrated_dataset_rebuild.py::test_filter_mode_removes_unsaturated_max_green_violations -q</automated>
  </verify>
  <done>Filter-mode calibrated dataset core removes saturation-policy violations while preserving acceptable examples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve protocol, hard constraints, and deterministic split indexes</name>
  <read_first>
    @tsc_cycle/v4_gates/dataset_rebuild.py `_index_row`, prompt/assistant hash, split writing, and tokenization path sections
    @tsc_cycle/prompt_builder.py
    @tsc_cycle/constraint_lint.py
    @tests/test_v4_phase8_dataset_rebuild.py split/hash fixture assertions
    @.planning/phases/18-calibrated-dataset-rebuild/18-PATTERNS.md Split preservation and Hard constraint first sections
  </read_first>
  <files>tsc_cycle/v4_gates/calibrated_dataset_rebuild.py, tests/test_v4_phase18_calibrated_dataset_rebuild.py</files>
  <behavior>
    - Retained rows pass `constraint_lint.validate` against their original input and solution.
    - Retained rows can be rendered through `build_user_prompt` and `build_full_assistant` with required protocol tags.
    - Split indexes are written for train/val/ood_val preserving retained sample membership from Phase 8 indexes.
    - Index rows include deterministic record/input/solution/prompt/assistant hashes and source provenance.
  </behavior>
  <action>Implement Phase 8-compatible split index reconstruction for retained rows. Read source split indexes into sample-id membership; write Phase 18 `train.index.jsonl`, `val.index.jsonl`, `ood_val.index.jsonl`, and split `manifest.json` under the configured split dir. Use canonical JSON hashing and prompt/assistant builders as Phase 8 does. Add tests proving retained split membership is preserved and protocol/hard-constraint checks hold for every retained row.</action>
  <acceptance_criteria>
    - No retained row fails hard-constraint validation in tests.
    - Protocol rendering contains `<start_working_out>`, `</end_working_out>`, `<SOLUTION>`, and `</SOLUTION>`.
    - Split counts equal retained samples per original split, and rejected samples do not appear in any Phase 18 split index.
    - Split manifest includes split id hashes and DATA-02 coverage.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase18_calibrated_dataset_rebuild.py::test_retained_rows_preserve_hard_constraints_and_protocol tests/test_v4_phase18_calibrated_dataset_rebuild.py::test_split_indexes_preserve_retained_membership_and_hashes -q</automated>
  </verify>
  <done>Calibrated retained rows keep the v4 protocol/hard-constraint contract and deterministic split lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit reconstruction report, CLI, and path safety gates</name>
  <read_first>
    @tsc_cycle/v4_gates/phase17_audit.py threshold gate and safe output path sections
    @tsc_cycle/v4_gates/dataset_rebuild.py CLI/report sections
    @tests/test_v4_phase17_saturation_policy.py path guard and gate report tests
    @tests/test_v4_phase8_dataset_rebuild.py parser default and report assertions
    @.planning/phases/18-calibrated-dataset-rebuild/18-VALIDATION.md Per-Task Verification Map
  </read_first>
  <files>tsc_cycle/v4_gates/calibrated_dataset_rebuild.py, tests/test_v4_phase18_calibrated_dataset_rebuild.py</files>
  <behavior>
    - `reconstruction_report.json` shows source counts, retained/rejected/relabelled counts, policy pass rates, hard-constraint pass rates, dataset hashes, split counts/hashes, output paths, and representative rejections.
    - Post-calibration policy gate passes for retained evidence with Phase 17 thresholds.
    - CLI defaults read v4 Phase 8 inputs and write only v4.2 Phase 18 outputs.
    - Unsafe output roots including frozen v1, repo root, `data/v4/phase8`, and `artifacts/v4/phase8` are rejected.
  </behavior>
  <action>Add `build_parser()` and `main()` with nonzero exit on red report. Implement safe path validation for output JSONL, split dir, tokenized dir placeholder/report paths, and artifact report path. Implement top-level report writing with input/output hashes, pre/post audit/policy summaries, hard-constraint counts, split artifact paths, and representative rejections. Add tests for report fields, CLI defaults, and path guards. After tests pass, run the CLI against real Phase 8 inputs in dry/report mode if available to generate default Phase 18 artifacts.</action>
  <acceptance_criteria>
    - Report includes `ok`, `next_phase_allowed`, `requirements_covered`, `counts`, `policy`, `hard_constraints`, `dataset_hashes`, `splits`, `representative_rejections`, and `paths`.
    - Report records `relabelled_rows: 0` for filter mode.
    - CLI parser defaults point to `data/v4/phase8/labeled_merged.jsonl`, `data/v4/phase8/splits`, `data/v4_2/phase18`, and `artifacts/v4_2/phase18`.
    - Path guard tests prove Phase 18 cannot overwrite Phase 8 or frozen v1 artifacts.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase18_calibrated_dataset_rebuild.py::test_reconstruction_report_contains_counts_hashes_and_policy_pass_rates tests/test_v4_phase18_calibrated_dataset_rebuild.py::test_cli_defaults_and_path_guards_keep_phase18_outputs_isolated -q</automated>
  </verify>
  <done>Maintainer can run one Phase 18 CLI and inspect a DATA-02-complete reconstruction report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| v4 Phase 8 dataset -> Phase 18 calibrator | Existing labels may encode bad teacher behavior, malformed rows, or hard-constraint invalid outputs. |
| Phase 8 split indexes -> v4.2 split artifacts | Split metadata controls train/val/OOD comparisons and must not drift silently. |
| Offline policy -> training dataset | Filtering can hide policy failures unless rejection evidence is preserved. |
| CLI args -> filesystem writes | Maintainer-provided output paths must not overwrite Phase 8/frozen/source artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-18-01 | Tampering | Calibration decision | mitigate | Reuse Phase 17 `classify_violation`; tests cover unsaturated max-green, saturated max-green, and forced trivial ranges. |
| T-18-02 | Tampering | Hard-constraint/protocol preservation | mitigate | Validate retained rows with `constraint_lint.validate`; render prompt/assistant protocol in tests. |
| T-18-03 | Repudiation | Split reconstruction | mitigate | Preserve original split membership and emit split id hashes/manifests. |
| T-18-04 | Repudiation | Reconstruction report | mitigate | Emit source/rejected/retained counts, pass rates, hashes, and representative rejection examples. |
| T-18-05 | Tampering | Output paths | mitigate | Reject frozen v1, Phase 8, repo root, and broad source output targets; default to v4.2 Phase 18 paths. |
| T-18-SC | Supply Chain | Dependency changes | mitigate | No package installs planned; use stdlib and existing project modules only. |
</threat_model>

<verification>
Run quick Phase 18 tests after each task:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase18_calibrated_dataset_rebuild.py -q</automated>

Run adjacent regression after Plan 01:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase18_calibrated_dataset_rebuild.py tests/test_v4_phase17_saturation_policy.py tests/test_v4_phase8_dataset_rebuild.py -q</automated>
</verification>

<success_criteria>
- DATA-01 is satisfied by a v4.2 calibrated JSONL generated from v4 Phase 8 sources with saturation-policy violations filtered out.
- Retained examples remain hard-constraint valid and render through the unchanged v4 prompt/assistant protocol.
- DATA-02 is satisfied by a reconstruction report containing counts, pass rates, hashes, split artifacts, and deterministic rejection examples.
- Phase 18 outputs are isolated from v4 Phase 8 and frozen v1 artifacts.
</success_criteria>

<output>
Create `.planning/phases/18-calibrated-dataset-rebuild/18-01-SUMMARY.md` when done.
</output>
