---
phase: "20-evaluation-reality-replay-handoff"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "tsc_cycle/v4_gates/phase20_eval.py"
  - "scripts/run_v4_phase20_eval.sh"
  - "tests/test_v4_phase20_evaluation_handoff.py"
autonomous: true
requirements:
  - "EVAL-01"
must_haves:
  truths:
    - "Maintainer can evaluate the calibrated v4.2 model without using teacher-MAE as a blocking success gate."
    - "Parse, hard-constraint lint, output protocol, and saturation policy failures fail Phase 20 evaluation closed."
    - "Accepted evaluation evidence is scoped to v4.2 artifacts and validates the Phase 19 export handoff first."
  artifacts:
    - path: "tsc_cycle/v4_gates/phase20_eval.py"
      provides: "EVAL-01 evaluation gate, report writer, and report validator"
      exports: ["evaluate_phase20_outputs", "write_phase20_eval_report", "validate_phase20_eval_report", "main"]
    - path: "scripts/run_v4_phase20_eval.sh"
      provides: "DGX Spark-safe v4.2 evaluation launcher"
    - path: "artifacts/v4_2/phase20/eval_report.json"
      provides: "Accepted EVAL-01 evidence report"
  key_links:
    - from: "tsc_cycle/v4_gates/phase20_eval.py"
      to: "tsc_cycle.v4_gates.phase19_export.validate_phase19_export_report"
      via: "Phase 19 export preflight before accepting eval evidence"
      pattern: "validate_phase19_export_report"
    - from: "tsc_cycle/v4_gates/phase20_eval.py"
      to: "tsc_cycle.v4_gates.phase17_audit.evaluate_saturation_policy_gate"
      via: "saturation policy gate over projected eval rows"
      pattern: "evaluate_saturation_policy_gate"
    - from: "tsc_cycle/v4_gates/phase20_eval.py"
      to: "tsc_cycle.prompt_builder.parse_assistant_output"
      via: "protocol parser that rejects native think tags"
      pattern: "parse_assistant_output"
---

<objective>
Build the Phase 20 v4.2 evaluation gate for EVAL-01.

Purpose: Maintainers need a pass/fail evaluation that rewards hard constraints, custom output protocol, and saturation-policy behavior, not reproduction of old bad teacher labels.
Output: A tested v4.2 evaluation module, DGX-safe launcher, and accepted `artifacts/v4_2/phase20/eval_report.json` contract.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-evaluation-reality-replay-handoff/20-RESEARCH.md
@.planning/phases/20-evaluation-reality-replay-handoff/20-PATTERNS.md
@.planning/phases/20-evaluation-reality-replay-handoff/20-VALIDATION.md
@.planning/phases/17-audit-saturation-policy-gate/17-01-SUMMARY.md
@.planning/phases/17-audit-saturation-policy-gate/17-02-SUMMARY.md
@.planning/phases/19-4b-qlora-retrain-export/19-02-SUMMARY.md

<interfaces>
Use the contracts and analog excerpts in `20-PATTERNS.md`:
- Phase 20 eval follows `tsc_cycle/eval/phase11_metrics.py` aggregation shape, but teacher-MAE is advisory only.
- Phase 20 eval calls `tsc_cycle.v4_gates.phase19_export.validate_phase19_export_report` before trusting `runs/v4.2-4B-20260518T111519Z`.
- Phase 20 eval uses `tsc_cycle.prompt_builder.parse_assistant_output`, `tsc_cycle.constraint_lint.validate`, and `tsc_cycle.v4_gates.phase17_audit.evaluate_saturation_policy_gate` as source-of-truth gates.
- Canonical run root: `runs/v4.2-4B-20260518T111519Z`.
- Canonical artifact root: `artifacts/v4_2/phase20`.
</interfaces>
</context>

<source_audit>
GOAL covered: Maintainer can decide whether v4.2 is better than v4.0 by evaluating hard constraints, output protocol, saturation policy behavior, and not rewarding bad teacher labels.
REQ covered: EVAL-01.
RESEARCH covered: Evaluation Gate slice, Phase 19 handoff validation, teacher-MAE advisory-only, v4.2-scoped outputs, no vLLM, unchanged prompt protocol.
CONTEXT covered: STATE decisions keep saturation rules offline-only, keep v4 inference prompt unchanged, stay on Qwen3-4B/DGX-safe stack, and exclude Evo integration/deferred imatrix or ablation work.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 20-01-01: Create EVAL-01 fail-closed evaluation contracts and module</name>
  <files>tests/test_v4_phase20_evaluation_handoff.py, tsc_cycle/v4_gates/phase20_eval.py</files>
  <read_first>
    <file>tests/test_v4_phase20_evaluation_handoff.py</file>
    <file>tsc_cycle/v4_gates/phase20_eval.py</file>
    <file>tsc_cycle/eval/phase11_metrics.py</file>
    <file>tsc_cycle/v4_gates/phase17_audit.py</file>
    <file>tsc_cycle/v4_gates/saturation_policy.py</file>
    <file>tsc_cycle/v4_gates/phase19_export.py</file>
    <file>tsc_cycle/prompt_builder.py</file>
    <file>tsc_cycle/constraint_lint.py</file>
    <file>.planning/phases/20-evaluation-reality-replay-handoff/20-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test that importing `tsc_cycle.v4_gates.phase20_eval` does not import torch, transformers, peft, bitsandbytes, or vllm.
    - Test that `validate_phase19_export_report` must be green before `validate_phase20_eval_report` can return `ok: true`.
    - Test that native `<think>` or `</think>` tags, parse failures, hard-constraint lint failures, or saturation threshold failures produce `ok: false`, `next_phase_allowed: false`, empty `requirements_covered`, and non-empty `fatal_failures`.
    - Test that teacher-MAE may appear only under an advisory/diagnostic key and never under blocking decision inputs or fatal gate criteria.
  </behavior>
  <action>Create `tsc_cycle/v4_gates/phase20_eval.py` for EVAL-01. Define constants for `PROJECT_ROOT`, `DEFAULT_RUN_ROOT = PROJECT_ROOT / "runs" / "v4.2-4B-20260518T111519Z"`, `ARTIFACT_ROOT = PROJECT_ROOT / "artifacts" / "v4_2" / "phase20"`, `EVAL_REPORT_PATH`, and `REQUIREMENTS_COVERED = ("EVAL-01",)`. Implement lightweight pure-Python functions named `evaluate_phase20_outputs`, `write_phase20_eval_report`, `validate_phase20_eval_report`, and `main`. The report must follow Phase 19 fail-closed shape: `ok`, `next_phase_allowed`, `requirements_covered`, `gates`, `fatal_failures`, `warnings`, and artifact/report paths. For every evaluated output, parse with `parse_assistant_output`, reject native Qwen `<think>` tags via the existing parser behavior, lint with `constraint_lint.validate`, project per-phase rows using Phase 17 saturation helpers, and call `evaluate_saturation_policy_gate(..., source_type="eval")`. Keep teacher-MAE advisory-only: if computed or loaded, place it under `advisory.teacher_mae` and do not include it in any blocking gate. Validate the Phase 19 export report with `validate_phase19_export_report` before any report can be accepted. Do not modify deployment prompt builders or add saturation instructions to prompts.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py -q</automated>
  </verify>
  <acceptance_criteria>
    <source_assertion>`tsc_cycle/v4_gates/phase20_eval.py` contains the four public functions named in this task and imports no heavyweight model stack at module import time.</source_assertion>
    <behavior_assertion>A malformed, native-think, hard-lint-invalid, or saturation-invalid eval fixture fails closed with `requirements_covered == []`.</behavior_assertion>
    <behavior_assertion>A valid eval fixture produces `requirements_covered == ["EVAL-01"]` and stores teacher-MAE only under advisory/diagnostic fields.</behavior_assertion>
    <test_command>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py -q</test_command>
  </acceptance_criteria>
  <done>`phase20_eval.py` can produce and validate a v4.2-scoped EVAL-01 report that blocks on parse/lint/protocol/saturation/export-handoff gates and never blocks on teacher-MAE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 20-01-02: Add DGX-safe Phase 20 evaluation launcher</name>
  <files>tests/test_v4_phase20_evaluation_handoff.py, scripts/run_v4_phase20_eval.sh</files>
  <read_first>
    <file>tests/test_v4_phase20_evaluation_handoff.py</file>
    <file>scripts/run_v4_phase20_eval.sh</file>
    <file>scripts/run_v4_phase11_eval_matrix.sh</file>
    <file>scripts/run_v4_phase19_export.sh</file>
    <file>scripts/dgx_spark/run_safe.sh</file>
    <file>tsc_cycle/v4_gates/phase20_eval.py</file>
    <file>.planning/phases/20-evaluation-reality-replay-handoff/20-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Test that the wrapper defaults to `runs/v4.2-4B-20260518T111519Z` and rejects non-v4.2 or v4.0 run roots.
    - Test that the wrapper uses `/home/samuel/TSC_CYCLE/.venv/bin/python`, exports `PYTHONPATH`, and invokes `scripts/dgx_spark/run_safe.sh 100G --` for any model-generation step.
    - Test that the wrapper invokes the Phase 20 eval module and writes under `artifacts/v4_2/phase20`, not `artifacts/v4/phase11` or v4.0 Phase 12 paths.
  </behavior>
  <action>Create `scripts/run_v4_phase20_eval.sh` with `set -euo pipefail`, `PROJECT_ROOT=/home/samuel/TSC_CYCLE`, `PYTHON=/home/samuel/TSC_CYCLE/.venv/bin/python`, default `RUN_ROOT=runs/v4.2-4B-20260518T111519Z`, and `ARTIFACT_ROOT=/home/samuel/TSC_CYCLE/artifacts/v4_2/phase20`. Follow the Phase 19 wrapper path guard style: accept only `runs/v4.2-4B-*` or absolute paths under `/home/samuel/TSC_CYCLE/runs/v4.2-4B-*`; reject v4.0/frozen roots. The wrapper must run the Phase 19 export preflight through the Phase 20 eval module, generate or consume Phase 20 eval outputs as implemented by `phase20_eval.py`, and write `eval_report.json`. Use `scripts/dgx_spark/run_safe.sh 100G --` around any HF generation call. Do not install packages, do not call vLLM, and do not mutate Phase 11 defaults.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py -q</automated>
  </verify>
  <acceptance_criteria>
    <source_assertion>`scripts/run_v4_phase20_eval.sh` contains the default run root `runs/v4.2-4B-20260518T111519Z`, artifact root `artifacts/v4_2/phase20`, and no `vllm` invocation.</source_assertion>
    <source_assertion>Any generation path in the wrapper is protected by `scripts/dgx_spark/run_safe.sh 100G --`.</source_assertion>
    <behavior_assertion>The wrapper rejects a `runs/v4.0-4B-*` argument before invoking evaluation.</behavior_assertion>
    <test_command>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py -q</test_command>
  </acceptance_criteria>
  <done>Maintainer has a deterministic command path for producing the accepted EVAL-01 v4.2 evaluation report under `artifacts/v4_2/phase20/eval_report.json`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 19 reports/artifacts -> Phase 20 eval | On-disk run artifacts are untrusted until path, hash, and export report validation pass. |
| Model output text -> parser/lint/policy gates | Generated text is untrusted and must pass protocol parsing, hard constraints, and saturation policy before acceptance. |
| CLI args -> filesystem writes | Maintainer-supplied run/out paths must not authorize v4.0/frozen roots or broad repo writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `validate_phase20_eval_report` | mitigate | Revalidate Phase 19 export via `validate_phase19_export_report`, recompute report status from rows, and require v4.2-scoped artifact roots before `ok: true`. |
| T-20-02 | Information Disclosure | evaluation report | mitigate | Record hashes, paths, counts, gates, and advisory metrics only; do not include secrets or environment variables. |
| T-20-03 | Repudiation | teacher-MAE demotion | mitigate | Report explicitly separates blocking gates from `advisory.teacher_mae` so success cannot be attributed to bad-label reproduction. |
| T-20-SC | Tampering | package installs | mitigate | No npm/pip/cargo install tasks are allowed in this plan; wrappers must not install dependencies or invoke vLLM. |
</threat_model>

<verification>
Run the focused Phase 20 contract suite after each task:
`/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py -q`

After the plan completes, run adjacent regression:
`/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v4_phase20_evaluation_handoff.py /home/samuel/TSC_CYCLE/tests/test_v4_phase19_training_export.py /home/samuel/TSC_CYCLE/tests/test_v4_phase17_saturation_policy.py -q`
</verification>

<success_criteria>
- EVAL-01 appears in the accepted Phase 20 evaluation report only when Phase 19 export, parse, lint, protocol, and saturation gates all pass.
- Teacher-MAE is not a blocking metric in code, tests, or reports.
- No v4.0 Phase 11/12 defaults are overwritten or used as the only Phase 20 evidence.
</success_criteria>

<output>
Create `.planning/phases/20-evaluation-reality-replay-handoff/20-01-SUMMARY.md` when done.
</output>
