---
phase: 03-dataset-rebuild-qwen3-5-retokenize-split
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - tsc_cycle/v3_gates/dataset_rebuild_v3.py
  - scripts/run_v3_phase3_dataset_rebuild.sh
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "A single wrapper command rebuilds Phase 3 split indices, Qwen3.5 tokenized Arrow files, manifest, and final report from the Phase 2 merged JSONL."
    - "The final Phase 3 report proves all DATA-01..04 gates passed with exact counts, truncation_rate <= 0.05, and no duplicate/overlapping split IDs."
    - "The wrapper protects v1 paths and `data/labeled.jsonl` from writes and verifies baseline diff cleanliness."
    - "The final artifact manifest lists every Phase 3 output file with hashes for Phase 4/6 consumption."
  artifacts:
    - path: "scripts/run_v3_phase3_dataset_rebuild.sh"
      provides: "Canonical Phase 3 end-to-end rebuild wrapper"
    - path: "data/splits/v3/manifest.json"
      provides: "Final split/tokenization manifest and DATA-01..04 evidence"
    - path: "data/splits/v3/rebuild_report.json"
      provides: "Final gate report for Phase 3"
    - path: "data/tokenized/v3/train.arrow"
      provides: "Final train Arrow IPC artifact"
    - path: "data/tokenized/v3/val.arrow"
      provides: "Final validation Arrow IPC artifact"
    - path: "data/tokenized/v3/ood_val.arrow"
      provides: "Final OOD validation Arrow IPC artifact"
  key_links:
    - from: "scripts/run_v3_phase3_dataset_rebuild.sh"
      to: "tsc_cycle.v3_gates.dataset_rebuild_v3"
      via: "python -m tsc_cycle.v3_gates.dataset_rebuild_v3"
      pattern: "python -m tsc_cycle\.v3_gates\.dataset_rebuild_v3"
    - from: "tsc_cycle/v3_gates/dataset_rebuild_v3.py"
      to: "data/splits/v3/rebuild_report.json"
      via: "--report-out"
      pattern: "rebuild_report\.json"
---

<objective>
Wire the Phase 3 dataset rebuild into an executable CLI/wrapper with final gate reporting and artifact manifest.

Purpose: Phase 4 and Phase 6 need a single reproducible Phase 3 artifact contract, not ad-hoc function calls. The wrapper must run the real Phase 2 merged dataset through split, tokenization, truncation, and reproducibility gates while protecting frozen v1 data.
Output: CLI completeness in `dataset_rebuild_v3.py`, `scripts/run_v3_phase3_dataset_rebuild.sh`, and final report/manifest artifacts under `data/splits/v3/` plus Arrow files under `data/tokenized/v3/`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-PATTERNS.md
@/home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-VALIDATION.md
@/home/samuel/TSC_CYCLE/.planning/phases/02-10k-7k/02-VERIFICATION.md
@/home/samuel/TSC_CYCLE/scripts/run_v3_phase2_merge.sh
@/home/samuel/TSC_CYCLE/tests/test_v3_dataset_rebuild.py

<interfaces>
CLI contract:
```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.dataset_rebuild_v3 \
  --merged-jsonl data/v3/phase2/labeled_merged.jsonl \
  --phase2-report data/v3/phase2/merge_report.json \
  --memory-budget artifacts/v3/phase1/memory_budget.json \
  --tokenizer-audit artifacts/v3/phase1/tokenizer_audit.json \
  --splits-dir data/splits/v3 \
  --tokenized-dir data/tokenized/v3 \
  --report-out data/splits/v3/rebuild_report.json \
  --seed 42 \
  --expected-train 7601 \
  --expected-val 950 \
  --expected-ood-val 950 \
  --max-truncation-rate 0.05
```
Wrapper contract:
```bash
scripts/run_v3_phase3_dataset_rebuild.sh
```
The wrapper must use `/home/samuel/TSC_CYCLE/.venv/bin/python`, not system Python, and must not install dependencies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete CLI/report contract and final manifest fields</name>
  <files>tsc_cycle/v3_gates/dataset_rebuild_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-VALIDATION.md
    - /home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-PATTERNS.md
    - /home/samuel/TSC_CYCLE/tests/test_v3_dataset_rebuild.py
  </read_first>
  <behavior>
    - `python -m tsc_cycle.v3_gates.dataset_rebuild_v3 --help` exposes all CLI arguments in the interface block.
    - Real run returns exit 0 only when DATA-01..04 gates all pass; otherwise returns exit 1 and includes fatal_failures in report.
    - `rebuild_report.json` includes `ok`, `requirements_covered=[DATA-01,DATA-02,DATA-03,DATA-04]`, split counts, truncation stats, duplicate/overlap evidence, v1 OOD alignment, path safety evidence, and artifact hashes.
    - `manifest.json` lists final outputs: three index JSONL files, v1_ood_alignment.json, rebuild_report.json, and three Arrow files with SHA-256 hashes.
  </behavior>
  <action>Finish `build_parser()` and `main()` in `dataset_rebuild_v3.py` following v3 gate patterns. Add arguments for all input/output paths, expected counts, seed, model name, max truncation rate, memory/tokenizer evidence files, and `--dry-run` if useful. Validate Phase 2 report `ok=true` and merged_valid=9501 before processing. Validate output paths stay under `data/splits/v3` and `data/tokenized/v3`. Ensure the final report is written on both success and failure, but successful index/Arrow artifacts are written only when all gates pass. Add artifact SHA computation for every written output.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_rebuild.py -q && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.dataset_rebuild_v3 --help >/tmp/phase3_dataset_rebuild_help.txt</automated>
  </verify>
  <acceptance_criteria>Full Phase 3 focused tests pass; CLI help runs without importing GPU-only dependencies or downloading a model; report contract includes all DATA-01..04 evidence fields.</acceptance_criteria>
  <done>CLI/report path is complete and unit/integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical end-to-end wrapper and run real Phase 3 rebuild</name>
  <files>scripts/run_v3_phase3_dataset_rebuild.sh, tsc_cycle/v3_gates/dataset_rebuild_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-RESEARCH.md
    - /home/samuel/TSC_CYCLE/scripts/run_v3_phase2_merge.sh
    - /home/samuel/TSC_CYCLE/data/v3/phase2/merge_report.json
  </read_first>
  <action>Create `scripts/run_v3_phase3_dataset_rebuild.sh` with `set -euo pipefail`, repo-root resolution, `.venv/bin/python` preflight, existence checks for `data/v3/phase2/labeled_merged.jsonl`, `data/v3/phase2/merge_report.json`, `artifacts/v3/phase1/memory_budget.json`, and `artifacts/v3/phase1/tokenizer_audit.json`. Before and after the CLI run, execute `git diff --quiet -- data/labeled.jsonl` to enforce read-only baseline. Invoke the CLI exactly with seed=42, expected counts 7601/950/950, max truncation rate 0.05, output dirs `data/splits/v3` and `data/tokenized/v3`, and report `data/splits/v3/rebuild_report.json`. Run the wrapper once on the real Phase 2 merged data. The run may use CPU tokenization and existing cached Qwen3.5 tokenizer; if the tokenizer cache is unavailable, stop with a clear error rather than installing dependencies.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && bash -n scripts/run_v3_phase3_dataset_rebuild.sh && scripts/run_v3_phase3_dataset_rebuild.sh && /home/samuel/TSC_CYCLE/.venv/bin/python - <<'PY'
import json
from pathlib import Path
report = json.loads(Path('data/splits/v3/rebuild_report.json').read_text(encoding='utf-8'))
assert report['ok'] is True
assert report['split_counts'] == {'ood_val': 950, 'train': 7601, 'val': 950}
assert report['requirements_covered'] == ['DATA-01', 'DATA-02', 'DATA-03', 'DATA-04']
assert report['truncation']['rate'] <= 0.05
for path in ['data/tokenized/v3/train.arrow','data/tokenized/v3/val.arrow','data/tokenized/v3/ood_val.arrow','data/splits/v3/train.index.jsonl','data/splits/v3/val.index.jsonl','data/splits/v3/ood_val.index.jsonl','data/splits/v3/manifest.json','data/splits/v3/v1_ood_alignment.json']:
    assert Path(path).exists(), path
PY
git diff --quiet -- data/labeled.jsonl</automated>
  </verify>
  <acceptance_criteria>The wrapper syntax passes, the real Phase 3 rebuild exits 0, final report `ok=true`, exact counts and truncation gate pass, all required artifacts exist, and `data/labeled.jsonl` remains byte-clean.</acceptance_criteria>
  <done>Phase 3 can be rebuilt from one canonical command with complete DATA-01..04 report evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell -> Phase 3 CLI | Wrapper arguments determine production artifact locations and must be fixed/safe. |
| Phase 2 artifacts -> final Phase 3 report | Phase 3 trusts Phase 2 only after report ok/count gates are validated. |
| Final artifacts -> Phase 4/6 | Downstream consumers depend on manifest hashes, exact counts, and path contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-duplicate-split | Tampering | final CLI gate report | mitigate | Report split count, duplicate ID count, overlap count, and fail nonzero unless all are zero/exact. |
| T-03-path-drift | Tampering | wrapper and CLI output paths | mitigate | Wrapper uses fixed v3 paths and git diff guard for `data/labeled.jsonl`; CLI validates output roots. |
| T-03-truncation | Integrity | final real-data run | mitigate | Report truncation rate and fail nonzero if rate exceeds 0.05; include truncated sample IDs sample. |
| T-03-reproducibility | Repudiation | final manifest/report | mitigate | Manifest records seed=42, input/report SHAs, output artifact SHAs, counts, tokenizer/memory evidence, and v1 OOD alignment. |
</threat_model>

<verification>
Final phase verification command:
```bash
cd /home/samuel/TSC_CYCLE && \
/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_rebuild.py tests/test_hashing.py tests/test_prompt_builder.py -q && \
bash -n scripts/run_v3_phase3_dataset_rebuild.sh && \
scripts/run_v3_phase3_dataset_rebuild.sh && \
git diff --quiet -- data/labeled.jsonl
```
</verification>

<success_criteria>
- `scripts/run_v3_phase3_dataset_rebuild.sh` is the canonical Phase 3 command.
- `data/splits/v3/rebuild_report.json` has `ok=true` and covers DATA-01..04.
- Split counts are exactly train=7601, val=950, ood_val=950.
- `ood_val` alignment proves all 300 v1 OOD rows are included plus 650 v3 OOD rows.
- Arrow IPC files exist exactly at `data/tokenized/v3/{train,val,ood_val}.arrow`.
- Truncation rate is <=5% and measured before truncation.
- Manifest contains hashes for every final artifact.
- `data/labeled.jsonl` has no git diff.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-04-SUMMARY.md`
</output>
