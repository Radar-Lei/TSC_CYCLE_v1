---
phase: 03-dataset-rebuild-qwen3-5-retokenize-split
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - tsc_cycle/v3_gates/dataset_rebuild_v3.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "Merged Phase 2 rows split deterministically into train=7601, val=950, ood_val=950 with seed=42."
    - "ood_val contains all 300 v1.0 OOD rows plus exactly 650 sampled v3 OOD rows."
    - "No sample_id appears in more than one split and duplicate source sample IDs fail closed."
    - "Split index files and alignment manifest contain hashes and seed/source evidence for Phase 6 comparability."
  artifacts:
    - path: "tsc_cycle/v3_gates/dataset_rebuild_v3.py"
      provides: "Phase 3 deterministic split and index/manifest implementation"
      exports:
        - "DatasetRebuildConfig"
        - "build_split_plan"
        - "build_phase3_dataset"
    - path: "data/splits/v3/train.index.jsonl"
      provides: "Train split membership and hashes"
    - path: "data/splits/v3/val.index.jsonl"
      provides: "Validation split membership and hashes"
    - path: "data/splits/v3/ood_val.index.jsonl"
      provides: "OOD validation membership with pinned v1 OOD IDs"
    - path: "data/splits/v3/manifest.json"
      provides: "Seed, sizes, input SHA, gates, requirements, and reproducibility evidence"
    - path: "data/splits/v3/v1_ood_alignment.json"
      provides: "Proof that all v1.0 OOD rows are included in ood_val"
  key_links:
    - from: "tsc_cycle/v3_gates/dataset_rebuild_v3.py"
      to: "data/v3/phase2/labeled_merged.jsonl"
      via: "default --merged-jsonl input"
      pattern: "labeled_merged\.jsonl"
    - from: "tsc_cycle/v3_gates/dataset_rebuild_v3.py"
      to: "data/splits/v3/*.index.jsonl"
      via: "write_split_indices after all gates pass"
      pattern: "index\.jsonl"
---

<objective>
Implement the deterministic Phase 3 split and reproducibility artifact layer.

Purpose: DATA-01 and DATA-04 require an exact, replayable 80/10/10 split that pins v1.0 OOD rows for cross-milestone comparability and prevents duplicate/overlap drift.
Output: `tsc_cycle/v3_gates/dataset_rebuild_v3.py` with split planning, fail-closed gates, index JSONL writers, manifest JSON, and v1 OOD alignment evidence.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-PATTERNS.md
@/home/samuel/TSC_CYCLE/.planning/phases/02-10k-7k/02-VERIFICATION.md
@/home/samuel/TSC_CYCLE/data/v3/phase2/merge_report.json
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase2_datagen_report.py
@/home/samuel/TSC_CYCLE/tsc_cycle/hashing.py
@/home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py
@/home/samuel/TSC_CYCLE/tests/test_v3_dataset_rebuild.py

<interfaces>
Required public contracts:
```python
DEFAULT_SEED = 42
DEFAULT_TRAIN_SIZE = 7601
DEFAULT_VAL_SIZE = 950
DEFAULT_OOD_VAL_SIZE = 950
DEFAULT_MAX_SEQ_LENGTH = 2048

@dataclass(frozen=True)
class DatasetRebuildConfig:
    merged_jsonl: Path = Path("data/v3/phase2/labeled_merged.jsonl")
    splits_dir: Path = Path("data/splits/v3")
    tokenized_dir: Path = Path("data/tokenized/v3")
    seed: int = 42
    max_seq_length: int = 2048
    model_name: str = "Qwen/Qwen3.5-9B"


def build_split_plan(rows: list[dict[str, Any]], *, seed: int = 42) -> dict[str, Any]: ...
def build_phase3_dataset(config: DatasetRebuildConfig, *, tokenizer: Any | None = None, write_tokenized: bool = True) -> dict[str, Any]: ...
```
This plan may implement tokenization stubs only as no-op placeholders if needed for imports, but must not mark DATA-02/DATA-03 complete; those are implemented in Plan 03-03. Avoid prohibited scope-reduction language in code comments and summaries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fail-closed merged-row loading and exact split planner</name>
  <files>tsc_cycle/v3_gates/dataset_rebuild_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-PATTERNS.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase2_datagen_report.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/hashing.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_dataset_rebuild.py
  </read_first>
  <behavior>
    - `build_split_plan(rows, seed=42)` returns split rows/counts train=7601, val=950, ood_val=950 for 9501 input rows per DATA-01.
    - All old/v1 rows whose `input.split_hint == "ood"` or equivalent root split_hint/source lineage marks v1 OOD are pinned into `ood_val` per DATA-01.
    - Exactly 650 new OOD rows are selected with `random.Random(42).sample(sorted(new_ood_ids), 650)` to complete ood_val per DATA-01.
    - Duplicate sample IDs, insufficient v1 OOD count, insufficient new OOD candidates, or cross-split overlap produces `ok=false`/fatal failures and prevents writes per T-03-duplicate-split.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/dataset_rebuild_v3.py` using project gate style from `phase2_datagen_report.py`: future annotations, argparse-compatible dataclass/config, JSONL reader returning rows+errors, `_record_sample_id`, `_record_input`, `_record_solution`, `_record_source`, `_record_reasoning`, `_record_lineage`, `_add_gate`, and `_failure`. Implement `build_split_plan` to sort candidate IDs before seed sampling. Classification must distinguish old/v1 rows from new rows using available lineage fields (`source`, `metadata`, input/root markers) and Phase 2 merged row shape; do not rely on row order alone except as a final fallback recorded in the report. Never write or modify `data/labeled.jsonl`, `data/tokenized/{train,val_id,val_ood}`, or any v1 path per T-03-path-drift.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_rebuild.py::test_split_exact_sizes_and_v1_ood_alignment -q</automated>
  </verify>
  <acceptance_criteria>The DATA-01 split test passes; duplicate/overlap failure paths are represented in gates/fatal_failures; no production data artifact is required for this unit-level task.</acceptance_criteria>
  <done>`build_split_plan` is deterministic, exact-sized, fail-closed, and DATA-01 test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement split index, manifest, and v1 OOD alignment writers</name>
  <files>tsc_cycle/v3_gates/dataset_rebuild_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-PATTERNS.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/hashing.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_dataset_rebuild.py
  </read_first>
  <behavior>
    - Index files `train.index.jsonl`, `val.index.jsonl`, `ood_val.index.jsonl` contain one JSON object per selected row per DATA-04.
    - Every index object includes `sample_id`, `split`, `lineage`, `source`, `record_hash`, `input_hash`, `solution_hash`, `prompt_hash`, `assistant_hash`, `raw_index`, `seed`, and `is_v1_ood` per DATA-04.
    - `manifest.json` records `seed=42`, split sizes, total rows=9501, source input SHA-256, Phase 2 merge report path/ok, requirements covered DATA-01/DATA-04, and all gate outcomes per DATA-04.
    - `v1_ood_alignment.json` records expected_v1_ood_count=300, observed_v1_ood_count=300, missing_v1_ood_ids=[], and ood_val_v1_ood_count=300 per DATA-01/DATA-04.
  </behavior>
  <action>Implement pure helpers to compute canonical hashes using `tsc_cycle.hashing.canonical_json`/`sha256_hex` and prompt/assistant strings using `build_user_prompt` + `build_full_assistant`. Implement `write_split_artifacts` or equivalent so index/manifest/alignment files are written only after all split gates pass. Use JSON with `ensure_ascii=False`, `sort_keys=True`, newline termination. The function must create only `data/splits/v3` descendants. If a gate fails, write only a report object when explicitly requested by caller, and do not overwrite existing successful index files.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_rebuild.py::test_split_indices_persist_hashes_and_manifest -q</automated>
  </verify>
  <acceptance_criteria>The DATA-04 test passes; generated tmp_path index and manifest artifacts contain exact fields and v1 OOD alignment evidence; no path-drift to v1 artifacts occurs.</acceptance_criteria>
  <done>Split reproducibility artifacts are implemented and DATA-04 test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/v3/phase2/labeled_merged.jsonl` -> split planner | Local generated data is treated as untrusted until duplicate, lineage, lint, and size gates pass. |
| Split planner -> persisted indices | Split membership becomes a Phase 4/6 contract and must be immutable/replayable. |
| Project output paths -> filesystem | A typo could overwrite v1 data or legacy tokenized artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-duplicate-split | Tampering | `build_split_plan` | mitigate | Build global sample_id sets, reject duplicates and pairwise split overlaps before writing any index. |
| T-03-path-drift | Tampering | artifact writers | mitigate | Hard-code defaults to `data/splits/v3`; validate output paths do not resolve to `data/labeled.jsonl` or legacy `data/tokenized` v1 directories. |
| T-03-truncation | Integrity | split-only plan | accept | Token truncation is not performed in this plan; Plan 03-03 mitigates it before Arrow writes. |
| T-03-reproducibility | Repudiation | `manifest.json` and `*.index.jsonl` | mitigate | Persist seed, split sizes, source SHA, record/input/prompt/assistant hashes, raw indices, and v1 OOD alignment evidence. |
</threat_model>

<verification>
Run focused split/reproducibility tests:
```bash
cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v3_dataset_rebuild.py::test_split_exact_sizes_and_v1_ood_alignment tests/test_v3_dataset_rebuild.py::test_split_indices_persist_hashes_and_manifest -q
```
</verification>

<success_criteria>
- `build_split_plan` satisfies DATA-01 exact split counts and v1 OOD inclusion.
- Index and manifest writers satisfy DATA-04 reproducibility requirements.
- Failing gates do not write split artifacts.
- Production defaults point to `data/splits/v3` only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dataset-rebuild-qwen3-5-retokenize-split/03-02-SUMMARY.md`
</output>
