---
phase: "04-qlora-sft-9b-batch-1"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - "04-02"
files_modified:
  - "tsc_cycle/v3_gates/sft_dry_run_v3.py"
  - "scripts/run_v3_phase4_dry_run.sh"
autonomous: true
requirements:
  - "SFT-04"
  - "SFT-06"
  - "SFT-07"
  - "SFT-08"
must_haves:
  truths:
    - "Full training is impossible unless the 500-sample dry-run gate passes."
    - "Dry-run report proves OOD hard-constraint pass rate is at least 95%."
    - "Dry-run report proves first-200-step grad_norm p99 is below 3.0 and losses are finite."
    - "Dry-run executes under DGX Spark memory protection and writes only under a v3.0-9B run root."
  artifacts:
    - path: "tsc_cycle/v3_gates/sft_dry_run_v3.py"
      provides: "Dry-run gate report evaluator and 500-sample OOD lint harness"
    - path: "scripts/run_v3_phase4_dry_run.sh"
      provides: "Safe dry-run wrapper using env.sh and run_safe.sh 100G"
  key_links:
    - from: "scripts/run_v3_phase4_dry_run.sh"
      to: "tsc_cycle.student.train"
      via: "run_safe 100G dry-run invocation"
      pattern: "run_safe.sh 100G --.*tsc_cycle.student.train"
    - from: "tsc_cycle/v3_gates/sft_dry_run_v3.py"
      to: "tsc_cycle.constraint_lint.validate"
      via: "generated SOLUTION lint"
      pattern: "validate\("
    - from: "tsc_cycle/v3_gates/sft_dry_run_v3.py"
      to: "data/splits/v3/ood_val.index.jsonl + data/v3/phase2/labeled_merged.jsonl"
      via: "deterministic 500-sample OOD raw input recovery by raw_index/sample_id"
      pattern: "ood_val.index.jsonl|labeled_merged.jsonl"
---

<objective>
Implement the Phase 4 500-sample dry-run gate and safe wrapper so failed OOD lint or gradient stability evidence aborts before any full run.

Purpose: Satisfy SFT-04 and SFT-06 without relying on loss-only or adapter-exists false greens.
Output: A dry-run validation module and wrapper script that produce `dry_run_report.json` with `ok`, `full_run_allowed`, `gates`, `fatal_failures`, sample hashes, grad stats, and v1.0 FROZEN evidence.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
@/home/samuel/TSC_CYCLE/scripts/dgx_spark/env.sh
@/home/samuel/TSC_CYCLE/scripts/dgx_spark/run_safe.sh

<interfaces>
Contracts produced by Plan 04-02:
```python
from tsc_cycle.student.sft_v3 import (
    WANDB_PROJECT,
    ensure_v1_frozen,
    evaluate_grad_gate,
    load_arrow_split,
    validate_run_root,
)
```
Existing parser/lint contracts:
```python
from tsc_cycle.prompt_builder import parse_assistant_output
from tsc_cycle.constraint_lint import validate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dry-run gate evaluator and OOD lint report</name>
  <files>tsc_cycle/v3_gates/sft_dry_run_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/constraint_lint.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py
    - /home/samuel/TSC_CYCLE/data/splits/v3/rebuild_report.json
  </read_first>
  <behavior>
    - SFT-04 / D-07: `evaluate_dry_run_gate(report)` requires `sample_count == 500`, `ood_hard_constraint_pass_rate >= 0.95`, `elapsed_seconds <= 3600` for dry-run only, and `dry_run_adapter_path` exists or is explicitly recorded by the runner.
    - SFT-06 / D-07: dry-run evaluator incorporates `evaluate_grad_gate()` result and requires `grad_norm_p99 < 3.0`, finite losses, and first-200-step evidence.
    - Native-think leakage: generated token IDs or parsed outputs with native `<think>` leakage count as fatal failures.
    - Report shape: output includes `ok`, `full_run_allowed`, `gates`, `fatal_failures`, `requirements_covered=["SFT-04","SFT-06"]`, deterministic sample IDs/hash, pass/fail violation counts, and an evidence JSONL with 500 rows containing `sample_id`, `raw_index`, `prediction_input`, `generated_text`, `parsed_solution`, and `lint_result`.
    - Dry-run OOD lint uses `model.generate` from the dry-run adapter/checkpoint on raw `prediction_input` recovered from `data/v3/phase2/labeled_merged.jsonl` through `data/splits/v3/ood_val.index.jsonl`; it must not compute lint from Arrow token IDs alone.
  </behavior>
  <action>Create `tsc_cycle/v3_gates/sft_dry_run_v3.py` with pure `evaluate_dry_run_gate(report)` plus CLI helpers. The CLI must read dry-run training outputs from a run root, recover exactly 500 deterministic OOD raw prediction inputs from `data/v3/phase2/labeled_merged.jsonl` using `data/splits/v3/ood_val.index.jsonl` `raw_index`/`sample_id` mapping, load the dry-run adapter/checkpoint, run `model.generate` for each prompt, parse generated text with `parse_assistant_output`, lint with `constraint_lint.validate`, and write both `dry_run_ood_generations.jsonl` and summary JSON via existing v3 gate style. If actual generation artifacts are absent, fail closed rather than manufacturing success. Use Arrow tokenized OOD data only for alignment/hash evidence, not as the source for `prediction_input` or constraint lint.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'sample_count.*500'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c '0.95'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'full_run_allowed'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'constraint_lint'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'labeled_merged.jsonl'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'ood_val.index.jsonl'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'model.generate\|generate('` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'prediction_input'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py | grep -c 'elapsed_seconds.*3600\|3600'` returns at least 1.
  </acceptance_criteria>
  <done>Dry-run gate tests pass and the evaluator cannot mark a run green without 500 OOD samples, 95% lint pass, and grad gate success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe Phase 4 dry-run wrapper</name>
  <files>scripts/run_v3_phase4_dry_run.sh</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/scripts/dgx_spark/env.sh
    - /home/samuel/TSC_CYCLE/scripts/dgx_spark/run_safe.sh
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
  </read_first>
  <behavior>
    - D-09: wrapper sources `scripts/dgx_spark/env.sh` and launches trainer via `scripts/dgx_spark/run_safe.sh 100G -- /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.student.train --mode dry-run ...`.
    - D-10: wrapper creates or accepts only a `runs/v3.0-9B-{utc}` root and exports `WANDB_PROJECT=tsc-cycle-v3-9b`.
    - D-11: wrapper verifies `runs/20260507T032419Z/FROZEN.md` exists/read-only evidence before training and rechecks after dry-run.
    - SFT-04/SFT-06: wrapper invokes dry-run OOD generation and gate report generation after trainer exits and exits non-zero unless `dry_run_report.json` has `ok=true`, `full_run_allowed=true`, and `elapsed_seconds <= 3600`.
    - SFT-04: wrapper/trainer records enough run-root evidence for the evaluator to load the dry-run adapter and generate 500 OOD outputs before full-run approval.
  </behavior>
  <action>Create executable Bash wrapper with `set -euo pipefail`, fixed `ROOT=/home/samuel/TSC_CYCLE`, fixed `PY=/home/samuel/TSC_CYCLE/.venv/bin/python`, and no user-provided command string. Check required inputs: Phase 1 gate report, Phase 3 rebuild report, `data/tokenized/v3/train.arrow`, `val.arrow`, `ood_val.arrow`, and v1.0 root. Use argv arrays where possible. Enforce the SFT-04 dry-run early-exit wall-clock gate by recording elapsed seconds and failing if the dry-run exceeds 3600 seconds; do not add any 6h/full-run timeout because D-08 forbids the old full-run cap.</action>
  <verify>
    <automated>bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -x /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh` succeeds.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh | grep -c 'run_safe.sh 100G --'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh | grep -c 'tsc-cycle-v3-9b'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh | grep -c 'FROZEN.md'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh | grep -c '3600'` returns at least 1.
  </acceptance_criteria>
  <done>Dry-run wrapper is syntax-valid, executable, uses DGX safe scope, writes isolated reports, and fails closed unless the dry-run gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wrapper -> shell/systemd | Bash wrapper constructs fixed argv for run_safe and must not eval user strings. |
| Dry-run adapter -> OOD evaluator | Generated model outputs cross into lint validation and must be parsed strictly. |
| Dry-run report -> full-run permission | `full_run_allowed` becomes the gate for Plan 04-04. |
| DGX runtime -> host stability | Dry-run consumes GPU memory and must run under MemoryMax=100G / MemorySwapMax=0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | v1.0 artifact root | mitigate | Wrapper checks FROZEN before and after dry-run and never passes v1.0 root as output. |
| T-04-14 | Tampering/Repudiation | dry-run dataset | mitigate | Evaluator records deterministic 500 OOD sample IDs/hash from `data/splits/v3/ood_val.index.jsonl` and raw `prediction_input` recovered from `data/v3/phase2/labeled_merged.jsonl`. |
| T-04-15 | Integrity | native think leakage | mitigate | Evaluator treats native leakage as fatal and records violation counts. |
| T-04-16 | Denial of Service | dry-run training | mitigate | Wrapper uses `scripts/dgx_spark/run_safe.sh 100G --` and sources `env.sh`. |
| T-04-17 | Elevation/Tampering | wrapper env/args | mitigate | Fixed `PY`, fixed module invocation, no `eval`, no user command string. |
| T-04-18 | Repudiation | false-green dry-run | mitigate | Report requires explicit sample_count, pass rate, grad p99, finite losses, and `full_run_allowed=true`. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q && bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh</automated>

Long-run execution gate after implementation:
<automated>/home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh</automated>
</verification>

<success_criteria>
- `scripts/run_v3_phase4_dry_run.sh` exits non-zero if any dry-run gate fails.
- Dry-run report has `ok=true`, `full_run_allowed=true`, `sample_count=500`, `ood_hard_constraint_pass_rate>=0.95`, `elapsed_seconds<=3600`, `grad_norm_p99<3.0`, and no NaN/Inf before full-run can be launched.
- Dry-run evidence includes 500 generated outputs from the dry-run adapter with per-sample `prediction_input`, `generated_text`, `parsed_solution`, and `lint_result`.
- No direct trainer invocation bypasses `run_safe.sh` in the wrapper.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-03-SUMMARY.md`.
</output>
