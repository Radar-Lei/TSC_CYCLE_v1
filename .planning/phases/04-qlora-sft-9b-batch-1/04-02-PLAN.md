---
phase: "04-qlora-sft-9b-batch-1"
plan: "02"
type: "execute"
wave: 1
depends_on:
  - "04-01"
files_modified:
  - "tsc_cycle/student/sft_v3.py"
  - "tsc_cycle/student/train.py"
  - "tests/test_v3_sft_grad_gate.py"
autonomous: true
requirements:
  - "SFT-01"
  - "SFT-02"
  - "SFT-03"
  - "SFT-05"
  - "SFT-06"
  - "SFT-07"
  - "SFT-08"
must_haves:
  truths:
    - "Trainer bootstrap uses Qwen/Qwen3.5-9B with bnb NF4, bf16, SDPA, and no flash-attn/vLLM."
    - "D-01: Trainer stack remains HF Transformers + TRL + PEFT + bitsandbytes, with no Unsloth, Axolotl, new PyTorch version, or vLLM."
    - "D-02: Trainer uses the project/DGX Spark verified venv assumptions and /dgx-spark-training constraints."
    - "D-03: Trainer forces SDPA attention and forbids upstream flash-attn CUDA 12 dependency paths."
    - "Trainer consumes Phase 3 Arrow IPC artifacts directly and never falls back to legacy parquet data."
    - "Locked QLoRA and TrainingArguments values are machine-checkable before GPU training starts."
    - "D-04: Helper/trainer config locks r=64, lora_alpha=64, lora_dropout=0.0, and target_modules=all-linear."
    - "D-05: Helper/trainer config locks batch_size=1, gradient_accumulation_steps=16, packing=False, and non-reentrant gradient checkpointing."
    - "D-06: Helper/trainer config locks adamw_torch_fused, lr=1e-4, cosine schedule with warmup, and max_grad_norm=0.5."
    - "SFT-06 / D-07: Trainer immediately stops through `GradNormAbortCallback` on NaN/Inf, and after first-200-step evidence stops when grad_norm p99 >= 3.0."
    - "SFT-06 / D-07: `grad_gate.json` is written by the trainer callback with first-200-step loss/grad_norm evidence and pass/fail status for dry-run and full-run manifests."
    - "LoRA coverage evidence proves 24 GatedDeltaNet linear-attention layers and 8 full-attention layers have projection adapters, or fails closed."
    - "Run outputs are restricted to `runs/v3.0-9B-{utc}` and wandb project `tsc-cycle-v3-9b`."
    - "D-10: Output validation restricts Phase 4 artifacts to runs/v3.0-9B-{utc} and wandb project tsc-cycle-v3-9b."
    - "D-11: Trainer foundation includes the FROZEN guard for runs/20260507T032419Z and treats that path as read-only."
  artifacts:
    - path: "tsc_cycle/student/sft_v3.py"
      provides: "Phase 4 config, Arrow loader, FROZEN guard, GradNormAbortCallback, grad_gate.json writer, LoRA coverage helpers"
    - path: "tsc_cycle/student/train.py"
      provides: "Phase 4 trainer entrypoint wired to helper contracts and Trainer(callbacks=[GradNormAbortCallback(...), EarlyStoppingCallback(...)])"
    - path: "tests/test_v3_sft_grad_gate.py"
      provides: "SFT-06 tests asserting callback stop behavior, grad_gate.json shape, p99 threshold, and NaN/Inf immediate abort"
  key_links:
    - from: "tsc_cycle/student/train.py"
      to: "tsc_cycle.student.sft_v3"
      via: "imports locked config, loader helpers, and GradNormAbortCallback"
      pattern: "from tsc_cycle.student.sft_v3 import"
    - from: "tsc_cycle/student/train.py"
      to: "GradNormAbortCallback"
      via: "Trainer callbacks list for dry-run and full-run paths"
      pattern: "callbacks=.*GradNormAbortCallback|GradNormAbortCallback\\("
    - from: "tsc_cycle/student/train.py"
      to: "grad_gate.json"
      via: "run manifest records callback artifact path/status"
      pattern: "grad_gate\\.json"
    - from: "tsc_cycle/student/sft_v3.py"
      to: "data/tokenized/v3/*.arrow"
      via: "pyarrow.ipc.open_file loader"
      pattern: "pa\.ipc\.open_file"
    - from: "tsc_cycle/student/sft_v3.py"
      to: "runs/20260507T032419Z/FROZEN.md"
      via: "ensure_v1_frozen"
      pattern: "FROZEN.md"
---

<objective>
Implement the shared Phase 4 trainer foundation: Arrow IPC loading, locked QLoRA/training config, LoRA coverage reporting, FROZEN guard, output-root validation, SFT-06 immediate grad gate abort callback, and trainer wiring.

Purpose: Convert the existing v1.0 trainer scaffold into a Phase 4-safe Qwen3.5-9B SFT entrypoint without changing the locked stack or bypassing safety gates.
Output: `tsc_cycle/student/sft_v3.py` helper module, updated `tsc_cycle/student/train.py`, and callback-focused `tests/test_v3_sft_grad_gate.py` coverage that satisfy Wave 0 config, Arrow, FROZEN, grad abort, and artifact isolation tests.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-VALIDATION.md
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/memory_budget_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py

<interfaces>
Existing source signatures to preserve or adapt:
```python
# tsc_cycle.v3_gates.memory_budget_v3
MODEL_NAME = "Qwen/Qwen3.5-9B"
def load_qlora_model(model_name: str, lora_r: int, lora_alpha: int): ...
def iter_trainable_parameters(model): ...
```
```python
# tsc_cycle.v3_gates.dataset_rebuild_v3
def write_arrow_split(path: Path, rows_tok: list[dict[str, Any]]) -> None: ...
```
```python
# tsc_cycle.student.train currently has
def boot_tokenizer_check(tokenizer) -> None: ...
def smoke_generate(model, tokenizer, n: int = 5, device: str = "cuda") -> dict: ...
```
Required new SFT-06 callback contract:
```python
# tsc_cycle.student.sft_v3
class GradNormAbortCallback(TrainerCallback):
    """Collect first-200 optimizer-step loss/grad_norm logs, write grad_gate.json, and stop Trainer on SFT-06 failures."""

def grad_gate_report_path(run_root: Path, mode: str) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 4 SFT helper contracts and grad abort callback</name>
  <files>tsc_cycle/student/sft_v3.py, tests/test_v3_sft_grad_gate.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/memory_budget_v3.py
  </read_first>
  <behavior>
    - SFT-01 / D-04: `locked_lora_config_kwargs()` returns `r=64`, `lora_alpha=64`, `lora_dropout=0.0`, `target_modules="all-linear"`, `bias="none"`, `task_type="CAUSAL_LM"`.
    - SFT-02/SFT-03/SFT-05 / D-05/D-06/D-08: `locked_training_arguments_kwargs(output_dir)` returns exact locked Trainer kwargs for lr, optimizer, max_grad_norm, batch, grad accum, eval/save every 200 steps, early stopping compatibility, and max epochs 5.
    - SFT-06 / D-07: implement `GradNormAbortCallback` (or equivalent `TrainerCallback`) in `tsc_cycle/student/sft_v3.py` that collects loss and `grad_norm` logs for the first 200 optimizer steps, writes `grad_gate.json`, sets `control.should_training_stop = True` immediately on any NaN/Inf loss or grad_norm, and sets `control.should_training_stop = True` once 200-step evidence shows `grad_norm_p99 >= 3.0`.
    - SFT-06 / D-07: `grad_gate.json` includes `ok`, `status`, `gate_steps=200`, `observed_steps`, `loss_rows`, `grad_norm_rows`, `grad_norm_p99`, `p99_limit=3.0`, `fatal_failures`, and `stopped_early`; missing first-200-step evidence is fail-closed for gate evaluation.
    - SFT-06 / D-07: `tests/test_v3_sft_grad_gate.py` asserts callback stop behavior through a fake Trainer state/control flow, including NaN/Inf immediate stop and p99>=3.0 stop at/after 200 steps, not just a pure post-hoc evaluator.
    - SFT-07 / D-10: `validate_run_root()` accepts only project-local `runs/v3.0-9B-*` roots and rejects v1.0 or shell-metacharacter paths.
    - SFT-08 / D-11: `ensure_v1_frozen()` writes `FROZEN.md`, recursively removes write bits where possible, and returns evidence containing path, frozen marker, chmod attempted, and hashes/mtimes.
    - SFT-01 / D-04: `write_lora_coverage_report()` records `expected_gated_deltanet_layers=24`, `expected_full_attention_layers=8`, observed layer counts, projection names, trainable LoRA module names, and `ok=false` with fatal failures on any mismatch.
    - Arrow loader consumes `pa.ipc.open_file` files from `data/tokenized/v3` and can drop metadata columns for Trainer.
  </behavior>
  <action>Create `tsc_cycle/student/sft_v3.py` with constants `MODEL_NAME="Qwen/Qwen3.5-9B"`, `RUN_ROOT_PREFIX="v3.0-9B-"`, `WANDB_PROJECT="tsc-cycle-v3-9b"`, `MODEL_COLUMNS`, and `GRAD_GATE_FILENAME="grad_gate.json"`. Implement pure helper functions first so RED tests pass without loading GPU models. Implement `GradNormAbortCallback` as a `transformers.TrainerCallback` subclass with `on_log`/`on_step_end` behavior that records first-200-step `loss` and `grad_norm` rows, writes `grad_gate.json` under the mode-specific report directory whenever status changes, immediately stops on non-finite loss or grad_norm, and stops at/after step 200 when computed p99 is `>= 3.0`. Keep an optional pure report evaluator only as a reader/formatter for the callback artifact; do not make SFT-06 a post-hoc-only gate. Update `tests/test_v3_sft_grad_gate.py` if necessary so it simulates callback calls with fake `state.global_step` and fake `control`, and asserts `control.should_training_stop` plus the written `grad_gate.json` content. Use SHA-256 helper style from `dataset_rebuild_v3.py`; do not install or import vLLM/flash-attn; do not create new dependencies. Add a LoRA coverage helper that inspects `model.named_modules()`/trainable adapter names and emits `lora_coverage.json` with exact SFT-01 coverage fields for 24 GatedDeltaNet linear-attention layers and 8 full-attention layers. Use sorted-index p99 consistently and treat missing/insufficient 200-step evidence as failure in the report schema.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'target_modules.*all-linear'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'expected_gated_deltanet_layers.*24'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'expected_full_attention_layers.*8'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'adamw_torch_fused'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'pa.ipc.open_file'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'FROZEN.md'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'class GradNormAbortCallback'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'should_training_stop'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py | grep -c 'grad_gate.json'` returns at least 1.
  </acceptance_criteria>
  <done>Helper module passes all fast tests for locked config, Arrow loader, FROZEN guard, run-root allowlist, callback-driven grad gate abort semantics, and grad_gate.json reporting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor trainer entrypoint to use locked Phase 4 helpers and callbacks</name>
  <files>tsc_cycle/student/train.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
  </read_first>
  <behavior>
    - D-01/D-02/D-03: trainer uses HF Transformers + PEFT + bitsandbytes, project venv assumptions, and `attn_implementation="sdpa"`; no vLLM, no flash-attn, no Unsloth/Axolotl.
    - D-04/D-05/D-06/D-08: CLI defaults and internal validation cannot diverge from `sft_v3.py` locked values.
    - SFT-05 / D-08: Trainer is constructed with `EarlyStoppingCallback(early_stopping_patience=3)` and compatible eval/save every 200 steps for dry-run and full-run paths.
    - SFT-06 / D-07: Trainer is constructed for both `--mode dry-run` and `--mode full` with `callbacks=[GradNormAbortCallback(...), EarlyStoppingCallback(...)]` or an equivalent list containing both callbacks.
    - SFT-06 / D-07: dry-run and full-run manifests include the `grad_gate.json` artifact path, callback status, `grad_norm_p99`, `observed_steps`, and fatal failures; full-run cannot report green when `grad_gate.json` is missing or failing.
    - SFT-01: write `lora_coverage.json` under the run root after PEFT injection, including trainable module names/counts, `expected_gated_deltanet_layers=24`, `expected_full_attention_layers=8`, observed layer/projection coverage, and a fail-closed `ok` boolean.
    - SFT-07: default output root is `runs/v3.0-9B-{utc_timestamp}` and WANDB_PROJECT is set/validated as `tsc-cycle-v3-9b` when wandb is enabled.
    - SFT-08: trainer calls `ensure_v1_frozen()` before any run-root write.
  </behavior>
  <action>Update `train.py` to import `sft_v3` helpers including `GradNormAbortCallback`. Replace legacy defaults: model becomes `Qwen/Qwen3.5-9B`, data-dir becomes `data/tokenized/v3`, batch-size 1, grad-accum 16, epochs 5, lora-alpha 64, lora-dropout 0.0, target_modules `all-linear`, optimizer `adamw_torch_fused`, max_grad_norm 0.5, eval/save steps 200. Replace parquet loader with `load_arrow_split` for `train.arrow` and `val.arrow`; keep `DataCollatorForSeq2Seq`. Add `--mode {dry-run,full}` and `--output-root` if tests require it. When constructing `Trainer`, pass explicit callbacks containing `GradNormAbortCallback(run_root=..., mode=args.mode, gate_steps=200, p99_limit=3.0)` and `EarlyStoppingCallback(early_stopping_patience=3)` for both dry-run and full-run paths. Ensure trainer manifests for both modes record `grad_gate_path`, `grad_gate_status`, `grad_norm_p99`, and `grad_gate_fatal_failures` by reading the callback artifact after `trainer.train()` returns or stops. Do not add a wall-clock timeout; D-08 forbids a 6h cap. Keep GPU model loading behind `main()` so unit tests can import helpers without CUDA.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'Qwen/Qwen3.5-9B'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'data.parquet'` returns 0.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'attn_implementation="sdpa"'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'EarlyStoppingCallback'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'GradNormAbortCallback'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'callbacks='` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'grad_gate.json\|grad_gate_path'` returns at least 1.
  </acceptance_criteria>
  <done>Trainer entrypoint uses locked Phase 4 config, reads Arrow IPC data, wires immediate SFT-06 abort callback plus early stopping into dry-run and full-run Trainer construction, writes v3-isolated reports, and satisfies Wave 0 config/artifact/grad-gate tests without launching a GPU run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 Arrow -> trainer | Immutable tokenized datasets enter training; wrong paths or schema drift must fail. |
| CLI args -> output filesystem | User-supplied run roots must not redirect writes into v1.0 or arbitrary paths. |
| Trainer logs -> gate reports | Numeric logs become security/safety gate decisions; missing values fail closed. |
| Trainer callback -> training control | Callback decisions can stop GPU training; non-finite values and p99 threshold must fail closed. |
| Trainer -> DGX runtime | Python code must rely on wrappers for cgroup memory protection, not bypass them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | `runs/20260507T032419Z` | mitigate | `ensure_v1_frozen()` runs before output creation and `validate_run_root()` rejects v1.0 path. |
| T-04-08 | Tampering/Repudiation | Arrow loader | mitigate | Loader only reads `*.arrow` IPC with required columns and reportable hashes; no parquet fallback. |
| T-04-09 | Integrity | tokenizer/native think handling | mitigate | Trainer uses active tokenizer checks and avoids v1.0 native think constants for v3 validation. |
| T-04-10 | Denial of Service | Qwen3.5 training | mitigate | Plan requires wrappers in later waves; trainer records `run_safe_required` and does not invoke direct unsafe long runs. |
| T-04-11 | Elevation/Tampering | output-root argument | mitigate | Reject absolute/outside-project roots and shell metacharacters; use argv, not shell strings. |
| T-04-12 | Repudiation | false-green config | mitigate | Locked config helpers and unit tests prove exact SFT-01/SFT-02/SFT-03/SFT-05/SFT-07 values before GPU execution. |
| T-04-19 | Denial of Service/Integrity | `GradNormAbortCallback` | mitigate | Callback sets `control.should_training_stop=True` on NaN/Inf immediately and on first-200-step p99>=3.0; writes `grad_gate.json` for audit. |
</threat_model>

<verification>
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q</automated>
</verification>

<success_criteria>
- Wave 0 tests for config, Arrow loader, FROZEN guard, callback-driven grad gate abort, and artifacts pass.
- No `data.parquet` path remains in active Phase 4 trainer code.
- No dependency on vLLM, flash-attn, Unsloth, Axolotl, or a new PyTorch version is introduced.
- Trainer defaults and helper constants match D-01 through D-11 where applicable.
- SFT-06 is enforced inside Trainer execution by `GradNormAbortCallback`, not only by a post-hoc dry-run evaluator.
- Dry-run and full-run manifests include `grad_gate.json` path/status and fail closed when it is absent or failing.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-02-SUMMARY.md`.
</output>
