---
phase: "04-qlora-sft-9b-batch-1"
plan: "04"
type: "execute"
wave: 3
depends_on:
  - "04-03"
files_modified:
  - "scripts/run_v3_phase4_full.sh"
  - "tsc_cycle/student/train.py"
autonomous: false
requirements:
  - "SFT-05"
  - "SFT-06"
  - "SFT-07"
  - "SFT-08"
must_haves:
  truths:
    - "Full training starts only after a green dry-run report."
    - "Full training has no 6h wall-clock cap and relies on early stopping with val loss patience=3."
    - "The best/final adapter and full-run manifest live under the same `runs/v3.0-9B-{utc}` root that already contains the green dry-run report."
    - "wandb telemetry is isolated to project `tsc-cycle-v3-9b` when enabled."
  artifacts:
    - path: "scripts/run_v3_phase4_full.sh"
      provides: "Safe full-run wrapper gated by dry-run report"
    - path: "tsc_cycle/student/train.py"
      provides: "Full-run manifest writing and early-stopping trainer behavior"
  key_links:
    - from: "scripts/run_v3_phase4_full.sh"
      to: "dry_run_report.json"
      via: "preflight JSON ok/full_run_allowed check"
      pattern: "full_run_allowed"
    - from: "scripts/run_v3_phase4_full.sh"
      to: "scripts/dgx_spark/run_safe.sh"
      via: "run_safe 100G full invocation"
      pattern: "run_safe.sh 100G --"
    - from: "tsc_cycle/student/train.py"
      to: "runs/v3.0-9B-*/sft_manifest.json"
      via: "manifest write after trainer completion"
      pattern: "sft_manifest.json"
---

<objective>
Add the full-run execution wrapper and manifest wiring that run Qwen3.5-9B SFT to early-stopping convergence after the dry-run gate passes.

Purpose: Deliver SFT-05/SFT-07 while preserving SFT-06/SFT-08 safety: no 6h cap, no unsafe direct GPU run, no v1.0 artifact writes, and clear evidence for downstream export/eval phases.
Output: `scripts/run_v3_phase4_full.sh` plus trainer manifest updates that produce a best adapter path and `sft_manifest.json` under the isolated run root.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
@/home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh
@/home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py
@/home/samuel/TSC_CYCLE/scripts/dgx_spark/env.sh
@/home/samuel/TSC_CYCLE/scripts/dgx_spark/run_safe.sh

<interfaces>
Dry-run gate consumed from Plan 04-03:
```json
{
  "ok": true,
  "full_run_allowed": true,
  "sample_count": 500,
  "ood_hard_constraint_pass_rate": 0.95,
  "grad_norm_p99": 2.99
}
```
Trainer manifest expected after full run:
```json
{
  "ok": true,
  "mode": "full",
  "run_root": "runs/v3.0-9B-{utc}",
  "adapter_path": "runs/v3.0-9B-{utc}/adapter",
  "best_model_checkpoint": "...",
  "early_stopping": {"patience": 3, "eval_steps": 200, "max_epochs": 5},
  "early_stopping_triggered": true,
  "stop_reason": "early_stopping",
  "wandb_project": "tsc-cycle-v3-9b"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire full-run manifest and early-stopping evidence</name>
  <files>tsc_cycle/student/train.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
  </read_first>
  <behavior>
    - SFT-05 / D-08: Trainer uses `EarlyStoppingCallback(early_stopping_patience=3)`, `eval_steps=200`, `save_steps=200`, `num_train_epochs=5`, and no wall-clock timeout or 6h kill switch.
    - SFT-05 / D-08: Full-run manifest records `early_stopping_triggered=true` and `stop_reason="early_stopping"`; if max epochs end without early stopping, `ok=false` and the aggregate report must fail.
    - SFT-06 / D-07: Full-run manifest includes grad gate artifact path and does not ignore `ok=false` grad evidence.
    - SFT-07 / D-10: Manifest includes run root, adapter path, best checkpoint, wandb project, input Arrow hashes, LoRA coverage path, and package/model evidence.
    - SFT-08 / D-11: Manifest includes FROZEN guard evidence from preflight.
  </behavior>
  <action>Extend `train.py` so both dry-run and full-run modes write a structured `sft_manifest.json` under the run root. For full mode, include Trainer state fields after training: elapsed seconds, global_step, best_model_checkpoint, best_metric/eval_loss if available, `early_stopping_triggered`, `stop_reason`, adapter path, LoRA coverage path, grad gate path, FROZEN evidence, and input artifact hashes. Mark the manifest green only when early stopping actually triggered convergence (`early_stopping_triggered=true` and `stop_reason="early_stopping"`); max-epoch completion without early stopping is a failed convergence gate. Ensure the final adapter is saved to `run_root/adapter`, not nested legacy `runs/{ts}/train/adapter`. Keep D-08 explicit: no timeout, no max wall-clock check, no 6h cap.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'sft_manifest.json'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'EarlyStoppingCallback'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'early_stopping_patience=3'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'early_stopping_triggered'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c 'stop_reason.*early_stopping'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py | grep -c '6h\|six hour\|timeout'` returns 0.
  </acceptance_criteria>
  <done>Trainer produces full-run manifest evidence and retains locked early-stopping behavior without a wall-clock cap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve launch of long full-run training</name>
  <files>runs/v3.0-9B-*/dry_run_report.json</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/runs/v3.0-9B-*/dry_run_report.json
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
  </read_first>
  <what-built>Plan 04-03 automated the dry-run gate. This checkpoint occurs after `/home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh` has produced a green `dry_run_report.json` with `full_run_allowed=true`.</what-built>
  <action>Pause before launching the long no-6h-cap full training run. Present the dry-run report path and the exact green fields to the user. Do not launch `scripts/run_v3_phase4_full.sh` until the user explicitly replies `approved`.</action>
  <how-to-verify>
    1. Inspect the dry-run report path printed by `scripts/run_v3_phase4_dry_run.sh`.
    2. Confirm it contains `ok=true`, `full_run_allowed=true`, `sample_count=500`, `ood_hard_constraint_pass_rate>=0.95`, `grad_norm_p99<3.0`, and no NaN/Inf failures.
    3. Confirm you are ready to allow a long DGX Spark full training run with no 6h cap per D-08.
  </how-to-verify>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -c 'import json, glob, sys; paths=glob.glob("/home/samuel/TSC_CYCLE/runs/v3.0-9B-*/dry_run_report.json"); assert paths, "missing dry_run_report"; p=max(paths); d=json.load(open(p)); assert d.get("ok") is True and d.get("full_run_allowed") is True; assert d.get("sample_count") == 500; assert float(d.get("ood_hard_constraint_pass_rate", 0)) >= 0.95; assert float(d.get("grad_norm_p99", 999)) < 3.0; print(p)'</automated>
  </verify>
  <acceptance_criteria>
    - A dry-run report exists under a `runs/v3.0-9B-*` run root.
    - The automated check above passes.
    - User replies exactly `approved` before Task 3 launches the full run.
  </acceptance_criteria>
  <resume-signal>Type `approved` to allow the executor to run the full training wrapper, or describe the issue to stop.</resume-signal>
  <done>User explicitly approved launching the long full-run training after green dry-run evidence was shown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add and run gated safe full-run wrapper</name>
  <files>scripts/run_v3_phase4_full.sh</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh
    - /home/samuel/TSC_CYCLE/scripts/dgx_spark/env.sh
    - /home/samuel/TSC_CYCLE/scripts/dgx_spark/run_safe.sh
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py
  </read_first>
  <behavior>
    - Wrapper requires a positional `RUN_ROOT` argument that points to the existing dry-run run root; it refuses to create a fresh root in full mode.
    - Wrapper refuses to run unless `$RUN_ROOT/dry_run_report.json` exists and has `ok=true` and `full_run_allowed=true`.
    - Wrapper uses fixed `/home/samuel/TSC_CYCLE/.venv/bin/python`, sources DGX env, exports `WANDB_PROJECT=tsc-cycle-v3-9b`, and launches via `scripts/dgx_spark/run_safe.sh 100G --`.
    - Wrapper writes only under the provided `runs/v3.0-9B-{utc}` dry-run root and verifies FROZEN evidence before/after.
    - Wrapper exits non-zero if `sft_manifest.json` is missing or `ok` is not true after training.
  </behavior>
  <action>Create executable `scripts/run_v3_phase4_full.sh` with `set -euo pipefail`. The wrapper must require exactly one `RUN_ROOT` positional argument and validate it with `validate_run_root`; full mode must not create a fresh UTC root by default. Preflight-check Phase 3 Arrow files, Phase 1 reports, v1.0 FROZEN marker, and `$RUN_ROOT/dry_run_report.json` green evidence in that same root. Then run `python -m tsc_cycle.student.train --mode full --data-dir data/tokenized/v3 --output-root "$RUN_ROOT" --model Qwen/Qwen3.5-9B`. After completion, validate manifest JSON with project Python and exit fail-closed. Do not add timeout or watchdog kill for full run.</action>
  <verify>
    <automated>bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -x /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh` succeeds.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh | grep -c 'full_run_allowed'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh | grep -c 'run_safe.sh 100G --'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh | grep -c 'tsc-cycle-v3-9b'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh | grep -c 'timeout\|6h\|21600'` returns 0.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh | grep -c 'Usage:.*RUN_ROOT\|RUN_ROOT=.*\$1\|RUN_ROOT="\$1"'` returns at least 1.
  </acceptance_criteria>
  <done>Full-run wrapper is syntax-valid, executable, dry-run-gated, safe-run protected, and capable of launching the full no-6h-cap training after human approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dry-run report -> full-run launch | Green dry-run JSON controls whether expensive full training is allowed. |
| Human checkpoint -> autonomous wrapper | User approves long no-cap GPU run only after automated gates are green. |
| Full trainer -> filesystem | Adapter/checkpoints/manifests must stay inside `runs/v3.0-9B-*`. |
| Wrapper -> DGX runtime | Full training must run under cgroup memory cap and no swap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Tampering | v1.0 artifact root | mitigate | Full wrapper validates FROZEN pre/post and never accepts v1.0 root as output. |
| T-04-20 | Tampering/Repudiation | dry-run gate input | mitigate | Wrapper parses JSON and requires exact `ok=true`/`full_run_allowed=true`; missing report fails. |
| T-04-21 | Integrity | native think leakage | mitigate | Full manifest carries tokenizer/native leakage smoke evidence from trainer callbacks. |
| T-04-22 | Denial of Service | full training | mitigate | Full wrapper uses `run_safe.sh 100G --`; no direct CUDA long run path. |
| T-04-23 | Elevation/Tampering | shell/env injection | mitigate | Fixed project root/PY/module invocation; optional run root is validated before use. |
| T-04-24 | Repudiation | false-green full run | mitigate | Wrapper validates `sft_manifest.json ok=true`, best checkpoint, adapter path, and gate evidence after training. |
</threat_model>

<verification>
Fast checks:
<automated>bash -n /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh && /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py -q</automated>

Long full-run gate after checkpoint approval:
<automated>bash -lc 'RUN_ROOT=$(/home/samuel/TSC_CYCLE/.venv/bin/python -c '''import glob,json,os; c=[]
for m in glob.glob("/home/samuel/TSC_CYCLE/runs/v3.0-9B-*/dry_run_report.json"):
    d=json.load(open(m));
    if d.get("ok") is True and d.get("full_run_allowed") is True: c.append(os.path.dirname(m))
assert c, "no green dry-run RUN_ROOT found"
print(sorted(c)[-1])'''); /home/samuel/TSC_CYCLE/scripts/run_v3_phase4_full.sh "$RUN_ROOT"'</automated>
</verification>

<success_criteria>
- Full wrapper cannot run without a provided dry-run `RUN_ROOT` containing green dry-run evidence.
- Full training uses early stopping patience=3, eval every 200 steps, max epochs=5, no 6h cap, and manifest requires `early_stopping_triggered=true` with `stop_reason="early_stopping"`.
- Final adapter and manifest are under the same `runs/v3.0-9B-{utc}` root that contains the green dry-run evidence.
- Manifest provides enough evidence for Plan 04-05 aggregate report, including early-stopping-triggered convergence evidence.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-04-SUMMARY.md`.
</output>
