---
phase: "04-qlora-sft-9b-batch-1"
plan: "01"
type: "execute"
wave: 0
depends_on: []
files_modified:
  - "tests/test_v3_sft_config.py"
  - "tests/test_v3_sft_arrow_loader.py"
  - "tests/test_v3_sft_dry_run.py"
  - "tests/test_v3_sft_grad_gate.py"
  - "tests/test_v3_sft_frozen.py"
  - "tests/test_v3_sft_artifacts.py"
autonomous: true
requirements:
  - "SFT-01"
  - "SFT-02"
  - "SFT-03"
  - "SFT-04"
  - "SFT-05"
  - "SFT-06"
  - "SFT-07"
  - "SFT-08"
must_haves:
  truths:
    - "Phase 4 implementation cannot silently use v1.0 trainer defaults."
    - "Full training cannot start unless dry-run and grad gates are green."
    - "v1.0 production artifacts are frozen before any Phase 4 write path is accepted."
  artifacts:
    - path: "tests/test_v3_sft_config.py"
      provides: "RED tests for SFT-01/SFT-02/SFT-03/SFT-05/SFT-07 locked config"
    - path: "tests/test_v3_sft_arrow_loader.py"
      provides: "RED tests for Phase 3 Arrow IPC consumption"
    - path: "tests/test_v3_sft_dry_run.py"
      provides: "RED tests for SFT-04 dry-run fail-closed gate"
    - path: "tests/test_v3_sft_grad_gate.py"
      provides: "RED tests for SFT-06 grad_norm/NaN abort gate"
    - path: "tests/test_v3_sft_frozen.py"
      provides: "RED tests for SFT-08 FROZEN guard and output allowlist"
    - path: "tests/test_v3_sft_artifacts.py"
      provides: "RED tests for SFT-07 artifact isolation and final manifest"
  key_links:
    - from: "tests/test_v3_sft_config.py"
      to: "tsc_cycle.student.sft_v3"
      via: "imports locked config helpers"
      pattern: "from tsc_cycle.student.sft_v3 import"
    - from: "tests/test_v3_sft_frozen.py"
      to: "runs/20260507T032419Z"
      via: "FROZEN guard fixture"
      pattern: "20260507T032419Z"
    - from: "tests/test_v3_sft_dry_run.py"
      to: "tsc_cycle.v3_gates.sft_dry_run_v3"
      via: "dry-run report evaluator"
      pattern: "evaluate_dry_run_gate"
---

<objective>
Create the Phase 4 RED test contract before implementation so every locked SFT decision and safety gate is executable and fail-closed.

Purpose: Prevent the existing v1.0 trainer defaults, legacy parquet path, non-isolated run roots, or missing dry-run gates from being accepted silently.
Output: Six pytest files that initially fail against missing or outdated Phase 4 implementation and define the contract for Plans 04-02 through 04-05.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-VALIDATION.md
@/home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/memory_budget_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py

<interfaces>
Expected new contracts downstream tests should import:
```python
# tsc_cycle.student.sft_v3
MODEL_NAME = "Qwen/Qwen3.5-9B"
RUN_ROOT_PREFIX = "v3.0-9B-"
WANDB_PROJECT = "tsc-cycle-v3-9b"
MODEL_COLUMNS = ["input_ids", "attention_mask", "labels"]

def locked_lora_config_kwargs() -> dict: ...
def locked_training_arguments_kwargs(output_dir: str) -> dict: ...
def load_arrow_split(path: str | Path, *, keep_metadata: bool = False): ...
def validate_run_root(path: str | Path) -> Path: ...
def ensure_v1_frozen(root: str | Path = "runs/20260507T032419Z") -> dict: ...
def evaluate_grad_gate(log_rows: list[dict], *, gate_steps: int = 200) -> dict: ...
```
```python
# tsc_cycle.v3_gates.sft_dry_run_v3
def evaluate_dry_run_gate(report: dict) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED locked-config and Arrow loader tests</name>
  <files>tests/test_v3_sft_config.py, tests/test_v3_sft_arrow_loader.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
    - /home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/train.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py
  </read_first>
  <behavior>
    - Test SFT-01 / D-04: `locked_lora_config_kwargs()` returns `r=64`, `lora_alpha=64`, `lora_dropout=0.0`, `target_modules="all-linear"`, `bias="none"`, `task_type="CAUSAL_LM"`.
    - Test SFT-01 / D-04: LoRA coverage contract requires `expected_gated_deltanet_layers=24`, `expected_full_attention_layers=8`, per-layer projection coverage evidence, and fail-closed mismatch behavior in tests and `lora_coverage.json`.
    - Test SFT-02 / D-06: `locked_training_arguments_kwargs()` includes `learning_rate=1e-4`, `lr_scheduler_type="cosine"`, `warmup_ratio>0`, `optim="adamw_torch_fused"`, `max_grad_norm=0.5`.
    - Test SFT-03 / D-05: locked args include `per_device_train_batch_size=1`, `gradient_accumulation_steps=16`, `gradient_checkpointing=True`, `gradient_checkpointing_kwargs={"use_reentrant": False}`, and no packing flag or chat-template preprocessing.
    - Test SFT-05 / D-08: locked args include `num_train_epochs=5`, `eval_strategy="steps"`, `eval_steps=200`, `save_strategy="steps"`, `save_steps=200`, `load_best_model_at_end=True`, `metric_for_best_model="eval_loss"`, `greater_is_better=False`.
    - Test Arrow loader: create a tiny Arrow IPC file with Phase 3 columns and assert `load_arrow_split(..., keep_metadata=False)` keeps only `input_ids`, `attention_mask`, `labels`; assert no legacy `data.parquet` path is required.
  </behavior>
  <action>Create deterministic pytest tests using tmp_path and tiny in-memory Arrow IPC files. Reference decisions D-04, D-05, D-06, D-08 in assertion comments. Do not import GPU-only model loading in these tests; they must remain CPU-fast and fail because the Phase 4 helper contract is not implemented yet.</action>
  <verify>
    <automated>bash -lc 'set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py -q; code=$?; test "$code" -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py | grep -c 'Qwen/Qwen3.5-9B'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py | grep -c 'adamw_torch_fused'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py | grep -c 'expected_gated_deltanet_layers.*24'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_config.py | grep -c 'expected_full_attention_layers.*8'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_arrow_loader.py | grep -c 'data.parquet'` returns at least 1 as a negative assertion.
  </acceptance_criteria>
  <done>RED tests exist, run in under 60 seconds, and fail because `tsc_cycle.student.sft_v3` does not yet satisfy the locked contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED dry-run and grad gate tests</name>
  <files>tests/test_v3_sft_dry_run.py, tests/test_v3_sft_grad_gate.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
    - /home/samuel/TSC_CYCLE/tsc_cycle/constraint_lint.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py
  </read_first>
  <behavior>
    - Test SFT-04 / D-07: dry-run gate fails closed unless `sample_count == 500`, `ood_hard_constraint_pass_rate >= 0.95`, and `full_run_allowed is True` only when all gates pass.
    - Test SFT-06 / D-07: grad gate fails when any loss is NaN/Inf, when `grad_norm_p99 >= 3.0`, or when fewer than 200 steps are available for the gate.
    - Test false-green prevention: adapter/checkpoint existence alone does not make dry-run report `ok=true`.
  </behavior>
  <action>Create pure-function tests for `evaluate_dry_run_gate()` and `evaluate_grad_gate()`. Include synthetic passing and failing report dictionaries. Use threshold boundaries exactly: 0.95 passes, 0.949999 fails; 2.999 passes, 3.0 fails. Reference SFT-04, SFT-06, and D-07 in test names or comments.</action>
  <verify>
    <automated>bash -lc 'set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py -q; code=$?; test "$code" -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py | grep -c '0.95'` returns at least 2.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_grad_gate.py | grep -c '3.0'` returns at least 2.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_dry_run.py | grep -c 'full_run_allowed'` returns at least 1.
  </acceptance_criteria>
  <done>RED tests encode dry-run and gradient fail-closed behavior before any full training path can pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED artifact isolation and FROZEN guard tests</name>
  <files>tests/test_v3_sft_frozen.py, tests/test_v3_sft_artifacts.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/.planning/STATE.md
    - /home/samuel/TSC_CYCLE/.planning/ROADMAP.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
    - /home/samuel/TSC_CYCLE/scripts/dgx_spark/run_safe.sh
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test SFT-08 / D-11: `ensure_v1_frozen()` writes `FROZEN.md`, removes owner/group/other write bits where platform allows, and returns pre/post hash or mtime evidence.
    - Test SFT-07 / D-10: `validate_run_root()` accepts only `runs/v3.0-9B-{utc}` paths and rejects `runs/20260507T032419Z`, `runs/{timestamp}`, absolute paths outside project, and shell metacharacter-containing roots.
    - Test artifact report contract: final manifest includes `requirements_covered=["SFT-01",...,"SFT-08"]`, run root, dry/full report paths, adapter path, Arrow hashes, LoRA coverage path, wandb project, and FROZEN evidence.
  </behavior>
  <action>Create tmp_path-based filesystem tests; do not chmod the real `runs/20260507T032419Z` during RED tests. Assert wrapper/script command strings are fixed and not user-provided shell snippets. Reference D-09, D-10, D-11 in test comments.</action>
  <verify>
    <automated>bash -lc 'set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q; code=$?; test "$code" -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_frozen.py | grep -c '20260507T032419Z'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py | grep -c 'tsc-cycle-v3-9b'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py | grep -c 'SFT-08'` returns at least 1.
  </acceptance_criteria>
  <done>RED tests prove artifact isolation, FROZEN protection, and manifest coverage are mandatory and currently unmet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 artifacts -> trainer tests | Tokenized Arrow paths and hashes must be treated as immutable inputs, not regenerated training data. |
| Test contract -> implementation | RED tests define fail-closed requirements that later production code must satisfy exactly. |
| Filesystem guard -> run outputs | Output allowlist protects `runs/20260507T032419Z/` from Phase 4 writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | v1.0 artifact root | mitigate | Tests require `FROZEN.md`, read-only chmod evidence, and rejection of `runs/20260507T032419Z` as an output root. |
| T-04-02 | Tampering/Repudiation | dataset artifact selection | mitigate | Arrow loader tests require Phase 3 Arrow IPC paths and reject legacy parquet dependence. |
| T-04-03 | Information Disclosure/Integrity | native think leakage | mitigate | Config tests require dynamic Qwen3.5 tokenizer safety contract and no v1.0 native ID constants for v3 checks. |
| T-04-04 | Denial of Service | DGX memory/swap safety | mitigate | Artifact tests require wrappers to use `scripts/dgx_spark/run_safe.sh 100G --` rather than direct trainer execution. |
| T-04-05 | Elevation/Tampering | wrapper shell/env injection | mitigate | FROZEN/artifact tests reject shell metacharacter output roots and require fixed command argv patterns. |
| T-04-06 | Repudiation | false-green training gates | mitigate | Dry-run tests require explicit 500-sample pass rate, grad p99, finite loss, and `full_run_allowed`; adapter existence alone cannot pass. |
</threat_model>

<verification>
Run all Wave 0 RED tests and confirm they fail for missing Phase 4 implementation:

<automated>bash -lc 'set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_*.py -q; code=$?; test "$code" -ne 0'</automated>
</verification>

<success_criteria>
- Six test files exist and encode SFT-01..SFT-08.
- Tests are CPU-fast and do not load Qwen3.5-9B.
- RED status is intentional and documented by failing imports/assertions that Plans 04-02..04-05 will satisfy.
- No real v1.0 artifact chmod or mutation occurs during tests.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-01-SUMMARY.md`.
</output>
