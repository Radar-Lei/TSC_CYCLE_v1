---
phase: "04-qlora-sft-9b-batch-1"
plan: "05"
type: "execute"
wave: 4
depends_on:
  - "04-04"
files_modified:
  - "tsc_cycle/v3_gates/sft_report_v3.py"
  - "tests/test_v3_sft_artifacts.py"
autonomous: true
requirements:
  - "SFT-01"
  - "SFT-02"
  - "SFT-03"
  - "SFT-04"
  - "SFT-05"
  - "SFT-06"
  - "SFT-07"
  - "SFT-08"
must_haves:
  truths:
    - "Phase 4 cannot be marked complete unless every SFT-01..SFT-08 gate is proven green."
    - "The final report includes hashes and paths for all training inputs and outputs."
    - "D-10: Aggregate report verifies Phase 4 run-root isolation under runs/v3.0-9B-{utc} and wandb project tsc-cycle-v3-9b evidence."
    - "The final report proves v1.0 artifacts remained frozen and untouched."
    - "D-11: Aggregate report requires FROZEN.md/read-only evidence for runs/20260507T032419Z and rejects v1.0 output paths."
    - "Phase 5 can locate the best adapter and run root from machine-readable manifest output."
  artifacts:
    - path: "tsc_cycle/v3_gates/sft_report_v3.py"
      provides: "Aggregate Phase 4 report evaluator for SFT-01..SFT-08"
    - path: "tests/test_v3_sft_artifacts.py"
      provides: "Final report contract tests updated for aggregate evaluator"
  key_links:
    - from: "tsc_cycle/v3_gates/sft_report_v3.py"
      to: "runs/v3.0-9B-*/sft_manifest.json"
      via: "manifest load"
      pattern: "sft_manifest.json"
    - from: "tsc_cycle/v3_gates/sft_report_v3.py"
      to: "data/splits/v3/manifest.json"
      via: "input artifact hash verification"
      pattern: "manifest.json"
    - from: "tsc_cycle/v3_gates/sft_report_v3.py"
      to: "runs/20260507T032419Z/FROZEN.md"
      via: "FROZEN evidence gate"
      pattern: "FROZEN.md"
---

<objective>
Create the final Phase 4 aggregate report gate that proves SFT-01 through SFT-08 are satisfied and exposes adapter artifacts for Phase 5.

Purpose: Prevent completing Phase 4 from ad-hoc logs or adapter existence; completion requires machine-readable evidence for config locks, dry-run/full-run gates, run isolation, hashes, and FROZEN v1.0 protection.
Output: `tsc_cycle/v3_gates/sft_report_v3.py` and updated artifact tests validating the aggregate report contract.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
@/home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
@/home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py
@/home/samuel/TSC_CYCLE/data/splits/v3/manifest.json
@/home/samuel/TSC_CYCLE/data/splits/v3/rebuild_report.json

<interfaces>
Aggregate report CLI contract:
```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.sft_report_v3 \
  --run-dir runs/v3.0-9B-{utc} \
  --out runs/v3.0-9B-{utc}/phase4_sft_report.json
```
Report shape:
```json
{
  "ok": true,
  "next_phase_allowed": true,
  "requirements_covered": ["SFT-01", "SFT-02", "SFT-03", "SFT-04", "SFT-05", "SFT-06", "SFT-07", "SFT-08"],
  "gates": {},
  "fatal_failures": [],
  "artifact_manifest": {"paths": {}, "sha256": {}}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend artifact tests for aggregate Phase 4 report</name>
  <files>tests/test_v3_sft_artifacts.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
    - /home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-VALIDATION.md
    - /home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test aggregate report fails if any SFT ID is missing from `requirements_covered`.
    - Test aggregate report fails if dry-run report is absent or not `ok=true/full_run_allowed=true`.
    - Test aggregate report fails if full `sft_manifest.json` is absent, not `ok=true`, lacks adapter path, lacks best checkpoint evidence, lacks `early_stopping_triggered=true`, lacks `stop_reason="early_stopping"`, or points outside `runs/v3.0-9B-*`.
    - Test aggregate report includes artifact hashes for train/val/ood Arrow inputs, dry/full reports, adapter files, LoRA coverage report, and FROZEN marker evidence.
    - Test SFT-01 coverage report contract includes `expected_gated_deltanet_layers=24`, `expected_full_attention_layers=8`, and projection coverage evidence; mismatch fails closed.
  </behavior>
  <action>Update `tests/test_v3_sft_artifacts.py` to import `evaluate_gates` or equivalent from `tsc_cycle.v3_gates.sft_report_v3`. Use tmp_path fixtures that create minimal JSON reports and fake adapter files. Keep tests CPU-fast and deterministic. Include explicit assertions for `next_phase_allowed`, `requirements_covered`, `fatal_failures`, `artifact_manifest.paths`, and `artifact_manifest.sha256`.</action>
  <verify>
    <automated>bash -lc 'set +e; /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q; code=$?; test "$code" -ne 0 || test -f /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py | grep -c 'next_phase_allowed'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py | grep -c 'SFT-08'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py | grep -c 'artifact_manifest'` returns at least 1.
  </acceptance_criteria>
  <done>Artifact tests describe the final aggregate report and fail until the evaluator is implemented or pass if it already satisfies the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement aggregate SFT report evaluator and CLI</name>
  <files>tsc_cycle/v3_gates/sft_report_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase1_report.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/dataset_rebuild_v3.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/student/sft_v3.py
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_dry_run_v3.py
    - /home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - SFT-01 gate: verifies manifest/coverage records `r=64`, `alpha=64`, `dropout=0.0`, `target_modules="all-linear"`, `expected_gated_deltanet_layers=24`, `expected_full_attention_layers=8`, observed layer counts, and projection coverage evidence for GatedDeltaNet/full-attention layers.
    - SFT-02 gate: verifies lr, cosine warmup, `max_grad_norm=0.5`, and `optim="adamw_torch_fused"` evidence.
    - SFT-03 gate: verifies batch=1, grad_accum=16, packing false/no packing evidence, and non-reentrant gradient checkpointing.
    - SFT-04 gate: verifies dry-run report `ok=true`, `full_run_allowed=true`, `sample_count=500`, pass rate >=0.95.
    - SFT-05 gate: verifies full manifest `ok=true`, max epochs=5, eval steps=200, patience=3, `early_stopping_triggered=true`, `stop_reason="early_stopping"`, best checkpoint evidence, and no wall-clock cap field.
    - SFT-06 gate: verifies grad gate success with p99<3.0 and finite losses.
    - SFT-07 gate: verifies run root prefix and wandb project `tsc-cycle-v3-9b`.
    - SFT-08 gate: verifies FROZEN marker/evidence for `runs/20260507T032419Z` and no v1 output paths.
  </behavior>
  <action>Create `sft_report_v3.py` using existing v3 gate patterns: `_load_json`, `_add_result`, `_sha256_file`, `evaluate_gates(run_dir, phase3_manifest=..., out=...)`, and `main()`. Read `sft_manifest.json`, `dry_run_report.json`, LoRA coverage report, Phase 3 manifest, and FROZEN marker evidence. Fail closed unless the manifest proves early stopping actually triggered convergence (`early_stopping_triggered=true` and `stop_reason="early_stopping"`) and the LoRA coverage report proves expected 24 GatedDeltaNet + 8 full-attention layer/projection coverage. Build `artifact_manifest` with paths and SHA-256 hashes for all existing evidence files. `ok` and `next_phase_allowed` must be true only when no fatal failures exist. Print and write JSON; exit 0 only if `ok=true`.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'SFT-01'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'next_phase_allowed'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'sha256'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c '20260507T032419Z'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'expected_gated_deltanet_layers.*24'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'expected_full_attention_layers.*8'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'early_stopping_triggered'` returns at least 1.
    - `grep -v '^#' /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py | grep -c 'stop_reason.*early_stopping'` returns at least 1.
  </acceptance_criteria>
  <done>Aggregate evaluator passes tests and fails closed on missing or inconsistent Phase 4 evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Generate final Phase 4 report for completed run</name>
  <files>tsc_cycle/v3_gates/sft_report_v3.py</files>
  <read_first>
    - /home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/sft_report_v3.py
    - /home/samuel/TSC_CYCLE/runs/v3.0-9B-*/sft_manifest.json
    - /home/samuel/TSC_CYCLE/data/splits/v3/manifest.json
    - /home/samuel/TSC_CYCLE/data/splits/v3/rebuild_report.json
  </read_first>
  <action>After Plan 04-04 full run has produced a run root, execute the aggregate report CLI against the actual `runs/v3.0-9B-{utc}` directory. Resolve the latest valid run root by scanning `runs/v3.0-9B-*/sft_manifest.json` and accepting only roots whose `dry_run_report.json` is `ok=true/full_run_allowed=true`, whose manifest is `ok=true`, whose manifest has `early_stopping_triggered=true` and `stop_reason="early_stopping"`, and whose adapter path exists. Otherwise fail closed and require the executor to provide the correct run root. Write `phase4_sft_report.json` under that run root.</action>
  <verify>
    <automated>RUN_DIR="$(/home/samuel/TSC_CYCLE/.venv/bin/python - <<'PY'
import glob
import json
import os
valid = []
for manifest in glob.glob("/home/samuel/TSC_CYCLE/runs/v3.0-9B-*/sft_manifest.json"):
    run = os.path.dirname(manifest)
    dry = os.path.join(run, "dry_run_report.json")
    if not os.path.exists(dry):
        continue
    m = json.load(open(manifest))
    d = json.load(open(dry))
    adapter = m.get("adapter_path") or os.path.join(run, "adapter")
    if d.get("ok") is True and d.get("full_run_allowed") is True and m.get("ok") is True and m.get("early_stopping_triggered") is True and m.get("stop_reason") == "early_stopping" and os.path.exists(adapter):
        valid.append(run)
assert valid, "no valid Phase 4 full run root found"
print(sorted(valid)[-1])
PY
)" && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.sft_report_v3 --run-dir "$RUN_DIR" --out "$RUN_DIR/phase4_sft_report.json"</automated>
  </verify>
  <acceptance_criteria>
    - The discovered `$RUN_DIR/phase4_sft_report.json` exists.
    - The report JSON has `ok=true`, `next_phase_allowed=true`, empty `fatal_failures`, and all `SFT-01`..`SFT-08` in `requirements_covered`.
    - The report JSON includes `adapter_path` or an artifact path pointing to `$RUN_DIR/adapter`.
    - The report JSON proves `early_stopping_triggered=true`, `stop_reason="early_stopping"`, `expected_gated_deltanet_layers=24`, and `expected_full_attention_layers=8`.
  </acceptance_criteria>
  <done>Phase 4 aggregate report is green and provides Phase 5 with the adapter/run-root handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Run artifacts -> aggregate report | JSON/manifests from training become final phase-completion evidence and must be validated. |
| Phase 4 -> Phase 5 handoff | Adapter path and hashes will be consumed by export/merge phase. |
| Filesystem -> report hashes | Existing artifacts are hashed to prevent repudiation or silent drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Tampering | v1.0 production artifact | mitigate | Aggregate report requires FROZEN marker/evidence and rejects any output path under v1.0 root. |
| T-04-26 | Tampering/Repudiation | dataset artifacts | mitigate | Report hashes Phase 3 Arrow/manifest inputs and checks against expected paths. |
| T-04-27 | Integrity | native think leakage | mitigate | Report includes tokenizer/native leakage evidence from dry/full manifests and fails if leakage is recorded. |
| T-04-28 | Denial of Service | DGX run evidence | mitigate | Report requires `run_safe_required`/wrapper evidence from dry/full manifests. |
| T-04-29 | Elevation/Tampering | wrapper shell/env injection | mitigate | Report records fixed wrapper paths and rejects suspicious run roots outside allowlist. |
| T-04-30 | Repudiation | false-green final phase | mitigate | `ok=true` requires all SFT-01..08 gates; adapter existence alone is insufficient. |
</threat_model>

<verification>
Fast checks:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest /home/samuel/TSC_CYCLE/tests/test_v3_sft_artifacts.py -q</automated>

Actual report generation after full run:
<automated>RUN_DIR="$(/home/samuel/TSC_CYCLE/.venv/bin/python - <<'PY'
import glob
import json
import os
valid = []
for manifest in glob.glob("/home/samuel/TSC_CYCLE/runs/v3.0-9B-*/sft_manifest.json"):
    run = os.path.dirname(manifest)
    dry = os.path.join(run, "dry_run_report.json")
    if not os.path.exists(dry):
        continue
    m = json.load(open(manifest))
    d = json.load(open(dry))
    adapter = m.get("adapter_path") or os.path.join(run, "adapter")
    if d.get("ok") is True and d.get("full_run_allowed") is True and m.get("ok") is True and m.get("early_stopping_triggered") is True and m.get("stop_reason") == "early_stopping" and os.path.exists(adapter):
        valid.append(run)
assert valid, "no valid Phase 4 full run root found"
print(sorted(valid)[-1])
PY
)" && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v3_gates.sft_report_v3 --run-dir "$RUN_DIR" --out "$RUN_DIR/phase4_sft_report.json"</automated>
</verification>

<success_criteria>
- `phase4_sft_report.json` has `ok=true`, `next_phase_allowed=true`, empty `fatal_failures`, complete SFT-01..SFT-08 coverage, exact LoRA layer/projection coverage evidence, and early-stopping-triggered convergence evidence.
- Report includes adapter path and hashes needed by Phase 5.
- Report proves v1.0 frozen path was protected and Phase 4 outputs are isolated.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/04-qlora-sft-9b-batch-1/04-05-SUMMARY.md`.
</output>
