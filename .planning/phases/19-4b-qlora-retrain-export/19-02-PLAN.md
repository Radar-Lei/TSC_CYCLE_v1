---
phase: 19-4b-qlora-retrain-export
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - tsc_cycle/v4_gates/phase19_export.py
  - tsc_cycle/student/export_gguf.py
  - scripts/run_v4_phase19_export.sh
  - tests/test_v4_phase19_training_export.py
autonomous: true
requirements:
  - TRAIN-02
must_haves:
  truths:
    - "Maintainer can export a v4.2 trained adapter to merged HF, GGUF fp16, and GGUF q4_K_M artifacts under a v4.2 run root."
    - "Export reports record reproducible paths, sha256 hashes, llama.cpp command evidence, Phase 19 training handoff evidence, and TRAIN-02 coverage."
    - "Export launch/report contracts reject v4.0, frozen v1, broad, or unsupported runtime paths before artifacts are trusted."
  artifacts:
    - path: "tsc_cycle/v4_gates/phase19_export.py"
      provides: "Phase 19 v4.2 export planning, training handoff validation, output path safety, artifact hash/report gate, and CLI"
      exports: ["Phase19ExportConfig", "build_export_plan", "validate_phase19_export_report", "build_parser"]
    - path: "tsc_cycle/student/export_gguf.py"
      provides: "Existing heavy adapter merge/GGUF implementation with v4.2-compatible defaults or arguments"
    - path: "scripts/run_v4_phase19_export.sh"
      provides: "DGX Spark-safe v4.2 merge/GGUF export launcher"
    - path: "tests/test_v4_phase19_training_export.py"
      provides: "Phase 19 export plan/report/wrapper/path safety contract tests"
      contains: "test_v42_export_plan_and_report_require_merged_hf_and_gguf_hashes"
    - path: "runs/v4.2-4B-*/phase19_export_report.json"
      provides: "Completed v4.2 merged HF and GGUF export evidence for TRAIN-02 after the long-running export task"
  key_links:
    - from: "tsc_cycle/v4_gates/phase19_export.py"
      to: "runs/v4.2-4B-*/phase19_sft_report.json"
      via: "Phase 19 training handoff validation before export plan/report acceptance"
      pattern: "validate_phase19_training_handoff"
    - from: "tsc_cycle/v4_gates/phase19_export.py"
      to: "tsc_cycle/v4_gates/phase10_export.py"
      via: "reuse Phase 10 export safety/report semantics with v4.2 defaults"
      pattern: "ExportPlan"
    - from: "scripts/run_v4_phase19_export.sh"
      to: "tsc_cycle/student/export_gguf.py"
      via: "DGX-safe heavy merge and GGUF conversion command"
      pattern: "python -m tsc_cycle.student.export_gguf"
---

<objective>
Create the Phase 19 v4.2 export planning and report contracts.

Purpose: Reuse the existing adapter merge and llama.cpp GGUF conversion path for v4.2, while adding v4.2-specific export defaults, handoff validation, path safety, wrapper commands, artifact hashes, and reports needed for TRAIN-02.

Full merge/GGUF export may be long-running. This plan must make lightweight export plan/report contracts deterministic and testable before any heavy export is attempted.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-4b-qlora-retrain-export/19-CONTEXT.md
@.planning/phases/19-4b-qlora-retrain-export/19-RESEARCH.md
@.planning/phases/19-4b-qlora-retrain-export/19-VALIDATION.md
@.planning/phases/19-4b-qlora-retrain-export/19-PATTERNS.md
@.planning/phases/19-4b-qlora-retrain-export/19-01-PLAN.md
@tsc_cycle/v4_gates/phase10_export.py
@tsc_cycle/v4_gates/phase10_report.py
@tsc_cycle/student/export_gguf.py
@scripts/run_v4_phase10_export.sh
@tests/test_v4_phase10_gguf_contracts.py
@tests/test_v4_phase19_training_export.py

<interfaces>
Use existing merge/GGUF implementation and llama.cpp command discovery semantics; do not introduce a new export framework.
Default run roots and output paths must use `runs/v4.2-4B-*` and Phase 19 report names.
Export report must include merged HF, GGUF fp16, GGUF q4_K_M, tokenizer/materializer evidence when available, sha256 hashes, and command evidence.
Do not alter v4.0 Phase 10 defaults, reports, wrappers, or tests.
Do not use vLLM or add package installs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <id>19-02-01</id>
  <name>Task 1: Add v4.2 export plan and artifact report gate</name>
  <read_first>
    @tsc_cycle/v4_gates/phase10_export.py plan/report/path safety implementation
    @tsc_cycle/v4_gates/phase10_report.py aggregate report expectations
    @.planning/phases/19-4b-qlora-retrain-export/19-PATTERNS.md Export planning section
    @.planning/phases/19-4b-qlora-retrain-export/19-VALIDATION.md task 19-02-01 row
  </read_first>
  <files>tsc_cycle/v4_gates/phase19_export.py, tests/test_v4_phase19_training_export.py</files>
  <behavior>
    - Export plan validates a green v4.2 Phase 19 training report before accepting adapter/run-root inputs.
    - Planned outputs include merged HF directory, fp16 GGUF path, q4_K_M GGUF path, export report path, and command evidence under the same v4.2 run root.
    - Export report gate requires sha256 hashes for merged HF safetensors/material files, fp16 GGUF, and q4_K_M GGUF.
    - TRAIN-02 coverage is explicit in plan/report outputs.
  </behavior>
  <action>Add RED tests for v4.2 export plan/report acceptance and rejection. Implement `tsc_cycle/v4_gates/phase19_export.py` by reusing Phase 10 export semantics with v4.2 defaults and Phase 19 training report handoff validation. Include path normalization, hash helpers, report validation, and a CLI for planning/report validation.</action>
  <acceptance_criteria>
    - Valid fixture report records merged HF, fp16 GGUF, q4_K_M GGUF paths and hashes under `runs/v4.2-4B-*`.
    - Validator rejects wrong/missing training handoff, missing artifact hashes, v4.0/frozen roots, output paths outside run root, and missing TRAIN-02 coverage.
    - Plan/report command evidence names llama.cpp convert/quantize steps without executing them in tests.
    - Existing Phase 10 export tests continue to pass.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py::test_v42_export_plan_and_report_require_merged_hf_and_gguf_hashes -q</automated>
  </verify>
  <done>v4.2 export artifacts have reproducible plan/report contracts with merged HF and GGUF hash evidence.</done>
</task>

<task type="auto" tdd="true">
  <id>19-02-02</id>
  <name>Task 2: Wire DGX-safe v4.2 export wrapper and heavy export defaults</name>
  <read_first>
    @tsc_cycle/student/export_gguf.py existing heavy merge/GGUF argument handling
    @scripts/run_v4_phase10_export.sh wrapper command pattern
    @tsc_cycle/v4_gates/phase10_export.py unsafe path and command evidence guards
    @.planning/phases/19-4b-qlora-retrain-export/19-VALIDATION.md task 19-02-02 row
  </read_first>
  <files>tsc_cycle/student/export_gguf.py, scripts/run_v4_phase19_export.sh, tests/test_v4_phase19_training_export.py</files>
  <behavior>
    - Wrapper invokes existing export implementation with v4.2 run root/report defaults and Phase 19 training report input.
    - Export command path is DGX-safe and does not reference unsupported runtimes, dependency installs, worktrees, v4.0 outputs, or frozen roots.
    - `export_gguf.py` accepts v4.2 arguments/defaults where needed without breaking Phase 10/v4.0 behavior.
    - Lightweight tests inspect command/config/report contracts only; they do not run model merge or llama.cpp conversion.
  </behavior>
  <action>Add `scripts/run_v4_phase19_export.sh` using the existing DGX-safe launch convention and v4.2 report/run-root arguments. If `export_gguf.py` currently hard-codes v4.0 defaults, extend its parser/default selection with an explicit v4.2 phase/mode or arguments while preserving v4.0 defaults. Add tests that inspect wrapper text, parser defaults, and forbidden command substrings.</action>
  <acceptance_criteria>
    - Wrapper includes `scripts/dgx_spark/run_safe.sh` and `python -m tsc_cycle.student.export_gguf` with v4.2 run/report paths.
    - Wrapper and generated command evidence contain no `pip install`, `vllm`, `flash-attn`, `unsloth`, `axolotl`, `git worktree`, `runs/20260507T032419Z`, or v4.0 output root.
    - Parser/default changes are backwards compatible for existing Phase 10 tests.
    - Export path guards reject broad repo/data/artifact roots and outputs outside the v4.2 run root.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py::test_v42_wrappers_forbid_dependency_installs_unsupported_runtimes_and_frozen_roots -q</automated>
  </verify>
  <done>v4.2 export can be launched through the existing safe wrapper path and rejects unsupported command/root patterns.</done>
</task>

<task type="auto" tdd="false">
  <id>19-02-03</id>
  <name>Task 3: Run v4.2 merge and GGUF export to completion</name>
  <read_first>
    @tsc_cycle/v4_gates/phase19_export.py plan/report validation sections
    @tsc_cycle/student/export_gguf.py heavy merge and GGUF conversion implementation
    @scripts/run_v4_phase19_export.sh
    @.planning/phases/19-4b-qlora-retrain-export/19-01-SUMMARY.md
  </read_first>
  <files>runs/v4.2-4B-*</files>
  <behavior>
    - Export starts only after a validated Phase 19 training report and adapter exist.
    - The real export produces merged HF, GGUF fp16, and GGUF q4_K_M artifacts under the v4.2 run root.
    - Artifact hashes and command evidence are written to the Phase 19 export report.
    - If llama.cpp/tool/runtime failure blocks completion, preserve logs and mark Phase 19 blocked rather than treating export contracts as sufficient.
  </behavior>
  <action>After Plan 01 produces a valid training report, generate the v4.2 export plan, launch `scripts/run_v4_phase19_export.sh`, and monitor until merged HF plus both GGUF artifacts are produced or a concrete blocker appears. Validate the export report with `phase19_export.py`; record merged HF path/hash, fp16 GGUF path/hash, q4_K_M path/hash, commands, and TRAIN-02 coverage in the plan summary.</action>
  <acceptance_criteria>
    - A real v4.2 run root contains merged HF artifacts, fp16 GGUF, q4_K_M GGUF, export logs, and `phase19_export_report.json`.
    - All exported artifacts have sha256 hashes recorded in the export report.
    - The Phase 19 export report validator accepts the produced report.
    - TRAIN-02 is not marked complete unless real export artifact/report evidence exists or an explicit blocker report is written.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py tests/test_v4_phase10_gguf_contracts.py -q</automated>
    <manual>Launch `scripts/run_v4_phase19_export.sh` against the validated v4.2 run root, then validate `runs/v4.2-4B-*/phase19_export_report.json` and inspect recorded hashes for merged HF, fp16 GGUF, and q4_K_M GGUF.</manual>
  </verify>
  <done>Phase 19 has real merged HF and GGUF export artifacts with accepted TRAIN-02 report evidence, or a concrete runtime blocker is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Training report -> export plan | Export must only trust a v4.2 TRAIN-01-complete report, not v4.0/Phase 9 evidence. |
| Export plan -> filesystem writes | Merged HF and GGUF outputs must remain under the intended v4.2 run root. |
| llama.cpp command evidence -> artifact report | Report must preserve exact convert/quantize command evidence and produced artifact hashes. |
| Wrapper args -> heavy export runtime | Wrapper must not install dependencies, use unsupported runtimes, or point at frozen/v4.0 roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-19-04 | Repudiation | Export artifact report | mitigate | Require merged HF, fp16 GGUF, q4_K_M GGUF paths/hashes and TRAIN-02 coverage. |
| T-19-05 | Tampering | Export wrapper and roots | mitigate | Test wrapper/path guards reject dependency installs, unsupported runtimes, worktrees, v4.0 roots, frozen roots, and outputs outside run root. |
| T-19-SC | Supply Chain | Export stack | mitigate | Reuse existing export implementation and llama.cpp tooling; do not add dependencies or frameworks. |
</threat_model>

<verification>
Run quick Phase 19 tests after each task:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py -q</automated>

Run adjacent regression after Plan 02:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py tests/test_v4_phase18_calibrated_dataset_rebuild.py tests/test_v4_phase9_sft_contracts.py tests/test_v4_phase10_gguf_contracts.py -q</automated>
</verification>

<success_criteria>
- TRAIN-02 is satisfied by real v4.2 merged HF, GGUF fp16, and GGUF q4_K_M artifacts with accepted export report hashes.
- Export evidence is rooted in a completed v4.2 Phase 19 training handoff, not Phase 9/v4.0 artifacts.
- v4.2 export launch paths are reproducible, DGX-safe, and isolated from frozen/v4.0 roots.
- Existing v4.0 Phase 10 export contracts continue to pass.
</success_criteria>

<output>
Create `.planning/phases/19-4b-qlora-retrain-export/19-02-SUMMARY.md` when done.
</output>
