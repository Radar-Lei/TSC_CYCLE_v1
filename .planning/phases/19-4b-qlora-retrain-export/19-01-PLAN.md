---
phase: 19-4b-qlora-retrain-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/student/sft_v42.py
  - tsc_cycle/v4_gates/phase19_training.py
  - tsc_cycle/student/train.py
  - scripts/run_v4_phase19_train.sh
  - tests/test_v4_phase19_training_export.py
autonomous: true
requirements:
  - TRAIN-01
must_haves:
  truths:
    - "Maintainer can train v4.2 from Phase 18 calibrated data using Qwen/Qwen3-4B-Thinking-2507 and the existing DGX Spark-safe QLoRA stack."
    - "Phase 18 calibrated JSONL and preserved split indexes are tokenized into train/val/ood_val Arrow artifacts without prompt/protocol or native-think drift."
    - "v4.2 training reports prove adapter, model, run root, data manifest, Phase 18 hashes, and QLoRA settings before export handoff."
  artifacts:
    - path: "tsc_cycle/student/sft_v42.py"
      provides: "Lightweight v4.2 QLoRA constants, dataset defaults, run-root validation, tokenizer/data collator helpers, and training argument builder"
      exports: ["MODEL_NAME", "RUN_ROOT_PREFIX", "TOKENIZED_DIR", "build_training_arguments", "validate_run_root"]
    - path: "tsc_cycle/v4_gates/phase19_training.py"
      provides: "Phase 19 training handoff, Phase 18 report validation, tokenized split writer, training report gate, and CLI"
      exports: ["Phase19TrainingConfig", "tokenize_phase18_handoff", "validate_phase19_training_report", "build_parser"]
    - path: "tsc_cycle/student/train.py"
      provides: "Existing heavy QLoRA entrypoint with a v4_2 phase branch using Phase 19/v4.2 defaults"
    - path: "scripts/run_v4_phase19_train.sh"
      provides: "DGX Spark-safe v4.2 training launcher through scripts/dgx_spark/run_safe.sh"
    - path: "tests/test_v4_phase19_training_export.py"
      provides: "Phase 19 v4.2 training handoff, tokenization, report, wrapper, and path safety contract tests"
      contains: "test_v42_training_defaults_lock_phase18_data_and_qwen4b_stack"
    - path: "runs/v4.2-4B-*/phase19_sft_report.json"
      provides: "Completed v4.2 QLoRA training evidence for TRAIN-01 after the long-running training task"
  key_links:
    - from: "tsc_cycle/v4_gates/phase19_training.py"
      to: "data/v4_2/phase18/labeled_calibrated.jsonl"
      via: "Phase 18 calibrated source dataset read and tokenization"
      pattern: "tokenize_phase18_handoff"
    - from: "tsc_cycle/v4_gates/phase19_training.py"
      to: "data/v4_2/phase18/splits/*.index.jsonl"
      via: "preserved split membership controls Arrow split outputs"
      pattern: "_load_phase18_split_indexes"
    - from: "tsc_cycle/v4_gates/phase19_training.py"
      to: "artifacts/v4_2/phase18/reconstruction_report.json"
      via: "green Phase 18 report and hash validation before tokenization/training"
      pattern: "validate_phase18_handoff"
    - from: "tsc_cycle/student/train.py"
      to: "tsc_cycle/student/sft_v42.py"
      via: "--phase v4_2 branch imports v4.2 constants and reuses the existing QLoRA trainer flow"
      pattern: "args.phase == \"v4_2\""
---

<objective>
Create the Phase 19 v4.2 training handoff and report contracts.

Purpose: Make Phase 18 calibrated data launchable for the existing Qwen3-4B QLoRA training path by adding v4.2-specific constants, tokenized split handoff artifacts, a DGX Spark-safe training wrapper, and a report gate that proves TRAIN-01 evidence without changing v4.0 behavior.

Full model training may be long-running. This plan must make lightweight launch/report contracts deterministic and testable before any heavy run is attempted.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-4b-qlora-retrain-export/19-CONTEXT.md
@.planning/phases/19-4b-qlora-retrain-export/19-RESEARCH.md
@.planning/phases/19-4b-qlora-retrain-export/19-VALIDATION.md
@.planning/phases/19-4b-qlora-retrain-export/19-PATTERNS.md
@.planning/phases/18-calibrated-dataset-rebuild/18-VERIFICATION.md
@tsc_cycle/student/sft_v4.py
@tsc_cycle/student/train.py
@tsc_cycle/v4_gates/phase9_report.py
@tsc_cycle/v4_gates/dataset_rebuild.py
@tsc_cycle/v4_gates/calibrated_dataset_rebuild.py
@tsc_cycle/prompt_builder.py
@tests/test_v4_phase9_sft_contracts.py

<interfaces>
Use `Qwen/Qwen3-4B-Thinking-2507`, LoRA r=64/alpha=64/dropout=0, NF4, bf16, SDPA, gradient checkpointing, packing false, and no chat template as in the existing v4 QLoRA stack.
Do not alter v4.0 Phase 9 defaults, reports, or wrapper behavior.
Do not import torch, transformers, peft, datasets, bitsandbytes, or heavy training dependencies at test collection time.
Do not introduce vLLM, flash-attn, Unsloth, Axolotl, package installs, or new training frameworks.
Default outputs must be isolated under `data/v4_2/phase18/tokenized`, `artifacts/v4_2/phase19`, and `runs/v4.2-4B-*`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <id>19-01-01</id>
  <name>Task 1: Add v4.2 training constants and run-root guards</name>
  <read_first>
    @tsc_cycle/student/sft_v4.py constants, run-root validation, model/tokenizer config, LoRA config, training arguments
    @tsc_cycle/student/train.py parser and --phase v4 branch
    @tests/test_v4_phase9_sft_contracts.py defaults and no-heavy-import patterns
    @.planning/phases/19-4b-qlora-retrain-export/19-PATTERNS.md Training config lock and Path Safety sections
  </read_first>
  <files>tsc_cycle/student/sft_v42.py, tsc_cycle/student/train.py, tests/test_v4_phase19_training_export.py</files>
  <behavior>
    - v4.2 constants point to Phase 18 calibrated/tokenized data, Phase 18 reconstruction report, Phase 19 artifact dir, and `v4.2-4B-` run roots.
    - Model, LoRA, quantization, precision, and trainer defaults match the existing v4 QLoRA stack.
    - `tsc_cycle.student.train` accepts `--phase v4_2` without changing the existing `--phase v4` path.
    - Run-root guards reject frozen v1, v4.0, broad repo/data/artifact roots, and shell metacharacters.
  </behavior>
  <action>Add RED tests for v4.2 constants/defaults and parser phase support. Create `tsc_cycle/student/sft_v42.py` by reusing the semantics of `sft_v4.py` with v4.2 paths and run-prefix guards. Update `tsc_cycle/student/train.py` parser choices and phase dispatch so `--phase v4_2` loads v4.2 constants lazily and follows the same heavy training flow as `--phase v4`.</action>
  <acceptance_criteria>
    - `MODEL_NAME == "Qwen/Qwen3-4B-Thinking-2507"` and QLoRA parameters match Phase 9.
    - Defaults reference `data/v4_2/phase18/tokenized`, `artifacts/v4_2/phase18/reconstruction_report.json`, `artifacts/v4_2/phase19`, and `runs/v4.2-4B-*`.
    - `--phase v4_2` is a valid parser choice and does not import heavy libraries during lightweight tests.
    - `requirements_covered` includes `TRAIN-01` where applicable.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py::test_v42_training_defaults_lock_phase18_data_and_qwen4b_stack -q</automated>
  </verify>
  <done>v4.2 training defaults are locked to Phase 18 data, Qwen3-4B, existing QLoRA settings, and safe run roots.</done>
</task>

<task type="auto" tdd="true">
  <id>19-01-02</id>
  <name>Task 2: Tokenize Phase 18 handoff splits</name>
  <read_first>
    @tsc_cycle/v4_gates/dataset_rebuild.py tokenization, prompt/assistant hash, native-think checks, Arrow writing
    @tsc_cycle/v4_gates/calibrated_dataset_rebuild.py Phase 18 report and split index schema
    @tsc_cycle/prompt_builder.py build_user_prompt and build_full_assistant
    @tests/test_v4_phase8_dataset_rebuild.py tokenized artifact assertions
    @.planning/phases/19-4b-qlora-retrain-export/19-RESEARCH.md Upstream Phase 18 Inputs and Validation Strategy sections
  </read_first>
  <files>tsc_cycle/v4_gates/phase19_training.py, tests/test_v4_phase19_training_export.py</files>
  <behavior>
    - Phase 18 reconstruction report must be green and hash-match the calibrated JSONL before tokenization.
    - Split membership comes from Phase 18 `train/val/ood_val.index.jsonl`; no re-randomization is allowed.
    - Tokenized rows include sample_id/input_ids/attention_mask/labels/raw_length/truncated/prompt_hash/assistant_hash.
    - Prompt and assistant rendering uses unchanged v4 protocol and rejects native `<think>` token leakage before truncation.
  </behavior>
  <action>Add `tsc_cycle/v4_gates/phase19_training.py` with `Phase19TrainingConfig`, Phase 18 handoff validation, split-index loading, deterministic prompt/assistant hashing, tokenizer-driven Arrow split writing, tokenized manifest writing, and CLI wiring. Tests should use small fake tokenizer/dataset fixtures where possible and must not require downloading the real model.</action>
  <acceptance_criteria>
    - The handoff gate fails if Phase 18 report is not ok, dataset hash differs, split counts differ, or protocol hashes drift.
    - `train.arrow`, `val.arrow`, and `ood_val.arrow` are written under the configured tokenized dir with matching manifest counts/hashes.
    - The tokenization report records Phase 18 dataset/report/split hashes and `requirements_covered: ["TRAIN-01"]`.
    - No test imports the heavy training stack at collection time.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py::test_phase18_handoff_tokenizes_calibrated_splits_with_protocol_hashes -q</automated>
  </verify>
  <done>Phase 18 calibrated splits are tokenized into v4.2 Arrow handoff artifacts with protocol/hash evidence.</done>
</task>

<task type="auto" tdd="true">
  <id>19-01-03</id>
  <name>Task 3: Gate v4.2 training report and safe launcher</name>
  <read_first>
    @tsc_cycle/v4_gates/phase9_report.py final training report shape and adapter/data gates
    @scripts/run_v4_phase9_train.sh DGX Spark-safe wrapper pattern
    @tsc_cycle/student/train.py report writing and adapter save flow
    @.planning/phases/19-4b-qlora-retrain-export/19-VALIDATION.md task 19-01-03 row
  </read_first>
  <files>tsc_cycle/v4_gates/phase19_training.py, tsc_cycle/student/train.py, scripts/run_v4_phase19_train.sh, tests/test_v4_phase19_training_export.py</files>
  <behavior>
    - Training report gate validates v4.2 run root, base model, adapter path/hash, tokenizer/data manifest, Phase 18 hashes, loss curve, duration, VRAM, and QLoRA settings.
    - The heavy training branch writes v4.2 report names and data manifest names, not Phase 8/v4.0 names.
    - The wrapper launches through `scripts/dgx_spark/run_safe.sh 100G --` and does not install dependencies or use unsupported runtimes.
    - Unsafe/frozen/v4.0 roots are rejected before report acceptance or launch command emission.
  </behavior>
  <action>Extend `phase19_training.py` with a v4.2 training report validator and CLI subcommands for tokenization/report validation if useful. Update the `--phase v4_2` branch in `train.py` to call the same QLoRA primitives as v4 while writing Phase 19 report/data-manifest names and Phase 18 evidence. Add `scripts/run_v4_phase19_train.sh` using the existing DGX-safe wrapper. Add tests for report acceptance/rejection and wrapper command text.</action>
  <acceptance_criteria>
    - Report validator rejects missing adapter hashes, wrong base model, v4.0/frozen run roots, missing Phase 18 hash evidence, or missing TRAIN-01 coverage.
    - Valid fixture report accepts adapter/model/run-root/data-manifest/Phase 18 evidence under `runs/v4.2-4B-*`.
    - Wrapper contains `scripts/dgx_spark/run_safe.sh 100G --` and `tsc_cycle.student.train --phase v4_2`.
    - Wrapper contains no `pip install`, `vllm`, `flash-attn`, `unsloth`, `axolotl`, `git worktree`, or frozen root references.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py::test_phase19_training_report_gate_requires_v42_handoff_evidence -q</automated>
  </verify>
  <done>Phase 19 TRAIN-01 evidence is launchable and report-gated without touching v4.0 training behavior.</done>
</task>

<task type="auto" tdd="false">
  <id>19-01-04</id>
  <name>Task 4: Generate default handoff artifacts and complete v4.2 QLoRA training run</name>
  <read_first>
    @tsc_cycle/v4_gates/phase19_training.py CLI and report validation sections
    @tsc_cycle/student/train.py v4_2 branch
    @scripts/run_v4_phase19_train.sh
    @artifacts/v4_2/phase18/reconstruction_report.json
    @data/v4_2/phase18/splits/manifest.json
  </read_first>
  <files>data/v4_2/phase18/tokenized, artifacts/v4_2/phase19, runs/v4.2-4B-*</files>
  <behavior>
    - Default Phase 18 calibrated JSONL/splits are tokenized into the default v4.2 tokenized directory before training.
    - A real v4.2 QLoRA training run is launched through the DGX Spark-safe wrapper, not by bypassing the wrapper.
    - Training completion produces an adapter directory, data manifest, logs, and `phase19_sft_report.json` under `runs/v4.2-4B-*`.
    - If hardware/runtime failure blocks completion, preserve logs and mark Phase 19 blocked rather than treating contract tests as sufficient.
  </behavior>
  <action>After contract tests pass, run the Phase 19 tokenization CLI against default Phase 18 artifacts. Then launch the v4.2 training wrapper and monitor until completion or a concrete blocker appears. Validate the produced `phase19_sft_report.json` with `phase19_training.py`; record adapter hash, data manifest hash, Phase 18 hashes, run root, and TRAIN-01 coverage in the plan summary.</action>
  <acceptance_criteria>
    - Default tokenized `train.arrow`, `val.arrow`, `ood_val.arrow`, and tokenized manifest exist and match Phase 18 split counts.
    - A real `runs/v4.2-4B-*` run root contains adapter artifacts, logs, data manifest, and `phase19_sft_report.json`.
    - The Phase 19 training report validator accepts the produced report.
    - TRAIN-01 is not marked complete unless the real adapter/report evidence exists or an explicit blocker report is written.
  </acceptance_criteria>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py -q</automated>
    <manual>Run the Phase 19 tokenization CLI on default Phase 18 artifacts, launch `scripts/run_v4_phase19_train.sh`, then validate the produced `runs/v4.2-4B-*/phase19_sft_report.json` with the Phase 19 training report gate.</manual>
  </verify>
  <done>Phase 19 has real v4.2 tokenized handoff artifacts and a completed QLoRA adapter/report, or a concrete runtime blocker is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 18 artifacts -> training handoff | Calibrated dataset/report/split hashes must match before tokenized data is trusted. |
| Prompt protocol -> tokenizer | Tokenization must not introduce prompt drift or native `<think>` leakage. |
| Maintainer CLI args -> filesystem writes | Run roots and tokenized/report paths must stay inside v4.2-safe locations. |
| Lightweight tests -> heavy training stack | Contract tests must not accidentally import or launch torch/transformers/bitsandbytes training. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-19-01 | Tampering | Training defaults | mitigate | Lock model, QLoRA settings, Phase 18 paths, and `v4.2-4B-` run prefix in tests. |
| T-19-02 | Tampering | Tokenized handoff | mitigate | Validate Phase 18 report/hash/split membership; emit prompt/assistant hashes and Arrow manifest. |
| T-19-03 | Repudiation | Training report | mitigate | Require adapter/model/run-root/data-manifest/Phase 18 hash evidence and TRAIN-01 coverage. |
| T-19-SC | Supply Chain | Training wrapper | mitigate | No dependency installs or new frameworks; wrapper uses existing DGX Spark-safe launcher only. |
</threat_model>

<verification>
Run quick Phase 19 tests after each task:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py -q</automated>

Run adjacent regression after Plan 01:
<automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m pytest tests/test_v4_phase19_training_export.py tests/test_v4_phase18_calibrated_dataset_rebuild.py tests/test_v4_phase9_sft_contracts.py -q</automated>
</verification>

<success_criteria>
- TRAIN-01 is satisfied by v4.2 constants, tokenized Phase 18 handoff artifacts, and a completed report-gated Qwen3-4B QLoRA training run.
- Phase 18 dataset/report/split hashes are preserved through tokenization and real training report evidence.
- v4.2 launch/report paths are isolated from v4.0 and frozen v1 artifacts.
- Existing v4.0 Phase 9 training contracts continue to pass.
</success_criteria>

<output>
Create `.planning/phases/19-4b-qlora-retrain-export/19-01-SUMMARY.md` when done.
</output>
