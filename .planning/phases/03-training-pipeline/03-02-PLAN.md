---
phase: 03-training-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grpo/format_reward.py
  - src/grpo/simulation_reward.py
  - src/grpo/data_loader.py
autonomous: true

must_haves:
  truths:
    - "格式奖励采用分级评分（<think> 标签得部分分，JSON 可解析得部分分，字段完整得满分）"
    - "仿真失败时（SUMO 崩溃或超时）跳过该样本，不纳入梯度计算（返回 None 或 NaN）"
    - "GRPO 数据加载从 outputs/training/ 读取原始训练样本"
    - "格式奖励与仿真奖励权重保持 0.2/0.8 配比"
  artifacts:
    - path: "src/grpo/format_reward.py"
      provides: "分级格式奖励函数"
      contains: "graded_format_reward"
    - path: "src/grpo/simulation_reward.py"
      provides: "仿真奖励，支持失败跳过"
      contains: "None"
    - path: "src/grpo/data_loader.py"
      provides: "GRPO 数据加载器"
      contains: "outputs/training"
  key_links:
    - from: "src/grpo/format_reward.py"
      to: "GRPOTrainer reward_funcs"
      via: "TRL GRPOTrainer 奖励函数接口"
      pattern: "completions.*kwargs"
    - from: "src/grpo/simulation_reward.py"
      to: "src/grpo/sumo_evaluator.py"
      via: "SUMO 评估并计算仿真奖励"
      pattern: "evaluate_single"
---

<objective>
重构 GRPO 奖励函数系统，实现分级格式评分和仿真失败跳过机制。

Purpose: GRPO 训练的质量取决于奖励信号的准确性。分级格式评分给模型更细粒度的学习信号，仿真失败跳过避免错误梯度。
Output: 重构后的 format_reward.py 和 simulation_reward.py，支持分级评分和失败跳过。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/grpo/format_reward.py
@src/grpo/simulation_reward.py
@src/grpo/data_loader.py
@src/grpo/sumo_evaluator.py
@src/grpo/reward_combiner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构格式奖励为分级评分系统</name>
  <files>
    src/grpo/format_reward.py
  </files>
  <action>
    按锁定决策重构格式奖励函数，实现分级评分：

    **替换现有的 match_format_exactly 和 match_format_approximately 为统一的 graded_format_reward 函数。**

    `graded_format_reward(completions, **kwargs) -> List[float]`:

    分级评分逻辑（Claude's Discretion 选择具体分值）：

    1. **Level 0 - 无输出或完全无关** (0.0 分):
       - 空内容或无任何格式特征

    2. **Level 1 - `<think>` 标签正确** (+0.5 分):
       - 检测到 `<think>...</think>` 标签对
       - 不要求内容非空（CoT 空占位策略下，空内容也合法）

    3. **Level 2 - JSON 可解析** (+1.0 分，累计 1.5):
       - `</think>` 之后存在可解析的 JSON 数组 `[...]`
       - JSON 能被 json.loads() 成功解析

    4. **Level 3 - 字段完整** (+1.5 分，累计 3.0):
       - JSON 数组每个元素都是 dict
       - 每个 dict 都包含 `phase_id` (int) 和 `final` (int/float) 字段
       - `final` 值在合理范围 (5-120)

    注意事项：
    - 保留 `extract_json_from_completion` 辅助函数
    - 保留 `check_phase_validity` 函数不变（它检查 phase_id 是否在 phase_config 中，final 是否在 min/max 范围内，这是独立的约束奖励）
    - 删除 `match_format_exactly` 和 `match_format_approximately` 函数
    - 更新 `__main__` 自测试，验证分级评分逻辑
    - completions 参数格式为 `List[List[Dict]]`（每个 completion 是 messages 列表），提取 assistant role 的 content
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
from src.grpo.format_reward import graded_format_reward, check_phase_validity

# 测试分级评分
# Level 3 满分
completions = [[{'role': 'assistant', 'content': '<think>分析</think>[{\"phase_id\": 1, \"final\": 40}]'}]]
scores = graded_format_reward(completions)
assert scores[0] == 3.0, f'Expected 3.0, got {scores[0]}'
print(f'Level 3 (full marks): {scores[0]}')

# Level 1 只有 think 标签
completions = [[{'role': 'assistant', 'content': '<think>test</think>invalid'}]]
scores = graded_format_reward(completions)
assert scores[0] == 0.5, f'Expected 0.5, got {scores[0]}'
print(f'Level 1 (think only): {scores[0]}')

# Level 2 有 think + 可解析 JSON 但字段不完整
completions = [[{'role': 'assistant', 'content': '<think>x</think>[{\"foo\": 1}]'}]]
scores = graded_format_reward(completions)
assert scores[0] == 1.5, f'Expected 1.5, got {scores[0]}'
print(f'Level 2 (think + parseable JSON): {scores[0]}')

# Level 0 无输出
completions = [[{'role': 'assistant', 'content': 'random text'}]]
scores = graded_format_reward(completions)
assert scores[0] == 0.0, f'Expected 0.0, got {scores[0]}'
print(f'Level 0 (no format): {scores[0]}')

print('All graded format reward tests passed')
"
    # 确认旧函数已删除
    grep -c "match_format_exactly" src/grpo/format_reward.py | grep -q "0" && echo "match_format_exactly 已删除" || echo "WARNING: match_format_exactly 仍存在"
    grep -c "match_format_approximately" src/grpo/format_reward.py | grep -q "0" && echo "match_format_approximately 已删除" || echo "WARNING: match_format_approximately 仍存在"
  </verify>
  <done>
    格式奖励函数重构为统一的 graded_format_reward，实现三级分级评分：think 标签 +0.5、JSON 可解析 +1.0、字段完整 +1.5，满分 3.0。旧的 match_format_exactly 和 match_format_approximately 已删除。check_phase_validity 保留不变。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复仿真奖励的失败跳过机制和数据加载路径</name>
  <files>
    src/grpo/simulation_reward.py
    src/grpo/data_loader.py
  </files>
  <action>
    **A. 修复 `src/grpo/simulation_reward.py` 的仿真失败跳过：**

    按锁定决策：仿真失败时（SUMO 崩溃或超时）跳过该样本，不纳入梯度计算。

    1. 修改 `compute_simulation_reward()` 函数：
       - 当 JSON 解析失败时：返回 `float('nan')` 而不是 `0.0`（NaN 值在 TRL GRPOTrainer 中会被自动跳过不参与梯度计算）
       - 当评估失败（result.success == False）时：返回 `float('nan')` 而不是 `-1.0`
       - 评估成功时：保持现有的 compute_metric_reward 逻辑不变
       - 在函数开头添加注释说明 NaN 跳过策略

    2. 修改 `parallel_evaluate()` 函数：
       - 增加 timeout 参数（默认 120 秒），保持现有逻辑
       - 在评估结果中增加统计：成功数、失败数、跳过数
       - 打印统计信息（用于日志记录）

    **B. 修复 `src/grpo/data_loader.py` 的数据路径：**

    1. 修改 `load_training_data()` 默认路径：
       - data_dir 默认路径从文档中的 `data/training` 改为实际的 `outputs/training`
       - 这与 Phase 2 generate_training_data.py 的输出路径一致

    2. 确认 `prepare_grpo_dataset()` 正确构造 prompt 字段：
       - 当前逻辑已正确：构建 messages -> apply_chat_template -> formatted_prompt
       - 确认 state_file 和 tl_id 字段正确传递（用于仿真奖励评估）

    **C. 更新 train_grpo.py 中的奖励函数引用：**

    注意：不修改 train_grpo.py（属于 Plan 03-03 的范围），但需要确保 graded_format_reward 的函数签名与 TRL GRPOTrainer 的 reward_funcs 接口兼容。

    函数签名要求：
    - `graded_format_reward(completions: List[List[Dict]], **kwargs) -> List[float]`
    - 与 TRL GRPOTrainer 的 reward_funcs 调用方式一致
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
import math

# 验证 simulation_reward 的 NaN 跳过策略
from src.grpo.simulation_reward import parse_plan_from_completion
# 解析失败应返回 None
result = parse_plan_from_completion('invalid text')
assert result is None
print('parse_plan_from_completion: None for invalid text')

# 验证 data_loader 路径
from src.grpo.data_loader import load_training_data
import inspect
sig = inspect.signature(load_training_data)
print(f'load_training_data signature: {sig}')
print('Data loader import OK')
"
    grep -n "float('nan')" src/grpo/simulation_reward.py  # 应该存在 NaN 返回
    grep -n "outputs/training" src/grpo/data_loader.py  # 应该使用正确路径
  </verify>
  <done>
    仿真奖励在评估失败时返回 NaN（不参与梯度计算），而不是返回固定负奖励。GRPO 数据加载路径修正为 outputs/training/（与 Phase 2 输出一致）。
  </done>
</task>

</tasks>

<verification>
1. python3 验证 graded_format_reward 分级评分（Level 0/1/2/3 四级）
2. python3 验证 check_phase_validity 保持不变
3. grep 验证 match_format_exactly 和 match_format_approximately 已删除
4. grep 验证 simulation_reward.py 使用 NaN 跳过失败样本
5. grep 验证 data_loader.py 使用 outputs/training 路径
6. python3 验证所有模块可正常导入
</verification>

<success_criteria>
- 格式奖励采用三级分级评分（think 标签 / JSON 可解析 / 字段完整），满分 3.0
- 仿真失败时返回 NaN，不纳入梯度计算
- GRPO 数据加载路径与 Phase 2 输出一致（outputs/training/）
- 所有奖励函数签名与 TRL GRPOTrainer reward_funcs 接口兼容
</success_criteria>

<output>
After completion, create `.planning/phases/03-training-pipeline/03-02-SUMMARY.md`
</output>
