---
phase: 03-training-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/grpo/trainer.py
  - src/scripts/train_grpo.py
autonomous: true

must_haves:
  truths:
    - "GRPO 训练能基于 SFT 模型（outputs/sft/final/）进行强化学习"
    - "GRPO 模型加载使用 bf16 全精度 LoRA（无 4-bit 量化）"
    - "训练脚本使用重构后的 graded_format_reward（不再引用已删除的 match_format_exactly/match_format_approximately）"
    - "训练过程输出日志文件并支持从 checkpoint 恢复"
    - "GRPO 训练完成后保存 LoRA adapter 到 outputs/grpo/final/"
  artifacts:
    - path: "src/grpo/trainer.py"
      provides: "GRPO 训练器，bf16 模型加载"
      contains: "load_in_4bit.*False"
    - path: "src/scripts/train_grpo.py"
      provides: "GRPO 训练入口，使用 graded_format_reward"
      contains: "graded_format_reward"
  key_links:
    - from: "src/scripts/train_grpo.py"
      to: "src/grpo/format_reward.py"
      via: "导入 graded_format_reward 奖励函数"
      pattern: "graded_format_reward"
    - from: "src/scripts/train_grpo.py"
      to: "outputs/sft/final/"
      via: "加载 SFT 模型作为 GRPO 初始模型"
      pattern: "sft.*adapter|sft.*final"
    - from: "src/grpo/trainer.py"
      to: "src/grpo/simulation_reward.py"
      via: "仿真奖励评估"
      pattern: "compute_simulation_reward"
---

<objective>
修复 GRPO 训练流程，使其能正确加载 SFT 模型、使用重构后的奖励函数执行强化学习训练。

Purpose: GRPO 是训练流水线的第二阶段，基于 SFT 模型通过强化学习优化信号控制策略。
Output: 可执行的 GRPO 训练脚本，输出 `outputs/grpo/final/` LoRA adapter。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-training-pipeline/03-01-SUMMARY.md
@.planning/phases/03-training-pipeline/03-02-SUMMARY.md

@src/grpo/trainer.py
@src/scripts/train_grpo.py
@src/grpo/format_reward.py
@src/grpo/simulation_reward.py
@config/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 GRPO 模型加载和训练器配置</name>
  <files>
    src/grpo/trainer.py
  </files>
  <action>
    修复 GRPO 训练器的模型加载和训练配置。

    **A. 修复 `load_sft_model()` 函数：**

    当前问题：
    1. `load_in_4bit=True`（line 102）—— 违反锁定决策"去掉 4-bit 量化，使用 bf16 全精度 LoRA"
    2. 调用 `FastLanguageModel.get_peft_model()` 时没有传入完整 LoRA 参数（adapter 已存在时可能冲突）
    3. `fast_inference=False` 正确

    修改方案：
    1. 将 `load_in_4bit=True` 改为 `load_in_4bit=False`（bf16 全精度）
    2. 修复 `get_peft_model()` 调用：
       - 从 adapter_config.json 读取 LoRA 参数（r, lora_alpha, target_modules）
       - 传递完整参数给 get_peft_model，而不是依赖自动检测
       - 如果 adapter_config.json 存在，读取其中的 r 和 lora_alpha 值
    3. 添加打印：模型加载成功后打印 LoRA 参数信息

    **B. 修复 GRPOConfig：**

    1. 添加 `bf16: bool = True` 字段
    2. 添加 `save_total_limit: int = 3`（滚动删除，保留最近 3 个 checkpoint）
    3. 添加 `logging_dir: str = "{output_dir}/logs"` 字段

    **C. 修复 `create_grpo_trainer()` 函数：**

    1. 在 TRLGRPOConfig 中添加 `bf16=config.bf16`
    2. 在 TRLGRPOConfig 中添加 `save_total_limit=config.save_total_limit`
    3. 在 TRLGRPOConfig 中添加 `logging_dir=config.logging_dir.format(output_dir=config.output_dir)` 如果 logging_dir 包含 {output_dir}
    4. 确保所有参数正确传递给 TRL GRPOConfig

    **D. 更新自测试：**

    1. 更新 GRPOConfig 默认值断言
    2. 验证新增的 bf16、save_total_limit 字段
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
from src.grpo.trainer import GRPOConfig, create_sampling_params

config = GRPOConfig()
assert config.bf16 == True, 'bf16 should be True'
assert config.save_total_limit == 3, 'save_total_limit should be 3'
print(f'GRPOConfig: bf16={config.bf16}, save_total_limit={config.save_total_limit}')
print('GRPOConfig fields OK')
"
    grep -n "load_in_4bit" src/grpo/trainer.py  # 应该是 load_in_4bit=False
    grep -n "bf16" src/grpo/trainer.py  # 应该存在 bf16 字段
    grep -n "save_total_limit" src/grpo/trainer.py  # 应该存在
  </verify>
  <done>
    GRPO 模型加载使用 bf16 全精度（load_in_4bit=False），GRPOConfig 包含 bf16=True 和 save_total_limit=3 配置，create_grpo_trainer 正确传递所有配置给 TRL GRPOConfig。
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 GRPO 训练入口脚本，更新奖励函数引用和添加日志</name>
  <files>
    src/scripts/train_grpo.py
  </files>
  <action>
    修复 train_grpo.py 训练入口，集成重构后的奖励函数、日志和 checkpoint 恢复。

    **A. 更新奖励函数导入：**

    1. 替换旧导入：
       - 删除: `from src.grpo.format_reward import match_format_exactly, match_format_approximately, check_phase_validity`
       - 添加: `from src.grpo.format_reward import graded_format_reward, check_phase_validity`

    2. 修改 step [3/6] 奖励函数配置：
       - reward_funcs 列表改为：
         ```python
         reward_funcs = [
             graded_format_reward,
             check_phase_validity,
         ]
         ```
       - 更新打印信息，反映新的分级评分系统
       - 仿真奖励部分保持不变（lambda 包装 compute_simulation_reward）

    **B. 添加日志文件输出：**

    1. 在 main() 函数开头，配置 Python logging：
       - 输出到 `{output_dir}/training.log` 文件
       - 同时输出到终端（stdout）
       - 格式：`%(asctime)s - %(levelname)s - %(message)s`

    2. 关键信息记录到日志：
       - 配置参数
       - 数据统计
       - 训练进度
       - 最终结果

    **C. 添加 Checkpoint 恢复：**

    1. 添加 `--resume-from` 命令行参数
    2. 在 trainer.train() 调用时，如果指定了 resume_from：
       - `trainer.train(resume_from_checkpoint=args.resume_from)`
    3. 自动检测 output_dir 中的 checkpoint 目录并提示

    **D. 修复 SFT adapter 路径默认值：**

    1. 默认 sft_adapter 路径保持 `outputs/sft/final`（与 Plan 03-01 的输出一致）
    2. 添加检查：如果 SFT adapter 不存在，打印清晰错误信息并退出

    **E. 修复 GRPO 数据路径：**

    1. 默认 data_dir 改为 `outputs/training`（与 Phase 2 输出一致）
    2. 与 Plan 03-02 中 data_loader.py 的路径修复保持一致
  </action>
  <verify>
    python3 -c "
import sys; sys.path.insert(0, '.')
# 验证脚本语法正确
import src.scripts.train_grpo
print('train_grpo import OK')
"
    # 验证旧奖励函数引用已删除
    grep -c "match_format_exactly" src/scripts/train_grpo.py | grep -q "0" && echo "match_format_exactly 引用已删除" || echo "WARNING: 仍引用 match_format_exactly"
    grep -c "match_format_approximately" src/scripts/train_grpo.py | grep -q "0" && echo "match_format_approximately 引用已删除" || echo "WARNING: 仍引用 match_format_approximately"
    # 验证新奖励函数引用
    grep -n "graded_format_reward" src/scripts/train_grpo.py  # 应该存在
    # 验证日志和恢复支持
    grep -n "resume" src/scripts/train_grpo.py  # 应该有 resume 支持
    grep -n "training.log" src/scripts/train_grpo.py  # 应该有日志文件
    # 验证数据路径
    grep -n "outputs/training" src/scripts/train_grpo.py  # 应该使用正确路径
  </verify>
  <done>
    GRPO 训练入口使用重构后的 graded_format_reward（分级评分），不再引用已删除的 match_format_exactly/match_format_approximately。训练日志输出到终端 + {output_dir}/training.log。支持 --resume-from checkpoint 恢复训练。SFT adapter 路径默认 outputs/sft/final，GRPO 数据路径默认 outputs/training。
  </done>
</task>

</tasks>

<verification>
1. python3 验证 train_grpo.py 语法正确并可导入
2. grep 验证 trainer.py 使用 load_in_4bit=False（bf16 全精度）
3. grep 验证 train_grpo.py 导入 graded_format_reward（不是旧函数）
4. grep 验证 train_grpo.py 不再引用 match_format_exactly 和 match_format_approximately
5. grep 验证 train_grpo.py 有 resume 支持和日志文件输出
6. grep 验证 train_grpo.py 使用 outputs/training 数据路径
7. python3 验证 GRPOConfig 包含 bf16 和 save_total_limit 字段
</verification>

<success_criteria>
- GRPO 模型加载使用 bf16 全精度（无 4-bit 量化）
- 训练脚本使用 graded_format_reward + check_phase_validity + compute_simulation_reward 三个奖励函数
- 格式奖励与仿真奖励权重配比 0.2/0.8（通过 TRL GRPOTrainer 的多奖励函数机制实现）
- 训练日志输出到终端 + 日志文件
- 支持从 checkpoint 恢复训练
- Checkpoint 滚动删除（保留最近 3 个）
- SFT 模型通过固定路径 outputs/sft/final/ 传递给 GRPO
- GRPO 训练完成后保存到 outputs/grpo/final/
</success_criteria>

<output>
After completion, create `.planning/phases/03-training-pipeline/03-03-SUMMARY.md`
</output>
