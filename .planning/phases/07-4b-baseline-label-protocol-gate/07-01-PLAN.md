---
phase: 07-4b-baseline-label-protocol-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/prompt_builder.py
  - tsc_cycle/v4_gates/phase7_protocol.py
  - tests/test_prompt_builder.py
  - tests/test_v4_phase7_protocol.py
  - artifacts/v4/phase7/protocol_fixture.json
autonomous: true
requirements:
  - TAG-01
  - TAG-02
  - TAG-03
user_setup: []
must_haves:
  truths:
    - "协议解析只接受 <start_working_out>...</end_working_out><SOLUTION>...</SOLUTION>。"
    - "错误闭合标记 <end_working_out> 被视为 malformed 并 fail-closed。"
    - "任意输入或输出中出现 native <think> 或 </think> 都被拒绝。"
  artifacts:
    - path: "tsc_cycle/prompt_builder.py"
      provides: "共享协议常量、assistant prefill、assistant 输出组装与解析"
      contains: "TAG_THINK_CLOSE = \"</end_working_out>\""
    - path: "tsc_cycle/v4_gates/phase7_protocol.py"
      provides: "Phase 7 协议 fixture gate 与 protocol_fixture.json 写入入口"
      exports: ["evaluate_protocol_fixtures", "build_parser", "main"]
    - path: "tests/test_v4_phase7_protocol.py"
      provides: "slash close 正例、bare close/native think 反例覆盖"
    - path: "artifacts/v4/phase7/protocol_fixture.json"
      provides: "可审计协议门禁结果"
  key_links:
    - from: "tsc_cycle/v4_gates/phase7_protocol.py"
      to: "tsc_cycle/prompt_builder.py"
      via: "imports TAG_THINK_* and parse_assistant_output"
      pattern: "parse_assistant_output"
    - from: "tests/test_v4_phase7_protocol.py"
      to: "tsc_cycle/prompt_builder.py"
      via: "pytest imports constants/parser"
      pattern: "</end_working_out>"
---

<objective>
修正 Phase 7 共享标签协议，使代码、parser、fixture gate 与测试全部以 slash closing `</end_working_out>` 为唯一合法思考闭合标签。

Purpose: 在任何数据 rebuild 或训练前阻断错误 `<end_working_out>` 协议和 native `<think>`/`</think>` 泄漏，覆盖 TAG-01、TAG-02、TAG-03。
Output: 修正后的 `prompt_builder.py`、协议 gate 模块、协议测试，以及 `artifacts/v4/phase7/protocol_fixture.json`。
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md
@/home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py
@/home/samuel/TSC_CYCLE/tests/test_prompt_builder.py

<interfaces>
Existing shared protocol surface from `tsc_cycle/prompt_builder.py` that must be corrected in this plan:
```python
TAG_THINK_OPEN = "<start_working_out>"
TAG_THINK_CLOSE = "<end_working_out>"      # currently wrong for v4 Phase 7; change to "</end_working_out>"
TAG_SOLUTION_OPEN = "<SOLUTION>"
TAG_SOLUTION_CLOSE = "</SOLUTION>"
def build_user_prompt(prediction_input: dict[str, Any]) -> str: ...
def build_assistant_prefill() -> str: ...
def build_full_assistant(reasoning: str, solution: dict[str, int]) -> str: ...
def parse_assistant_output(text: str) -> tuple[str, dict[str, int] | None]: ...
```
Phase 7 required literals per REQUIREMENTS/CONTEXT: open `<start_working_out>`, close `</end_working_out>`, solution open `<SOLUTION>`, solution close `</SOLUTION>`. The bare marker `<end_working_out>` is the rejected malformed close marker, not a compatibility alias.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED protocol tests for slash close and native think rejection</name>
  <files>tests/test_v4_phase7_protocol.py, tests/test_prompt_builder.py</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md` lines 16-21 for TAG-01..TAG-04 wording.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md` lines 30-42 for Phase 7 test names.
    - `/home/samuel/TSC_CYCLE/tests/test_prompt_builder.py` existing tests that currently assert the opposite close-tag direction.
  </read_first>
  <behavior>
    - Valid full output `<start_working_out>reasoning</end_working_out><SOLUTION>{"1":60}</SOLUTION>` parses to reasoning `reasoning` and solution `{"1": 60}`.
    - Valid prefill-only output `reasoning</end_working_out><SOLUTION>{"1":60}</SOLUTION>` parses to reasoning `reasoning` and solution `{"1": 60}`.
    - Bare close output containing `<end_working_out>` returns `("", None)`.
    - Any `<think>` or `</think>` in reasoning, solution-adjacent text, or prompt fixture returns `("", None)` or fails the protocol gate.
  </behavior>
  <action>
    Create `tests/test_v4_phase7_protocol.py` with the four behavior groups above. Update `tests/test_prompt_builder.py` so its constants, prompt, roundtrip, and legacy-rejection assertions match TAG-01/TAG-02/TAG-03: `TAG_THINK_CLOSE == "</end_working_out>"`; `<end_working_out>` is absent from positive prompt/full-assistant output except when intentionally used as a negative fixture; native `<think>` and `</think>` are never accepted. Do not add compatibility tests that accept both close tags.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_prompt_builder.py</automated>
  </verify>
  <acceptance_criteria>
    RED tests fail only because production code still treats `<end_working_out>` as valid or fails to reject native think tags; pytest collection succeeds without requiring tokenizer/model downloads.
  </acceptance_criteria>
  <done>`tests/test_v4_phase7_protocol.py` exists, `tests/test_prompt_builder.py` v4 assertions point to `</end_working_out>`, and no positive assertion treats `<end_working_out>` as a valid close tag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct shared protocol constants, parser, and prompt text</name>
  <files>tsc_cycle/prompt_builder.py, tests/test_prompt_builder.py, tests/test_v4_phase7_protocol.py</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/tsc_cycle/prompt_builder.py` constants, USER_TEMPLATE, `build_full_assistant`, and `parse_assistant_output`.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` sections “Pitfall 1” and “Code Examples”.
  </read_first>
  <action>
    In `tsc_cycle/prompt_builder.py`, set `TAG_THINK_CLOSE = "</end_working_out>"` and define the rejected malformed marker as `MALFORMED_THINK_CLOSE = "<end_working_out>"` or equivalent. Update docstrings and `USER_TEMPLATE` output requirements to display `<start_working_out>...</end_working_out><SOLUTION>...</SOLUTION>`. Update `build_full_assistant` to emit slash close. Update `parse_assistant_output` to fail closed before parsing if text contains `<end_working_out>`, `<think>`, or `</think>`; then parse reasoning using slash close in both full and prefill-only forms. Do not use `apply_chat_template`; this plan is raw-text only per TAG-03.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_prompt_builder.py</automated>
  </verify>
  <acceptance_criteria>
    All protocol tests pass; grep gates over non-comment lines show no positive protocol path using `<end_working_out>`: `grep -R "<end_working_out>" tsc_cycle tests | grep -v "MALFORMED" | grep -v "negative" | grep -v "reject"` has no positive-use hits.
  </acceptance_criteria>
  <done>`build_user_prompt`, `build_full_assistant`, and `parse_assistant_output` all use `</end_working_out>` as the only accepted close marker, while bare close and native think literals fail closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add protocol fixture gate artifact writer</name>
  <files>tsc_cycle/v4_gates/phase7_protocol.py, tests/test_v4_phase7_protocol.py, artifacts/v4/phase7/protocol_fixture.json</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase1_report.py` for JSON artifact shape and `build_parser()/main()` pattern.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` lines 184-200 for fail-closed report pattern.
  </read_first>
  <action>
    Create `tsc_cycle/v4_gates/phase7_protocol.py` exposing `evaluate_protocol_fixtures() -> dict`, `build_parser()`, and `main(argv=None)`. The evaluator must test one accepted fixture using `<start_working_out>...</end_working_out><SOLUTION>...</SOLUTION>` and rejected fixtures for bare `<end_working_out>`, native `<think>`, native `</think>`, and mixed close tags. The payload must include `ok`, `accepted_fixture`, `rejected_fixtures`, `chat_template_used: false`, `fatal_failures`, and `requirements_covered: ["TAG-01", "TAG-02", "TAG-03"]`. Default output path is `artifacts/v4/phase7/protocol_fixture.json`; reject any `--out` path under `/home/samuel/TSC_CYCLE/runs/20260507T032419Z` by resolving paths before writing.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_prompt_builder.py</automated>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_protocol --out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/protocol_fixture.json</automated>
  </verify>
  <acceptance_criteria>
    `protocol_fixture.json` has `ok: true`, `chat_template_used: false`, all malformed/native fixtures marked rejected, and no write occurs under `runs/20260507T032419Z/`.
  </acceptance_criteria>
  <done>Protocol gate can be run independently and produces an auditable JSON artifact consumed by Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture text -> parser | Untrusted model/fixture output crosses into `parse_assistant_output`. |
| CLI `--out` -> filesystem | User-provided output path can attempt to write outside artifact root or into frozen baseline root. |
| shell -> pytest/module invocation | Executor runs commands; no shell-generated fixture text should be evaluated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | T/I | `parse_assistant_output` | high | mitigate/block | Fail closed if `<end_working_out>`, `<think>`, or `</think>` appears anywhere before JSON parsing. |
| T-07-01-02 | T | `phase7_protocol.py --out` | high | mitigate/block | Resolve output path and reject any descendant of `/home/samuel/TSC_CYCLE/runs/20260507T032419Z`. |
| T-07-01-03 | E | CLI/test commands | medium | mitigate | Use Python module invocation with fixed argv in verification; no `eval`, no shell interpolation in gate module. |
| T-07-01-04 | R | protocol evidence | medium | mitigate | Write `protocol_fixture.json` with accepted/rejected fixture results and requirements covered. |
</threat_model>

<verification>
Plan-level commands:
```bash
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_prompt_builder.py
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_protocol --out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/protocol_fixture.json
```
</verification>

<success_criteria>
- `TAG_THINK_CLOSE` is exactly `</end_working_out>`.
- `<end_working_out>` and native `<think>`/`</think>` are rejected by parser and fixture gate.
- `protocol_fixture.json` is green and covers TAG-01, TAG-02, TAG-03.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-01-SUMMARY.md`.
</output>
