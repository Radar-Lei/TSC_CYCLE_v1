---
phase: 07-4b-baseline-label-protocol-gate
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - tsc_cycle/v4_gates/phase7_report.py
  - scripts/run_v4_phase7_gate.sh
  - tests/test_v4_phase7_gate_report.py
  - artifacts/v4/phase7/phase7_gate_report.json
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04
user_setup: []
must_haves:
  truths:
    - "Operator can run one Phase 7 wrapper and produce an aggregate gate report."
    - "Aggregate report includes ok, fatal_failures, warnings, requirements_covered, and next_phase_allowed."
    - "Phase 8 is allowed only when all BASE/TAG sub-gates pass and all seven requirements are covered."
  artifacts:
    - path: "tsc_cycle/v4_gates/phase7_report.py"
      provides: "aggregate Phase 7 report evaluator and CLI"
      exports: ["evaluate_gates", "build_parser", "main"]
    - path: "scripts/run_v4_phase7_gate.sh"
      provides: "fixed-argv wrapper running protocol, baseline, tokenizer, and aggregate report gates"
    - path: "tests/test_v4_phase7_gate_report.py"
      provides: "aggregate report shape and fail-closed next_phase_allowed tests"
    - path: "artifacts/v4/phase7/phase7_gate_report.json"
      provides: "final Phase 7 handoff gate for Phase 8"
  key_links:
    - from: "tsc_cycle/v4_gates/phase7_report.py"
      to: "artifacts/v4/phase7/protocol_fixture.json"
      via: "sub-gate JSON load"
      pattern: "protocol_fixture"
    - from: "tsc_cycle/v4_gates/phase7_report.py"
      to: "artifacts/v4/phase7/environment.json"
      via: "sub-gate JSON load"
      pattern: "environment"
    - from: "tsc_cycle/v4_gates/phase7_report.py"
      to: "artifacts/v4/phase7/baseline_readonly.json"
      via: "sub-gate JSON load"
      pattern: "baseline_readonly"
    - from: "tsc_cycle/v4_gates/phase7_report.py"
      to: "artifacts/v4/phase7/tokenizer_audit.json"
      via: "sub-gate JSON load"
      pattern: "tokenizer_audit"
---

<objective>
聚合 Phase 7 所有子门禁并提供脚本 wrapper，生成阻断式 `phase7_gate_report.json`。

Purpose: 覆盖 required scope 的 aggregate gate report 和 script wrapper，确保 Phase 8 只有在 BASE-01..03 与 TAG-01..04 全部通过时才允许继续。
Output: `phase7_report.py`、`scripts/run_v4_phase7_gate.sh`、aggregate 测试、`artifacts/v4/phase7/phase7_gate_report.json`。
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase1_report.py
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-01-SUMMARY.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-02-SUMMARY.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-03-SUMMARY.md

<interfaces>
Expected sub-gate artifact contracts from prior plans:
```python
# protocol_fixture.json
{"ok": bool, "fatal_failures": list, "warnings": list, "requirements_covered": ["TAG-01", "TAG-02", "TAG-03"], "chat_template_used": False, ...}

# environment.json
{"ok": bool, "fatal_failures": list, "warnings": list, "requirements_covered": ["BASE-01", "BASE-02"], "mutation_actions": [], ...}

# baseline_readonly.json
{"ok": bool, "fatal_failures": list, "warnings": list, "requirements_covered": ["BASE-03"], "before": dict, "after": dict, "unchanged": True, ...}

# tokenizer_audit.json
{"ok": bool, "fatal_failures": list, "warnings": list, "requirements_covered": ["TAG-04", "TAG-03", "TAG-01"], "native_think_token_ids": list, "custom_tags": dict, ...}
```
Final aggregate report must include top-level `ok`, `fatal_failures`, `warnings`, `requirements_covered`, `next_phase_allowed`, and detailed `gates`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aggregate report and wrapper tests</name>
  <files>tests/test_v4_phase7_gate_report.py</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase1_report.py` for aggregate report style.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md` line 35 for aggregate test command.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` lines 184-200 for required aggregate fields.
  </read_first>
  <behavior>
    - Given four green sub-gate JSON files, `evaluate_gates(artifacts_dir)` returns `ok: true`, `next_phase_allowed: true`, empty `fatal_failures`, and all seven requirements sorted or in stable order.
    - Given any missing, malformed, or `ok:false` sub-gate, aggregate report returns `ok:false`, `next_phase_allowed:false`, and includes that gate in `fatal_failures`.
    - Given sub-gates that omit any of BASE-01, BASE-02, BASE-03, TAG-01, TAG-02, TAG-03, TAG-04, aggregate report fails closed.
    - Wrapper text uses fixed venv Python and fixed module invocations; tests check no `pip install`, no `uv pip install`, no `eval`, and no unquoted writable frozen-root output.
  </behavior>
  <action>
    Create `tests/test_v4_phase7_gate_report.py` using tmp_path JSON fixtures. Include shape assertions for top-level `ok`, `fatal_failures`, `warnings`, `gates`, `requirements_covered`, and `next_phase_allowed`. Include a script-content test that reads `scripts/run_v4_phase7_gate.sh` if it exists and checks the wrapper uses `/home/samuel/TSC_CYCLE/.venv/bin/python`, writes only `/home/samuel/TSC_CYCLE/artifacts/v4/phase7/`, and does not contain package-install commands or `eval`.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_gate_report.py</automated>
  </verify>
  <acceptance_criteria>
    RED tests fail only because `phase7_report.py` and/or wrapper are missing; fixture-based aggregate tests do not require live tokenizer/model downloads.
  </acceptance_criteria>
  <done>Aggregate report tests define the final Phase 7 gate contract and fail-closed missing-requirement behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement aggregate report evaluator</name>
  <files>tsc_cycle/v4_gates/phase7_report.py, artifacts/v4/phase7/phase7_gate_report.json</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/phase1_report.py` `_load_json`, `_gate`, and `evaluate_gates` style.
    - Prior plan summaries `07-01-SUMMARY.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md` for actual artifact fields if they differ from the planned contract.
  </read_first>
  <action>
    Create `tsc_cycle/v4_gates/phase7_report.py` with `REQUIREMENTS_COVERED = ["BASE-01", "BASE-02", "BASE-03", "TAG-01", "TAG-02", "TAG-03", "TAG-04"]`. Implement `_load_json(path)`, `evaluate_gates(artifacts="artifacts/v4/phase7")`, `build_parser()`, and `main(argv=None)`. Load `protocol_fixture.json`, `environment.json`, `baseline_readonly.json`, and `tokenizer_audit.json`; each gate passes only if payload `ok is True`. Aggregate warnings from sub-gates. Add fatal failure if any artifact is missing/malformed/ok false or if union of sub-gate `requirements_covered` omits any required ID. Set `next_phase_allowed` exactly equal to final `ok`. Default output path is `artifacts/v4/phase7/phase7_gate_report.json`; reject any output path under `/home/samuel/TSC_CYCLE/runs/20260507T032419Z` before writing.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_gate_report.py</automated>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_report --artifacts /home/samuel/TSC_CYCLE/artifacts/v4/phase7 --out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/phase7_gate_report.json</automated>
  </verify>
  <acceptance_criteria>
    Aggregate report has top-level `ok`, `fatal_failures`, `warnings`, `gates`, `requirements_covered`, and `next_phase_allowed`; it is green only when all sub-gates are green and all seven requirements are covered.
  </acceptance_criteria>
  <done>`phase7_gate_report.json` exists under `/home/samuel/TSC_CYCLE/artifacts/v4/phase7/` and blocks Phase 8 unless every Phase 7 gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixed-argv Phase 7 wrapper and run full Phase 7 validation</name>
  <files>scripts/run_v4_phase7_gate.sh, tests/test_v4_phase7_gate_report.py, artifacts/v4/phase7/phase7_gate_report.json</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/scripts/run_v3_phase4_dry_run.sh` if present, for wrapper safety pattern; if absent, use fixed argv style from existing `scripts/run_v3_*` wrappers.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md` lines 13-25 for canonical pytest commands.
  </read_first>
  <action>
    Create executable `scripts/run_v4_phase7_gate.sh` with `set -euo pipefail`, `ROOT="/home/samuel/TSC_CYCLE"`, `PY="$ROOT/.venv/bin/python"`, `ART="$ROOT/artifacts/v4/phase7"`, and `mkdir -p "$ART"`. Run fixed module invocations in order: `tsc_cycle.v4_gates.phase7_protocol`, `tsc_cycle.v4_gates.phase7_baseline`, `tsc_cycle.v4_gates.phase7_tokenizer`, then `tsc_cycle.v4_gates.phase7_report`, each with explicit absolute output paths under `$ART`. The wrapper must not accept arbitrary output args, must not run `pip`, `uv pip`, `sudo`, or `eval`, and must not write under `runs/20260507T032419Z/`. Mark executable. Then run the Phase 7 quick pytest command from VALIDATION plus the wrapper.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_v4_phase7_baseline_gate.py tests/test_v4_phase7_tokenizer_audit.py tests/test_v4_phase7_gate_report.py tests/test_prompt_builder.py tests/test_tokenizer_check.py</automated>
    <automated>/home/samuel/TSC_CYCLE/scripts/run_v4_phase7_gate.sh</automated>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    Full Phase 7 subset and full pytest pass; wrapper produces `phase7_gate_report.json` with `ok:true`, `next_phase_allowed:true`, `fatal_failures:[]`, and `requirements_covered` exactly covering BASE-01, BASE-02, BASE-03, TAG-01, TAG-02, TAG-03, TAG-04.
  </acceptance_criteria>
  <done>One command `/home/samuel/TSC_CYCLE/scripts/run_v4_phase7_gate.sh` creates all Phase 7 artifacts and final aggregate report without installing packages or writing into the frozen v1 baseline root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-gate JSON -> aggregate report | Malformed or missing evidence must fail closed, not be treated as warning-only. |
| wrapper -> local shell | Shell script must not allow injection, arbitrary outputs, package installs, or sudo. |
| aggregate `--out` -> filesystem | Output path must not target frozen v1 baseline root. |
| native think evidence -> phase handoff | Report must not allow next phase if tokenizer/protocol gates fail to reject native think. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | R/T | `phase7_report.py` JSON loading | high | mitigate/block | Missing, malformed, `ok:false`, or incomplete requirement coverage creates fatal failure and `next_phase_allowed:false`. |
| T-07-04-02 | E | `scripts/run_v4_phase7_gate.sh` | high | mitigate/block | Fixed absolute argv only; no arbitrary user args, no `eval`, no package installs, no `sudo`. |
| T-07-04-03 | T | report output path | high | mitigate/block | Resolve output path and reject descendants of frozen v1 root before writing. |
| T-07-04-04 | I/T | native `<think>` leakage handoff | high | mitigate/block | Aggregate depends on protocol and tokenizer gates; any native think gate failure blocks `next_phase_allowed`. |
| T-07-04-05 | R | environment drift evidence | medium | mitigate | Carry sub-gate warnings into top-level report; environment mutation actions must remain visible in `gates.environment`. |
</threat_model>

<verification>
Plan-level commands:
```bash
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_protocol.py tests/test_v4_phase7_baseline_gate.py tests/test_v4_phase7_tokenizer_audit.py tests/test_v4_phase7_gate_report.py tests/test_prompt_builder.py tests/test_tokenizer_check.py
/home/samuel/TSC_CYCLE/scripts/run_v4_phase7_gate.sh
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q
```
</verification>

<success_criteria>
- `artifacts/v4/phase7/phase7_gate_report.json` includes `ok`, `fatal_failures`, `warnings`, `requirements_covered`, and `next_phase_allowed`.
- Final report covers BASE-01, BASE-02, BASE-03, TAG-01, TAG-02, TAG-03, TAG-04.
- `next_phase_allowed` is true only when all sub-gates pass.
- Wrapper uses project `.venv` command paths from VALIDATION.md and performs no install/upgrade.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-04-SUMMARY.md`.
</output>
