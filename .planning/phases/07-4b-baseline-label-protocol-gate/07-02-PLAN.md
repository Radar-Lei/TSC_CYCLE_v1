---
phase: 07-4b-baseline-label-protocol-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/v4_gates/__init__.py
  - tsc_cycle/v4_gates/phase7_baseline.py
  - tests/test_v4_phase7_baseline_gate.py
  - artifacts/v4/phase7/environment.json
  - artifacts/v4/phase7/baseline_readonly.json
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
user_setup: []
must_haves:
  truths:
    - "Phase 7 gate reports student model locked to Qwen/Qwen3-4B-Thinking-2507 and rejects Qwen3.5-9B."
    - "Phase 7 records environment evidence from existing venvs and safe-wrapper paths without installing or upgrading packages."
    - "Phase 7 proves the frozen v1 baseline root runs/20260507T032419Z/ is only referenced read-only and unchanged before/after gate execution."
  artifacts:
    - path: "tsc_cycle/v4_gates/phase7_baseline.py"
      provides: "BASE-01/02/03 model, environment, and baseline read-only gate"
      exports: ["EXPECTED_MODEL_ID", "evaluate_baseline_gate", "snapshot_baseline_root", "assert_output_not_in_frozen_root", "build_parser", "main"]
    - path: "tests/test_v4_phase7_baseline_gate.py"
      provides: "model lock, env evidence, path guard, and baseline snapshot tests"
    - path: "artifacts/v4/phase7/environment.json"
      provides: "package/path evidence without mutation"
    - path: "artifacts/v4/phase7/baseline_readonly.json"
      provides: "v1 root before/after snapshot and cache path discovery"
  key_links:
    - from: "tsc_cycle/v4_gates/phase7_baseline.py"
      to: "runs/20260507T032419Z/"
      via: "resolved frozen root path guard and snapshot"
      pattern: "20260507T032419Z"
    - from: "tsc_cycle/v4_gates/phase7_baseline.py"
      to: "/home/samuel/dgx-spark-setup/.venv/bin/python"
      via: "environment evidence package matrix"
      pattern: "dgx-spark-setup"
---

<objective>
实现 v4 Phase 7 baseline/environment/read-only gate，锁定 Qwen3-4B 路线并证明不会污染 v1 baseline。

Purpose: 覆盖 BASE-01、BASE-02、BASE-03，在后续 dataset rebuild/training 前阻断选错模型、环境漂移和 frozen baseline 写入风险。
Output: `phase7_baseline.py`、baseline gate 测试、`environment.json` 与 `baseline_readonly.json`。
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md
@/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/env_smoke_v3.py

<interfaces>
Existing v3 parser pattern to reuse structurally but not semantically:
```python
def build_parser() -> argparse.ArgumentParser: ...
def main(argv: list[str] | None = None) -> int: ...
```
Phase 7 must not load 4B weights or install packages. It only records evidence. Constants required by this plan:
```python
EXPECTED_MODEL_ID = "Qwen/Qwen3-4B-Thinking-2507"
FORBIDDEN_MODEL_SUBSTRINGS = ("Qwen3.5", "Qwen3.5-9B", "qwen3.5")
FROZEN_BASELINE_ROOT = Path("/home/samuel/TSC_CYCLE/runs/20260507T032419Z")
PROJECT_VENV = Path("/home/samuel/TSC_CYCLE/.venv/bin/python")
DGX_SETUP_VENV = Path("/home/samuel/dgx-spark-setup/.venv/bin/python")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add baseline gate contract tests</name>
  <files>tests/test_v4_phase7_baseline_gate.py</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md` lines 10-15 for BASE-01..03.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` sections “Recommended Project Structure”, “Pattern 3”, and “Pitfall 4/5”.
    - `/home/samuel/TSC_CYCLE/tsc_cycle/v3_gates/env_smoke_v3.py` for parser/payload style only; do not copy Qwen3.5 defaults.
  </read_first>
  <behavior>
    - `assert_model_id("Qwen/Qwen3-4B-Thinking-2507")` passes; `assert_model_id("Qwen/Qwen3.5-9B")` and any Qwen3.5 string fail.
    - `assert_output_not_in_frozen_root(Path("/home/samuel/TSC_CYCLE/runs/20260507T032419Z/x.json"))` fails before writing.
    - Environment report contains `project_venv`, `dgx_setup_venv`, `run_safe`, `packages`, `warnings`, and `mutation_actions: []`.
    - Baseline report contains before/after snapshots with unchanged `file_count`, key artifact metadata, and actual q4 cache discovery including `gguf_q4_k_m` if present.
  </behavior>
  <action>
    Create `tests/test_v4_phase7_baseline_gate.py` using tmp_path for path guard/output tests and the real frozen root only for read-only stat/snapshot assertions. Tests must lazy-import `tsc_cycle.v4_gates.phase7_baseline` so collection succeeds before implementation. Include a test that monkeypatches output path into the frozen root and expects a raised `ValueError` before any file write. Include a test that the evaluator writes artifacts only under `artifacts/v4/phase7/` when given tmp_path-safe outputs.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_baseline_gate.py</automated>
  </verify>
  <acceptance_criteria>
    RED tests fail only on missing `tsc_cycle.v4_gates.phase7_baseline` symbols, not on collection errors or missing paid/GPU dependencies.
  </acceptance_criteria>
  <done>Baseline gate tests specify exact model ID lock, no-install environment evidence, frozen-root path guard, and before/after read-only snapshot behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model/environment/baseline read-only gate</name>
  <files>tsc_cycle/v4_gates/__init__.py, tsc_cycle/v4_gates/phase7_baseline.py, artifacts/v4/phase7/environment.json, artifacts/v4/phase7/baseline_readonly.json</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/.planning/STATE.md` lines 54-58 for frozen v1 baseline constraints.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-CONTEXT.md` lines 16-25 for locked decisions.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` lines 367-384 for environment availability and missing-dependency handling.
  </read_first>
  <action>
    Create `tsc_cycle/v4_gates/__init__.py` and `tsc_cycle/v4_gates/phase7_baseline.py`. Implement `assert_model_id(model_id)` as an exact equality check against `Qwen/Qwen3-4B-Thinking-2507` plus explicit rejection of Qwen3.5 substrings. Implement environment evidence using stdlib only: record `sys.executable`, existence/version probes for `/home/samuel/TSC_CYCLE/.venv/bin/python` and `/home/samuel/dgx-spark-setup/.venv/bin/python` via `subprocess.run([python, "-c", ...])`, package metadata for `torch`, `transformers`, `bitsandbytes`, `trl`, `pytest`, existence/executable bit for `scripts/dgx_spark/run_safe.sh`, and `mutation_actions: []`; missing bnb/trl in dgx setup venv is a warning, not an install step. Implement `snapshot_baseline_root(root)` with resolved root, mode/write_bits, file_count, latest_mtime_ns, key q4 artifact size/sha256 prefix, documented cache path `gguf_q4km` existence, and discovered q4 cache paths matching `*q4*`. Implement before/after compare and fail if any snapshot field that indicates mutation changes during gate execution. Default artifact outputs: `artifacts/v4/phase7/environment.json` and `artifacts/v4/phase7/baseline_readonly.json`.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_baseline_gate.py</automated>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_baseline --environment-out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/environment.json --baseline-out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/baseline_readonly.json</automated>
  </verify>
  <acceptance_criteria>
    Command exits 0 only when model ID is exact 4B value, environment evidence is recorded without package mutation, and baseline before/after snapshot is unchanged. Output path guard rejects descendants of `runs/20260507T032419Z/` before writing.
  </acceptance_criteria>
  <done>`environment.json` and `baseline_readonly.json` exist under `/home/samuel/TSC_CYCLE/artifacts/v4/phase7/`, include BASE-01/02/03 coverage, and no artifact is written under the frozen v1 root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> filesystem | Output paths can be attacker-controlled and must not target frozen baseline root. |
| local environment -> report | Package/version probes may fail or reveal drift; report must be auditable and non-mutating. |
| frozen v1 root -> gate | Baseline root is read-only evidence source, never output destination. |
| subprocess argv -> Python probes | Environment probes call Python interpreters and must avoid shell interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | T | output path handling | high | mitigate/block | Resolve every output path and reject descendants of `/home/samuel/TSC_CYCLE/runs/20260507T032419Z` before `mkdir` or write. |
| T-07-02-02 | T/R | baseline snapshot | high | mitigate/block | Compare before/after file_count, latest_mtime_ns, and q4 artifact hash/metadata; fail if changed. |
| T-07-02-03 | E | subprocess environment probes | medium | mitigate | Use argv lists only: `[python, "-c", code]`; no `shell=True`, no user-provided code. |
| T-07-02-04 | R | hidden environment drift | medium | mitigate | Record package matrix and warnings in JSON artifacts; set `mutation_actions: []`; do not install/upgrade anything. |
| T-07-02-05 | I | native think leakage | low | transfer | No protocol text is parsed in this plan; Plan 01 and Plan 03 mitigate native think leakage. |
</threat_model>

<verification>
Plan-level commands:
```bash
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_baseline_gate.py
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_baseline --environment-out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/environment.json --baseline-out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/baseline_readonly.json
```
</verification>

<success_criteria>
- BASE-01: report accepts only `Qwen/Qwen3-4B-Thinking-2507` and rejects Qwen3.5.
- BASE-02: environment evidence is read-only; no package install/upgrade action is performed.
- BASE-03: `runs/20260507T032419Z/` before/after snapshot is unchanged and all Phase 7 artifacts are outside that root.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-02-SUMMARY.md`.
</output>
