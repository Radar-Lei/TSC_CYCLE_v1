---
phase: 07-4b-baseline-label-protocol-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - tsc_cycle/tokenizer_check.py
  - tsc_cycle/v4_gates/phase7_tokenizer.py
  - tests/test_tokenizer_check.py
  - tests/test_v4_phase7_tokenizer_audit.py
  - artifacts/v4/phase7/tokenizer_audit.json
autonomous: true
requirements:
  - TAG-01
  - TAG-03
  - TAG-04
user_setup: []
must_haves:
  truths:
    - "Qwen3-4B tokenizer audit records all four custom protocol tags as multi sub-token encodings."
    - "Native <think> and </think> token IDs are dynamically recorded, not hardcoded."
    - "Tokenizer audit output can be consumed by later phases for native think leakage checks."
  artifacts:
    - path: "tsc_cycle/tokenizer_check.py"
      provides: "dynamic tokenizer helper using corrected custom tags"
      exports: ["CUSTOM_TAGS", "NATIVE_THINK_TAGS", "check_tokenizer", "lookup_native_think_ids", "native_think_token_ids"]
    - path: "tsc_cycle/v4_gates/phase7_tokenizer.py"
      provides: "Qwen3-4B tokenizer audit CLI and JSON artifact"
      exports: ["evaluate_tokenizer_audit", "build_parser", "main"]
    - path: "tests/test_v4_phase7_tokenizer_audit.py"
      provides: "fake-tokenizer payload contract plus optional live CLI-safe coverage"
    - path: "artifacts/v4/phase7/tokenizer_audit.json"
      provides: "custom/native tokenization evidence for Phase 8+"
  key_links:
    - from: "tsc_cycle/v4_gates/phase7_tokenizer.py"
      to: "tsc_cycle/tokenizer_check.py"
      via: "check_tokenizer and native_think_token_ids"
      pattern: "check_tokenizer"
    - from: "tsc_cycle/tokenizer_check.py"
      to: "tsc_cycle/prompt_builder.py"
      via: "CUSTOM_TAGS imports corrected TAG_THINK_CLOSE from Plan 01"
      pattern: "TAG_THINK_CLOSE"
---

<objective>
新增 Phase 7 Qwen3-4B tokenizer audit，证明自定义标签多 sub-token，并动态记录 native `<think>`/`</think>` token IDs。

Purpose: 覆盖 TAG-04，同时为 TAG-01/TAG-03 后续 Phase 8-11 泄漏检查提供可审计 token ID 证据。
Output: tokenizer audit CLI、测试、`artifacts/v4/phase7/tokenizer_audit.json`。
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/TSC_CYCLE/.planning/PROJECT.md
@/home/samuel/TSC_CYCLE/.planning/ROADMAP.md
@/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md
@/home/samuel/TSC_CYCLE/.planning/STATE.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-CONTEXT.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md
@/home/samuel/TSC_CYCLE/tsc_cycle/tokenizer_check.py
@/home/samuel/TSC_CYCLE/tests/test_tokenizer_check.py
@/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-01-SUMMARY.md

<interfaces>
Existing tokenizer helper surface:
```python
CUSTOM_TAGS = (TAG_THINK_OPEN, TAG_THINK_CLOSE, TAG_SOLUTION_OPEN, TAG_SOLUTION_CLOSE)
NATIVE_THINK_TAGS = ("<think>", "</think>")
MIN_CUSTOM_TAG_SUBTOKENS = 3
@dataclass
class CheckResult:
    ok: bool
    details: dict
def lookup_native_think_ids(tokenizer) -> dict[str, list[int]]: ...
def native_think_token_ids(tokenizer) -> set[int]: ...
def check_tokenizer(tokenizer, min_custom_subtokens: int = MIN_CUSTOM_TAG_SUBTOKENS) -> CheckResult: ...
def assert_no_native_think_in_ids(token_ids: Iterable[int], native_ids: set[int] | frozenset[int] | None = None) -> None: ...
```
Plan 01 changes `TAG_THINK_CLOSE` to `</end_working_out>`; this plan must use that corrected imported constant, not a duplicate string list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tokenizer audit payload contract tests</name>
  <files>tests/test_v4_phase7_tokenizer_audit.py, tests/test_tokenizer_check.py</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/tsc_cycle/tokenizer_check.py` helper functions.
    - `/home/samuel/TSC_CYCLE/tests/test_tokenizer_check.py` fake tokenizer pattern.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-VALIDATION.md` lines 33-42 for expected test file.
  </read_first>
  <behavior>
    - Fake tokenizer audit returns `ok: true` when each custom tag encodes to at least 3 IDs and native `<think>`/`</think>` each encode to one ID.
    - Payload includes `model_id: "Qwen/Qwen3-4B-Thinking-2507"`, `custom_tags`, `native_think`, `native_think_token_ids`, `min_custom_subtokens`, `chat_template_used: false`, and `requirements_covered` containing `TAG-04`.
    - Any custom tag with fewer than 3 IDs makes `ok: false` and adds a fatal failure.
    - Native IDs are taken from tokenizer output; tests must use non-Qwen constant IDs to prove no hardcoded 151667/151668 dependency.
  </behavior>
  <action>
    Create `tests/test_v4_phase7_tokenizer_audit.py` with fake tokenizer tests that import planned `evaluate_tokenizer_audit`. Update `tests/test_tokenizer_check.py` expectations so the corrected `TAG_THINK_CLOSE` (`</end_working_out>`) is included in `CUSTOM_TAGS` and native think IDs remain dynamic. Do not require network/model download in unit tests; the CLI can support live tokenizer loading, but tests must use fake tokenizer injection.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_tokenizer_audit.py tests/test_tokenizer_check.py</automated>
  </verify>
  <acceptance_criteria>
    RED tests fail only because `tsc_cycle.v4_gates.phase7_tokenizer` is missing or payload fields are absent; no HF download is attempted during unit tests.
  </acceptance_criteria>
  <done>Tokenizer audit tests specify dynamic native ID recording and custom tag multi-subtoken evidence for the corrected four custom tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Qwen3-4B tokenizer audit gate</name>
  <files>tsc_cycle/tokenizer_check.py, tsc_cycle/v4_gates/phase7_tokenizer.py, artifacts/v4/phase7/tokenizer_audit.json</files>
  <read_first>
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-RESEARCH.md` lines 203-214 for dynamic tokenizer ID audit pattern.
    - `/home/samuel/TSC_CYCLE/.planning/REQUIREMENTS.md` line 21 for TAG-04 wording.
    - `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-01-SUMMARY.md` to confirm Plan 01 corrected protocol literals before using imported `CUSTOM_TAGS`.
  </read_first>
  <action>
    Create `tsc_cycle/v4_gates/phase7_tokenizer.py` exposing `evaluate_tokenizer_audit(tokenizer=None, model_id="Qwen/Qwen3-4B-Thinking-2507")`, `build_parser()`, and `main(argv=None)`. If `tokenizer` is None, load `AutoTokenizer.from_pretrained(model_id)`; in tests, accept fake tokenizer injection. Call `check_tokenizer(tokenizer, min_custom_subtokens=3)`, record `custom_tags`, `native_think`, flattened `native_think_token_ids`, `chat_template_used: false`, `model_id`, `requirements_covered: ["TAG-04", "TAG-03", "TAG-01"]`, `fatal_failures`, and `warnings`. Fail if `model_id` is not exactly `Qwen/Qwen3-4B-Thinking-2507` or contains Qwen3.5. Write default output to `artifacts/v4/phase7/tokenizer_audit.json`, and reject output paths under `/home/samuel/TSC_CYCLE/runs/20260507T032419Z` before writing. Keep `tokenizer_check.py` comments aligned with v4/Qwen3-4B and corrected slash close; do not hardcode native think IDs.
  </action>
  <verify>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_tokenizer_audit.py tests/test_tokenizer_check.py</automated>
    <automated>PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_tokenizer --out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/tokenizer_audit.json</automated>
  </verify>
  <acceptance_criteria>
    `tokenizer_audit.json` contains all four corrected custom tags with token-id arrays length >=3, dynamically recorded native `<think>`/`</think>` encodings, flattened native ID list, `chat_template_used: false`, and `ok: true`.
  </acceptance_criteria>
  <done>Tokenizer audit gate is independently runnable and its artifact supplies the native ID set required for later leakage checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokenizer model id -> HF loader | Model ID controls tokenizer loading and must stay locked to Qwen3-4B. |
| tokenizer encodings -> audit report | Token IDs become downstream leakage-check evidence. |
| CLI `--out` -> filesystem | Output path must not target frozen v1 baseline root. |
| native token literals -> data policy | Native `<think>`/`</think>` IDs must be recorded, not accepted as protocol. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | T | model_id handling | high | mitigate/block | Accept only exact `Qwen/Qwen3-4B-Thinking-2507`; reject Qwen3.5 substrings. |
| T-07-03-02 | I/T | native think IDs | high | mitigate | Dynamically record native encodings and flattened IDs; do not hardcode or silently accept native tags. |
| T-07-03-03 | T | output path handling | high | mitigate/block | Resolve `--out` and reject descendants of `/home/samuel/TSC_CYCLE/runs/20260507T032419Z`. |
| T-07-03-04 | R | tokenizer audit evidence | medium | mitigate | Persist JSON with custom tag IDs, native IDs, `chat_template_used:false`, failures, warnings, requirements. |
| T-07-03-05 | E | shell invocation | medium | mitigate | CLI uses argparse and Python module invocation; no shell command construction. |
</threat_model>

<verification>
Plan-level commands:
```bash
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m pytest -q tests/test_v4_phase7_tokenizer_audit.py tests/test_tokenizer_check.py
PYTHONPATH=/home/samuel/TSC_CYCLE /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.v4_gates.phase7_tokenizer --out /home/samuel/TSC_CYCLE/artifacts/v4/phase7/tokenizer_audit.json
```
</verification>

<success_criteria>
- TAG-04: all four custom tags are multi sub-token under Qwen3-4B tokenizer.
- TAG-04/TAG-03: native `<think>`/`</think>` token IDs are dynamically recorded for leakage checks.
- Tokenizer audit artifact is green and outside frozen v1 baseline root.
</success_criteria>

<output>
After completion, create `/home/samuel/TSC_CYCLE/.planning/phases/07-4b-baseline-label-protocol-gate/07-03-SUMMARY.md`.
</output>
