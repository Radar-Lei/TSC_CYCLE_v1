---
phase: 03-assembly-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/glm5/assembler.py
  - tests/test_assembler.py
autonomous: true
requirements:
  - ASM-01
  - ASM-02
  - ASM-03

must_haves:
  truths:
    - "outputs/sft/sft_train.jsonl 包含从 results.jsonl 组装的 messages 格式数据"
    - "每条记录的 assistant content 使用 <start_working_out>...<end_working_out><SOLUTION>...</SOLUTION> 标签"
    - "src/sft/train.py 可以直接加载生成的 sft_train.jsonl 无报错"
  artifacts:
    - path: "src/glm5/assembler.py"
      provides: "GLM-5 结果到 SFT 训练数据的组装脚本"
      exports: ["main", "assemble_sft_record"]
    - path: "outputs/sft/sft_train.jsonl"
      provides: "SFT 训练数据文件"
      contains: "messages"
    - path: "tests/test_assembler.py"
      provides: "组装逻辑单元测试"
  key_links:
    - from: "src/glm5/assembler.py"
      to: "outputs/glm5/results.jsonl"
      via: "读取 Phase 2 输出"
      pattern: "results\\.jsonl"
    - from: "src/glm5/assembler.py"
      to: "outputs/sft/sft_train.jsonl"
      via: "写入 SFT 训练数据"
      pattern: "sft_train\\.jsonl"
    - from: "src/glm5/assembler.py"
      to: "src/data_generator/prompt_builder.py"
      via: "复用 SYSTEM_PROMPT 常量"
      pattern: "from src\\.data_generator\\.prompt_builder import SYSTEM_PROMPT"
---

<objective>
将 Phase 2 的 GLM-5 生成结果 (outputs/glm5/results.jsonl) 组装为 SFT 训练数据格式 (outputs/sft/sft_train.jsonl)，使其可直接被 src/sft/train.py 加载。

Purpose: 这是训练和导出的前置步骤，将 GLM-5 推理链转化为标准训练数据。
Output: src/glm5/assembler.py (组装脚本) + outputs/sft/sft_train.jsonl (训练数据)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/data_generator/prompt_builder.py
@src/sft/train.py
@src/glm5/validator.py
@config/config.json

<interfaces>
<!-- Phase 2 output format (outputs/glm5/results.jsonl, each line): -->
```json
{"prompt": "...", "prediction": {...}, "think": "...", "solution": [...], "metadata": {...}, "think_length": 123, "attempt": 1}
```

<!-- src/data_generator/prompt_builder.py exports: -->
```python
SYSTEM_PROMPT = (
    "你是交通信号配时优化专家。\n"
    "请认真分析预测得到的下个周期各个相位的交通状态，给出下个周期的配时方案，并给出你的推理过程。\n"
    "将推理过程放在 <start_working_out> 和 <end_working_out> 之间。\n"
    "然后，将你的最终方案放在 <SOLUTION> 和 </SOLUTION> 之间。"
)
```

<!-- src/sft/train.py load_sft_data() expects: -->
```python
# 每行: {"messages": [{"role": "system", "content": "..."}, {"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}
# data_path = os.path.join(config["paths"]["sft_data_dir"], "sft_train.jsonl")
# config["paths"]["sft_data_dir"] = "outputs/sft"
```

<!-- solution 在 results.jsonl 中是 list of dict: [{"phase_id": 0, "final": 20}, ...] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 SFT 数据组装脚本 src/glm5/assembler.py</name>
  <files>src/glm5/assembler.py, tests/test_assembler.py</files>
  <read_first>
    - src/data_generator/prompt_builder.py (SYSTEM_PROMPT 常量, 前 25 行)
    - src/sft/train.py (load_sft_data 函数, 行 143-190, 理解数据格式要求)
    - src/glm5/validator.py (ParsedOutput 结构, 前 35 行)
    - config/config.json (paths.sft_data_dir = "outputs/sft")
  </read_first>
  <behavior>
    - Test 1: assemble_sft_record() 将 results.jsonl 单行转换为 messages 格式，包含 system/user/assistant 三个角色
    - Test 2: assistant content 格式为 "<start_working_out>{think}<end_working_out><SOLUTION>{json_array}</SOLUTION>"，其中 json_array 是紧凑 JSON (json.dumps with ensure_ascii=False, separators=(',', ':'))
    - Test 3: solution 列表正确序列化为 JSON 数组字符串 (如 [{"phase_id":0,"final":20},{"phase_id":1,"final":15}])
    - Test 4: 跳过 solution 为空列表的无效记录，打印警告
    - Test 5: 组装统计输出包含总条数、跳过条数
  </behavior>
  <action>
    创建 src/glm5/assembler.py，包含:

    1. `assemble_sft_record(record: dict) -> dict | None`:
       - 从 record 提取 "prompt" (用户输入), "think" (推理链), "solution" (JSON 数组)
       - 从 src.data_generator.prompt_builder 导入 SYSTEM_PROMPT
       - 构建 assistant_content = f"<start_working_out>{think}<end_working_out><SOLUTION>{json.dumps(solution, ensure_ascii=False, separators=(',', ':'))}</SOLUTION>"
       - 返回 {"messages": [{"role": "system", "content": SYSTEM_PROMPT}, {"role": "user", "content": prompt}, {"role": "assistant", "content": assistant_content}]}
       - 如果 solution 为空列表或缺失，返回 None

    2. `main()` CLI 入口:
       - `--input` 默认 "outputs/glm5/results.jsonl"
       - `--output` 默认 "outputs/sft/sft_train.jsonl"
       - 逐行读取 input，调用 assemble_sft_record，写入 output
       - 打印统计: 总条数、成功组装数、跳过数
       - 使用 `if __name__ == "__main__": main()` 入口

    3. 创建 tests/test_assembler.py:
       - 测试 assemble_sft_record 的正常和异常 case
       - 验证 messages 格式、标签格式、JSON 序列化

    遵循项目约定: snake_case, 双引号, Google-style 中文 docstring, argparse CLI。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python -m pytest tests/test_assembler.py -v</automated>
  </verify>
  <acceptance_criteria>
    - src/glm5/assembler.py 存在且可被 `python -m src.glm5.assembler --help` 调用
    - tests/test_assembler.py 所有测试通过
    - assemble_sft_record 输出的 messages 数组有 3 个元素，role 分别为 system/user/assistant
    - assistant content 包含 `<start_working_out>` 和 `<end_working_out>` 和 `<SOLUTION>` 和 `</SOLUTION>`
  </acceptance_criteria>
  <done>assembler.py 通过所有单元测试，CLI --help 可用</done>
</task>

<task type="auto">
  <name>Task 2: 执行组装并验证输出兼容性</name>
  <files>outputs/sft/sft_train.jsonl</files>
  <read_first>
    - src/glm5/assembler.py (Task 1 产出)
    - outputs/glm5/results.jsonl (前 3 行，确认实际数据格式)
    - config/config.json (确认 paths.sft_data_dir)
  </read_first>
  <action>
    1. 运行组装:
       ```
       python -m src.glm5.assembler --input outputs/glm5/results.jsonl --output outputs/sft/sft_train.jsonl
       ```

    2. 验证输出文件:
       - 检查行数 > 0
       - 用 Python 读取前 3 行，验证 JSON 解析成功且包含 "messages" key
       - 验证每条 messages 有 3 个角色 (system, user, assistant)
       - 验证 assistant content 包含所有 4 个标签

    3. 兼容性快速验证 (不需要 GPU，只验证格式):
       ```python
       import json
       with open("outputs/sft/sft_train.jsonl") as f:
           for i, line in enumerate(f):
               record = json.loads(line)
               assert "messages" in record
               assert len(record["messages"]) == 3
               assert record["messages"][0]["role"] == "system"
               assert record["messages"][1]["role"] == "user"
               assert record["messages"][2]["role"] == "assistant"
               content = record["messages"][2]["content"]
               assert "<start_working_out>" in content
               assert "<end_working_out>" in content
               assert "<SOLUTION>" in content
               assert "</SOLUTION>" in content
               if i >= 9:
                   break
       print("格式验证通过")
       ```

    4. 打印统计信息: 总条数、assistant content 平均长度
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python -c "
import json
count = 0
with open('outputs/sft/sft_train.jsonl') as f:
    for line in f:
        r = json.loads(line)
        assert 'messages' in r and len(r['messages']) == 3
        c = r['messages'][2]['content']
        assert '<start_working_out>' in c and '<SOLUTION>' in c
        count += 1
print(f'OK: {count} records validated')
assert count > 0
"</automated>
  </verify>
  <acceptance_criteria>
    - outputs/sft/sft_train.jsonl 存在且行数 > 0
    - 每行都是合法 JSON，包含 "messages" 字段
    - 所有记录的 messages 有 system/user/assistant 三个角色
    - assistant content 包含 4 个必要标签
    - 文件路径与 config.json 中 paths.sft_data_dir + "sft_train.jsonl" 一致
  </acceptance_criteria>
  <done>sft_train.jsonl 生成完毕，格式验证全部通过，记录数 > 0</done>
</task>

</tasks>

<verification>
- outputs/sft/sft_train.jsonl 存在且每行格式正确
- src/glm5/assembler.py 可独立运行
- 数据格式与 src/sft/train.py 的 load_sft_data() 兼容
</verification>

<success_criteria>
- sft_train.jsonl 生成，记录数 > 0
- 所有记录的 messages 格式正确 (system/user/assistant)
- assistant content 使用正确的标签格式
- 单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-assembly-export/03-01-SUMMARY.md`
</output>
