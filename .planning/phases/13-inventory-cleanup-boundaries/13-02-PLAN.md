---
phase: 13-inventory-cleanup-boundaries
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
  - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md
  - tests/test_cleanup_inventory.py
  - tsc_cycle/cleanup_inventory.py
autonomous: false
requirements:
  - INV-01
  - INV-02
must_haves:
  truths:
    - "Maintainer can open a human-readable cleanup map and see classifications for root, data, artifacts, runs, planning, and tests."
    - "Maintainer can see explicit keep/archive/remove rationale for every high-impact file group before Phase 15 cleanup."
    - "Maintainer can distinguish canonical v4 no-delete assets from legacy, temporary, and removable candidates without inspecting historical phase directories."
  artifacts:
    - path: ".planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md"
      provides: "maintainer-facing cleanup boundary report"
    - path: "tests/test_cleanup_inventory.py"
      provides: "tests validating markdown/report coverage against JSON inventory"
    - path: "tsc_cycle/cleanup_inventory.py"
      provides: "markdown report generation CLI support"
  key_links:
    - from: ".planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md"
      to: ".planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json"
      via: "generated markdown mirrors JSON entries and group summaries"
      pattern: "Inventory & Cleanup Boundaries"
    - from: "tests/test_cleanup_inventory.py"
      to: ".planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md"
      via: "pytest asserts required sections and canonical asset names appear"
      pattern: "canonical v4"
---

<objective>
Create the maintainer-facing Phase 13 cleanup boundary report from the inventory JSON and add review validation.

Purpose: Phase 13 is only complete when a maintainer can read explicit keep/archive/remove rationale for high-impact groups, not just when a JSON file exists. This plan renders the human-readable inventory and blocks cleanup until the report is reviewed.

Output: `.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md`, markdown coverage tests, and a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-inventory-cleanup-boundaries/13-CONTEXT.md
@.planning/phases/13-inventory-cleanup-boundaries/13-RESEARCH.md
@.planning/phases/13-inventory-cleanup-boundaries/13-VALIDATION.md
@.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
@tsc_cycle/cleanup_inventory.py
@tests/test_cleanup_inventory.py

<interfaces>
Use the `tsc_cycle.cleanup_inventory` interface created by Plan 13-01.

Extend required module contract:
- `write_inventory_markdown(inventory: dict, output_path: Path | str) -> None`: writes the human-readable inventory report.
- `main(argv: list[str] | None = None) -> int`: CLI supports `--output-md` in addition to `--output-json`.

Required markdown sections:
- `# Phase 13 Inventory & Cleanup Boundaries`
- `## Scope and Non-Destructive Guarantee`
- `## Required Group Summary`
- `## Canonical v4.0 No-Delete Assets`
- `## High-Impact Cleanup Boundaries`
- `## Legacy / Temporary / Removable Candidates`
- `## Phase 15 Preconditions`

The markdown must mirror JSON classifications and actions; it must not invent a broader cleanup action than the JSON permits.
</interfaces>

Phase 13 must not implement deferred items:
- No destructive archive/remove actions; Phase 15 only.
- No minimal reproduction guide/package documentation; Phase 14 only.
- No post-cleanup verification/handoff; Phase 16 only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add markdown report coverage tests</name>
  <files>tests/test_cleanup_inventory.py</files>
  <read_first>
    - tests/test_cleanup_inventory.py
    - tsc_cycle/cleanup_inventory.py
    - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
    - .planning/phases/13-inventory-cleanup-boundaries/13-CONTEXT.md
    - .planning/phases/13-inventory-cleanup-boundaries/13-RESEARCH.md
    - .planning/phases/13-inventory-cleanup-boundaries/13-VALIDATION.md
  </read_first>
  <behavior>
    - Test markdown generation produces all required section headings listed in the interface block.
    - Test markdown includes required group names: root, data, artifacts, runs, planning, tests.
    - Test markdown includes canonical v4.0 asset paths and marks them with keep/no-delete language.
    - Test markdown includes high-impact fields `classification`, `recommended_action`, `phase15_allowed`, `rationale`, `risk_if_deleted`, and `evidence_paths` in human-readable table or list form.
    - Test markdown does not contain `.env` values, API key strings, or serialized local secret contents.
  </behavior>
  <action>Extend `tests/test_cleanup_inventory.py` with tests for `write_inventory_markdown` and CLI `--output-md`. Use a temporary output path under pytest `tmp_path` for tests. Assert that the report covers INV-01 group names and INV-02 high-impact rationale fields. Assert canonical v4.0 assets are visible in the report as keep/no-delete. Assert deferred Phase 15 destructive actions are not executed by the markdown path.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE &amp;&amp; if /home/samuel/TSC_CYCLE/.venv/bin/pytest tests/test_cleanup_inventory.py -q; then echo 'Expected RED failure for missing markdown support' >&amp;2; exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cleanup_inventory.py` includes markdown-specific tests for section headings, group coverage, canonical no-delete visibility, high-impact rationale visibility, and secret-content exclusion.
    - RED verification command exits 0 only when pytest fails because `write_inventory_markdown` or CLI `--output-md` is absent.
    - Tests do not require GPU/model runtime imports.
  </acceptance_criteria>
  <done>Markdown/report behavior is test-covered before or alongside implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and generate human-readable inventory report</name>
  <files>tsc_cycle/cleanup_inventory.py, .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json, .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md, tests/test_cleanup_inventory.py</files>
  <read_first>
    - tsc_cycle/cleanup_inventory.py
    - tests/test_cleanup_inventory.py
    - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
    - .planning/phases/13-inventory-cleanup-boundaries/13-CONTEXT.md
    - .planning/phases/13-inventory-cleanup-boundaries/13-RESEARCH.md
    - .planning/phases/13-inventory-cleanup-boundaries/13-VALIDATION.md
  </read_first>
  <action>Add `write_inventory_markdown` to `tsc_cycle/cleanup_inventory.py` and extend `main` with `--output-md`. The markdown report must render required group summary counts, canonical v4.0 no-delete assets, high-impact cleanup boundaries with explicit rationale/risk/evidence, and legacy/temporary/removable candidates. Use conservative wording: `remove_candidate` only for cache/local temporary groups already represented that way in JSON; ambiguous v3/v1/raw response/model output groups must be `archive_candidate`, `keep_or_archive`, or `manual_review_before_remove`. Generate `.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md` from the current JSON/inventory build. Do not create Phase 14 reproduction-guide content, do not archive/remove files, and do not run post-cleanup verification work reserved for Phase 16.</action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE &amp;&amp; /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.cleanup_inventory --repo-root /home/samuel/TSC_CYCLE --output-json /home/samuel/TSC_CYCLE/.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json --output-md /home/samuel/TSC_CYCLE/.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md &amp;&amp; /home/samuel/TSC_CYCLE/.venv/bin/pytest tests/test_cleanup_inventory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md` exists.
    - Markdown contains the seven required headings from this plan.
    - Markdown names all required groups and all canonical v4.0 no-delete assets.
    - Markdown includes explicit `classification`, `recommended_action`, `phase15_allowed`, `rationale`, `risk_if_deleted`, and `evidence_paths` for high-impact groups.
    - Markdown states Phase 13 is non-destructive and Phase 15 must consume the inventory before archive/remove actions.
    - Quick pytest command passes.
  </acceptance_criteria>
  <done>Human-readable cleanup boundary report exists and is validated against the inventory JSON and tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of cleanup boundary rationale</name>
  <files>.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md, .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json</files>
  <read_first>
    - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md
    - .planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
    - .planning/phases/13-inventory-cleanup-boundaries/13-CONTEXT.md
    - .planning/phases/13-inventory-cleanup-boundaries/13-RESEARCH.md
  </read_first>
  <action>Pause after automated generation and validation so the maintainer can review the Phase 13 cleanup boundary rationale. Do not perform cleanup, archive, deletion, movement, or rewrite work during this checkpoint. If the maintainer reports unclear or unsafe classifications, update only `tsc_cycle/cleanup_inventory.py`, `tests/test_cleanup_inventory.py`, `inventory.json`, and `inventory.md` to correct the rationale and rerun the automated pytest command before asking again.</action>
  <what-built>Claude generated the Phase 13 machine-readable inventory JSON and maintainer-facing Markdown cleanup boundary report without deleting, moving, archiving, or rewriting existing repository assets.</what-built>
  <how-to-verify>
    1. Open `/home/samuel/TSC_CYCLE/.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md`.
    2. Confirm the report covers root, data, artifacts, runs, planning, and tests groups.
    3. Confirm canonical v4.0 assets, including `/home/samuel/TSC_CYCLE/runs/v4.0-4B-20260509T184844Z/gguf/model.q4_K_M.gguf` and `/home/samuel/TSC_CYCLE/reality_test.log`, are marked keep/no-delete.
    4. Confirm every high-impact group has clear classification, recommended action, Phase 15 allowance, rationale, risk if deleted, and evidence paths.
    5. Confirm no Phase 13 output asks you to delete/archive anything immediately; destructive cleanup remains deferred to Phase 15.
  </how-to-verify>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE &amp;&amp; /home/samuel/TSC_CYCLE/.venv/bin/pytest tests/test_cleanup_inventory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Automated pytest command passes immediately before checkpoint handoff.
    - User replies `approved` if the rationale is clear enough for Phase 14/15 planning.
    - If user reports unclear or unsafe classifications, executor updates JSON/Markdown/tests before completing this plan.
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe specific groups whose classification/rationale must be corrected.</resume-signal>
  <done>Maintainer has reviewed and approved the Phase 13 cleanup boundary rationale, or requested concrete corrections that are applied before plan completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory JSON -> markdown report | Machine-readable metadata is rendered into human-facing cleanup guidance. |
| markdown report -> maintainer decision | Cleanup guidance may influence later destructive Phase 15 actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | markdown/report generation | mitigate | Report generation writes only Phase 13 inventory markdown and JSON paths; no delete/move/archive actions are implemented. |
| T-13-02 | Information Disclosure | human-readable report | mitigate | Tests assert `.env` values, API key patterns, and local secret contents are absent; report uses metadata-only summaries for local/ignored paths. |
| T-13-03 | Tampering / Repudiation | canonical v4 no-delete report section | mitigate | Markdown tests assert canonical v4.0 assets appear under keep/no-delete guidance and checkpoint requires human confirmation. |
| T-13-04 | Tampering / Information Disclosure | JSON-to-markdown path rendering | mitigate | Markdown is generated from repo-relative inventory entries already guarded by Plan 13-01 outside-root path checks. |
</threat_model>

<verification>
Overall checks:
- `cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.cleanup_inventory --repo-root /home/samuel/TSC_CYCLE --output-json /home/samuel/TSC_CYCLE/.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json --output-md /home/samuel/TSC_CYCLE/.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md`
- `cd /home/samuel/TSC_CYCLE && /home/samuel/TSC_CYCLE/.venv/bin/pytest tests/test_cleanup_inventory.py -q`
- Human checkpoint approval for rationale quality.
</verification>

<success_criteria>
- INV-01: Maintainer can view generated JSON and Markdown inventory covering root, data, artifacts, runs, planning, and tests classifications.
- INV-02: Maintainer can read explicit keep/archive/remove rationale for every high-impact file group before destructive cleanup.
- Canonical v4.0 no-delete assets are visible in the report.
- Deferred Phase 14/15/16 work is not implemented in Phase 13.
</success_criteria>

<output>
After completion, create `.planning/phases/13-inventory-cleanup-boundaries/13-02-SUMMARY.md`.
</output>
