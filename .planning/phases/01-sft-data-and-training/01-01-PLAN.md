---
phase: 01-sft-data-and-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/sample_selector.py
autonomous: true

must_haves:
  truths:
    - "从 1588 条 train.jsonl 中选出约 100 条样本"
    - "样本覆盖所有 34 个交叉口(tl_id)"
    - "样本覆盖不同饱和度区间(zero/low/medium/high)"
    - "样本覆盖两个场景(arterial4x4_10 和 chengdu)"
    - "样本覆盖不同相位数(2/3/4 相位)"
  artifacts:
    - path: "src/scripts/sample_selector.py"
      provides: "样本抽取脚本"
      min_lines: 80
    - path: "outputs/sft/sampled_100.jsonl"
      provides: "抽取后的 100 条样本"
  key_links:
    - from: "src/scripts/sample_selector.py"
      to: "outputs/data/train.jsonl"
      via: "读取原始训练数据"
      pattern: "outputs/data/train.jsonl"
    - from: "src/scripts/sample_selector.py"
      to: "outputs/sft/sampled_100.jsonl"
      via: "写出抽取结果"
      pattern: "outputs/sft/sampled_100.jsonl"
---

<objective>
从 train.jsonl (1588 条) 中均匀抽取约 100 条样本，确保覆盖所有交叉口、不同饱和度分布和场景。

Purpose: 为后续 AI 生成 think+solution 内容提供代表性样本集 (SFT-01)
Output: outputs/sft/sampled_100.jsonl
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@outputs/data/train.jsonl
@config/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建样本抽取脚本并执行抽取</name>
  <files>src/scripts/sample_selector.py</files>
  <action>
创建 Python 脚本 src/scripts/sample_selector.py，功能如下：

1. 读取 outputs/data/train.jsonl (共 1588 条)
2. 抽取策略（综合策略，per user decision）：
   - 第一步：确保覆盖所有 34 个 tl_id，每个 tl_id 至少抽 1 条 -> 34 条
   - 第二步：在剩余配额(约 66 条)中，按饱和度分布补充：
     - 优先补充 high (saturation >= 1.0) 场景，因为这类样本较少(448个相位)但训练价值高
     - 确保 medium (0.5-1.0)、low (0-0.5)、zero (0.0) 都有合理覆盖
     - 饱和度按样本最大相位饱和度计算（一个样本有多个相位，取最大值作为该样本的饱和度代表）
   - 第三步：确保两个场景(arterial4x4_10, chengdu)各约 50 条
   - 第四步：确保 2/3/4 相位的样本都有覆盖（4 相位只有 14 条，全选或选大部分）
3. 随机种子固定为 42，确保可复现
4. 输出到 outputs/sft/sampled_100.jsonl，每行一个 JSON 对象，格式与 train.jsonl 一致
5. 打印抽取统计：总数、按场景分布、按 tl_id 分布、按饱和度区间分布、按相位数分布

脚本应支持命令行运行：
```
python3 -m src.scripts.sample_selector --input outputs/data/train.jsonl --output outputs/sft/sampled_100.jsonl --count 100
```

注意：创建 outputs/sft/ 目录如果不存在。
  </action>
  <verify>
运行脚本：
```
python3 -m src.scripts.sample_selector --input outputs/data/train.jsonl --output outputs/sft/sampled_100.jsonl --count 100
```
验证：
1. outputs/sft/sampled_100.jsonl 存在且行数在 90-120 之间
2. 统计输出显示覆盖所有 34 个 tl_id
3. 两个场景都有样本
4. 饱和度各区间都有覆盖
5. 每行都是合法 JSON
  </verify>
  <done>
outputs/sft/sampled_100.jsonl 包含约 100 条样本，覆盖所有 tl_id、两个场景、不同饱和度区间和相位数，统计信息已打印确认。
  </done>
</task>

</tasks>

<verification>
- `wc -l outputs/sft/sampled_100.jsonl` 输出在 90-120 之间
- `python3 -c "import json; [json.loads(l) for l in open('outputs/sft/sampled_100.jsonl')]"` 无报错
- 统计确认分布合理
</verification>

<success_criteria>
从 train.jsonl 成功抽取约 100 条代表性样本，保存为 outputs/sft/sampled_100.jsonl，覆盖所有交叉口和饱和度分布。
</success_criteria>

<output>
After completion, create `.planning/phases/01-sft-data-and-training/01-01-SUMMARY.md`
</output>
