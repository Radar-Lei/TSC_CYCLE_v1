---
phase: 14-canonical-v4-reproduction-package
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_v4_reproduction_package.py
  - tsc_cycle/reproduction_manifest.py
  - reproduction/v4.0-qwen3-4b-9k-manifest.json
  - reproduction/v4.0-qwen3-4b-9k-guide.md
autonomous: true
requirements:
  - REPRO-01
  - REPRO-03
  - DOC-01
must_haves:
  truths:
    - "Reproducer can start from repo-level reproduction/v4.0-qwen3-4b-9k-manifest.json or reproduction/v4.0-qwen3-4b-9k-guide.md and find the canonical v4.0 Qwen3-4B 9k package without opening .planning/phases/ as the source of truth."
    - "Reproducer can identify required v4 evidence, required v4 data/source inputs, optional rebuild caches, obsolete legacy artifacts, and local temporary paths as separate categories."
    - "Reproducer can see current SHA-256 hashes, sizes, line/count facts, final artifact names, and verification commands for the canonical v4 package."
    - "Phase 14 code recomputes hashes/counts from disk and performs no delete, archive, move, retrain, dataset regeneration, or model inference action."
  artifacts:
    - path: "tests/test_v4_reproduction_package.py"
      provides: "Fail-closed pytest contract for REPRO-01, REPRO-03, DOC-01, and T-14 security constraints"
    - path: "tsc_cycle/reproduction_manifest.py"
      provides: "Deterministic stdlib manifest/guide builder and validator CLI"
      exports: ["build_package_manifest", "write_manifest_json", "write_guide_markdown", "validate_manifest_against_disk", "main"]
    - path: "reproduction/v4.0-qwen3-4b-9k-manifest.json"
      provides: "Repo-level machine-readable canonical v4.0 reproduction package boundary"
    - path: "reproduction/v4.0-qwen3-4b-9k-guide.md"
      provides: "Repo-level human reproduction entry point"
  key_links:
    - from: "tsc_cycle/reproduction_manifest.py"
      to: ".planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json"
      via: "read-only Phase 13 inventory input per CONTEXT Phase 13 Inputs"
      pattern: "inventory.*json"
    - from: "reproduction/v4.0-qwen3-4b-9k-manifest.json"
      to: "runs/v4.0-4B-20260509T184844Z/gguf/model.q4_K_M.gguf"
      via: "required final q4_K_M asset entry with recomputed sha256"
      pattern: "model\\.q4_K_M\\.gguf"
    - from: "tests/test_v4_reproduction_package.py"
      to: "tsc_cycle/reproduction_manifest.py"
      via: "pytest imports builder/validator and recomputes disk facts"
      pattern: "build_package_manifest|validate_manifest_against_disk"
---

<objective>
Create the canonical repo-level v4.0 Qwen3-4B 9k reproduction package boundary.

Purpose: Phase 14 promotes the Phase 13 cleanup boundary into a reproducer-facing manifest and guide so external users do not need historical GSD phase archaeology to find the shipped v4.0 model, evidence, data inputs, counts, hashes, and verification commands.

Output: A tested stdlib builder/validator, repo-level manifest JSON, and repo-level guide Markdown under `reproduction/`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-canonical-v4-reproduction-package/14-CONTEXT.md
@.planning/phases/14-canonical-v4-reproduction-package/14-RESEARCH.md
@.planning/phases/14-canonical-v4-reproduction-package/14-VALIDATION.md
@.planning/phases/13-inventory-cleanup-boundaries/13-01-SUMMARY.md
@.planning/phases/13-inventory-cleanup-boundaries/13-02-SUMMARY.md
@.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
@.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md
@tsc_cycle/cleanup_inventory.py
@tsc_cycle/hashing.py
@tests/test_cleanup_inventory.py
@pyproject.toml

<interfaces>
Existing contracts executor should reuse directly:

From `tsc_cycle.cleanup_inventory`:
```python
CANONICAL_V4_ASSETS: dict[str, str]
def resolve_repo_path(path: Path | str, repo_root: Path | str = Path.cwd()) -> Path
def build_inventory(repo_root: Path | str = Path.cwd()) -> dict[str, Any]
```

From `tsc_cycle.hashing`:
```python
def canonical_json(obj: Any) -> str
def sha256_hex(s: str) -> str
```

Required Phase 14 CLI contract to create in `tsc_cycle/reproduction_manifest.py`:
```python
def build_package_manifest(repo_root: Path | str, inventory_path: Path | str | None = None) -> dict[str, Any]
def write_manifest_json(manifest: dict[str, Any], output_path: Path | str) -> None
def write_guide_markdown(manifest: dict[str, Any], output_path: Path | str) -> None
def validate_manifest_against_disk(manifest: dict[str, Any], repo_root: Path | str) -> list[str]
def main(argv: list[str] | None = None) -> int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED reproduction package contract tests</name>
  <files>tests/test_v4_reproduction_package.py</files>
  <read_first>
    - @.planning/REQUIREMENTS.md
    - @.planning/phases/14-canonical-v4-reproduction-package/14-CONTEXT.md
    - @.planning/phases/14-canonical-v4-reproduction-package/14-RESEARCH.md
    - @.planning/phases/14-canonical-v4-reproduction-package/14-VALIDATION.md
    - @.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
    - @tests/test_cleanup_inventory.py
  </read_first>
  <behavior>
    - Test 1: `test_manifest_lists_required_v4_assets` imports `build_package_manifest(REPO_ROOT)` and asserts the manifest includes required final evidence paths: `runs/v4.0-4B-20260509T184844Z/gguf/model.q4_K_M.gguf`, `reality_test.log`, `artifacts/v4/phase8/phase8_gate_report.json`, `runs/v4.0-4B-20260509T184844Z/phase9_sft_report.json`, `runs/v4.0-4B-20260509T184844Z/phase10_gguf_report.json`, `artifacts/v4/phase11/phase11_gate_report.json`, `artifacts/v4/phase12/phase12_report.json`, `artifacts/v4/phase12/manifest.json`, and `artifacts/v4/phase12/per_sample.jsonl` per CONTEXT specifics.
    - Test 2: The same test asserts required source/data inputs include `data/v4/phase8/labeled_merged.jsonl`, `data/v4/phase8/splits/manifest.json`, `data/v4/phase8/splits/train.index.jsonl`, `data/v4/phase8/splits/val.index.jsonl`, and `data/v4/phase8/splits/ood_val.index.jsonl`; tokenized Arrow files are not categorized as required final evidence.
    - Test 3: `test_manifest_hashes_and_counts_match_disk` recomputes SHA-256 using chunked binary reads and checks manifest `sha256`, `size_bytes`, and count fields against current disk files; it must assert 426 Phase 12 outputs and 9,501 labeled merged rows using disk facts, not copied prose.
    - Test 4: `test_manifest_classifies_required_optional_and_obsolete_assets` asserts categories include `required_evidence`, `required_source`, `optional_rebuild_cache`, `optional_audit`, `obsolete_legacy`, and `local_temporary`; it asserts the v1 q4_K_M path is not the required v4 target.
    - Test 5: `test_guide_exposes_hashes_counts_and_commands` verifies the guide contains the manifest filename, final q4_K_M path and hash, `reality_test.log`, `426`, `9501`, and commands for `python -m tsc_cycle.reproduction_manifest --check` and the Phase 14 pytest file.
    - Test 6: `test_reproduction_manifest_is_non_destructive_and_secret_safe` inspects `tsc_cycle.reproduction_manifest` source to forbid destructive tokens `unlink(`, `rmtree(`, `remove(`, `rename(`, `replace(`, `shutil.move`, and `shutil.rmtree`, and asserts serialized manifest/guide text does not contain `OPENAI_API_KEY=`, `sk-`, `.env` contents, `.venv/`, `.claude/`, or worktree payloads.
    - Test 7: `test_repo_relative_path_guard_rejects_outside_root` covers T-14-01 by asserting outside-root relative and absolute paths raise `ValueError`.
  </behavior>
  <action>Create `tests/test_v4_reproduction_package.py` with the RED tests above. Use the existing `tests/test_cleanup_inventory.py` style: `REPO_ROOT = Path(__file__).resolve().parents[1]`, stdlib assertions, no network/model/GPU imports. Tests must encode Phase 14 source-of-truth behavior: `.planning/phases/` may appear only as builder provenance input, never as a reproducer-facing required package path. Per CONTEXT scope constraints, do not write tests that expect deletion/archive/move/retrain/regeneration/inference.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python - <<'PY'
import subprocess, sys
cmd = ["/home/samuel/TSC_CYCLE/.venv/bin/pytest", "/home/samuel/TSC_CYCLE/tests/test_v4_reproduction_package.py", "-q"]
result = subprocess.run(cmd, cwd="/home/samuel/TSC_CYCLE", text=True, capture_output=True)
combined = result.stdout + result.stderr
assert result.returncode != 0, "RED tests unexpectedly passed before implementation"
assert "reproduction_manifest" in combined or "ModuleNotFoundError" in combined or "ImportError" in combined or "AssertionError" in combined, combined
print("RED reproduction package tests fail for the expected missing implementation/artifacts")
PY</automated>
  </verify>
  <acceptance_criteria>Test file exists, names REPRO-01/REPRO-03/DOC-01 behaviors concretely, includes hash/count recomputation, includes non-destructive/source-inspection checks, and fails before the builder/artifacts are implemented.</acceptance_criteria>
  <done>Phase 14 has executable RED coverage for required assets, category boundaries, guide content, hash/count freshness, repo-root path guards, secret exclusion, and non-destructive behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic reproduction manifest builder and validator CLI</name>
  <files>tsc_cycle/reproduction_manifest.py</files>
  <read_first>
    - @tsc_cycle/cleanup_inventory.py
    - @tsc_cycle/hashing.py
    - @.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.json
    - @artifacts/v4/phase12/phase12_report.json
    - @artifacts/v4/phase12/manifest.json
    - @data/v4/phase8/splits/manifest.json
    - @artifacts/v4/phase11/phase11_gate_report.json
    - @runs/v4.0-4B-20260509T184844Z/phase10_gguf_report.json
    - @runs/v4.0-4B-20260509T184844Z/phase9_sft_report.json
  </read_first>
  <action>Implement `tsc_cycle/reproduction_manifest.py` using Python stdlib only. Import and reuse `CANONICAL_V4_ASSETS` and `resolve_repo_path` from `tsc_cycle.cleanup_inventory` so Phase 14 preserves Phase 13 no-delete boundaries per CONTEXT Phase 13 Inputs. Create constants for: required evidence paths from `CANONICAL_V4_ASSETS`; required source paths `data/v4/phase8/labeled_merged.jsonl`, `data/v4/phase8/splits/manifest.json`, `data/v4/phase8/splits/train.index.jsonl`, `data/v4/phase8/splits/val.index.jsonl`, `data/v4/phase8/splits/ood_val.index.jsonl`; optional rebuild cache paths `data/v4/phase8/tokenized/train.arrow`, `data/v4/phase8/tokenized/val.arrow`, `data/v4/phase8/tokenized/ood_val.arrow`; optional audit paths from Phase 11 matrix/metrics/per_sample reports; obsolete legacy paths `runs/20260507T032419Z/gguf/model.q4_K_M.gguf`, `data/v3`, `artifacts/v3`, `runs/v3.0-gates`, `raw_responses`, and `reality.log`; and local temporary metadata paths from Phase 13 inventory. For each file asset, compute `exists`, `size_bytes`, `sha256` via chunked binary reads, and `line_count` for `.jsonl`/`.log`/`.md`/`.py` text files. Read JSON reports only for structured count fields and final artifact names; never serialize file payloads. Manifest top-level fields must include `schema_version`, `package_id: v4.0-qwen3-4b-9k`, `source_of_truth` pointing to the two repo-level `reproduction/` files, `phase14_scope` stating no delete/archive/move/retrain/regenerate/inference, `assets`, `verification_commands`, and `provenance_inputs`. Use sorted deterministic JSON writing with date-only or stable generation metadata so repeated generation does not churn except when disk facts change. Provide `--repo-root`, `--inventory`, `--output-json`, `--output-guide`, and `--check` CLI options. `--check` must read an existing manifest, recompute disk facts, return nonzero if any required asset is missing or hash/count/size differs, and print concise mismatch lines. Do not call shell commands or destructive filesystem APIs.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python - <<'PY'
from pathlib import Path
import tempfile
from tsc_cycle.reproduction_manifest import build_package_manifest, validate_manifest_against_disk, write_manifest_json, write_guide_markdown
repo = Path('/home/samuel/TSC_CYCLE')
manifest = build_package_manifest(repo)
assert manifest['package_id'] == 'v4.0-qwen3-4b-9k'
errors = validate_manifest_against_disk(manifest, repo)
assert errors == [], errors
with tempfile.TemporaryDirectory() as tmp:
    out_json = Path(tmp) / 'manifest.json'
    out_md = Path(tmp) / 'guide.md'
    write_manifest_json(manifest, out_json)
    write_guide_markdown(manifest, out_md)
    assert out_json.exists() and out_md.exists()
PY</automated>
  </verify>
  <acceptance_criteria>`tsc_cycle.reproduction_manifest` exposes the required functions and CLI, uses repo-root path guards, recomputes hashes/counts/sizes from disk, reads Phase 13 inventory as input, and performs no destructive or model/data-generation action.</acceptance_criteria>
  <done>Builder can construct and validate the v4.0 reproduction package manifest in memory and render both JSON and Markdown outputs to a temporary location.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate repo-level manifest and guide, then pass Phase 14 contracts</name>
  <files>reproduction/v4.0-qwen3-4b-9k-manifest.json, reproduction/v4.0-qwen3-4b-9k-guide.md</files>
  <read_first>
    - @tsc_cycle/reproduction_manifest.py
    - @tests/test_v4_reproduction_package.py
    - @.planning/phases/14-canonical-v4-reproduction-package/14-CONTEXT.md
    - @.planning/phases/13-inventory-cleanup-boundaries/inventory/inventory.md
  </read_first>
  <action>Create the repo-level `reproduction/` directory and run the new CLI to write `reproduction/v4.0-qwen3-4b-9k-manifest.json` and `reproduction/v4.0-qwen3-4b-9k-guide.md`. The guide must start with the canonical v4.0 package identity, name the final q4_K_M path, state that v1/v3/raw outputs are not the v4 target, list required vs optional categories, show hashes/counts by pointing to the manifest, and include exact verification commands. The guide may mention Phase 13 inventory as provenance, but must not instruct the reproducer to inspect `.planning/phases/` as the source of truth. Per CONTEXT scope constraints, do not delete, archive, move, retrain, regenerate datasets, or run model inference.</action>
  <verify>
    <automated>/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.reproduction_manifest --repo-root /home/samuel/TSC_CYCLE --check /home/samuel/TSC_CYCLE/reproduction/v4.0-qwen3-4b-9k-manifest.json && /home/samuel/TSC_CYCLE/.venv/bin/pytest /home/samuel/TSC_CYCLE/tests/test_v4_reproduction_package.py /home/samuel/TSC_CYCLE/tests/test_cleanup_inventory.py -q</automated>
  </verify>
  <acceptance_criteria>Repo-level manifest and guide exist outside `.planning/phases/`, pass validator and pytest contracts, expose hashes/counts/final artifact names/verification commands, and keep required/optional/obsolete/local categories unambiguous.</acceptance_criteria>
  <done>REPRO-01, REPRO-03, and DOC-01 are satisfied by tested repo-level reproduction package artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/user path -> repository filesystem | `--repo-root`, `--inventory`, `--output-json`, `--output-guide`, and `--check` paths cross from user input into filesystem reads/writes. |
| Phase 13 inventory/report JSON -> Phase 14 manifest | Existing planning artifacts are consumed as untrusted structured input and must not be copied blindly into source-of-truth facts. |
| Local repository files -> manifest/guide serialization | File metadata is serialized; secret/cache/local-state payloads must not be serialized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `tsc_cycle/reproduction_manifest.py` path resolution | mitigate | Reuse or mirror `resolve_repo_path` so every input/output path must resolve under `/home/samuel/TSC_CYCLE`; add pytest coverage for `../outside` and `/tmp/outside-tsc-cycle`. |
| T-14-02 | Tampering | Manifest hashes/counts | mitigate | Recompute SHA-256, file sizes, and line/count fields from disk in builder and tests; `--check` returns nonzero when manifest facts are stale or forged. |
| T-14-03 | Information Disclosure | Manifest/guide serialization | mitigate | Serialize metadata only; tests assert no `OPENAI_API_KEY=`, `sk-`, `.env` contents, `.venv/`, `.claude/`, cache, or worktree payloads appear. |
| T-14-04 | Tampering | Phase 14 cleanup scope | mitigate | Source-inspection tests forbid destructive calls (`unlink`, `rmtree`, `remove`, `rename`, `replace`, `shutil.move`, `shutil.rmtree`); task actions explicitly forbid delete/archive/move/retrain/regenerate/inference. |
| T-14-05 | Repudiation | Reproducer package provenance | mitigate | Manifest includes `provenance_inputs` naming Phase 13 inventory and v4 reports while `source_of_truth` points to repo-level reproduction files, preserving auditability without phase archaeology. |
</threat_model>

<verification>
Overall checks after all tasks:

```bash
/home/samuel/TSC_CYCLE/.venv/bin/python -m tsc_cycle.reproduction_manifest --repo-root /home/samuel/TSC_CYCLE --check /home/samuel/TSC_CYCLE/reproduction/v4.0-qwen3-4b-9k-manifest.json
/home/samuel/TSC_CYCLE/.venv/bin/pytest /home/samuel/TSC_CYCLE/tests/test_v4_reproduction_package.py /home/samuel/TSC_CYCLE/tests/test_cleanup_inventory.py -q
```

Before finalizing, inspect `reproduction/v4.0-qwen3-4b-9k-guide.md` and confirm it is the reproducer-facing entry point and does not tell users to inspect `.planning/phases/` as the source of truth.
</verification>

<success_criteria>
- `reproduction/v4.0-qwen3-4b-9k-manifest.json` exists and names canonical v4.0 Qwen3-4B 9k required evidence, required source data, optional caches/audit files, obsolete legacy files, hashes, sizes, counts, and verification commands.
- `reproduction/v4.0-qwen3-4b-9k-guide.md` exists and gives a concise starting point for external reproducers.
- `tsc_cycle/reproduction_manifest.py --check` passes against the generated manifest.
- Phase 14 pytest contracts pass together with Phase 13 inventory tests.
- No task deletes, archives, moves, retrains, regenerates datasets, or runs model inference.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 14 | Reproducer can locate and understand the minimal v4.0 Qwen3-4B 9k reproduction package without historical phase directories | 14-01 | COVERED | Repo-level `reproduction/` manifest and guide are generated and tested.
REQ | REPRO-01 | Identify canonical v4.0 Qwen3-4B 9k inputs, manifests, reports, final q4_K_M GGUF artifact, and `reality_test.log` without inspecting historical phase directories | 14-01 | COVERED | Required evidence/source tests and manifest entries cover exact paths.
REQ | REPRO-03 | Distinguish required reproduction assets from optional audit artifacts and obsolete v1/v2/v3/v4 intermediate files | 14-01 | COVERED | Manifest categories and tests cover required/optional/obsolete/local boundaries.
REQ | DOC-01 | Start from concise repo-level reproduction guide or manifest naming canonical artifacts, expected hashes/counts, and verification commands | 14-01 | COVERED | Guide and manifest outputs include hashes/counts/commands.
RESEARCH | — | Use deterministic stdlib builder/validator and pytest, no new deps | 14-01 | COVERED | Task 2 implements stdlib builder; tests use pytest.
RESEARCH | — | Consume Phase 13 inventory; do not reclassify from scratch | 14-01 | COVERED | Task 2 imports `CANONICAL_V4_ASSETS` and reads inventory input.
RESEARCH | — | Include dataset inputs, not only final model/reports | 14-01 | COVERED | Task 1/2 require `data/v4/phase8` labeled and split manifest/index paths.
RESEARCH | — | Avoid `.planning/phases/` as reproducer source of truth | 14-01 | COVERED | Tests and guide requirements enforce repo-level source-of-truth files.
RESEARCH | — | Recompute hashes/counts and avoid stale facts | 14-01 | COVERED | Task 2 builder and `--check`; Task 1 tests recompute disk facts.
CONTEXT | Phase 13 Inputs | Use Phase 13 inventory JSON/Markdown and preserve keep/no_delete/local conservative decisions | 14-01 | COVERED | Task actions reference Phase 13 inputs and no-delete preservation.
CONTEXT | Scope Constraints | No delete/archive/move/retrain/regenerate/new capability; cleanup deferred to Phase 15 and verification handoff to Phase 16 | 14-01 | COVERED | All tasks are non-destructive packaging/test work only.
CONTEXT | Deferred Ideas | Destructive cleanup, post-cleanup verification, deployment, imatrix/q5_K_M, thinking ablation | NONE | EXCLUDED | Explicitly out of Phase 14 scope and not implemented.
</source_audit>

<output>
After completion, create `.planning/phases/14-canonical-v4-reproduction-package/14-01-SUMMARY.md`.
</output>
