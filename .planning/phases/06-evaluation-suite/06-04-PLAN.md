---
phase: 06-evaluation-suite
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/
autonomous: true
parallel: false  # wave 2 sequential：等 06-03 释放 llama-server / GPU 后启动
requirements: [EVL-01, EVL-02]
must_haves:
  truths:
    - "GGUF q4_K_M backend 在 600 prompt 上完成 greedy 生成，参数与 hf_bf16 / gguf_bf16 完全一致"
    - "复用 plan 06-03 的 generate_gguf.py 入口（仅 --gguf-path / --backend-label / --cache-dir 三参数变化）"
    - "脚本退出后 llama-server 进程及 GPU 全部释放，wave 3 metrics 阶段可启动"
  artifacts:
    - path: "runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/"
      provides: "600 个 gguf_q4_k_m cache 文件"
  key_links:
    - from: "runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/{sample_id}.json"
      to: "runs/20260507T032419Z/gguf/model.q4_K_M.gguf"
      via: "tsc_cycle.eval.generate_gguf --backend-label gguf_q4_k_m"
      pattern: "model.q4_K_M.gguf"
---

<objective>
复用 plan 06-03 的 `tsc_cycle/eval/generate_gguf.py`，对 q4_K_M GGUF backend 跑同样 600 prompt
生成；产出 `gen_cache/gguf_q4_k_m/{sample_id}.json` × 600。

Purpose: 完成 wave 2 第三 backend；提供量化退化分析的对比基线。
Output: 600 个 gguf_q4_k_m cache 文件 + server log。

注：Phase 5 parity 已显示 q4_K_M 对 HF bf16 MAE=4.51s（>3s 阈值），EVL 阶段会再现此现象，
   metrics + decision plan 据此判断 imatrix 重量化是否触发。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-evaluation-suite/06-CONTEXT.md
@runs/20260507T032419Z/PHASE05_REPORT.md

<interfaces>
依赖 plan 06-03 产出的 `tsc_cycle/eval/generate_gguf.py`（CLI 已参数化）。
GGUF q4_K_M 路径: `runs/20260507T032419Z/gguf/model.q4_K_M.gguf`（Phase 5 EXP-03 产物）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跑通 gguf_q4_k_m backend（600 prompt → 600 cache）</name>
  <files>runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/</files>
  <read_first>
    - tsc_cycle/eval/generate_gguf.py（确认 CLI 签名已支持参数化）
    - runs/20260507T032419Z/gguf/model.q4_K_M.gguf（确认产物存在；ls -lh）
  </read_first>
  <action>
    1. 验证前置：
       ```bash
       test -f runs/20260507T032419Z/gguf/model.q4_K_M.gguf || exit 2
       test $(ls runs/20260507T032419Z/eval/gen_cache/gguf_bf16/*.json | wc -l) -eq 600 || exit 2
       pgrep -f "llama-server" && echo "FAIL: llama-server still running" && exit 2
       ```

    2. 启动 generation（等待 plan 06-03 完全退出后）：
       ```bash
       cd /home/samuel/TSC_CYCLE
       /home/samuel/dgx-spark-setup/.venv/bin/python -m tsc_cycle.eval.generate_gguf \
           --gguf-path runs/20260507T032419Z/gguf/model.q4_K_M.gguf \
           --backend-label gguf_q4_k_m \
           --prompts runs/20260507T032419Z/eval/eval_prompts.jsonl \
           --cache-dir runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m \
           --n-predict 384 \
           2>&1 | tee runs/20260507T032419Z/eval/gen_gguf_q4_k_m.log
       ```

    3. 退出码必须 0；脚本退出后 `nvidia-smi` 应显示 GPU 已释放，无残留 llama-server。

    严禁：传任何与 plan 06-03 不同的 generation 参数（--n-predict / temperature / top_k / seed）；
    临时修改 generate_gguf.py 源码（plan 06-03 已锁定行为）。
  </action>
  <verify>
    <automated>test $(ls runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/*.json 2>/dev/null | wc -l) -eq 600 && /home/samuel/dgx-spark-setup/.venv/bin/python -c "import json,glob; files=glob.glob('runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/*.json'); assert len(files)==600; r=json.load(open(files[0])); assert r['backend']=='gguf_q4_k_m' and 'solution' in r and 'raw_text' in r and r['n_predict']==384, r"</automated>
  </verify>
  <acceptance_criteria>
    - `ls runs/20260507T032419Z/eval/gen_cache/gguf_q4_k_m/*.json | wc -l` == 600
    - 任一 cache 文件 `backend` 字段 == `"gguf_q4_k_m"`
    - 任一 cache 文件 `n_predict` == 384，`seed` == 42
    - server log 文件 `runs/20260507T032419Z/eval/server_gguf_q4_k_m.log` 存在
    - 脚本退出后 `pgrep -f "llama-server"` 无输出
    - 三 backend cache 文件总数：`ls runs/20260507T032419Z/eval/gen_cache/*/*.json | wc -l` == 1800
  </acceptance_criteria>
  <done>三 backend 共 1800 cache 文件齐全，wave 3 metrics 可消费</done>
</task>

</tasks>

<verification>
- 600 cache + backend 字段正确
- 1800 = 600×3 总数验证
- server / GPU 全部释放
</verification>

<success_criteria>
- gen_cache/gguf_q4_k_m/ 600 文件
- 三 backend 1800 文件总数验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-suite/06-04-SUMMARY.md`
</output>
