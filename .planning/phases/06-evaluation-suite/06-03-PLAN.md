---
phase: 06-evaluation-suite
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - tsc_cycle/eval/generate_gguf.py
  - runs/20260507T032419Z/eval/gen_cache/gguf_bf16/
autonomous: true
parallel: false  # wave 2 sequential：等 06-02 释放 GPU 后启动 llama-server
requirements: [EVL-01, EVL-02]
must_haves:
  truths:
    - "GGUF bf16 backend 在 600 prompt 上完成 greedy 生成，参数与 HF bf16 一致"
    - "llama-server 单次启动覆盖全部 600 prompt（不 per-prompt cold-start）"
    - "脚本可同时供 q4_K_M backend 复用（仅 --gguf-path / --backend-label / --cache-dir 三参数差异）"
  artifacts:
    - path: "tsc_cycle/eval/generate_gguf.py"
      provides: "GGUF backend 通用 generation runner（参数化 model 路径与 backend label）"
      min_lines: 100
    - path: "runs/20260507T032419Z/eval/gen_cache/gguf_bf16/"
      provides: "600 个 gguf_bf16 cache 文件"
  key_links:
    - from: "tsc_cycle/eval/generate_gguf.py"
      to: "/home/samuel/llama.cpp/build/bin/llama-server"
      via: "subprocess.Popen + urllib POST /completion"
      pattern: "/home/samuel/llama.cpp/build/bin/llama-server"
---

<objective>
基于 `tsc_cycle/student/parity_gguf.py` 改造为 EVL 通用 GGUF generation runner（参数化 backend label
与 model 路径），先用于 gguf_bf16 backend 的 600 prompt 生成。同一脚本将在 plan 06-04 复用。

Purpose: 一次启动 llama-server 完成 600 prompt（避免 cold-start）；产出 gen_cache/gguf_bf16/。
Output: 600 个 `gen_cache/gguf_bf16/{sample_id}.json` + server log。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-evaluation-suite/06-CONTEXT.md
@tsc_cycle/student/parity_gguf.py
@tsc_cycle/prompt_builder.py

<interfaces>
From tsc_cycle/student/parity_gguf.py:
```python
def _find_free_port() -> int
def _wait_health(port: int, timeout_sec: int) -> bool
def _post_completion(port, prompt, n_predict, timeout_sec) -> tuple[str, dict]
def _spawn_server(llama_server, gguf_path, port, ngl, threads, ctx_size, log_path) -> subprocess.Popen
def _kill_server(proc) -> None
```
llama-server 启动命令 verbatim（来自 parity_gguf.py:127-136）：
  `/home/samuel/llama.cpp/build/bin/llama-server -m <gguf> --host 127.0.0.1 --port <p> -ngl 99 -t 4 -c 4096 --no-webui`
POST /completion body verbatim：
  `{"prompt": <str>, "n_predict": 384, "temperature": 0.0, "top_k": 1, "seed": 42, "cache_prompt": True, "stream": False}`
GGUF bf16 路径: `runs/20260507T032419Z/gguf/model.bf16.gguf`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 generate_gguf.py（参数化 + 断点续跑）</name>
  <files>tsc_cycle/eval/generate_gguf.py</files>
  <read_first>
    - tsc_cycle/student/parity_gguf.py（_spawn_server / _post_completion / _wait_health / _kill_server 实现细节）
    - tsc_cycle/prompt_builder.py
  </read_first>
  <action>
    创建 `tsc_cycle/eval/generate_gguf.py`，参数化 backend：
    `python -m tsc_cycle.eval.generate_gguf --gguf-path <path> --backend-label <label> --prompts ... --cache-dir ... --n-predict 384`

    实现要点：
    1. argparse：
       - `--gguf-path` (required)
       - `--backend-label` (required, e.g. `gguf_bf16` or `gguf_q4_k_m`)
       - `--prompts` default `runs/20260507T032419Z/eval/eval_prompts.jsonl`
       - `--cache-dir` (required)
       - `--llama-server` default `/home/samuel/llama.cpp/build/bin/llama-server`
       - `--n-predict` default 384
       - `--timeout-sec` default 600
       - `--server-startup-sec` default 180
       - `--ngl` default 99, `--threads` default 4, `--ctx-size` default 4096

    2. 直接 import 复用 parity_gguf 的辅助函数：
       ```python
       from tsc_cycle.student.parity_gguf import (
           _find_free_port, _wait_health, _post_completion,
           _spawn_server, _kill_server,
       )
       ```

    3. 断点续跑：
       ```python
       cache_dir.mkdir(parents=True, exist_ok=True)
       todo = [r for r in records if not (cache_dir / f"{r['sample_id']}.json").exists()]
       if not todo:
           print(f"[GEN-GGUF {label}] OK all-cached n={len(records)}")
           return 0
       ```

    4. 启动 server（仅在 todo 非空时）：
       ```python
       port = _find_free_port()
       server_log = cache_dir.parent / f"server_{label}.log"
       proc = _spawn_server(Path(args.llama_server), Path(args.gguf_path),
                            port, args.ngl, args.threads, args.ctx_size, server_log)
       try:
           if not _wait_health(port, args.server_startup_sec):
               return 3
           # ... 循环 todo
       finally:
           _kill_server(proc)
       ```

    5. 每条 prompt：
       ```python
       user_prompt = build_user_prompt(rec["input"])
       full = user_prompt + "\n" + build_assistant_prefill()
       text, meta = _post_completion(port, full, args.n_predict, args.timeout_sec)
       if meta["timeout"]:
           sol, err = None, "timeout"
       elif meta.get("http_status") is None:
           sol, err = None, f"http_error: {meta.get('error','unknown')}"
       else:
           _, sol = parse_assistant_output(text)
           err = None if sol is not None else "solution_unparseable"
       out = {"sample_id": rec["sample_id"], "split_hint": rec["split_hint"],
              "backend": args.backend_label, "solution": sol, "parse_error": err,
              "raw_text": text, "elapsed_sec": meta["elapsed_sec"],
              "n_predict": args.n_predict, "seed": 42}
       (cache_dir / f"{rec['sample_id']}.json").write_text(
           json.dumps(out, ensure_ascii=False, indent=2), encoding="utf-8")
       ```

    6. 每 10 条进度打印 `[GEN-GGUF {label}] progress {i}/{n_todo} elapsed={t:.1f}s`
    7. 结束打印 `[GEN-GGUF {label}] OK generated={n_todo} total_cached={len(records)} server_log={server_log}`

    严禁：用 EvoProgTSC/llama.cpp 的 CPU build；per-prompt llama-cli；temperature>0 / top_k!=1。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && /home/samuel/dgx-spark-setup/.venv/bin/python -c "import ast; ast.parse(open('tsc_cycle/eval/generate_gguf.py').read())" && grep -q "/home/samuel/llama.cpp/build/bin/llama-server" tsc_cycle/eval/generate_gguf.py && grep -q "from tsc_cycle.student.parity_gguf import" tsc_cycle/eval/generate_gguf.py && grep -q "backend_label" tsc_cycle/eval/generate_gguf.py && grep -q "all-cached" tsc_cycle/eval/generate_gguf.py</automated>
  </verify>
  <acceptance_criteria>
    - `tsc_cycle/eval/generate_gguf.py` 存在且 ≥100 行
    - grep `/home/samuel/llama.cpp/build/bin/llama-server` 命中（非 EvoProgTSC CPU build）
    - grep `from tsc_cycle.student.parity_gguf import` 命中（复用辅助函数）
    - grep `backend_label` 命中 ≥3（参数化）
    - grep `all-cached` 命中（断点续跑分支）
    - 不出现 `EvoProgTSC/llama.cpp`（grep -L 等价：`grep -c "EvoProgTSC/llama.cpp" generate_gguf.py` == 0）
    - python -c "import ast" 解析通过
  </acceptance_criteria>
  <done>generate_gguf.py 落盘，可参数化驱动 bf16/q4_K_M 两 backend</done>
</task>

<task type="auto">
  <name>Task 2: 端到端跑通 gguf_bf16 backend（600 prompt → 600 cache）</name>
  <files>runs/20260507T032419Z/eval/gen_cache/gguf_bf16/</files>
  <read_first>
    - tsc_cycle/eval/generate_gguf.py（CLI 签名）
  </read_first>
  <action>
    确认 plan 06-02 已退出（GPU 释放）后启动：
    ```bash
    cd /home/samuel/TSC_CYCLE
    /home/samuel/dgx-spark-setup/.venv/bin/python -m tsc_cycle.eval.generate_gguf \
        --gguf-path runs/20260507T032419Z/gguf/model.bf16.gguf \
        --backend-label gguf_bf16 \
        --prompts runs/20260507T032419Z/eval/eval_prompts.jsonl \
        --cache-dir runs/20260507T032419Z/eval/gen_cache/gguf_bf16 \
        --n-predict 384 \
        2>&1 | tee runs/20260507T032419Z/eval/gen_gguf_bf16.log
    ```
    退出码必须 0；llama-server 由脚本 finally 段 SIGTERM 回收；脚本退出后 `nvidia-smi` 显示 GPU 已释放。
  </action>
  <verify>
    <automated>test $(ls runs/20260507T032419Z/eval/gen_cache/gguf_bf16/*.json 2>/dev/null | wc -l) -eq 600 && /home/samuel/dgx-spark-setup/.venv/bin/python -c "import json,glob; files=glob.glob('runs/20260507T032419Z/eval/gen_cache/gguf_bf16/*.json'); assert len(files)==600; r=json.load(open(files[0])); assert r['backend']=='gguf_bf16' and 'solution' in r and 'raw_text' in r, r"</automated>
  </verify>
  <acceptance_criteria>
    - `ls runs/20260507T032419Z/eval/gen_cache/gguf_bf16/*.json | wc -l` == 600
    - 任一 cache 文件 `backend` 字段 == `"gguf_bf16"`
    - server log 文件 `runs/20260507T032419Z/eval/server_gguf_bf16.log` 存在
    - 脚本退出后无残留 llama-server 进程：`pgrep -f "llama-server.*model.bf16"` 应无输出
  </acceptance_criteria>
  <done>600 个 gguf_bf16 cache 文件落盘，server 已回收</done>
</task>

</tasks>

<verification>
- 600 cache 文件 + schema 一致 + backend 字段正确
- llama-server 启动一次 + 干净退出
- 同脚本可被 plan 06-04 复用（CLI 参数化已验证）
</verification>

<success_criteria>
- gen_cache/gguf_bf16/ 600 文件
- generate_gguf.py 已参数化、不依赖 EvoProgTSC CPU build
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-suite/06-03-SUMMARY.md`
</output>
