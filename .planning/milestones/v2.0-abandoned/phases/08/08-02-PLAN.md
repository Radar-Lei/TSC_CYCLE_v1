---
phase: 08
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tsc_cycle/student/dataset.py
  - scripts/dist_check.py
  - tests/test_dataset_report.py
  - tests/test_dist_check.py
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "student/dataset.py 在 build 时输出 data/dataset_report.md（含三源比例 + KS 表 + v1.0 对比）"
    - "student/dataset.py 在 build 时输出 data/split_manifest.json"
    - "scripts/dist_check.py 能为 inputs/ood_inputs/targeted_inputs 三个文件分别产出 KS 报表"
    - "split 桶哈希决定性：同一 sample_id 永远落入同一 split (train/val_id/val_ood)"
    - "dataset_report 引用真实 v1.0 baseline 指标 (98.7% / 99.3% / MAE 7.7s)"
  artifacts:
    - path: "tsc_cycle/student/dataset.py"
      provides: "main() 末尾调用 build_split_manifest + 写 dataset_report.md"
      contains: "dataset_report.md"
    - path: "scripts/dist_check.py"
      provides: "支持 --targeted-inputs 参数，输出三源 KS 表"
      contains: "targeted"
    - path: "tests/test_dataset_report.py"
      provides: "dataset_report 字段完整性单测（fixture 驱动）"
    - path: "tests/test_dist_check.py"
      provides: "dist_check KS 行为单测（fixture 数据）"
  key_links:
    - from: "student/dataset.py main()"
      to: "tsc_cycle.manifest.build_split_manifest"
      via: "import + call after split"
      pattern: "build_split_manifest"
    - from: "student/dataset.py main()"
      to: "data/dataset_report.md"
      via: "Path.write_text"
      pattern: "dataset_report"
---

<objective>
扩展 `student/dataset.py` 在数据 split 阶段额外产出 `data/dataset_report.md`（DATA-02 报告）和 `data/split_manifest.json`（DATA-03 metadata）；扩展 `scripts/dist_check.py` 支持 targeted 文件 KS 检验。落地对应单测。

Purpose: DATA-02（数据集报告）和 DATA-03（split metadata 落盘）是 Phase 8 的两个直接交付物，必须在 dataset 构建路径里以代码形式固化，不能依赖手工编辑 markdown。

Output: 可在小 fixture 上跑通的 dataset_report 生成器 + dist_check targeted 支持 + 两个测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-CONTEXT.md
@CLAUDE.md

@tsc_cycle/student/dataset.py
@scripts/dist_check.py
@tsc_cycle/manifest.py

<interfaces>
<!-- 关键 API 契约（Plan 01 即将实现的在 Wave 1 内并行；本 plan 假定 manifest.build_split_manifest 可用） -->

From tsc_cycle/manifest.py（Plan 01 实现，本 plan 仅调用）:
```python
def build_split_manifest(
    inputs_jsonl: Path,
    ood_inputs_jsonl: Path,
    targeted_inputs_jsonl: Path | None,
    dist_prior_path: Path,
    labeled_jsonl: Path,
    splits: dict,
    seeds: dict,
    out_path: Path,
    teacher_model: str = "gpt-5.5",
    teacher_effort: str = "high",
    reproduction_command: str | None = None,
) -> dict
```

From scripts/dist_check.py (现有):
```python
# 现有签名（待确认 actual）：CLI --reality / --inputs / --output 写 dist_check_report.md
# scipy.stats.ks_2samp on pred_wait, pred_saturation, etc.
```

v1.0 baseline 数字（写死在 dataset_report.md 的 v1.0 比较表）:
- v1.0 q4_K_M OOD lint=98.7%
- v1.0 HF bf16 OOD lint=99.3%
- v1.0 teacher MAE Δ +0.18s
- v1.0 total_labeled=3000, ood_labeled=300
- v1.0 phase_count={7,8} 训练样本=29
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 dist_check.py 支持 targeted 输入 + 测试</name>
  <files>
    scripts/dist_check.py,
    tests/test_dist_check.py
  </files>
  <behavior>
    dist_check.py 现状（先 Read 确认实际签名）需扩展:
      - CLI 加 `--targeted-inputs PATH`（可选）
      - 输出 dist_check_report.md 中 KS 表多一列：targeted KS p
      - 维持向后兼容（不传 --targeted-inputs 时输出与 v1.0 一致）

    test_dist_check.py:
      - test_id_passes_ks: 用 mini_dist_prior fixture 生成 100 个同分布样本 → KS p>0.05 跨所有字段
      - test_ood_fails_ks: 用 mini_dist_prior 生成 50 个 OOD 样本（pred_wait 故意偏移 3σ）→ KS p<0.001
      - test_targeted_section_present: 当传入 targeted_inputs 时报告含 "targeted KS p" 列；不传时不含
  </behavior>
  <action>
    1. Read scripts/dist_check.py 当前实现，确认 KS 报表生成函数签名。
    2. 加 argparse 选项 --targeted-inputs，并在 KS 表生成函数里加一个可选第三列。函数签名加 targeted_records: list[dict] | None = None。
    3. 创建 tests/test_dist_check.py，3 个测试，使用 conftest.py 的 mini_dist_prior fixture（依赖 Plan 01 Task 1 已建好的 conftest；如果 Plan 01 conftest 还没合并，本 task 在 test_dist_check.py 内 inline 定义 mini fixture，避免阻塞）。
    4. 跑 pytest tests/test_dist_check.py -x -q 全绿。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/test_dist_check.py -x -q</automated>
  </verify>
  <done>
    - scripts/dist_check.py --help 显示 --targeted-inputs
    - tests/test_dist_check.py 3 个测试全绿
    - 对 v1.0 已有 inputs/ood_inputs 跑 dist_check 不传 targeted，输出与 v1.0 dist_check_report.md 字段集一致（向后兼容）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: student/dataset.py 输出 dataset_report.md + split_manifest.json + 测试</name>
  <files>
    tsc_cycle/student/dataset.py,
    tests/test_dataset_report.py
  </files>
  <behavior>
    在 student/dataset.py 现有 main() 末尾（split + parquet 写出之后）追加：

    1. 调用 tsc_cycle.manifest.build_split_manifest(...) 写 data/split_manifest.json
       splits 字典内容: {"train": {"n": ..., "id_count": ..., "targeted_count": ..., "ood_count": 0},
                         "val_id": {"n": ..., "id_count": ..., "targeted_count": 0, "ood_count": 0},
                         "val_ood": {"n": ..., "id_count": 0, "targeted_count": 0, "ood_count": ...}}
       通过 source 字段（来自 sample_inputs 写入的 sidecar）从 inputs.jsonl/ood_inputs.jsonl/targeted_inputs.jsonl 反查 sample_id → source 映射。

    2. 写 data/dataset_report.md，按 RESEARCH.md §"DATA-02 Report Schema" 严格 5 个 section:
       - Source Composition（n_input / n_labeled / reject_rate per source）
       - Field Distribution vs reality.log（KS p per source per field）
       - Boundary Coverage（per OOD dimension：phase_count modes / range_combo / wait OOD / saturation OOD）
       - v1.0 Comparison（v1.0 vs v2.0 数字 + Δ；v1.0 baseline 写死）
       - Targeted Sample Source（来源文件路径 + 失败种子数 + 扰动策略 + 与 v1.0 OOD val_ood sample_id 重叠数 == 0 断言）

    3. 在写 dataset_report 前断言 targeted_seed_overlap_count == 0（per RESEARCH §"Pitfall 2"），否则 raise SystemExit 并打印泄漏的 sample_id 子集。

    4. 保留对 split 桶哈希决定性的现有行为，不动 sample_id-hash 切分逻辑。

    test_dataset_report.py:
      - test_report_sections: build 完成后 dataset_report.md 含上述 5 个 section header
      - test_v1_baseline_present: v1.0 Comparison section 含 "98.7%"、"99.3%"、"+6740"
      - test_split_manifest_written: data/split_manifest.json 存在且 schema 完整（与 Plan 01 test_split_manifest 互补：本测试是 dataset.py 端到端层面）
      - test_split_bucket_stable: 同 sample_id 在两次 build 中落同一 split（决定性 DATA-03）
      - test_targeted_no_overlap_assertion: 故意构造 targeted_inputs 含 v1.0 OOD sample_id → build 必须 SystemExit
  </behavior>
  <action>
    1. Read tsc_cycle/student/dataset.py 现有 main()，定位 split + parquet 写出的位置。
    2. 添加 build_dataset_report() 内部函数，组装 markdown。v1.0 数字硬编码（98.7% / 99.3% / 3000 / 300 / 29 / 7.7s）并在源码注释里标 "// v1.0 baseline from runs/20260507T032419Z/eval/report.md, frozen for v2.0 diff"。
    3. 用 hashlib 算 sha256 给 build_split_manifest，复用 tsc_cycle.manifest.build_split_manifest。
    4. 加 targeted_seed_overlap_count assert（读 v1.0 per_sample.jsonl 取 sample_id 集合 → 与 targeted_inputs.jsonl sample_id 求交）。如果 per_sample.jsonl 路径不存在（fixture 测试场景），跳过此 assert（用 --skip-leak-check 选项控制，默认 False but tests can override；CLI 加 `--v1-eval-path` 默认 runs/20260507T032419Z/eval/per_sample.jsonl，缺失时 warn 而非 fail）。
    5. 写 tests/test_dataset_report.py，5 个测试，使用 tmp_path + 极小 fixture（10 行 labeled.jsonl + 5 + 3 + 2 三源 inputs）跑端到端 build → 验证产物。
    6. 跑 pytest tests/test_dataset_report.py -x -q 全绿。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/test_dataset_report.py -x -q</automated>
  </verify>
  <done>
    - dataset.py main() 完成后 data/dataset_report.md 和 data/split_manifest.json 都存在
    - tests/test_dataset_report.py 5 个测试全绿
    - 5 个 section header 都存在；v1.0 baseline 数字硬编码可在 grep 中找到
    - targeted_seed_overlap_count > 0 时 build 报错退出（防泄漏）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → dataset.py | inputs/labeled jsonl 来自本地 trusted pipeline |
| dataset_report.md → 用户 | markdown 渲染，无 sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | targeted sample_id 泄漏到 v1.0 OOD val_ood | mitigate | dataset.py main() 强制 assert targeted_seed_overlap_count==0 (Task 2) |
| T-08-05 | Information Disclosure | dataset_report.md 含敏感教师 prompt 片段 | accept | report 仅含计数和 KS p 值，不含 prompt 文本 |
</threat_model>

<verification>
- pytest tests/test_dataset_report.py tests/test_dist_check.py -x -q 全绿
- python -m tsc_cycle.student.dataset --help 不变（向后兼容；新增可选参数 --v1-eval-path）
- 端到端 fixture：构造 mini inputs 三源 + mini labeled → main() 产出 split_manifest.json (schema OK) + dataset_report.md (5 sections)
</verification>

<success_criteria>
- DATA-02 报告生成器代码就位且单测覆盖
- DATA-03 split_manifest 由 dataset.py 自动写出
- 防泄漏 assert 通过 fixture 验证可触发
- v1.0 baseline 数字硬编码进 dataset_report 模板，diff 可读
</success_criteria>

<output>
完成后创建 .planning/phases/08/08-02-SUMMARY.md
</output>
