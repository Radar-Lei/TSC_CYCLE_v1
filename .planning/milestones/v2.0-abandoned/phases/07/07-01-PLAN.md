---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_prompt_builder.py
  - tsc_cycle/prompt_builder.py
  - tsc_cycle/eval/metrics_reasoning.py
  - tsc_cycle/student/tokenize_sanity.py
autonomous: true
requirements:
  - TAG-01
  - TAG-02
must_haves:
  truths:
    - "新协议字面值 <end_working_out> 出现在 prompt、assistant 拼装、parser 与 tokenize_sanity CUSTOM_TAGS 中"
    - "parser 对仅含旧标签 </end_working_out> 的输入返回 ('' , None) 解析失败"
    - "parser 对新标签 prefill-only 输入仍能正确返回 reasoning 与 solution"
    - "USER_TEMPLATE 与所有源代码路径不再把 </end_working_out> 当作正例（grep 反例守门通过）"
    - "tokenizer_check 对新标签确认 multi sub-token，不注册 added token"
    - "tests/test_prompt_builder.py 同时覆盖：新标签正例、旧标签反例、prefill-only 新闭标签反向用例、常量字面值锁定"
  artifacts:
    - path: "tsc_cycle/prompt_builder.py"
      provides: "TAG_THINK_CLOSE = '<end_working_out>' + parser 旧标签拒绝分支 + USER_TEMPLATE/docstring 文案统一"
      contains: "<end_working_out>"
    - path: "tsc_cycle/eval/metrics_reasoning.py"
      provides: "docstring 引用新闭标签"
      contains: "<end_working_out>"
    - path: "tsc_cycle/student/tokenize_sanity.py"
      provides: "CUSTOM_TAGS 列表使用新闭标签字面值（或改为 import prompt_builder 常量）"
      contains: "<end_working_out>"
    - path: "tests/test_prompt_builder.py"
      provides: "新增反例与常量锁定测试，更新 prefill 用例"
      contains: "test_parse_rejects_old_close_tag"
  key_links:
    - from: "tsc_cycle/prompt_builder.py::TAG_THINK_CLOSE"
      to: "下游 dataset/eval/train/tokenizer_check"
      via: "Python import 符号引用"
      pattern: "from tsc_cycle.prompt_builder import.*TAG_THINK_CLOSE"
    - from: "tsc_cycle/prompt_builder.py::parse_assistant_output"
      to: "tsc_cycle/eval/metrics_constraints.py（solution=None 失败路径）"
      via: "返回 (\"\", None) 触发 lint_ok=False"
      pattern: "parse_assistant_output"
---

<objective>
Phase 7 单 plan，全链路把思考结束标签从 `</end_working_out>` 切换到 `<end_working_out>`，并让 parser 显式拒绝旧标签。

Purpose: 让训练/推理/评测产出的协议字面值与 v2.0 milestone 锁定值一致；旧标签作为反例不可解析，避免数据治理混淆。

Output: prompt_builder/eval docstring/tokenize_sanity 字面值同步、parser 增加旧标签拒绝分支、tests/test_prompt_builder.py 扩展 5 个测试用例。
</objective>

<execution_context>
@/home/samuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-RESEARCH.md

@tsc_cycle/prompt_builder.py
@tsc_cycle/eval/metrics_reasoning.py
@tsc_cycle/eval/metrics_constraints.py
@tsc_cycle/student/dataset.py
@tsc_cycle/student/tokenize_sanity.py
@tsc_cycle/tokenizer_check.py
@tests/test_prompt_builder.py

<interfaces>
<!-- 协议常量 single source of truth（D-01）。下游全部通过这些符号 import。 -->

From tsc_cycle/prompt_builder.py（迁移后目标值）:
```python
TAG_THINK_OPEN     = "<start_working_out>"
TAG_THINK_CLOSE    = "<end_working_out>"     # was "</end_working_out>"
TAG_SOLUTION_OPEN  = "<SOLUTION>"
TAG_SOLUTION_CLOSE = "</SOLUTION>"

CUSTOM_TAGS = [TAG_THINK_OPEN, TAG_THINK_CLOSE, TAG_SOLUTION_OPEN, TAG_SOLUTION_CLOSE]

def build_full_assistant(reasoning: str, solution: dict[str, int]) -> str: ...
def parse_assistant_output(text: str) -> tuple[str, dict[str, int] | None]: ...
def build_user_prompt(example_input: dict) -> str: ...
```

旧标签反例字面值（仅允许出现在 parser 拒绝分支与负例测试）：
```
"</end_working_out>"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 tests/test_prompt_builder.py 覆盖 D-07 全部场景（先红）</name>
  <files>tests/test_prompt_builder.py</files>
  <read_first>
    - tests/test_prompt_builder.py（当前已有的 roundtrip / prefill / user_prompt 用例与字面值断言）
    - tsc_cycle/prompt_builder.py（确认 build_user_prompt / build_full_assistant / parse_assistant_output / 4 个常量符号是否可直接 import）
    - .planning/phases/07/07-RESEARCH.md 第 312–359 行（File Change Map 测试段 + 新增测试样板代码）
  </read_first>
  <behavior>
    - 正例：build_user_prompt 输出包含 "&lt;start_working_out&gt;" 与 "&lt;end_working_out&gt;"，且不含 "&lt;/end_working_out&gt;"
    - 正例：build_full_assistant("r", {"1": 60}) 输出含 "&lt;end_working_out&gt;" 且不含 "&lt;/end_working_out&gt;"
    - 反例（含开标签 + 旧闭标签）：parse_assistant_output 返回 ("", None)
    - 反例（prefill-only：无开标签 + 旧闭标签 + SOLUTION）：parse_assistant_output 返回 (_, None)
    - prefill-only 新闭标签场景（D-04）：parse_assistant_output(prefill_with_new_close) 仍返回非空 reasoning + 解析成功的 solution
    - 常量字面值锁定（D-02）：TAG_THINK_OPEN/TAG_THINK_CLOSE/TAG_SOLUTION_OPEN/TAG_SOLUTION_CLOSE 与协议字符串完全一致
  </behavior>
  <action>
    依据 RESEARCH File Change Map 行 312–359 修改与新增测试。具体步骤：

    1. **更新现有断言**（per D-08，去掉旧字面值正例）：
       - 行 32：`assert "<start_working_out>" in p and "</end_working_out>" in p`
         → `assert "<start_working_out>" in p and "<end_working_out>" in p`
       - 行 60–61 prefill 用例：
         - body 字面值 `"step-by-step</end_working_out><SOLUTION>{\"1\":60}</SOLUTION>"`
           → `"step-by-step<end_working_out><SOLUTION>{\"1\":60}</SOLUTION>"`
         - 注释更新为：`# Output as model would emit: prefilled <start_working_out> NOT in text, only the new close`

    2. **新增 5 个测试函数**（每个对应 D-07 一个场景，per D-03/D-04/D-08/D-02）。Import 顶部需要 `from tsc_cycle.prompt_builder import (TAG_THINK_OPEN, TAG_THINK_CLOSE, TAG_SOLUTION_OPEN, TAG_SOLUTION_CLOSE, build_user_prompt, build_full_assistant, parse_assistant_output)`：

       - `test_constants_match_protocol`：四个常量字面值锁定（per D-02）。
       - `test_parse_rejects_old_close_tag`：含 TAG_THINK_OPEN + "x</end_working_out>" + TAG_SOLUTION_OPEN + '{"1":60}' + TAG_SOLUTION_CLOSE 时返回 ("", None)（per D-03 / TAG-02）。
       - `test_parse_old_close_in_prefill_form`：prefill-only 旧闭标签 `"step-by-step</end_working_out><SOLUTION>{\"1\":60}</SOLUTION>"` 也必须 solution=None（per D-03）。
       - `test_user_prompt_no_old_close_tag`：build_user_prompt(EX_INPUT) 输出不含 "&lt;/end_working_out&gt;" 且含 "&lt;end_working_out&gt;"（per D-08 + TAG-01）。
       - `test_full_assistant_uses_new_close_tag`：build_full_assistant("r", {"1": 60}) 输出不含 "&lt;/end_working_out&gt;" 且含 "&lt;end_working_out&gt;"（per TAG-01）。

       使用 `EX_INPUT` 时复用文件中已有的示例输入（若不存在则构造一个最小合法 input dict，per RESEARCH 中 build_user_prompt 签名）。

    3. **不引入** `validate_protocol()` 之类新抽象（per RESEARCH Open Question #2，CONTEXT Discretion 反对新抽象层）。

    4. **不新增** 可选 tokenizer 多 token 测试（避免 transformers 模型下载阻塞 CI）；该断言放到 Task 3 的人工 verify 命令中通过 `tsc_cycle.tokenizer_check.check_tokenizer` CLI 执行。

    运行测试，**确认这些新测试在常量未改前是 RED**（除 test_full_assistant_uses_new_close_tag 在常量未改前应当失败、test_constants_match_protocol 应当失败、两个 parse 反例当前会通过解析返回非空 solution 因此 assert 失败）。RED 状态截图/输出贴在 commit message 中。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/test_prompt_builder.py -x -q 2>&amp;1 | tail -30</automated>
    预期：在 Task 2 完成前 RED（多个新测试失败）；本任务结束时只 commit RED 状态，由 Task 2 转 GREEN。
  </verify>
  <acceptance_criteria>
    - tests/test_prompt_builder.py 含 5 个新增测试函数，名称与 behavior 对齐
    - 现有行 32 与行 60–61 prefill 用例已更新为新字面值
    - 文件 import 顶部能直接拿到所有需要的常量与 helper（不重复定义字面值）
    - 在常量尚未迁移时 `pytest tests/test_prompt_builder.py -x -q` 至少有 3 个新测试 FAIL（test_constants_match_protocol、test_parse_rejects_old_close_tag、test_user_prompt_no_old_close_tag），证明测试有约束力
  </acceptance_criteria>
  <done>
    新测试已写、RED 状态已观察、文件已保存。Task 2 将让所有测试转 GREEN。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 迁移协议常量、parser 拒绝分支与字面值同步点（红转绿）</name>
  <files>tsc_cycle/prompt_builder.py, tsc_cycle/eval/metrics_reasoning.py, tsc_cycle/student/tokenize_sanity.py</files>
  <read_first>
    - tsc_cycle/prompt_builder.py（确认行号；RESEARCH 给出 7、10、24、51、55、95 行的精确替换；parse_assistant_output 完整逻辑）
    - tsc_cycle/eval/metrics_reasoning.py（行 3 docstring 字面值）
    - tsc_cycle/student/tokenize_sanity.py（行 8 docstring + 行 38–41 CUSTOM_TAGS 字面值列表）
    - tsc_cycle/tokenizer_check.py（确认其通过 import 自动随动，本任务**不修改**该文件）
    - tsc_cycle/student/train.py（确认通过符号引用，本任务**不修改**）
    - tsc_cycle/student/dataset.py（确认 docstring 不含 `</end_working_out>` 字面值，本任务**不修改**）
    - .planning/phases/07/07-RESEARCH.md 第 247–310 行（File Change Map 精确到行）
  </read_first>
  <behavior>
    - prompt_builder.TAG_THINK_CLOSE 字面值 == "&lt;end_working_out&gt;"
    - prompt_builder.USER_TEMPLATE 中文文案不含 "&lt;/end_working_out&gt;" 且至少出现一次 "&lt;end_working_out&gt;"
    - parse_assistant_output("&lt;start_working_out&gt;x&lt;/end_working_out&gt;&lt;SOLUTION&gt;{\"1\":60}&lt;/SOLUTION&gt;") == ("", None)
    - parse_assistant_output("step-by-step&lt;/end_working_out&gt;&lt;SOLUTION&gt;{\"1\":60}&lt;/SOLUTION&gt;") 返回 solution is None
    - parse_assistant_output 对新标签 prefill-only 仍返回非空 reasoning + dict solution
    - tokenize_sanity 的 CUSTOM_TAGS 列表含 "&lt;end_working_out&gt;"，不含 "&lt;/end_working_out&gt;"
    - eval/metrics_reasoning 模块 docstring 引用新闭标签
  </behavior>
  <action>
    严格按 RESEARCH File Change Map 修改 3 个文件，**不要扩大范围**（per CONTEXT Discretion / D-08）。

    **1. tsc_cycle/prompt_builder.py**（per D-01 / D-02 / D-03）

    精确改 7 处：
    - 行 7（模块 docstring 协议示意）：`</end_working_out>` → `<end_working_out>`
    - 行 10（模块 docstring 替换说明）：`</end_working_out>` → `<end_working_out>`
    - 行 24（常量定义）：`TAG_THINK_CLOSE = "</end_working_out>"` → `TAG_THINK_CLOSE = "<end_working_out>"`
    - 行 51（USER_TEMPLATE 中文指令）：`<start_working_out>...</end_working_out>` → `<start_working_out>...<end_working_out>`
    - 行 55（USER_TEMPLATE 中文指令）：同上替换
    - 行 95（注释）：`# Reasoning: between <start_working_out> and </end_working_out>` → `... and <end_working_out>`

    在 `parse_assistant_output(text)` 函数体首部插入旧标签拒绝分支（per D-03 / TAG-02）：

    ```python
    def parse_assistant_output(text: str) -> tuple[str, dict[str, int] | None]:
        # TAG-02 (per D-03): 显式拒绝旧结束标签。仅当出现旧字面值且新字面值缺失时返回失败。
        # 注意：<end_working_out> 不是 </end_working_out> 的子串（前者无 '/'），子串关系无歧义。
        OLD_THINK_CLOSE = "</end_working_out>"
        if OLD_THINK_CLOSE in text and TAG_THINK_CLOSE not in text:
            return "", None
        # ... 原有逻辑保持不变
    ```

    **不修改** parse_assistant_output 其余原有逻辑；不引入 validate_protocol() 等新公开函数。

    **2. tsc_cycle/eval/metrics_reasoning.py**（per D-08）

    - 行 3 docstring：`<start_working_out>...</end_working_out>` → `<start_working_out>...<end_working_out>`

    **3. tsc_cycle/student/tokenize_sanity.py**（per D-08，pitfall #2 选项 B）

    采取 RESEARCH 推荐的 import 版本（避免未来漏改风险）：
    - 行 8 docstring：`</end_working_out>` → `<end_working_out>`
    - 删除行 37–42 的 `CUSTOM_TAGS = [...]` 字面值列表
    - 改为：
      ```python
      from tsc_cycle.prompt_builder import (
          TAG_THINK_OPEN,
          TAG_THINK_CLOSE,
          TAG_SOLUTION_OPEN,
          TAG_SOLUTION_CLOSE,
      )
      CUSTOM_TAGS = [TAG_THINK_OPEN, TAG_THINK_CLOSE, TAG_SOLUTION_OPEN, TAG_SOLUTION_CLOSE]
      ```
    - 若该文件已有 `from tsc_cycle.prompt_builder import ...`，仅追加缺失的常量名而非新写一行。

    **不修改** 文件：tsc_cycle/tokenizer_check.py、tsc_cycle/student/train.py、tsc_cycle/student/dataset.py（per RESEARCH 已审查无字面值改动需要）。

    完成后运行 Task 1 的测试套件，全部转 GREEN。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest tests/test_prompt_builder.py -x -q 2>&amp;1 | tail -20</automated>
    预期：所有测试 PASS，包括 Task 1 新增的 5 个用例 + 原有 roundtrip / prefill 用例。
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prompt_builder.py -x -q` 全部 GREEN
    - prompt_builder.py 行 7/10/24/51/55/95 字面值已更新（人工 diff 核对）
    - parse_assistant_output 函数体首部含旧标签拒绝分支（5 行内）
    - tokenize_sanity.py 不再含字面值列表，改为 import prompt_builder 常量
    - eval/metrics_reasoning.py docstring 行 3 已更新
    - 未触碰：tokenizer_check.py / student/train.py / student/dataset.py / data/labeled.jsonl
  </acceptance_criteria>
  <done>
    协议字面值统一，parser 拒绝旧标签，所有单元测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: 全仓反例 grep 守门 + 全测试套件 + tokenizer 多 token 验证</name>
  <files>（仅校验，无文件改动；如发现遗漏旧字面值再回到 prompt_builder.py / tokenize_sanity.py 修补）</files>
  <read_first>
    - .planning/phases/07/07-RESEARCH.md 第 414–429 行（Validation Architecture - 反例 grep 命令）
    - tsc_cycle/tokenizer_check.py（确认 check_tokenizer 接口；CUSTOM_TAGS 来源是 prompt_builder.import）
  </read_first>
  <action>
    Phase gate 综合验证，per D-08 / TAG-01 / TAG-02 / D-05 / D-06：

    **步骤 1：全仓旧字面值守门（D-08 必须通过）**

    ```bash
    cd /home/samuel/TSC_CYCLE
    grep -rn '</end_working_out>' tsc_cycle scripts 2>/dev/null
    ```

    预期输出：**仅 1 处合法命中** — `tsc_cycle/prompt_builder.py` 中 `parse_assistant_output` 内 `OLD_THINK_CLOSE = "</end_working_out>"` 拒绝分支字面值。

    若出现其他命中（除已知合法点外），说明遗漏，回到 Task 2 补改。

    在 tests/ 目录下，旧字面值仅允许出现在反例测试函数体内（test_parse_rejects_old_close_tag、test_parse_old_close_in_prefill_form、test_user_prompt_no_old_close_tag、test_full_assistant_uses_new_close_tag 的负例 assert），人工核对：

    ```bash
    grep -n '</end_working_out>' tests/test_prompt_builder.py
    ```

    所有命中行必须位于上述四个测试函数内或注释/docstring 中标记为 "OLD/REJECT/NEGATIVE" 上下文。

    **步骤 2：全测试套件（TAG-01 / TAG-02）**

    ```bash
    pytest tests/ -x -q
    ```

    预期：仓内所有测试通过（test_prompt_builder.py / test_constraint_lint.py / test_hashing.py），无 regression。

    **步骤 3：tokenizer 多 sub-token 验证（D-05 / D-06）**

    使用本机已知良好 venv（per CLAUDE.md 不重装 torch）：

    ```bash
    /home/samuel/dgx-spark-setup/.venv/bin/python -c "
    from transformers import AutoTokenizer
    from tsc_cycle.tokenizer_check import check_tokenizer
    tok = AutoTokenizer.from_pretrained('Qwen/Qwen3-4B-Thinking-2507')
    res = check_tokenizer(tok)
    print('ok =', res.ok)
    print('details =', res.details)
    assert res.ok, res.details
    # D-05: 新闭标签必须是 multi sub-token
    new_close_ids = tok.encode('<end_working_out>', add_special_tokens=False)
    assert len(new_close_ids) >= 2, f'<end_working_out> tokenized to single token: {new_close_ids}'
    # D-06: 原生 think token 不应在 CUSTOM_TAGS encode 中泄漏
    print('PASS: new close tag is multi-token; tokenizer parity OK')
    "
    ```

    预期：`ok = True`、新闭标签拆成 ≥ 2 sub-token，无 added token 注册。

    若该模型已在本地缓存，命令在 30s 内完成；若需要下载，允许首次 5 min 内完成。失败则记录到 SUMMARY 的 Open Issues。

    **步骤 4：写阶段 SUMMARY**

    创建 `.planning/phases/07/07-01-SUMMARY.md`，记录：
    - 实际改动文件清单与 diff 行数
    - Task 1/2/3 各自验证命令的输出摘要（PASS/FAIL）
    - tokenizer_check 输出的 details
    - 已审查但未改动的文件清单（tokenizer_check.py / train.py / dataset.py / metrics_constraints.py）
    - data/labeled.jsonl 未迁移的原因（reasoning 字段无标签，per RESEARCH 验证）
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && (echo '--- D-08 grep gate ---' &amp;&amp; (grep -rn '</end_working_out>' tsc_cycle scripts 2>/dev/null | grep -v 'OLD_THINK_CLOSE' | grep -v '^#' | wc -l | awk '{ if ($1==0) print "PASS: only OLD_THINK_CLOSE allowed"; else { print "FAIL: " $1 " unexpected hits"; exit 1 } }') &amp;&amp; echo '--- full pytest ---' &amp;&amp; pytest tests/ -x -q 2>&amp;1 | tail -10)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '</end_working_out>' tsc_cycle scripts` 仅返回 prompt_builder.py 中 OLD_THINK_CLOSE 一行（拒绝分支）
    - tests/test_prompt_builder.py 中旧字面值仅出现在已命名的反例测试函数体内
    - `pytest tests/ -x -q` 全部 PASS（无 regression）
    - tokenizer_check 输出 ok=True 且 `<end_working_out>` 拆成 ≥ 2 sub-token
    - 07-01-SUMMARY.md 已创建并记录上述输出
  </acceptance_criteria>
  <done>
    Phase 7 全部 success criteria 满足：协议统一、旧标签拒绝、tokenizer 安全、测试覆盖、grep 守门通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型输出 → parser | 学生/教师模型生成的 assistant 文本进入 parse_assistant_output；可能含旧标签或畸形格式 |
| 训练数据生成 → tokenizer | build_full_assistant 拼装的字符串进入 tokenizer；自定义标签必须保持 multi sub-token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | parse_assistant_output 输入 | mitigate | 旧 `</end_working_out>` 字面值显式返回 (\"\", None)；下游 metrics_constraints 已通过 solution=None 路径标记 lint_ok=False，避免污染训练集（per D-03） |
| T-07-02 | Information disclosure | tokenized 训练样本 | mitigate | tokenizer_check.check_tokenizer 已校验自定义标签 multi sub-token；D-06 禁止原生 `<think>` 单 token 出现在 input_ids；本阶段保持该校验路径不变（per D-05/D-06） |
| T-07-03 | Spoofing | 协议字面值多源不一致 | mitigate | tokenize_sanity.py 改为 import prompt_builder 常量，消除字面值重复；grep 守门作为 phase gate 防御未来回潮（per D-01/D-08） |
| T-07-04 | Repudiation | 测试无法约束协议字面值 | mitigate | 新增 test_constants_match_protocol + test_user_prompt_no_old_close_tag + test_parse_rejects_old_close_tag 三重断言（per D-02/D-07/D-08） |
| T-07-05 | DoS / Elevation | — | accept | 本阶段无网络/auth/输入校验改动；ASVS V2-V6 不适用 |
</threat_model>

<verification>
- 单元测试：`pytest tests/test_prompt_builder.py -x -q` 全 PASS
- 仓内反例守门：`grep -rn '</end_working_out>' tsc_cycle scripts` 仅返回 prompt_builder.py 拒绝分支一行
- 全测试套件：`pytest tests/ -x -q` 全 PASS
- Tokenizer 安全：check_tokenizer ok=True 且 `<end_working_out>` 拆成 ≥ 2 sub-token
- 已审查未改动文件：tokenizer_check.py / student/train.py / student/dataset.py / eval/metrics_constraints.py / data/labeled.jsonl
</verification>

<success_criteria>
1. ✅ 所有生成/解析/训练/评测路径输出协议为 `<start_working_out>...<end_working_out><SOLUTION>...</SOLUTION>` —— 通过 prompt_builder 常量与 USER_TEMPLATE 改动 + tokenize_sanity import 链确认
2. ✅ 解析器和 lint 对 `</end_working_out>` 旧标签样本给出失败结果 —— parse_assistant_output 拒绝分支 + metrics_constraints 通过 solution=None 标记 lint_ok=False（自动随动）
3. ✅ tokenizer 检查确认新标签仍按普通 sub-token 拆分，不注册 added token —— Task 3 步骤 3 现场验证
4. ✅ 单元测试覆盖新标签正例、旧标签反例、缺失标签反例 —— Task 1 新增 5 个测试 + 现有 roundtrip/prefill
</success_criteria>

<output>
完成后创建 `.planning/phases/07/07-01-SUMMARY.md`，记录改动 diff、验证命令输出、未改动文件审查清单。
</output>
