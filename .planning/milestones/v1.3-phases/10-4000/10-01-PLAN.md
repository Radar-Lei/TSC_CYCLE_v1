---
phase: 10-4000
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - outputs/grpo_simple/qwen3-8b/validation_results.json
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "validate.py 以 --num-samples 4000 对 GRPO 8B 模型完成推理和验证"
    - "格式通过率 ≥ 80%"
    - "约束通过率 ≥ 80%"
  artifacts:
    - path: "outputs/grpo_simple/qwen3-8b/validation_results.json"
      provides: "4000 条验证结果 JSON"
  key_links:
    - from: "outputs/grpo_simple/qwen3-8b/model"
      to: "outputs/grpo_simple/qwen3-8b/validation_results.json"
      via: "validate.py --model-path ... --num-samples 4000"
      pattern: "validation_results.*qwen3-8b"
---

<objective>
用 validate.py 对 GRPO 产出的 Qwen3-8B 模型跑 4000 条数据验证，确认格式/约束/饱和度通过率达标。

Purpose: 验证 8B 模型在训练后的实际输出质量，与 v1.2 的 4B 模型验证标准对齐。
Output: outputs/grpo_simple/qwen3-8b/validation_results.json
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 运行 4000 条数据验证</name>
  <files>outputs/grpo_simple/qwen3-8b/validation_results.json</files>
  <action>
对 GRPO 产出的 8B 模型运行验证：

```bash
python validate.py --model-path outputs/grpo_simple/qwen3-8b/model --num-samples 4000 --output outputs/grpo_simple/qwen3-8b/validation_results.json
```

验证结果需满足：格式通过率 ≥ 80%，约束通过率 ≥ 80%。
  </action>
  <verify>
    <automated>test -f outputs/grpo_simple/qwen3-8b/validation_results.json && python -c "import json; d=json.load(open('outputs/grpo_simple/qwen3-8b/validation_results.json')); assert d['format_pass_rate'] >= 0.8 and d['constraint_pass_rate'] >= 0.8 and d['total_samples'] == 4000, f'FAIL: {d}'; print('PASS')"</automated>
  </verify>
  <done>4000 条验证完成，格式和约束通过率均达标，结果保存至 validation_results.json</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/10-4000/10-01-SUMMARY.md`
</output>
