---
phase: 08-script-parameterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config_8b.json
autonomous: true
requirements:
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "开发者用 --config config/config_8b.json 运行 SFT 脚本时，输出目录为 outputs/sft/qwen3-8b/model"
    - "开发者用 --config config/config_8b.json 运行 GRPO 脚本时，输出目录为 outputs/grpo_simple/qwen3-8b/model"
    - "8B 模型以全精度加载（load_in_4bit=false），不涉及 BnB 量化"
    - "4B 产物目录 outputs/sft/model 和 outputs/grpo_simple/model 不受影响"
  artifacts:
    - path: "config/config_8b.json"
      provides: "Qwen3-8B 全精度训练配置"
      contains: "unsloth/Qwen3-8B"
  key_links:
    - from: "config/config_8b.json"
      to: "src/sft/train.py"
      via: "--config 参数传递"
      pattern: "load_config.*config_path"
    - from: "config/config_8b.json"
      to: "src/grpo_simple/train.py"
      via: "--config 参数传递"
      pattern: "load_config.*config_path"
---

<objective>
验证并固化 Qwen3-8B 的训练配置文件 config/config_8b.json，确保模型名参数化、输出目录隔离和全精度加载三项需求均已满足。

Purpose: Phase 8 是 v1.3 的起点，确保 8B 配置正确是后续 SFT/GRPO 训练（Phase 9）的前提。
Output: 经过验证的 config/config_8b.json，纳入版本控制。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@config/config.json
@config/config_8b.json
@config/config_32b.json
@src/sft/train.py
@src/grpo_simple/train.py

<interfaces>
<!-- 训练脚本配置加载接口 -->

From src/sft/train.py:
```python
def load_config(config_path: str) -> dict:
    """从配置文件加载超参数和路径配置"""

def setup_model(config: dict):
    """加载模型并配置 LoRA，使用 config["training"]["sft"]["model"]["load_in_4bit"] 控制量化"""

def main():
    parser.add_argument("--config", type=str, default="config/config.json")
```

From src/grpo_simple/train.py:
```python
def load_config(config_path: str) -> dict:

def setup_model(config: dict):
    """使用 config["training"]["grpo_simple"]["model"]["load_in_4bit"] 控制量化"""

def main():
    parser.add_argument("--config", type=str, default="config/config.json")
```

config.json 路径键:
- paths.sft_output → SFT 模型输出目录
- paths.grpo_simple_output → GRPO 模型输出目录
- paths.grpo_simple_checkpoints → GRPO 检查点目录
- training.sft.model.model_name → 基座模型名/路径
- training.sft.model.load_in_4bit → 是否 4-bit 量化
- training.grpo_simple.model.model_name → GRPO 输入模型路径
- training.grpo_simple.model.load_in_4bit → 是否 4-bit 量化
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证并完善 config_8b.json 配置</name>
  <files>config/config_8b.json</files>
  <read_first>
    - config/config_8b.json
    - config/config.json
    - config/config_32b.json
  </read_first>
  <action>
验证 config/config_8b.json 中以下关键字段的正确性（ENV-01 + ENV-02）:

1. **模型名参数化 (ENV-01)**:
   - `training.sft.model.model_name` 必须为 `"unsloth/Qwen3-8B"`
   - `training.sft.model.model_id` 必须为 `"unsloth/Qwen3-8B"`
   - `training.grpo_simple.model.model_name` 必须为 `"outputs/sft/qwen3-8b/model"`（指向 SFT 产出）

2. **输出目录隔离 (ENV-01)**:
   - `paths.sft_output` 必须为 `"outputs/sft/qwen3-8b/model"`
   - `paths.grpo_simple_output` 必须为 `"outputs/grpo_simple/qwen3-8b/model"`
   - `paths.grpo_simple_checkpoints` 必须为 `"outputs/grpo_simple/qwen3-8b/checkpoints"`

3. **全精度加载 (ENV-02)**:
   - `training.sft.model.load_in_4bit` 必须为 `false`
   - `training.grpo_simple.model.load_in_4bit` 必须为 `false`

4. **训练参数**:
   - `training.sft.num_train_epochs` 必须为 `2`（参考 MEMORY.md 中的经验：1 epoch 不够充分）

如果以上字段均正确则无需修改。如有不一致则修正为上述值。

注意：`paths.sft_data_dir` 保持 `"outputs/sft"`（SFT 训练数据是共用的，不按模型隔离），`paths.grpo_simple_data_dir` 保持 `"outputs/grpo_simple"`（GRPO 数据同理）。
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
c = json.load(open('config/config_8b.json'))
checks = [
    (c['training']['sft']['model']['model_name'] == 'unsloth/Qwen3-8B', 'SFT model_name'),
    (c['training']['sft']['model']['load_in_4bit'] == False, 'SFT load_in_4bit'),
    (c['training']['grpo_simple']['model']['load_in_4bit'] == False, 'GRPO load_in_4bit'),
    (c['paths']['sft_output'] == 'outputs/sft/qwen3-8b/model', 'sft_output path'),
    (c['paths']['grpo_simple_output'] == 'outputs/grpo_simple/qwen3-8b/model', 'grpo_simple_output path'),
    (c['paths']['grpo_simple_checkpoints'] == 'outputs/grpo_simple/qwen3-8b/checkpoints', 'checkpoints path'),
    (c['training']['grpo_simple']['model']['model_name'] == 'outputs/sft/qwen3-8b/model', 'GRPO model_name'),
    (c['training']['sft']['num_train_epochs'] == 2, 'SFT epochs'),
]
failed = [name for ok, name in checks if not ok]
if failed:
    print('FAILED:', failed)
    sys.exit(1)
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['training']['sft']['model']['model_name'])"` 输出 `unsloth/Qwen3-8B`
    - `python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['training']['sft']['model']['load_in_4bit'])"` 输出 `False`
    - `python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['paths']['sft_output'])"` 输出 `outputs/sft/qwen3-8b/model`
    - `python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['paths']['grpo_simple_output'])"` 输出 `outputs/grpo_simple/qwen3-8b/model`
    - config_8b.json 是合法 JSON（python3 -c "json.load(open('config/config_8b.json'))" 不报错）
  </acceptance_criteria>
  <done>config_8b.json 所有关键字段均正确：模型名为 unsloth/Qwen3-8B，输出路径含 qwen3-8b 隔离目录，load_in_4bit 均为 false，SFT epochs 为 2</done>
</task>

<task type="auto">
  <name>Task 2: 端到端 dry-run 验证脚本可用性</name>
  <files>config/config_8b.json</files>
  <read_first>
    - config/config_8b.json
    - src/sft/train.py
    - src/grpo_simple/train.py
    - docker/sft_train.sh
    - docker/grpo_simple_train.sh
  </read_first>
  <action>
验证训练脚本能正确解析 config_8b.json 并提取路径（不实际训练，只验证配置解析链路）:

1. 验证 SFT 脚本配置解析:
   ```bash
   python3 -c "
   from src.sft.train import load_config
   c = load_config('config/config_8b.json')
   assert c['training']['sft']['model']['model_name'] == 'unsloth/Qwen3-8B'
   assert c['paths']['sft_output'] == 'outputs/sft/qwen3-8b/model'
   print('SFT config load OK')
   "
   ```

2. 验证 GRPO 脚本配置解析:
   ```bash
   python3 -c "
   from src.grpo_simple.train import load_config
   c = load_config('config/config_8b.json')
   assert c['training']['grpo_simple']['model']['model_name'] == 'outputs/sft/qwen3-8b/model'
   assert c['paths']['grpo_simple_output'] == 'outputs/grpo_simple/qwen3-8b/model'
   print('GRPO config load OK')
   "
   ```

3. 验证 Docker shell 脚本路径提取（模拟 shell 脚本中的 python3 -c 调用）:
   ```bash
   SFT_OUT=$(python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['paths'].get('sft_output', 'outputs/sft/model'))")
   [ "$SFT_OUT" = "outputs/sft/qwen3-8b/model" ] && echo "Docker SFT path OK" || echo "FAIL"
   GRPO_OUT=$(python3 -c "import json; c=json.load(open('config/config_8b.json')); print(c['paths'].get('grpo_simple_output', 'outputs/grpo_simple/model'))")
   [ "$GRPO_OUT" = "outputs/grpo_simple/qwen3-8b/model" ] && echo "Docker GRPO path OK" || echo "FAIL"
   ```

4. 确认 4B 产物目录配置（config/config.json）未被修改:
   ```bash
   python3 -c "
   import json
   c = json.load(open('config/config.json'))
   assert c['paths']['sft_output'] == 'outputs/sft/model', 'config.json sft_output changed!'
   assert c['paths']['grpo_simple_output'] == 'outputs/grpo_simple/model', 'config.json grpo_simple_output changed!'
   print('4B config untouched OK')
   "
   ```
  </action>
  <verify>
    <automated>python3 -c "
from src.sft.train import load_config as sft_load
from src.grpo_simple.train import load_config as grpo_load
import json, sys

# 8B config via SFT loader
c8 = sft_load('config/config_8b.json')
assert c8['training']['sft']['model']['model_name'] == 'unsloth/Qwen3-8B'
assert c8['paths']['sft_output'] == 'outputs/sft/qwen3-8b/model'

# 8B config via GRPO loader
c8g = grpo_load('config/config_8b.json')
assert c8g['training']['grpo_simple']['model']['model_name'] == 'outputs/sft/qwen3-8b/model'
assert c8g['paths']['grpo_simple_output'] == 'outputs/grpo_simple/qwen3-8b/model'

# 4B config untouched
c4 = json.load(open('config/config.json'))
assert c4['paths']['sft_output'] == 'outputs/sft/model'
assert c4['paths']['grpo_simple_output'] == 'outputs/grpo_simple/model'

print('ALL DRY-RUN CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from src.sft.train import load_config; c=load_config('config/config_8b.json'); print(c['paths']['sft_output'])"` 输出 `outputs/sft/qwen3-8b/model`
    - `python3 -c "from src.grpo_simple.train import load_config; c=load_config('config/config_8b.json'); print(c['paths']['grpo_simple_output'])"` 输出 `outputs/grpo_simple/qwen3-8b/model`
    - config/config.json 的 paths.sft_output 仍为 `outputs/sft/model`（4B 产物不受影响）
  </acceptance_criteria>
  <done>SFT 和 GRPO 训练脚本均能正确加载 config_8b.json 并解析出隔离路径，Docker shell 脚本路径提取逻辑正确，4B 配置未被改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> training script | 配置文件控制模型加载和输出路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | config_8b.json | accept | 配置文件在版本控制中，无敏感数据（模型名和路径） |
| T-08-02 | Information Disclosure | paths in config | accept | 输出路径为本地相对路径，无外部暴露 |
</threat_model>

<verification>
1. config/config_8b.json 存在且为合法 JSON
2. SFT 模型名为 `unsloth/Qwen3-8B`，load_in_4bit 为 false
3. GRPO 模型名指向 SFT 8B 产出 `outputs/sft/qwen3-8b/model`，load_in_4bit 为 false
4. 所有输出路径包含 `qwen3-8b` 隔离段
5. 4B 配置 config/config.json 未变
6. 训练脚本的 load_config 函数能正确解析 config_8b.json
</verification>

<success_criteria>
- config_8b.json 通过所有字段校验
- SFT/GRPO 脚本 dry-run 验证通过
- 4B 产物目录配置不受影响
- config_8b.json 已纳入版本控制
</success_criteria>

<output>
After completion, create `.planning/phases/08-script-parameterization/08-01-SUMMARY.md`
</output>
