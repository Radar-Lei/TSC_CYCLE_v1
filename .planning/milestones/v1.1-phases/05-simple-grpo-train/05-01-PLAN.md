---
phase: 05-simple-grpo-train
plan: 01
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - src/scripts/generate_grpo_simple_data.py
  - src/grpo_simple/train.py
  - docker/grpo_simple_data.sh
  - docker/grpo_simple_train.sh
  - config/config.json
autonomous: true
requirements: [PIPE-00, PIPE-01, PIPE-02, PIPE-03, VERI-01]

must_haves:
  truths:
    - "简化版 GRPO 数据直接来自 outputs/data/train.jsonl，而不是 GLM-5 SFT 样本"
    - "GRPO 初始化模型来自 outputs/sft/model"
    - "数据生成与训练都通过 Unsloth Docker 执行"
    - "简化版 checkpoints 与模型输出写到 outputs/grpo_simple/"
  artifacts:
    - path: "src/scripts/generate_grpo_simple_data.py"
      provides: "从原始 train.jsonl 生成简化版 GRPO prompt 数据"
    - path: "src/grpo_simple/train.py"
      provides: "简化版 GRPO 训练入口"
    - path: "docker/grpo_simple_data.sh"
      provides: "Unsloth Docker 数据生成脚本"
    - path: "docker/grpo_simple_train.sh"
      provides: "Unsloth Docker 训练脚本"
    - path: "config/config.json"
      provides: "grpo_simple 的模型、数据与输出路径配置"
  key_links:
    - from: "docker/grpo_simple_data.sh"
      to: "docker/Dockerfile"
      via: "使用 qwen3-tsc-grpo:latest，镜像基于 unsloth/unsloth:dgxspark-latest"
      pattern: "qwen3-tsc-grpo:latest"
    - from: "src/grpo_simple/train.py"
      to: "outputs/sft/model"
      via: "作为 GRPO 初始化模型"
      pattern: "outputs/sft/model"
    - from: "src/scripts/generate_grpo_simple_data.py"
      to: "outputs/data/train.jsonl"
      via: "直接读取原始样本生成 prompt 数据"
      pattern: "outputs/data/train.jsonl"
---

<objective>
实现简化版 GRPO 的数据生成与训练入口，明确“基于 SFT 模型初始化，但不使用 SFT 数据作为 GRPO 数据源”，并固定到 Unsloth Docker 流程。

Purpose: 为真正运行简化版 GRPO 做好独立的数据、训练和输出闭环。
Output: 数据生成脚本 + 训练脚本 + 独立输出路径配置。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simple-grpo-train/05-CONTEXT.md

@docker/Dockerfile
@src/scripts/generate_grpo_data.py
@src/grpo/train.py
@config/config.json
</context>

<interfaces>
Input model:
```text
outputs/sft/model
```

Input data:
```text
outputs/data/train.jsonl
```

Generated GRPO-simple data:
```text
outputs/grpo_simple/grpo_train.jsonl
```

Training outputs:
```text
outputs/grpo_simple/checkpoints/
outputs/grpo_simple/model/
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 生成简化版 GRPO 数据，不复用 SFT 样本</name>
  <files>src/scripts/generate_grpo_simple_data.py, docker/grpo_simple_data.sh, config/config.json</files>
  <behavior>
    - 直接从 `outputs/data/train.jsonl` 生成 `outputs/grpo_simple/grpo_train.jsonl`
    - prompt 格式兼容 GRPOTrainer
    - metadata 保留 `state_file`、`tl_id` 等原始上下文
  </behavior>
  <action>
    1. 创建 `src/scripts/generate_grpo_simple_data.py`
    2. 创建 `docker/grpo_simple_data.sh`
    3. 在 `config/config.json` 中将 `paths.grpo_simple_data_dir` 指向 `outputs/grpo_simple`
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python -m src.scripts.generate_grpo_simple_data --input outputs/data/train.jsonl --output /tmp/grpo_simple_preview.jsonl</automated>
  </verify>
  <done>简化版 GRPO 数据可从原始 train.jsonl 独立生成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 训练入口绑定 SFT 模型初始化和 Unsloth Docker</name>
  <files>src/grpo_simple/train.py, docker/grpo_simple_train.sh, config/config.json</files>
  <behavior>
    - 初始化模型路径为 `outputs/sft/model`
    - 数据路径为 `outputs/grpo_simple/grpo_train.jsonl`
    - 训练和输出目录都在 `outputs/grpo_simple/`
    - Docker 入口使用现有 Unsloth 镜像
  </behavior>
  <action>
    1. 在 `src/grpo_simple/train.py` 中:
       - 从 `training.grpo_simple.model.model_name` 读取初始化模型
       - 默认读取 `paths.grpo_simple_data_dir`
       - 输出到 `paths.grpo_simple_output` 和 `paths.grpo_simple_checkpoints`
    2. 在 `docker/grpo_simple_train.sh` 中:
       - 检查 `outputs/sft/model`
       - 检查 `outputs/grpo_simple/grpo_train.jsonl`
       - 通过 Docker 运行 `python3 -m src.grpo_simple.train`
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python -m py_compile src/grpo_simple/train.py src/scripts/generate_grpo_simple_data.py</automated>
  </verify>
  <done>训练入口与 Docker 脚本都已绑定到 SFT 模型初始化和独立输出目录</done>
</task>

</tasks>

<verification>
- `pytest -q tests/test_grpo_simple_rewards.py`
- `python -m py_compile src/scripts/generate_grpo_simple_data.py src/grpo_simple/train.py`
- `python -m src.scripts.generate_grpo_simple_data --input outputs/data/train.jsonl --output /tmp/grpo_simple_preview.jsonl`
</verification>

<success_criteria>
- 简化版 GRPO 数据默认来自原始 train.jsonl
- 简化版 GRPO 初始化模型默认来自 outputs/sft/model
- 数据生成和训练都能通过 Unsloth Docker 触发
- 输出目录与旧版 GRPO 完全隔离
</success_criteria>

<output>
After completion, create `.planning/phases/05-simple-grpo-train/05-01-SUMMARY.md`
</output>
