---
phase: 04-simple-grpo-reward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grpo_simple/rewards.py
  - tests/test_grpo_simple_rewards.py
  - config/config.json
autonomous: true
requirements: [FORM-01, FORM-02, CONS-01, CONS-02, SATR-01, SATR-02]

must_haves:
  truths:
    - "简化版 reward 不依赖 SUMO 仿真或 baseline 文件"
    - "completion 继续遵守 <start_working_out>/<end_working_out>/<SOLUTION> 标签协议"
    - "solution 继续按 phase 顺序输出 {phase_id, final} 列表"
    - "目标绿灯时间按 round(max_green * pred_saturation) 计算，再裁剪到 [min_green, max_green]"
    - "格式错误、JSON 解析失败、phase 顺序错误、越界 final 都会被降分"
  artifacts:
    - path: "src/grpo_simple/rewards.py"
      provides: "简化版 reward 核心实现"
      exports:
        - "init_rewards"
        - "match_format_exactly"
        - "match_format_approximately"
        - "check_constraints"
        - "saturation_proportional_reward"
        - "calculate_target_green"
    - path: "tests/test_grpo_simple_rewards.py"
      provides: "简化版 reward 单元测试"
      contains: "test_"
    - path: "config/config.json"
      provides: "training.grpo_simple.reward 配置"
  key_links:
    - from: "src/grpo_simple/rewards.py"
      to: "outputs/data/train.jsonl prompt 内容"
      via: "解析 prompt 中的 prediction.phase_waits"
      pattern: "\"phase_waits\""
    - from: "src/grpo_simple/rewards.py"
      to: "config/config.json"
      via: "读取 training.grpo_simple.reward"
      pattern: "\"grpo_simple\""
---

<objective>
实现简化版 GRPO 的 reward 与解析核心，确保模型在不依赖 SUMO 的前提下，仍然围绕格式、约束和饱和度比例目标进行优化。

Purpose: 为后续简化版 GRPO 训练入口提供稳定 reward。
Output: `src/grpo_simple/rewards.py` + 单元测试 + 对应配置。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-simple-grpo-reward/04-CONTEXT.md

@src/grpo/rewards.py
@src/scripts/generate_grpo_simple_data.py
@config/config.json
</context>

<interfaces>
From raw train sample prompt:
```json
{
  "prediction": {
    "phase_waits": [
      {"phase_id": 0, "pred_saturation": 0.1, "min_green": 8, "max_green": 105},
      {"phase_id": 2, "pred_saturation": 0.6, "min_green": 5, "max_green": 65}
    ]
  }
}
```

Completion format:
```text
思考过程<end_working_out><SOLUTION>[{"phase_id":0,"final":10},{"phase_id":2,"final":39}]</SOLUTION>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现简化版 completion 解析和约束评分</name>
  <files>src/grpo_simple/rewards.py, tests/test_grpo_simple_rewards.py</files>
  <behavior>
    - 能从 completion 中提取 `<SOLUTION>` 内的 JSON 数组
    - 能识别 phase 顺序错误、solution 长度不匹配、final 非整数、final 越界
    - 格式 reward 与约束 reward 都可单独调用
  </behavior>
  <action>
    1. 在 `src/grpo_simple/rewards.py` 中实现:
       - `extract_solution_from_completion`
       - `match_format_exactly`
       - `match_format_approximately`
       - `check_constraints`
    2. 保持接口兼容 `trl.GRPOTrainer` 的 reward 函数签名
    3. 在 `tests/test_grpo_simple_rewards.py` 中覆盖:
       - 格式正确
       - phase 顺序错误
       - 越界输出
       - JSON 结构错误
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest -q tests/test_grpo_simple_rewards.py</automated>
  </verify>
  <done>completion 解析和约束评分实现完成，基础测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现按饱和度比例分配的目标绿灯 reward</name>
  <files>src/grpo_simple/rewards.py, tests/test_grpo_simple_rewards.py, config/config.json</files>
  <behavior>
    - 目标值公式是 `round(max_green * pred_saturation)` 再裁剪到 `[min_green, max_green]`
    - 更接近目标值的 completion 得分高于明显偏离的 completion
    - 不合法 completion 直接给低分或 0 分
  </behavior>
  <action>
    1. 在 `src/grpo_simple/rewards.py` 中实现:
       - `calculate_target_green`
       - `saturation_proportional_reward`
    2. 在 `config/config.json` 中添加 `training.grpo_simple.reward` 配置
    3. 在 `tests/test_grpo_simple_rewards.py` 中覆盖:
       - 命中目标
       - 偏离目标
       - 因最小绿约束被裁剪
       - 因最大绿约束被裁剪
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && pytest -q tests/test_grpo_simple_rewards.py</automated>
  </verify>
  <done>饱和度比例 reward 实现完成，能区分命中与偏离方案</done>
</task>

</tasks>

<verification>
- `pytest -q tests/test_grpo_simple_rewards.py`
- `python -m py_compile src/grpo_simple/rewards.py tests/test_grpo_simple_rewards.py`
</verification>

<success_criteria>
- reward 完全不依赖 SUMO 或 baseline
- completion 解析、格式检查、约束检查可独立工作
- 饱和度比例 reward 能对更接近目标的方案打更高分
- 单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-simple-grpo-reward/04-01-SUMMARY.md`
</output>
