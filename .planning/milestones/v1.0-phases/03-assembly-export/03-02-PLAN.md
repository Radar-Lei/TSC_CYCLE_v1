---
phase: 03-assembly-export
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - config/config.json
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02

must_haves:
  truths:
    - "src/sft/train.py 可以加载 outputs/sft/sft_train.jsonl 并开始训练"
    - "训练 loss 正常下降，无格式错误或 NaN"
    - "训练完成后 outputs/sft/model/ 目录包含模型文件"
  artifacts:
    - path: "outputs/sft/model/config.json"
      provides: "训练后模型配置"
    - path: "outputs/sft/model/"
      provides: "训练后完整模型 (safetensors)"
  key_links:
    - from: "docker/sft_train.sh"
      to: "src/sft/train.py"
      via: "Docker 容器执行训练"
      pattern: "python3 -m src.sft.train"
    - from: "src/sft/train.py"
      to: "outputs/sft/sft_train.jsonl"
      via: "加载训练数据"
      pattern: "sft_train\\.jsonl"
---

<objective>
在 Docker 容器中运行 SFT 训练 (1 epoch)，验证新生成的 5000 条数据格式兼容性和训练 loss 收敛情况。

Purpose: 验证 Phase 2 产出的数据质量足以训练模型，确认端到端流程可行。
Output: outputs/sft/model/ (训练后的模型)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-assembly-export/03-01-SUMMARY.md

@docker/sft_train.sh
@src/sft/train.py
@config/config.json

<interfaces>
<!-- config/config.json 关键字段: -->
```json
{
  "training": {
    "sft": {
      "model": {"model_name": "model/Qwen3-4B-Base", "max_seq_length": 2048, "lora_rank": 16},
      "num_train_epochs": 2,
      "max_steps": 100,
      "logging_steps": 5
    }
  },
  "paths": {
    "sft_data_dir": "outputs/sft",
    "sft_output": "outputs/sft/model"
  }
}
```

<!-- docker/sft_train.sh: -->
<!-- 自动从 outputs/sft/sft_train.jsonl 读取数据，在 Docker 中运行 python -m src.sft.train -->
<!-- 注意: sft_train.sh 有一段检查 sft_train.jsonl 是否存在的逻辑，如果不存在会尝试从旧管道生成 -->
<!-- 由于 Plan 01 已经生成了 sft_train.jsonl，这段逻辑会被跳过 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 调整 config.json 训练参数为 1 epoch</name>
  <files>config/config.json</files>
  <read_first>
    - config/config.json (完整文件，确认当前 training.sft 配置)
    - src/sft/train.py (行 193-260, 确认 num_train_epochs 和 max_steps 如何被使用)
  </read_first>
  <action>
    修改 config/config.json 中的 training.sft 部分:

    1. 将 "num_train_epochs" 从 2 改为 1（仅用于本次验证训练，验证后可改回）
    2. 移除或注释掉 "max_steps": 100 限制（改为 -1 或删除该字段），让训练跑完整个 1 epoch
       - 注意: 如果 max_steps 存在且 > 0，SFTTrainer 会在 max_steps 步后停止，可能不会跑完 epoch
       - 对于 5000 条数据、batch_size=1，1 epoch = 5000 steps，如果 max_steps=100 只会训练 100 步
       - 将 max_steps 设为 -1 表示不限制

    不修改其他参数（lora_rank=16, learning_rate=2e-4, bf16=true 等保持不变）。
  </action>
  <verify>
    <automated>cd /home/samuel/TSC_CYCLE && python -c "
import json
with open('config/config.json') as f:
    c = json.load(f)
sft = c['training']['sft']
assert sft['num_train_epochs'] == 1, f'Expected 1 epoch, got {sft[\"num_train_epochs\"]}'
ms = sft.get('max_steps', -1)
assert ms == -1 or ms < 0, f'max_steps should be -1, got {ms}'
print('config OK: 1 epoch, no max_steps limit')
"</automated>
  </verify>
  <acceptance_criteria>
    - config.json 中 training.sft.num_train_epochs == 1
    - config.json 中 training.sft.max_steps == -1 (或字段不存在)
    - 其余配置不变
  </acceptance_criteria>
  <done>config.json 已调整为 1 epoch 无 step 限制</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 用户在 Docker 中执行 SFT 训练并检查 loss</name>
  <read_first>
    - docker/sft_train.sh (完整文件，理解执行流程)
    - config/config.json (确认 Task 1 的修改已到位)
  </read_first>
  <action>
    用户需要在 DGX-Spark 上执行以下命令启动 SFT 训练:

    ```bash
    chmod +x docker/sft_train.sh
    ./docker/sft_train.sh
    ```

    训练预期行为:
    - 加载 model/Qwen3-4B-Base 模型
    - 读取 outputs/sft/sft_train.jsonl
    - 以 LoRA rank=16, BF16, AdamW 8-bit 训练 1 epoch
    - 每 5 步打印一次 loss
    - 完成后保存模型到 outputs/sft/model/

    需要关注:
    1. 数据加载是否成功（无格式报错）
    2. 训练 loss 是否从较高值（约 2-4）逐步下降
    3. 训练是否正常完成（无 OOM、NaN 等错误）
  </action>
  <what-built>SFT 训练数据已组装完成，config.json 已调整为 1 epoch</what-built>
  <how-to-verify>
    1. 运行 `./docker/sft_train.sh`
    2. 观察训练日志:
       - 数据加载行: "[数据加载] 原始样本数: XXXX, 过滤后: XXXX" — 确认样本被成功加载
       - loss 日志: 每 5 步一次，确认 loss 数值在下降
       - 无 "Error", "NaN", "OOM" 等错误
    3. 训练完成后检查: `ls outputs/sft/model/` 应包含 config.json 和 .safetensors 文件
  </how-to-verify>
  <resume-signal>训练完成后输入 "training done"，附上最终 loss 值；如有问题请描述错误信息</resume-signal>
  <acceptance_criteria>
    - docker/sft_train.sh 运行成功，无报错退出
    - 训练 loss 从初始值逐步下降（非停滞或 NaN）
    - outputs/sft/model/ 目录包含 config.json 和至少一个 .safetensors 文件
  </acceptance_criteria>
  <done>SFT 训练 1 epoch 完成，loss 正常收敛，模型文件已保存</done>
</task>

</tasks>

<verification>
- config.json 已调整为 1 epoch
- docker/sft_train.sh 成功执行
- 训练 loss 正常收敛
- outputs/sft/model/ 包含完整模型文件
</verification>

<success_criteria>
- SFT 训练在 Docker 容器中成功完成 1 epoch
- 训练 loss 正常下降（TRAIN-02）
- 模型文件保存到 outputs/sft/model/
</success_criteria>

<output>
After completion, create `.planning/phases/03-assembly-export/03-02-SUMMARY.md`
</output>
